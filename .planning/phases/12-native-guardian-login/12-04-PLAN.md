---
phase: 12-native-guardian-login
plan: 04
type: execute
wave: 3
depends_on: [12-02, 12-03]
files_modified:
  - .env.example
  - deploy/k8s/kreds-secret.yaml
  - deploy/k8s/kreds-deployment.yaml
  - tests/unit/env.test.ts
  - tests/e2e/guardian-native-auth.spec.ts
autonomous: false
requirements: [R1, R3, R4, R5, R6, R7, R8, R10, R11, R12]
user_setup:
  - service: zitadel-login-service-account
    why: "IAM_LOGIN_CLIENT precisa de uma JWT-profile key dedicada, com escopo mínimo, antes do deploy."
    env_vars: [IAM_LOGIN_CLIENT]
---

<objective>
Fechar a integração da Phase 12 com configuração de runtime, testes de boot/regressão e verificação ponta a ponta em ambiente seguro.
</objective>

<context>
@.planning/phases/12-native-guardian-login/12-SPEC.md
@.planning/phases/12-native-guardian-login/12-VALIDATION.md
@.planning/phases/12-native-guardian-login/12-RESEARCH.md
@.planning/CONVENTIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Secret e fail-closed</name>
  <action>Documentar `IAM_LOGIN_CLIENT` em `.env.example` e adicionar a referência ao Secret Kubernetes existente/planejado, sem commitar credenciais reais. Confirmar que somente módulos server-only leem a variável. Estender teste de env para ausência, JSON inválido e chave válida.</action>
  <verify><automated>npx vitest run tests/unit/env.test.ts</automated></verify>
  <done>Boot falha explicitamente sem secret válido e o secret não pode chegar ao bundle cliente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regressão Auth.js e teste E2E</name>
  <action>Rodar a suíte completa, revisar diff de `loginWithProvider`, `loginWithPasskey` e `SocialAuthButtons`, e adicionar/ajustar E2E para login nativo, erro genérico, signup, reset e redirecionamento de email não verificado. Executar apenas contra configuração de teste; não criar usuários reais sem credencial/fixture explícita.</action>
  <verify><automated>npx vitest run && npx playwright test tests/e2e/guardian-native-auth.spec.ts</automated></verify>
  <done>Acceptance criteria da SPEC estão cobertos, social/passkey permanecem OIDC e a Phase 12 está pronta para verificação visual/produção.</done>
</task>

</tasks>

<verification>
- Full suite verde antes do checkpoint humano.
- Checklist manual R11/R12 anexado ao summary.
- Nenhum deploy é feito até `IAM_LOGIN_CLIENT` existir no namespace `kreds` e o usuário aprovar o checkpoint.
</verification>
