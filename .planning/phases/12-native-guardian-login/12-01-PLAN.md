---
phase: 12-native-guardian-login
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/env.ts
  - src/lib/zitadel/login-client.ts
  - src/lib/auth/guardian-sync.ts
  - auth.ts
  - src/app/actions/guardian-auth.ts
  - tests/unit/zitadel-login-client.test.ts
  - tests/unit/guardian-sync.test.ts
autonomous: true
requirements: [R1, R2, R3, R4, R10, R12]
---

<objective>
Construir a fundação server-only do login nativo: cliente REST autenticado para Zitadel, validação fail-closed de `IAM_LOGIN_CLIENT`, sincronização de identidade/família e provider Credentials no Auth.js, preservando o provider OIDC social.
</objective>

<context>
@.planning/phases/12-native-guardian-login/12-SPEC.md
@.planning/phases/12-native-guardian-login/12-CONTEXT.md
@.planning/phases/12-native-guardian-login/12-RESEARCH.md
@auth.ts
@src/app/actions/guardian-auth.ts
@src/lib/env.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zitadel client e segredo server-only</name>
  <action>Adicionar o schema Zod de `IAM_LOGIN_CLIENT` em `src/lib/env.ts`, validando JSON de JWT-profile key e falhando no boot quando ausente ou inválido. Criar `src/lib/zitadel/login-client.ts` com `import 'server-only'`, assinatura do service-account JWT usando `node:crypto` e wrappers tipados para `POST /v2/sessions`, leitura de usuário, grants, criação de usuário e reset. Nunca incluir senha em logs, erros serializados ou respostas. Normalizar qualquer falha de login para um erro interno genérico.</action>
  <verify><automated>npx vitest run tests/unit/zitadel-login-client.test.ts</automated></verify>
  <done>Cliente server-only cobre as chamadas necessárias e converte usuário inexistente/senha errada para o mesmo erro sem expor credenciais.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Credentials provider e sincronização Auth.js</name>
  <action>Extrair a sincronização de `kreds_identities` para `src/lib/auth/guardian-sync.ts`, incluindo auto-cura de membership/família e busca não bloqueante de `systemRoles`. Adicionar `Credentials` ao array existente de providers em `auth.ts`; `authorize()` deve chamar Session API, obter perfil, detectar `email_verified=false` e retornar a forma de usuário já consumida pelos callbacks. Atualizar somente `loginWithCredentials` para usar `signIn('credentials')`; manter `loginWithProvider` e `loginWithPasskey` com comportamento OIDC atual. Cobrir erro genérico, email não verificado, roles e falha não bloqueante nos testes.</action>
  <verify><automated>npx vitest run tests/unit/guardian-sync.test.ts</automated></verify>
  <done>Login válido cria a mesma sessão downstream do OIDC, login inválido não enumera contas e falhas de grants/sync não bloqueiam autenticação.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/zitadel-login-client.test.ts tests/unit/guardian-sync.test.ts`
- Diff confirma que os fluxos social/passkey não foram alterados em comportamento.
- Busca por senha confirma ausência de `console`, `pino`, `db.insert` ou resposta client-side contendo plaintext.
</verification>
