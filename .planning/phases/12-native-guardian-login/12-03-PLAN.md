---
phase: 12-native-guardian-login
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/app/actions/guardian-reset.ts
  - src/app/login/reset/page.tsx
  - src/app/login/reset/confirm/page.tsx
  - src/components/auth/guardian-reset-form.tsx
  - src/components/auth/guardian-reset-confirm-form.tsx
  - tests/unit/guardian-reset.test.ts
autonomous: true
requirements: [R8, R9, R12]
---

<objective>
Conectar o reset de senha existente ao Zitadel sem redirect hospedado, incluindo a página de confirmação necessária para o link recebido por email.
</objective>

<context>
@.planning/phases/12-native-guardian-login/12-SPEC.md
@.planning/phases/12-native-guardian-login/12-RESEARCH.md
@src/app/login/reset/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Solicitação genérica de reset</name>
  <action>Criar action server-only que aceite qualquer email, procure o usuário sem expor existência e dispare o fluxo de notificação de senha do Zitadel com `urlTemplate` apontando para a confirmação no Kreds. A UI existente deve sempre mostrar a mesma confirmação de sucesso, inclusive para email inexistente ou erro externo controlado.</action>
  <verify><automated>npx vitest run tests/unit/guardian-reset.test.ts</automated></verify>
  <done>Solicitação de reset não enumera contas e não envia o usuário à UI hospedada do Zitadel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirmação e troca de senha</name>
  <action>Criar `/login/reset/confirm` para receber código/token e nova senha, enviar a confirmação ao Zitadel via server action e exibir sucesso/erro sem vazar senha ou código. Repassar mensagens de política retornadas pelo Zitadel; não criar regex de complexidade em Kreds. Cobrir código ausente, inválido, senha válida e violação de policy.</action>
  <verify><automated>npx vitest run tests/unit/guardian-reset.test.ts</automated></verify>
  <done>O link de reset termina dentro do Kreds e a nova senha é aplicada pelo Zitadel.</done>
</task>

</tasks>

<verification>
- Existing e inexistente produzem a mesma resposta de solicitação.
- Nenhuma rota de reset redireciona para `auth.hasslab.pro`.
- Busca por `password` confirma que o valor nunca é logado, persistido ou retornado em erro.
</verification>
