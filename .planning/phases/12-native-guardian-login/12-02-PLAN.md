---
phase: 12-native-guardian-login
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/app/actions/guardian-signup.ts
  - src/app/signup/page.tsx
  - src/components/auth/guardian-signup-form.tsx
  - src/components/auth/guardian-login-form.tsx
  - tests/unit/guardian-signup.test.ts
  - tests/integration/guardian-signup.test.ts
autonomous: true
requirements: [R5, R6, R7, R9, R12]
---

<objective>
Entregar signup nativo com criação do usuário Zitadel, bootstrap transacional de família/membership, vínculo correto a convite pendente e login imediato.
</objective>

<context>
@.planning/phases/12-native-guardian-login/12-SPEC.md
@.planning/phases/12-native-guardian-login/12-CONTEXT.md
@.planning/phases/12-native-guardian-login/12-RESEARCH.md
@src/lib/db/schema/index.ts
@src/app/actions/children.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Formulário e action de signup</name>
  <action>Criar `/signup` seguindo o padrão Server Component + Client Component. Validar email, senha e confirmação apenas para igualdade; não adicionar regex local de complexidade. Criar `guardian-signup.ts` para chamar criação de human user no Zitadel, tratar `already_exists` com mensagem genérica e chamar `signIn('credentials')` após persistência. Ligar o link Criar conta do login para `/signup`.</action>
  <verify><automated>npx vitest run tests/unit/guardian-signup.test.ts</automated></verify>
  <done>Usuário consegue submeter signup dentro do Kreds e recebe mensagens não enumerativas para duplicidade e validação.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bootstrap transacional e convite pendente</name>
  <action>Após a criação Zitadel, procurar convite pendente na tabela `guardianInvitations` (não em `family_memberships`). Sem convite, inserir em uma única transação `kreds_identities`, `families` com fallback `Família` e membership guardian. Com convite, anexar a identidade à família convidada e consumir/atualizar o convite de forma idempotente. Nenhum campo de role/familyId pode vir do cliente. Testar duplicidade, convite, rollback e auto-cura no próximo login usando Testcontainers.</action>
  <verify><automated>npx vitest run tests/integration/guardian-signup.test.ts</automated></verify>
  <done>Signup novo chega a um dashboard utilizável; convite não cria família duplicada; falha Postgres pode ser reparada pelo login seguinte.</done>
</task>

</tasks>

<verification>
- Testcontainers verifica identity, family e membership após signup.
- Teste negativo confirma que input do cliente não injeta role, familyId ou permissões.
- Senha plaintext não é persistida nem aparece em erros/logs.
</verification>
