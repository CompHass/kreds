---
phase: 03-kreds-engine-ledger-and-audit-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - src/modules/ledger/engine.ts
  - src/app/api/ledger/[childId]/post-adjustment/route.ts
  - src/app/(app)/guardian/[childId]/adjustment/page.tsx
autonomous: true
requirements:
  - LEDG-05

must_haves:
  truths:
    - "Guardian pode registrar ajuste negativo com reason obrigatório para um child (D-07, LEDG-05)"
    - "Ajuste negativo posta linha com amount negativo no account available (D-07)"
    - "Reason vazia ou ausente é rejeitada com 422 antes de qualquer insert (D-07)"
    - "Guardian vê formulário de ajuste negativo em /guardian/[childId]/adjustment"
    - "Rota POST retorna 201 com o header da transação em caso de sucesso"
  artifacts:
    - path: "src/modules/ledger/engine.ts"
      provides: "postNegativeAdjustment() — posta linha negative adjustment atomicamente"
      exports: ["postNegativeAdjustment"]
    - path: "src/app/api/ledger/[childId]/post-adjustment/route.ts"
      provides: "POST /api/ledger/[childId]/post-adjustment — valida AdjustmentCommandSchema, chama postNegativeAdjustment"
      contains: "AdjustmentCommandSchema"
    - path: "src/app/(app)/guardian/[childId]/adjustment/page.tsx"
      provides: "Formulário SSR+client para guardian registrar ajuste negativo com campo reason obrigatório"
      min_lines: 40
  key_links:
    - from: "src/app/api/ledger/[childId]/post-adjustment/route.ts"
      to: "src/modules/ledger/engine.ts"
      via: "import { postNegativeAdjustment } from '@/modules/ledger/engine'"
      pattern: "postNegativeAdjustment"
    - from: "src/app/(app)/guardian/[childId]/adjustment/page.tsx"
      to: "src/app/api/ledger/[childId]/post-adjustment/route.ts"
      via: "fetch POST com body { commandId, amount, reason }"
      pattern: "post-adjustment"
---

<objective>
Slice 2 — "Ajuste Negativo": Implementar postNegativeAdjustment com reason obrigatório, rota POST de posting e formulário de guardian. Per D-07, LEDG-05.

Propósito: Guardians precisam registrar deduções com justificativa (ex.: tarefa não completada, comportamento). A reason é obrigatória para auditabilidade — sem texto de motivo, o ledger é opaco para o child mais tarde.

Saída: postNegativeAdjustment no engine, rota POST /api/ledger/[childId]/post-adjustment, página de formulário para guardian.
</objective>

<execution_context>
@/Users/hass/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-kreds-engine-ledger-and-audit-foundation/03-CONTEXT.md
@.planning/phases/03-kreds-engine-ledger-and-audit-foundation/03-RESEARCH.md
@.planning/phases/03-kreds-engine-ledger-and-audit-foundation/03-02-SUMMARY.md
@src/modules/ledger/engine.ts
@src/modules/ledger/commands.ts
@src/app/api/ledger/[childId]/post-earning/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: postNegativeAdjustment no engine + rota POST</name>

  <read_first>
    src/modules/ledger/engine.ts — implementação atual de postEarning para replicar o padrão; verificar o stub de postNegativeAdjustment para substituir
    src/modules/ledger/commands.ts — AdjustmentCommandSchema e tipo AdjustmentCommand; verificar campos: commandId, familyId, childProfileId, guardianIdentityId, amount (positivo — o valor do debit), reason (min 1), restorationNote (opcional)
    src/lib/db/schema/ledger.ts — ledgerTransactions, ledgerLines e enums; verificar que 'negative_adjustment' está em transactionTypeEnum
    src/app/api/ledger/[childId]/post-earning/route.ts — padrão de rota POST com Zod + 23505 handler para replicar
    .planning/phases/03-kreds-engine-ledger-and-audit-foundation/03-RESEARCH.md — Pitfall 2 (saldo negativo: D-07 não impõe limite, mas RESEARCH.md recomenda verificação getBalance pré-insert com retorno 422)
  </read_first>

  <behavior>
    postNegativeAdjustment(command: AdjustmentCommand):
    - Usa db.transaction(async (tx) => {...}) para atomicidade (LEDG-01)
    - Verifica pré-insert: getBalance(command.childProfileId, 'available') → se debit amount > available balance, retornar erro legível (não lançar exception genérica) — "Insufficient balance"
    - Insert em ledgerTransactions: transactionType='negative_adjustment', note=command.reason (armazena o reason no campo note do header), commandId=command.commandId
    - Insert em ledgerLines: UMA linha — { accountType: 'available', amount: -command.amount } (negativo — representa débito do saldo available)
    - restorationNote de command é armazenado: adicionar como segundo campo text 'restoration_note' no note JSONB ou como campo separado. Decision: armazenar restoration_note diretamente no note do header como JSON string "{\"reason\":\"...\",\"restorationNote\":\"...\"}" para não precisar de novo campo no schema. Documentar esta choice no SUMMARY.
    - Retorna o txHeader

    POST /api/ledger/[childId]/post-adjustment:
    - AdjustmentCommandSchema.safeParse({ ...body, childProfileId: params.childId }) → 422 se inválido
    - reason ausente ou vazia → capturada pelo safeParse (z.string().min(1)) → 422 automaticamente
    - Chama postNegativeAdjustment(parsed.data) → 201 com { data: txHeader }
    - Captura erro 'Insufficient balance' → 422 { error: 'Saldo insuficiente para este ajuste' }
    - Captura erro 23505 command_id → 409 { status: 'already_posted' }
  </behavior>

  <files>
    src/modules/ledger/engine.ts
    src/app/api/ledger/[childId]/post-adjustment/route.ts
  </files>

  <action>
    Abrir src/modules/ledger/engine.ts e adicionar importação de AdjustmentCommand de './commands' e getBalance de './queries'. Substituir o stub de postNegativeAdjustment pela implementação real conforme behavior.

    Verificação pré-insert de saldo: await getBalance(command.childProfileId, 'available') dentro do db.transaction (não fora, para consistência) — se amount > balance, lançar new Error('Insufficient balance: adjustment amount exceeds available balance').

    Para armazenar reason e restorationNote sem novo campo de schema: usar o campo 'note' (text) do header para armazenar JSON.stringify({ reason: command.reason, restorationNote: command.restorationNote ?? null }). Assim o guardian audit view pode fazer JSON.parse(note) para exibir. Documentar este choice no action do SUMMARY.

    Linha de ledger: uma única linha com accountType: 'available' e amount: -command.amount (negativo). NÃO criar linha de firstfruits para negative adjustment — apenas debita o available (D-07 é debit livre, sem withholding).

    Criar src/app/api/ledger/[childId]/post-adjustment/route.ts seguindo o mesmo padrão de post-earning/route.ts. Imports: { NextResponse } de 'next/server', { postNegativeAdjustment } de '@/modules/ledger/engine', { AdjustmentCommandSchema } de '@/modules/ledger/commands'. Adicionar tratamento específico para 'Insufficient balance' como 422.
  </action>

  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && pnpm exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/modules/ledger/engine.ts NÃO contém "not implemented" na função postNegativeAdjustment
    - postNegativeAdjustment contém "db.transaction" (grep confirma)
    - postNegativeAdjustment contém "Insufficient balance" ou "getBalance" (grep confirma verificação pré-insert)
    - Linha de ledger inserida tem amount negativo: grep "amount: -command.amount" confirma
    - ledgerLines insert em postNegativeAdjustment NÃO cria linha 'firstfruits' (grep verifica ausência)
    - src/app/api/ledger/[childId]/post-adjustment/route.ts exporta POST (grep -c "export async function POST" retorna 1)
    - route.ts de post-adjustment contém "AdjustmentCommandSchema" (grep confirma)
    - route.ts de post-adjustment contém "Insufficient balance" ou "422" para tratamento de saldo insuficiente
    - route.ts de post-adjustment contém "already_posted" (grep confirma 409)
    - pnpm exec tsc --noEmit retorna 0
  </acceptance_criteria>

  <done>postNegativeAdjustment implementado com verificação de saldo, débito de linha negativa e reason armazenada no note. Rota POST com validação Zod (reason min(1) → 422) e tratamentos 409 e 422.</done>
</task>

<task type="auto">
  <name>Task 2: Formulário de ajuste negativo para guardian</name>

  <read_first>
    src/app/(app)/child/[childId]/balance/page.tsx — padrão de Server Component do Plano 02 para seguir estrutura de página
    src/modules/ledger/queries.ts — getBalance para exibir saldo atual do child antes do formulário
    src/app/api/ledger/[childId]/post-adjustment/route.ts — rota destino do POST do formulário; verificar path exato
    .planning/phases/02-family-access-tenancy-roles-and-profiles/02-CONTEXT.md — D-17 (guardians veem dados do child), D-18 (UI parent-readable, timeline simples)
  </read_first>

  <behavior>
    Página /guardian/[childId]/adjustment:
    - Server component: carrega saldo available atual do child via getBalance(childId, 'available') e exibe como contexto ("Saldo atual: {balance} Kreds")
    - Client component embutido (ou formulário HTML nativo) para o formulário de ajuste
    - Campos do formulário: amount (number, min=1, obrigatório), reason (textarea, obrigatório, placeholder "Ex.: Tarefa não completada"), restorationNote (textarea, opcional, placeholder "Opcional: note de restauração")
    - Submit gera crypto.randomUUID() para commandId no client e envia POST para /api/ledger/{childId}/post-adjustment
    - Sucesso: exibe "Ajuste registrado com sucesso" e limpa o formulário
    - Erro 422 de reason vazia: não deve ocorrer (HTML required attribute), mas se ocorrer exibe mensagem
    - Erro 422 de saldo insuficiente: exibe "Saldo insuficiente para este ajuste"
    - Linguagem guardian-friendly: "Registrar Ajuste" (botão), não "Punir" ou linguagem punitiva
  </behavior>

  <files>
    src/app/(app)/guardian/[childId]/adjustment/page.tsx
  </files>

  <action>
    Criar src/app/(app)/guardian/[childId]/adjustment/page.tsx como Server Component externo com um componente Client embutido para o formulário.

    O Server Component: importa getBalance de '@/modules/ledger/queries'; recebe props { params: Promise<{ childId: string }> }; await params; chama getBalance(childId, 'available') para mostrar saldo atual. Renderiza título "Registrar Ajuste para [childId]" e o saldo atual, depois passa childId como prop para o AdjustmentForm Client Component definido no mesmo arquivo.

    O AdjustmentForm ('use client'): state para amount (string), reason (string), restorationNote (string), status ('idle'|'loading'|'success'|'error'), errorMessage (string). handleSubmit: gera commandId = crypto.randomUUID(), familyId deve vir de sessão (para este slice: usar placeholder fixo ou receber como prop do server — usar prop guardianIdentityId e familyId como string vazia temporariamente, documentar TODO). POST fetch para /api/ledger/${childId}/post-adjustment com body { commandId, familyId: '', guardianIdentityId: '', childProfileId: childId, amount: parseInt(amount), reason, restorationNote: restorationNote || undefined }. Tratar response.ok → status 'success'; response.status === 422 com 'Saldo insuficiente' → errorMessage; outros → errorMessage genérico.

    Nota: familyId e guardianIdentityId ficam como placeholder neste slice — serão populados pelo helper de autenticação da Fase 2 quando o módulo de ledger for wired ao auth. Documentar como TODO no código.

    Campos HTML: input type="number" min="1" required para amount; textarea required para reason; textarea para restorationNote. Botão "Registrar Ajuste". Mensagem de sucesso em verde, erro em vermelho (Tailwind: text-green-600, text-red-600).
  </action>

  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && pnpm exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/app/(app)/guardian/[childId]/adjustment/page.tsx existe
    - Arquivo contém "'use client'" para o componente de formulário (grep confirma presença do 'use client')
    - Arquivo contém "getBalance" (grep confirma carregamento do saldo no server)
    - Arquivo contém "post-adjustment" na URL do fetch (grep confirma destino correto)
    - Arquivo contém campo "reason" no formulário com atributo "required" (grep confirma)
    - Arquivo contém "crypto.randomUUID()" para geração de commandId no client (grep confirma)
    - Arquivo contém "Insufficient balance" ou "Saldo insuficiente" para tratamento de erro (grep)
    - Arquivo contém "Registrar Ajuste" (linguagem guardian-friendly, grep confirma)
    - pnpm exec tsc --noEmit retorna 0
  </acceptance_criteria>

  <done>Formulário de ajuste negativo para guardian completo: saldo atual visível, campos reason obrigatório e restorationNote opcional, submit via fetch POST, feedback de sucesso/erro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP body → AdjustmentCommandSchema | amount deve ser inteiro positivo (o sinal negativo é aplicado pelo engine, não pelo caller); reason deve ter >= 1 char |
| engine → getBalance | verificação de saldo pré-insert dentro de db.transaction; race condition teórica mas aceitável para v1 |
| guardian formulário → API | sem autenticação neste slice; guard da Fase 2 adicionado quando wired |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | amount em AdjustmentCommandSchema | mitigate | z.number().int().positive() — amount negativo no caller é rejeitado; engine aplica o sinal |
| T-03-08 | Tampering | reason vazia (LEDG-05) | mitigate | z.string().min(1) no schema Zod → 422 automático; HTML required attribute no formulário |
| T-03-09 | Elevation of Privilege | Guardian ajusta child de outra família | accept | Mesmo que T-03-05 — guard Fase 2; aceitável neste slice de desenvolvimento |
| T-03-10 | Tampering | amount > saldo available → saldo negativo | mitigate | getBalance pré-insert no engine; 422 'Saldo insuficiente' na rota |
| T-03-SC | Tampering | npm installs | accept | Nenhum pacote novo |
</threat_model>

<verification>
- grep -c "db.transaction" src/modules/ledger/engine.ts retorna >= 2 (postEarning + postNegativeAdjustment)
- grep "amount: -command.amount" src/modules/ledger/engine.ts confirma linha negativa (ou equivalente)
- grep -c "export async function POST" src/app/api/ledger/[childId]/post-adjustment/route.ts retorna 1
- grep "reason" src/app/(app)/guardian/[childId]/adjustment/page.tsx confirma campo reason no formulário
- pnpm exec tsc --noEmit: 0 errors
</verification>

<success_criteria>
Slice 2 completo quando:
1. postNegativeAdjustment posta linha negativa no account available com reason no note
2. Rota POST rejeita reason vazia com 422 (Zod .min(1))
3. Rota POST rejeita debit > saldo com 422 "Saldo insuficiente"
4. Formulário guardian exibe saldo atual e permite submissão com reason obrigatório
5. TypeScript compila sem erros
</success_criteria>

<output>
Criar `.planning/phases/03-kreds-engine-ledger-and-audit-foundation/03-03-SUMMARY.md` quando concluído.
</output>

## Artifacts this phase produces

| Symbol | Type | File | Purpose |
|--------|------|------|---------|
| `postNegativeAdjustment` | async function (real) | src/modules/ledger/engine.ts | Débito de available com reason; rejeita se saldo insuficiente |
| POST handler | Next.js Route Handler | src/app/api/ledger/[childId]/post-adjustment/route.ts | Valida AdjustmentCommandSchema; trata 409 e 422 |
| adjustment page | React Server + Client Component | src/app/(app)/guardian/[childId]/adjustment/page.tsx | Formulário guardian com saldo atual e reason obrigatório |
</output>
