---
phase: 03-kreds-engine-ledger-and-audit-foundation
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/lib/db/schema/ledger.ts
  - src/lib/db/schema/index.ts
  - src/modules/ledger/calculate.ts
  - src/modules/ledger/commands.ts
  - src/modules/glossary/terms.ts
  - tests/unit/ledger-calculate.test.ts
  - tests/unit/ledger-queries.test.ts
  - tests/integration/ledger-engine.test.ts
autonomous: true
requirements:
  - LEDG-01
  - LEDG-02
  - LEDG-03
  - LEDG-04
  - LEDG-05
  - LEDG-06
  - LEDG-07
  - LEDG-08

must_haves:
  truths:
    - "calculateFirstfruits(1)=1, (7)=1, (10)=1, (11)=2, (100)=10 via Math.ceil (D-01)"
    - "calculateFirstfruits lança erro para input float ou não-positivo (LEDG-02)"
    - "Schema ledger_transactions tem UNIQUE constraint em command_id (LEDG-06)"
    - "Schema ledger_lines tem integer amount e check non_zero_amount (LEDG-02)"
    - "Testes RED existem para LEDG-01 a LEDG-08 antes de qualquer implementação de engine"
  artifacts:
    - path: "src/lib/db/schema/ledger.ts"
      provides: "Drizzle schema: ledger_transactions, ledger_lines, transactionTypeEnum, accountTypeEnum"
      contains: "uniqueIndex.*command_id"
    - path: "src/modules/ledger/calculate.ts"
      provides: "calculateFirstfruits(amount): number — função pura, zero side-effects"
      exports: ["calculateFirstfruits", "FIRSTFRUITS_RATE"]
    - path: "src/modules/ledger/commands.ts"
      provides: "Zod schemas e tipos TypeScript para EarningCommand, AdjustmentCommand, ReversalCommand"
      exports: ["EarningCommandSchema", "AdjustmentCommandSchema", "ReversalCommandSchema", "EarningCommand", "AdjustmentCommand", "ReversalCommand"]
    - path: "tests/unit/ledger-calculate.test.ts"
      provides: "Testes RED/GREEN para calculateFirstfruits edge cases (D-01, LEDG-02, LEDG-03)"
      contains: "calculateFirstfruits"
    - path: "tests/unit/ledger-queries.test.ts"
      provides: "Testes RED para visibilidade diferenciada guardian vs child (D-09, D-10, LEDG-07)"
      contains: "getChildLedgerHistory"
    - path: "tests/integration/ledger-engine.test.ts"
      provides: "Testes RED para postEarning, idempotência, adjustment, reversal (LEDG-01, LEDG-04 a LEDG-08)"
      contains: "postEarning"
  key_links:
    - from: "src/lib/db/schema/ledger.ts"
      to: "src/lib/db/schema/index.ts"
      via: "export * from './ledger'"
      pattern: "export \\* from"
    - from: "src/modules/ledger/commands.ts"
      to: "src/modules/ledger/engine.ts (futuro, Wave 1)"
      via: "EarningCommand, AdjustmentCommand, ReversalCommand types"
      pattern: "EarningCommand|AdjustmentCommand|ReversalCommand"
---

<objective>
Wave 0: Scaffoldar testes RED e contratos de tipos/schema antes de qualquer implementação de engine. Per D-01 a D-10.

Propósito: Garantia Nyquist — nenhum código de ledger existe antes dos testes. Os contratos TypeScript (commands.ts) definem a interface que o engine da Wave 1 implementará, evitando renegociação de tipos durante execução.

Saída: schema ledger Drizzle, função pura calculateFirstfruits, tipos Zod, e três arquivos de teste (RED) cobrindo todos os requisitos LEDG-01 a LEDG-08.
</objective>

## Phase Goal

**As a** family using Kreds, **I want to** trust that every Kreds movement is recorded accurately and permanently, **so that** balance changes are always explainable and correctable without historical edits.

<execution_context>
@/Users/hass/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-kreds-engine-ledger-and-audit-foundation/03-CONTEXT.md
@.planning/phases/03-kreds-engine-ledger-and-audit-foundation/03-RESEARCH.md
@.planning/phases/03-kreds-engine-ledger-and-audit-foundation/03-PATTERNS.md
@src/lib/db/schema/index.ts
@src/lib/db/index.ts
@src/modules/glossary/terms.ts
@tests/unit/glossary.test.ts
@tests/integration/family-tenancy.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema Drizzle do Ledger + tipos de comando (contratos)</name>

  <read_first>
    src/lib/db/schema/index.ts — ver padrão de pgEnum, pgTable, uuid, integer, text, timestamp, index, uniqueIndex, check, e as tabelas existentes families/childProfiles/identities que o ledger referencia via FK
    src/modules/glossary/terms.ts — ver termos existentes (KREDS, FIRSTFRUITS, etc.) para não duplicar e saber quais adicionar
    src/lib/db/index.ts — padrão de import do db e uso de schema
  </read_first>

  <behavior>
    - calculateFirstfruits(1) → 1 (Math.ceil(0.10) = 1, per D-01)
    - calculateFirstfruits(7) → 1 (Math.ceil(0.70) = 1, per D-01)
    - calculateFirstfruits(10) → 1 (Math.ceil(1.0) = 1, per D-01)
    - calculateFirstfruits(11) → 2 (Math.ceil(1.1) = 2, per D-01)
    - calculateFirstfruits(100) → 10 (Math.ceil(10.0) = 10, per D-01)
    - calculateFirstfruits(3.5) → throws Error (LEDG-02: somente inteiros)
    - calculateFirstfruits(0) → throws Error (somente positivos)
    - calculateFirstfruits(-5) → throws Error (somente positivos)
    - ledger_transactions tem UNIQUE constraint em command_id (LEDG-06, D-06)
    - ledger_lines tem check non_zero_amount — amount != 0 (LEDG-02)
    - Nenhum updatedAt em ledger_transactions ou ledger_lines (LEDG-01: append-only)
    - transactionTypeEnum inclui: task_earning, negative_adjustment, reversal, donation_match
    - accountTypeEnum inclui: available, firstfruits
    - EarningCommandSchema: commandId uuid, familyId uuid, childProfileId uuid, guardianIdentityId uuid, amount int positivo, note opcional max 500
    - AdjustmentCommandSchema: adiciona reason min(1) max(500) obrigatório (D-07), restorationNote opcional
    - ReversalCommandSchema: adiciona correctsTransactionId uuid obrigatório, correctionNote min(1) max(500) obrigatório (D-08)
  </behavior>

  <files>
    src/lib/db/schema/ledger.ts
    src/lib/db/schema/index.ts
    src/modules/ledger/calculate.ts
    src/modules/ledger/commands.ts
    src/modules/glossary/terms.ts
    tests/unit/ledger-calculate.test.ts
  </files>

  <action>
    Criar src/lib/db/schema/ledger.ts seguindo padrão exato de src/lib/db/schema/index.ts.

    Imports: pgTable, pgEnum, uuid, integer, text, timestamp, index, uniqueIndex, check de 'drizzle-orm/pg-core'; sql de 'drizzle-orm'; families, childProfiles, identities de './index'.

    Definir transactionTypeEnum('transaction_type') com valores ['task_earning', 'negative_adjustment', 'reversal', 'donation_match']. Incluir 'donation_match' agora para evitar migration ALTER TYPE na Fase 7 (assumption A-04 do RESEARCH.md).

    Definir accountTypeEnum('account_type') com valores ['available', 'firstfruits'].

    Definir ledgerTransactions = pgTable('ledger_transactions', { id: uuid defaultRandom primaryKey, familyId: uuid notNull references families.id, childProfileId: uuid notNull references childProfiles.id, commandId: uuid notNull (NÃO defaultRandom — deve ser provido pelo caller, per pitfall 4 do RESEARCH.md), transactionType: transactionTypeEnum notNull, initiatedByIdentityId: uuid nullable references identities.id, correctsTransactionId: uuid nullable (FK lógico — sem self-loop via CHECK), note: text nullable, createdAt: timestamp defaultNow notNull. SEM updatedAt por design append-only (LEDG-01) }, (table) => ({ commandIdUnique: uniqueIndex('ledger_transactions_command_id_unique').on(table.commandId), childIdIdx: index('ledger_transactions_child_profile_id_idx').on(table.childProfileId), familyIdIdx: index('ledger_transactions_family_id_idx').on(table.familyId), selfReferenceCheck: check('no_self_correction', sql`${table.correctsTransactionId} IS NULL OR ${table.correctsTransactionId} != ${table.id}`) })).

    Definir ledgerLines = pgTable('ledger_lines', { id: uuid defaultRandom primaryKey, transactionId: uuid notNull references ledgerTransactions.id, childProfileId: uuid notNull references childProfiles.id, accountType: accountTypeEnum notNull, amount: integer notNull (pode ser negativo para débitos), createdAt: timestamp defaultNow notNull. SEM updatedAt (LEDG-01) }, (table) => ({ transactionIdIdx: index('ledger_lines_transaction_id_idx').on(table.transactionId), childIdIdx: index('ledger_lines_child_profile_id_idx').on(table.childProfileId), nonZeroCheck: check('non_zero_amount', sql`${table.amount} != 0`) })).

    Adicionar no final de src/lib/db/schema/index.ts: export * from './ledger' — sem modificar nenhuma linha existente.

    Criar src/modules/ledger/calculate.ts como módulo puro (sem imports externos além de nada — zero dependências): exportar FIRSTFRUITS_RATE = 0.10 as const. Exportar calculateFirstfruits(amount: number): number que lança Error('amount must be a positive integer') se !Number.isInteger(amount) || amount <= 0, e retorna Math.ceil(amount * FIRSTFRUITS_RATE).

    Criar src/modules/ledger/commands.ts com import { z } from 'zod'. Exportar EarningCommandSchema, AdjustmentCommandSchema, ReversalCommandSchema conforme especificado na seção behavior. Os tipos TypeScript são inferidos via z.infer<>. Note que commandId é z.string().uuid() — deve vir do caller (pitfall 4 RESEARCH.md, assumption A-02).

    Em src/modules/glossary/terms.ts: adicionar dentro do objeto TERMS existente, APÓS LEDGER_TRANSACTION, os termos: AVAILABLE_BALANCE: 'Available Balance', FIRSTFRUITS_BALANCE: 'Firstfruits Balance', REVERSAL: 'Reversal', ADJUSTMENT_REASON: 'Adjustment Reason', CORRECTION_NOTE: 'Correction Note'. NÃO duplicar KREDS, FIRSTFRUITS, FIRSTFRUITS_TREASURY, KREDS_ENGINE, NEGATIVE_ADJUSTMENT, LEDGER_TRANSACTION que já existem.

    Criar tests/unit/ledger-calculate.test.ts seguindo padrão de tests/unit/glossary.test.ts: imports { describe, it, expect } from 'vitest' + calculateFirstfruits/FIRSTFRUITS_RATE de '../../src/modules/ledger/calculate'. Implementar todos os casos da seção behavior incluindo os casos de erro (toThrow). Esta fase é GREEN imediato pois calculate.ts é criado junto.
  </action>

  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && pnpm test tests/unit/ledger-calculate.test.ts --run 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - tests/unit/ledger-calculate.test.ts roda com todos os 8 casos passando (GREEN)
    - pnpm test tests/unit/ledger-calculate.test.ts --run exibe "8 passed" ou equivalente sem nenhum "failed"
    - src/lib/db/schema/ledger.ts contém a string "command_id_unique" (uniqueIndex)
    - src/lib/db/schema/ledger.ts contém a string "non_zero_amount" (check)
    - src/lib/db/schema/ledger.ts NÃO contém "updatedAt" (append-only, LEDG-01)
    - src/lib/db/schema/index.ts contém a linha "export * from './ledger'" ao final
    - src/modules/ledger/commands.ts exporta EarningCommandSchema, AdjustmentCommandSchema, ReversalCommandSchema (grep -c "export const.*Schema" deve retornar 3)
    - AdjustmentCommandSchema contém "reason" com min(1) — confirmar via grep "reason.*min"
    - ReversalCommandSchema contém "correctsTransactionId" — confirmar via grep
    - src/modules/glossary/terms.ts contém AVAILABLE_BALANCE, CORRECTION_NOTE (grep -c "AVAILABLE_BALANCE\|CORRECTION_NOTE" retorna 2)
    - src/modules/glossary/terms.ts NÃO duplica KREDS ou FIRSTFRUITS existentes (apenas 1 ocorrência de cada)
    - FIRSTFRUITS_RATE = 0.10 exportado de calculate.ts (grep -c "FIRSTFRUITS_RATE" retorna >= 1)
    - TypeScript compila sem erros: pnpm exec tsc --noEmit retorna 0
  </acceptance_criteria>

  <done>Schema ledger Drizzle criado e re-exportado. calculateFirstfruits pura com todos os edge cases validados. Tipos de comando Zod completos. Termos de glossário adicionados sem duplicação. Testes de calculate passando GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes RED para engine (integração) e queries (unidade com mock)</name>

  <read_first>
    tests/integration/family-tenancy.test.ts — padrão exato de beforeAll/afterAll Testcontainers, PostgreSqlContainer, drizzle, migrate; copiar integralmente
    tests/unit/family-authorization.test.ts — padrão de testes de unidade para funções ainda não criadas (RED phase)
    src/lib/db/schema/ledger.ts — schema recém-criado na Task 1 para referenciar tabelas corretas
    src/modules/ledger/commands.ts — tipos EarningCommand, AdjustmentCommand, ReversalCommand para estruturar dados de teste
    .planning/phases/03-kreds-engine-ledger-and-audit-foundation/03-RESEARCH.md — Pitfall 5 (Docker/Podman), Validation Architecture (Wave 0 gaps)
  </read_first>

  <behavior>
    Testes de integração (ledger-engine.test.ts) — todos RED no início (módulos ainda não existem):
    - postEarning(amount=10) cria 1 header + 2 ledger lines (1 available +9, 1 firstfruits +1) atomicamente (LEDG-04, D-05)
    - postEarning dois vezes com mesmo commandId lança erro com code '23505' (LEDG-06, D-06)
    - postNegativeAdjustment(amount=5, reason='test') cria linha -5 em available (LEDG-05, D-07)
    - postReversal(correctsTransactionId=X) cria entradas negativas; linha original permanece inalterada (LEDG-08, D-08)
    - Nenhuma linha de ledger pode ser UPDATE-ada após insert (LEDG-01)

    Testes de unidade de queries (ledger-queries.test.ts) — RED pois queries.ts ainda não existe:
    - getChildLedgerHistory NÃO retorna campos commandId nem note (D-09, D-10, LEDG-07)
    - getGuardianLedgerHistory RETORNA commandId, note, correctsTransactionId (D-09, LEDG-07)
    - getBalance(childProfileId, 'available') retorna SUM correto (D-03, LEDG-04)
  </behavior>

  <files>
    tests/integration/ledger-engine.test.ts
    tests/unit/ledger-queries.test.ts
  </files>

  <action>
    Criar tests/integration/ledger-engine.test.ts copiando o padrão de beforeAll/afterAll de tests/integration/family-tenancy.test.ts integralmente: PostgreSqlContainer('postgres:18-alpine'), Pool, drizzle, migrate('./drizzle'), timeout 60000. Substituir import de schema para import * as schema from '../../src/lib/db/schema'.

    Estruturar os test cases como RED (importar de módulos que não existem ainda: '../../src/modules/ledger/engine'). Os imports de postEarning, postNegativeAdjustment, postReversal devem ser escritos como imports normais — o arquivo compilará com erro TS até a Wave 1 criar engine.ts, mas vitest com allowImportingTsExtensions ou ts-node pode tentar rodar. Usar vi.mock não é necessário — o teste RED falhará na importação.

    ALTERNATIVA para RED limpo: criar engine.ts stub vazio que exporta as funções lançando Error('not implemented') e queries.ts stub similar. Isso garante que os testes rodam (RED = assertions failing, não import error). Este é o padrão mais limpo de TDD.

    Para tests/integration/ledger-engine.test.ts: descrever suites describe('postEarning', ...), describe('idempotência command_id', ...), describe('postNegativeAdjustment', ...), describe('postReversal', ...). Cada it() faz INSERT real via Testcontainers e verifica com SELECT o estado resultante. Incluir verificação explícita que UPDATE em ledger_lines lança constraint (testar append-only via tentativa de update).

    Para tests/unit/ledger-queries.test.ts: importar getChildLedgerHistory, getGuardianLedgerHistory, getBalance de '../../src/modules/ledger/queries'. Com stubs esses testes falharão (not implemented). Os it() verificam ausência de campos via Object.keys() ou verificação de tipo retornado.

    Criar src/modules/ledger/engine.ts stub: import 'server-only'; export async function postEarning(): Promise<never> { throw new Error('not implemented') }; mesma estrutura para postNegativeAdjustment e postReversal.

    Criar src/modules/ledger/queries.ts stub: export async function getBalance(): Promise<number> { throw new Error('not implemented') }; mesma estrutura para getGuardianLedgerHistory e getChildLedgerHistory.

    Limitação Testcontainers/Podman: documentar comentário no topo do arquivo de integração: "// Requer Docker daemon. Com Podman: kubectl port-forward svc/postgres 5432:5432 -n kreds e definir DATABASE_URL antes de rodar." (STATE.md decision, pitfall 5 RESEARCH.md).
  </action>

  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && pnpm test tests/unit/ledger-queries.test.ts --run 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - tests/integration/ledger-engine.test.ts existe e contém os describes: "postEarning", "idempotência" ou "command_id", "postNegativeAdjustment", "postReversal" (grep -c "describe(" deve retornar >= 4)
    - tests/unit/ledger-queries.test.ts existe e contém getChildLedgerHistory e getGuardianLedgerHistory (grep verifica presença)
    - tests/unit/ledger-queries.test.ts roda e reporta falhas (RED) ou passes dependendo se stubs estão implementados — não deve ter erro de importação
    - src/modules/ledger/engine.ts existe e exporta postEarning, postNegativeAdjustment, postReversal (grep -c "export async function" retorna 3)
    - src/modules/ledger/queries.ts existe e exporta getBalance, getGuardianLedgerHistory, getChildLedgerHistory (grep -c "export async function" retorna 3)
    - engine.ts contém "import 'server-only'" como primeira linha não-comentada
    - pnpm exec tsc --noEmit retorna 0 (sem erros TypeScript em todos os arquivos criados)
    - Comentário sobre Testcontainers/Docker está presente no topo de ledger-engine.test.ts (grep "Testcontainers\|Docker daemon\|port-forward")
  </acceptance_criteria>

  <done>Testes RED existem para todos os requisitos LEDG-01 a LEDG-08. Stubs de engine e queries criados para permitir compilação TS. Scaffolding Nyquist completo — implementação real inicia na Wave 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema/index.ts → schema/ledger.ts | Tabelas de ledger referenciam childProfiles e families da Fase 2; FK viola isolamento se family_id não for sempre filtrado |
| commands.ts → engine.ts (futuro) | commandId deve vir do caller — se gerado no server, idempotência end-to-end falha (pitfall 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | ledger_transactions.commandId | mitigate | UNIQUE constraint DB-level + tratamento 409 na API — Wave 1 |
| T-03-02 | Tampering | ledger_lines.amount | mitigate | check('non_zero_amount') + Drizzle integer() — implementado neste plano |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase — todos aprovados Fases 1 e 2 |
</threat_model>

<verification>
- pnpm test tests/unit/ledger-calculate.test.ts --run: 8 passed
- pnpm exec tsc --noEmit: 0 errors
- grep -c "export \* from './ledger'" src/lib/db/schema/index.ts retorna 1
- grep -c "command_id_unique" src/lib/db/schema/ledger.ts retorna >= 1
- grep -c "non_zero_amount" src/lib/db/schema/ledger.ts retorna >= 1
- grep "updatedAt" src/lib/db/schema/ledger.ts retorna vazio (append-only)
</verification>

<success_criteria>
Wave 0 completa quando:
1. calculateFirstfruits passa todos os 8 edge cases (incluindo throws para float/zero/negativo)
2. Schema ledger_transactions e ledger_lines existem com constraints corretas
3. Tipos EarningCommand, AdjustmentCommand, ReversalCommand exportados de commands.ts
4. Testes de integração e unidade existem (RED ou GREEN via stubs)
5. TypeScript compila sem erros em toda a base
6. Glossário atualizado com 5 novos termos sem duplicação
</success_criteria>

<output>
Criar `.planning/phases/03-kreds-engine-ledger-and-audit-foundation/03-01-SUMMARY.md` quando concluído.
</output>

## Artifacts this phase produces

| Symbol | Type | File | Purpose |
|--------|------|------|---------|
| `transactionTypeEnum` | Drizzle pgEnum | src/lib/db/schema/ledger.ts | Enum PostgreSQL para tipos de transação |
| `accountTypeEnum` | Drizzle pgEnum | src/lib/db/schema/ledger.ts | Enum PostgreSQL: 'available' \| 'firstfruits' |
| `ledgerTransactions` | Drizzle pgTable | src/lib/db/schema/ledger.ts | Tabela append-only de headers de transação com UNIQUE command_id |
| `ledgerLines` | Drizzle pgTable | src/lib/db/schema/ledger.ts | Tabela append-only de linhas de ledger com check non_zero |
| `FIRSTFRUITS_RATE` | const | src/modules/ledger/calculate.ts | 0.10 — taxa de dízimo |
| `calculateFirstfruits` | function | src/modules/ledger/calculate.ts | Math.ceil(amount * 0.10) com validação de inteiro positivo |
| `EarningCommandSchema` | Zod schema | src/modules/ledger/commands.ts | Validação de comando de earning |
| `AdjustmentCommandSchema` | Zod schema | src/modules/ledger/commands.ts | Validação de ajuste negativo com reason obrigatório |
| `ReversalCommandSchema` | Zod schema | src/modules/ledger/commands.ts | Validação de reversal com correctsTransactionId obrigatório |
| `EarningCommand` | TypeScript type | src/modules/ledger/commands.ts | Tipo inferido de EarningCommandSchema |
| `AdjustmentCommand` | TypeScript type | src/modules/ledger/commands.ts | Tipo inferido de AdjustmentCommandSchema |
| `ReversalCommand` | TypeScript type | src/modules/ledger/commands.ts | Tipo inferido de ReversalCommandSchema |
| `postEarning` (stub) | async function | src/modules/ledger/engine.ts | Stub — implementado na Wave 1 |
| `postNegativeAdjustment` (stub) | async function | src/modules/ledger/engine.ts | Stub — implementado na Wave 2 |
| `postReversal` (stub) | async function | src/modules/ledger/engine.ts | Stub — implementado na Wave 2 |
| `getBalance` (stub) | async function | src/modules/ledger/queries.ts | Stub — implementado na Wave 1 |
| `getGuardianLedgerHistory` (stub) | async function | src/modules/ledger/queries.ts | Stub — implementado na Wave 2 |
| `getChildLedgerHistory` (stub) | async function | src/modules/ledger/queries.ts | Stub — implementado na Wave 2 |
| New migration | SQL file | drizzle/ | Gerado por pnpm db:generate após Task 1 |
</output>
