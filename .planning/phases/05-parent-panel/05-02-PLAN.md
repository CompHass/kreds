---
phase: 05-parent-panel
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/parent/parent-sidebar.tsx
  - src/components/parent/parent-topbar.tsx
  - src/components/parent/filter-chips.tsx
  - src/components/parent/category-icon.tsx
  - src/components/parent/task-toggle.tsx
  - src/components/parent/parent-task-card.tsx
autonomous: true
requirements: [PTASK-01, PTASK-02, PTASK-03, PTASK-04, PTASK-05, PTASK-09]
must_haves:
  truths:
    - "Sidebar 80px, topbar 64px com breadcrumb verde e badge de usuário renderizam"
    - "Filter chips mostram Todas + um chip por criança; selecionado fica verde"
    - "Task card mostra ícone de categoria colorido, toggle 42×24px e botão lápis; toggle é independente do lápis"
    - "Card recém-adicionado recebe a animação kredsNew quando justAdded é true"
  artifacts:
    - path: "src/components/parent/parent-sidebar.tsx"
      provides: "Sidebar fixa 80px com nav icons SVG inline"
      exports: ["ParentSidebar"]
    - path: "src/components/parent/parent-topbar.tsx"
      provides: "Topbar 64px breadcrumb + badge usuário"
      exports: ["ParentTopbar"]
    - path: "src/components/parent/filter-chips.tsx"
      provides: "Chips de filtro por criança"
      exports: ["FilterChips"]
    - path: "src/components/parent/category-icon.tsx"
      provides: "Ícone SVG por categoria com cor"
      exports: ["CategoryIcon"]
    - path: "src/components/parent/task-toggle.tsx"
      provides: "Switch 42×24px ativo/inativo"
      exports: ["TaskToggle"]
    - path: "src/components/parent/parent-task-card.tsx"
      provides: "Card de tarefa com categoria, toggle, lápis e flash kredsNew"
      exports: ["ParentTaskCard"]
  key_links:
    - from: "src/components/parent/parent-task-card.tsx"
      to: "var(--animate-kreds-new)"
      via: "prop justAdded"
      pattern: "justAdded \\? 'var\\(--animate-kreds-new\\)'"
    - from: "src/components/parent/parent-task-card.tsx"
      to: "src/components/parent/category-icon.tsx"
      via: "import CategoryIcon"
      pattern: "CategoryIcon"
---

<objective>
Construir os componentes de apresentação do shell desktop e da lista de tarefas: `ParentSidebar` (80px), `ParentTopbar` (64px), `FilterChips`, `CategoryIcon`, `TaskToggle` (42×24px) e `ParentTaskCard`. Todos são componentes "burros" que recebem props + callbacks; nenhum gerencia estado de lista (isso fica no `ParentPanelView` do Plano 03).

Purpose: Isolar a camada visual da lista para permitir execução paralela com o form panel (Plano 03). Estes componentes satisfazem PTASK-01..05 e o flash kredsNew (PTASK-09) no nível do card.
Output: 6 componentes em `src/components/parent/`.
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-parent-panel/05-CONTEXT.md
@.planning/phases/05-parent-panel/05-PATTERNS.md
@.planning/phases/05-parent-panel/05-UI-SPEC.md
@src/components/garden/garden-header.tsx
@src/components/tasks/task-card.tsx
@src/components/tasks/bottom-nav.tsx
@src/lib/seed/parent-seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ParentSidebar + ParentTopbar (PTASK-01, PTASK-02)</name>
  <files>src/components/parent/parent-sidebar.tsx, src/components/parent/parent-topbar.tsx</files>
  <read_first>
    - src/components/tasks/bottom-nav.tsx (padrão de nav fixa com SVG inline + aria-label por ícone)
    - src/components/garden/garden-header.tsx (padrão de header com dados do usuário + avatar com inicial)
    - .planning/phases/05-parent-panel/05-UI-SPEC.md (Component Inventory §Layout linhas 132-136; Color §Accent items 7 e 8; Typography breadcrumb linha 72; Spacing linhas 47-53; Accessibility Notes linhas 376-383)
    - .planning/phases/05-parent-panel/05-PATTERNS.md (ParentSidebar linhas 343-370; ParentTopbar linhas 374-424)
  </read_first>
  <action>
    Criar dois Client Components (`'use client'`).
    `ParentSidebar` (src/components/parent/parent-sidebar.tsx), export nomeado `ParentSidebar`, sem props: `<aside>` largura fixa 80px, minHeight 100vh, background `var(--color-kreds-card)`, borderRight `1px solid var(--color-kreds-border)`, flex column, alignItems center, padding '16px 0', `flexShrink: 0`. Conteúdo: logo 40×40px no topo (reutilizar o SVG do logo de access/[familyId]/page.tsx, gradiente verde), seguido de 5 nav icons SVG inline 44×44px (Claude's Discretion para os ícones exatos — ex.: tarefas, crianças, jardim, relatórios, config), e um avatar 38px circular no rodapé (usar `marginTop: 'auto'`). Cada nav icon é um `<button>` com `aria-label` obrigatório (sem texto visível). O primeiro ícone (tarefas) é o ativo: bg `#E7EFE8`, stroke `#3E6B4F` (Accent item 8 do UI-SPEC).
    `ParentTopbar` (src/components/parent/parent-topbar.tsx), export nomeado `ParentTopbar`, props `{ familyName: string; currentUserName: string }`: `<header>` height 64px, borderBottom `1px solid var(--color-kreds-border)`, background `var(--color-kreds-card)`, flex row alignItems center, padding '0 24px', `flexShrink: 0`. Esquerda: breadcrumb com o `familyName` em verde (`color: 'var(--color-kreds-primary)'`, fontWeight 700, fontSize 15). Spacer flex:1. Direita: badge do usuário — pill com bg branco, border `#ECE7DB`, contendo o `currentUserName` (fontSize 13, muted) + avatar circular 32px (bg verde, inicial maiúscula do nome em branco).
  </action>
  <verify>
    <automated>grep -q "export function ParentSidebar" src/components/parent/parent-sidebar.tsx && grep -q "aria-label" src/components/parent/parent-sidebar.tsx && grep -q "width: 80" src/components/parent/parent-sidebar.tsx && grep -q "export function ParentTopbar" src/components/parent/parent-topbar.tsx && grep -q "height: 64" src/components/parent/parent-topbar.tsx && grep -q "var(--color-kreds-primary)" src/components/parent/parent-topbar.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - parent-sidebar.tsx exporta `ParentSidebar`, contém `width: 80`, ≥5 botões de nav cada um com `aria-label`
    - parent-topbar.tsx exporta `ParentTopbar` com props `familyName` e `currentUserName`, contém `height: 64`
    - O breadcrumb usa `var(--color-kreds-primary)` (família em verde)
    - Badge do usuário renderiza `currentUserName` e a inicial no avatar
    - `npx tsc --noEmit` sem erro nos dois arquivos
  </acceptance_criteria>
  <done>ParentSidebar (80px) e ParentTopbar (64px, breadcrumb verde + badge) existem e compilam.</done>
</task>

<task type="auto">
  <name>Task 2: FilterChips + CategoryIcon + TaskToggle (PTASK-03, PTASK-05, PTASK-04)</name>
  <files>src/components/parent/filter-chips.tsx, src/components/parent/category-icon.tsx, src/components/parent/task-toggle.tsx</files>
  <read_first>
    - src/components/tasks/task-card.tsx (padrão de cor por estado selecionado/não; estrutura de switch/check button)
    - src/lib/seed/parent-seed.ts (CATEGORY_META com label/color/softBg por categoria; type Category — criado no Plano 05-01)
    - .planning/phases/05-parent-panel/05-UI-SPEC.md (FilterChips interação linhas 194-198; TaskToggle linhas 179-183; Tabela de Categorias linhas 101-109; Accessibility Notes linhas 376-383; Component Inventory linhas 138-142)
    - .planning/phases/05-parent-panel/05-PATTERNS.md (FilterChips linhas 428-472; CategoryIcon linhas 476-511)
  </read_first>
  <action>
    Criar três Client Components.
    `FilterChips` (src/components/parent/filter-chips.tsx), export `FilterChips`, props `{ familyChildren: Array<{ id: string; displayName: string; accentColor: string; avatarPreset: string }>; active: 'all' | string; onChange: (filter: 'all' | string) => void }`. Renderiza `<div>` flex row gap 8 wrap. Lista de chips: primeiro `{ id: 'all', displayName: 'Todas' }`, depois um por criança. Cada chip é `<button>` com `aria-pressed={isActive}`. Chip ativo (`active === chip.id`): bg `#3E6B4F`, texto branco, border `#3E6B4F`. Chip inativo: bg `var(--color-kreds-card)` (#FBFAF5), border `#E2DECF`, texto `var(--color-kreds-text)`. Para chips de criança, incluir mini avatar 24px (inicial do displayName sobre fundo accentColor — Claude's Discretion conforme D, A4 do RESEARCH). borderRadius `var(--radius-pill)`, padding '6px 14px', fontSize 13, fontWeight 600. Apenas um ativo por vez (controlado pelo pai).
    `CategoryIcon` (src/components/parent/category-icon.tsx), export `CategoryIcon`, props `{ category: Category; size?: number }` (importar `Category` de `@/lib/seed/parent-seed`). Usa `CATEGORY_META[category]` para color e softBg. Renderiza um container quadrado (default size 44px) borderRadius 13px, background = softBg da categoria, contendo um SVG inline 44×44px específico por categoria com stroke = color da categoria. Definir um SVG distinto por categoria (Claude's Discretion no traçado; quarto/higiene/estudos/casa/espiritual) seguindo o padrão de SVG inline do projeto. SVG com `aria-hidden="true"`.
    `TaskToggle` (src/components/parent/task-toggle.tsx), export `TaskToggle`, props `{ checked: boolean; onChange: () => void; label: string }`. `<button role="switch" aria-checked={checked} aria-label={label}>`: track 42×24px borderRadius pill, bg `#3E6B4F` quando checked / `#D7DBCC` quando não; knob 18×18px branco com `position: absolute` e `left: 20` (on) / `left: 3` (off), transição `left .2s ease`. Este componente é reutilizado tanto pelo toggle do card quanto pelo ApprovalToggle do form (Plano 03).
  </action>
  <verify>
    <automated>grep -q "export function FilterChips" src/components/parent/filter-chips.tsx && grep -q "aria-pressed" src/components/parent/filter-chips.tsx && grep -q "Todas" src/components/parent/filter-chips.tsx && grep -q "export function CategoryIcon" src/components/parent/category-icon.tsx && grep -q "CATEGORY_META" src/components/parent/category-icon.tsx && grep -q 'role="switch"' src/components/parent/task-toggle.tsx && grep -q "aria-checked" src/components/parent/task-toggle.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - filter-chips.tsx exporta `FilterChips`, renderiza chip "Todas" + `aria-pressed` por chip; chip ativo usa `#3E6B4F`
    - category-icon.tsx exporta `CategoryIcon`, lê cor/softBg de `CATEGORY_META[category]`, SVG com `aria-hidden`
    - task-toggle.tsx exporta `TaskToggle` com `role="switch"`, `aria-checked`, track `42`×`24` e knob com transição `left .2s`
    - `npx tsc --noEmit` sem erro nos três arquivos
  </acceptance_criteria>
  <done>FilterChips (chips por criança), CategoryIcon (5 cores) e TaskToggle (switch 42×24px acessível) existem e compilam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ParentTaskCard com flash kredsNew (PTASK-04, PTASK-05, PTASK-09)</name>
  <files>src/components/parent/parent-task-card.tsx</files>
  <read_first>
    - src/components/tasks/task-card.tsx (estrutura completa do card + padrão `animation: ... 'var(--animate-kreds-new)' : undefined` na linha 36)
    - src/lib/seed/parent-seed.ts (interface ParentTask, CATEGORY_META, rewardLabel)
    - src/components/parent/category-icon.tsx (assinatura CategoryIcon — criado na Task 2 deste plano)
    - src/components/parent/task-toggle.tsx (assinatura TaskToggle — criado na Task 2 deste plano)
    - .planning/phases/05-parent-panel/05-UI-SPEC.md (ParentTaskCard linhas 139-142; EditButton linha 142; Interaction States linhas 179-188; Animations linhas 162-173)
    - .planning/phases/05-parent-panel/05-PATTERNS.md (ParentTaskCard linhas 240-287; padrão kredsNew linhas 631-637)
  </read_first>
  <behavior>
    - Renderiza título, CategoryIcon da categoria, TaskToggle refletindo task.active e botão lápis (EditButton).
    - Quando justAdded é true, o card aplica `animation` com `var(--animate-kreds-new)`.
    - Clicar no toggle chama onToggle(task.id); clicar no lápis chama onEdit(task.id); são independentes.
    - Card inativo (task.active === false): opacity 0.5.
    - Card em edição (editing === true): bg `#F4F8F2`, border `#3E6B4F`; botão lápis fica ativo (border+stroke verde).
  </behavior>
  <action>
    Criar `ParentTaskCard` (src/components/parent/parent-task-card.tsx), Client Component, export `ParentTaskCard`, props `{ task: ParentTask; justAdded: boolean; editing: boolean; onToggle: (id: string) => void; onEdit: (id: string) => void }`. Importar `ParentTask` e `rewardLabel` de `@/lib/seed/parent-seed`, `CategoryIcon` de `./category-icon`, `TaskToggle` de `./task-toggle`. Container `<div>` flex row alignItems center gap 12, padding '12px 16px', borderRadius 16px, border `1.5px solid` (verde `#3E6B4F` se editing, senão `var(--color-kreds-border)`), background (`#F4F8F2` se editing, senão branco `#FBFAF5`), `opacity: task.active ? 1 : 0.5`, e `animation: justAdded ? 'var(--animate-kreds-new)' : undefined` (PTASK-09 / D-10 — copiar exatamente o padrão da linha 36 do task-card.tsx, trocando `task.done` por `justAdded`). Layout: `<CategoryIcon category={task.category} />` à esquerda; conteúdo central com título (fontSize 15, fontWeight 600) + linha de badges (recompensa via `rewardLabel(task.reward)` e dias); ações à direita — EditButton (botão lápis 32×32px borderRadius 10px; estado normal border `#E2DECF` bg `#FBFAF5` stroke `#7C8676`; estado editing border `#3E6B4F` bg `#EEF3EA` stroke `#3E6B4F`; `aria-label={`Editar tarefa: ${task.title}`}`, `aria-pressed={editing}`; onClick → onEdit(task.id) — trigger de edição D-08) e `<TaskToggle checked={task.active} onChange={() => onToggle(task.id)} label={`Ativar/desativar tarefa: ${task.title}`} />`. O toggle e o lápis NÃO devem compartilhar handler — clicar no toggle não dispara onEdit.
  </action>
  <verify>
    <automated>grep -q "export function ParentTaskCard" src/components/parent/parent-task-card.tsx && grep -q "justAdded ? 'var(--animate-kreds-new)'" src/components/parent/parent-task-card.tsx && grep -q "CategoryIcon" src/components/parent/parent-task-card.tsx && grep -q "TaskToggle" src/components/parent/parent-task-card.tsx && grep -q "rewardLabel" src/components/parent/parent-task-card.tsx && grep -q "Editar tarefa" src/components/parent/parent-task-card.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - parent-task-card.tsx exporta `ParentTaskCard` com props task/justAdded/editing/onToggle/onEdit
    - Contém `justAdded ? 'var(--animate-kreds-new)' : undefined` (PTASK-09)
    - Renderiza `CategoryIcon`, `TaskToggle` e botão de editar com `aria-label` contendo "Editar tarefa"
    - Card inativo aplica `opacity` 0.5; card editing usa border `#3E6B4F`
    - `npx tsc --noEmit` sem erro
  </acceptance_criteria>
  <done>ParentTaskCard exibe categoria, toggle independente e lápis; aplica kredsNew quando justAdded; compila.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 05-02)

Componentes novos (Client Components) em src/components/parent/ — excluir da verificação de drift:
- `ParentSidebar` (parent-sidebar.tsx)
- `ParentTopbar` (parent-topbar.tsx) — props { familyName, currentUserName }
- `FilterChips` (filter-chips.tsx) — props { familyChildren, active, onChange }
- `CategoryIcon` (category-icon.tsx) — props { category, size? }
- `TaskToggle` (task-toggle.tsx) — props { checked, onChange, label }
- `ParentTaskCard` (parent-task-card.tsx) — props { task, justAdded, editing, onToggle, onEdit }
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props (pai → componente) | dados já validados no ParentPanelView; componentes apenas renderizam |
| render de task.title | conteúdo textual exibido via JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | render de task.title (XSS) | mitigate | Renderizar título via JSX text node (React escapa automaticamente); proibido `dangerouslySetInnerHTML` em qualquer componente parent/* |
| T-05-04 | Information Disclosure | aria-label expondo título de tarefa | accept | Títulos de tarefa não são dados sensíveis; aria-label necessário para acessibilidade (PTASK-04) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo instalado (RESEARCH §Package Legitimacy: zero novas instalações) |
</threat_model>

<verification>
- `npx tsc --noEmit` sem erros nos 6 componentes
- grep confirma exports e tokens-chave (kredsNew, role=switch, breadcrumb verde)
- `pnpm lint` sem erros novos nos arquivos parent/*
</verification>

<success_criteria>
- 6 componentes criados em src/components/parent/, todos compilando
- Toggle 42×24px acessível e independente do lápis (PTASK-04)
- 5 categorias com cores distintas via CategoryIcon (PTASK-05)
- Flash kredsNew aplicado no card via prop justAdded (PTASK-09)
</success_criteria>

<output>
Create `.planning/phases/05-parent-panel/05-02-SUMMARY.md` when done
</output>
