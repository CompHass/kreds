---
phase: 05-parent-panel
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/components/parent/parent-panel-view.tsx
  - src/app/family/[familyId]/tasks/page.tsx
autonomous: false
requirements: [PTASK-01, PTASK-02, PTASK-03, PTASK-04, PTASK-06, PTASK-07, PTASK-08, PTASK-09, PTASK-10]
must_haves:
  truths:
    - "A rota /family/[familyId]/tasks renderiza via SSR com auth() e redireciona para /login sem sessão"
    - "ParentPanelView orquestra sidebar + topbar + filter chips + lista de cards + form panel em layout 3-colunas"
    - "Criar uma tarefa adiciona o card e dispara o flash kredsNew; editar atualiza; excluir remove; toggle ativa/desativa — tudo otimista local"
    - "A suite parent-panel.test.tsx passa em GREEN (PTASK-01..10)"
  artifacts:
    - path: "src/components/parent/parent-panel-view.tsx"
      provides: "Client Component raiz com todo o estado do CRUD"
      exports: ["ParentPanelView"]
    - path: "src/app/family/[familyId]/tasks/page.tsx"
      provides: "Server Component SSR com auth + query childProfiles"
      exports: ["default"]
  key_links:
    - from: "src/app/family/[familyId]/tasks/page.tsx"
      to: "auth()"
      via: "verificação de sessão SSR + redirect"
      pattern: "if \\(!session\\) redirect"
    - from: "src/app/family/[familyId]/tasks/page.tsx"
      to: "ParentPanelView"
      via: "render com props (familyChildren, initialTasks)"
      pattern: "ParentPanelView"
    - from: "src/components/parent/parent-panel-view.tsx"
      to: "newTaskId"
      via: "flashNew setTimeout 1200ms"
      pattern: "setNewTaskId"
---

<objective>
Integrar todos os componentes da Fase 5 no `ParentPanelView` (Client Component raiz que gerencia o estado do CRUD com `useState`, padrão `GardenView`) e criar a rota SSR `src/app/family/[familyId]/tasks/page.tsx` (auth + query de childProfiles, passando o seed mock). Levar a suite `parent-panel.test.tsx` para GREEN. Inclui checkpoint visual humano ao final.

Purpose: Esta é a costura final — a UI vira uma tela funcional navegável. Implementa as mutações otimistas (D-09), o flash kredsNew (D-10) e o roteamento (D-01). Sem Server Actions e sem API (Fase 6 faz a integração real).
Output: `parent-panel-view.tsx` + `page.tsx` + suite GREEN + verificação visual aprovada.
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-parent-panel/05-CONTEXT.md
@.planning/phases/05-parent-panel/05-RESEARCH.md
@.planning/phases/05-parent-panel/05-PATTERNS.md
@.planning/phases/05-parent-panel/05-UI-SPEC.md
@src/components/garden/garden-view.tsx
@src/app/family/access/[familyId]/page.tsx
@src/lib/seed/parent-seed.ts
@src/components/parent/task-form-panel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ParentPanelView — estado do CRUD + layout 3-colunas (PTASK-01,03,04,06,09; D-09,D-10)</name>
  <files>src/components/parent/parent-panel-view.tsx</files>
  <read_first>
    - src/components/garden/garden-view.tsx (padrão Client Component raiz: useState, derivados no render, handlers de mutação otimista, setTimeout para animação)
    - src/lib/seed/parent-seed.ts (ParentTask, MOCK_PARENT_TASKS)
    - src/components/parent/parent-sidebar.tsx, parent-topbar.tsx, filter-chips.tsx, parent-task-card.tsx (Plano 05-02 — assinaturas de props)
    - src/components/parent/task-form-panel.tsx (Plano 05-03 — TaskFormData, EMPTY_FORM, taskToFormData, props de TaskFormPanel)
    - .planning/phases/05-parent-panel/05-PATTERNS.md (ParentPanelView completo linhas 94-236)
    - .planning/phases/05-parent-panel/05-RESEARCH.md (Pattern 2 estado no raiz linhas 250-280; Pitfall 6 sentinela editingId linhas 394-404; Pitfall 2 prop children linhas 346-356)
    - .planning/phases/05-parent-panel/05-UI-SPEC.md (Page Structure linhas 258-296; Interaction States linhas 178-204)
  </read_first>
  <behavior>
    - Renderiza layout flex row: ParentSidebar (80px) + main (ParentTopbar + content). Content é flex row: lista de cards (flex:1) + TaskFormPanel (336px).
    - Estado inicial: form em modo idle (editingId === null).
    - Clicar "+ Nova tarefa": editingId='new', formData=EMPTY_FORM, painel em modo create.
    - Clicar lápis de um card: editingId=task.id, formData=taskToFormData(task), painel em modo edit; card recebe destaque editing.
    - Salvar em create: adiciona task ao array, dispara flashNew(newId) por 1200ms, volta a idle.
    - Salvar em edit: atualiza a task, dispara flashNew(id), volta a idle.
    - Excluir (modo edit): remove a task do array, volta a idle.
    - Toggle de um card: alterna task.active sem mudar editingId (independente do form).
    - FilterChips: filtra a lista por criança atribuída; 'all' mostra tudo.
  </behavior>
  <action>
    Criar `parent-panel-view.tsx` (Client Component, `'use client'`), export `ParentPanelView`. Seguir EXATAMENTE o padrão de `garden-view.tsx` (estado no raiz, derivados no render, handlers otimistas).
    Props `interface ParentPanelViewProps { familyId: string; familyName: string; currentUserName: string; familyChildren: Array<{ id: string; displayName: string; accentColor: string; avatarPreset: string }>; initialTasks: ParentTask[] }` — usar `familyChildren`, NUNCA `children` (Pitfall 2, conflito com prop reservada do React).
    Estado: `const [tasks, setTasks] = useState<ParentTask[]>(initialTasks)`, `const [filter, setFilter] = useState<'all' | string>('all')`, `const [editingId, setEditingId] = useState<string | 'new' | null>(null)` (sentinela: null=idle, 'new'=create, taskId=edit — Pitfall 6, NÃO criar boolean isCreating separado), `const [newTaskId, setNewTaskId] = useState<string | null>(null)`, `const [formData, setFormData] = useState<TaskFormData>(EMPTY_FORM)`.
    Derivados no render (sem useEffect): `formMode` = editingId==='new' ? 'create' : editingId!==null ? 'edit' : 'idle'; `filteredTasks` = filter==='all' ? tasks : tasks.filter(t => t.assigned.includes(filter)).
    `flashNew(id)`: setNewTaskId(id); setTimeout(() => setNewTaskId(null), 1200) — padrão do WaterDrops/garden-view.
    Handlers: `handleToggle(id)` → setTasks(map toggle active); `handleNewTask()` → setEditingId('new'), setFormData(EMPTY_FORM); `handleEditTask(id)` → setEditingId(id), setFormData(taskToFormData(tasks.find(t=>t.id===id)!)); `handleSave()` → se formMode==='create': cria task com `crypto.randomUUID()` a partir de formData (active:true), setTasks([...prev, newTask]), flashNew(newId), setEditingId(null); se 'edit': setTasks(map update do editingId com formData), flashNew(editingId), setEditingId(null); `handleDelete()` → setTasks(filter remove editingId), setEditingId(null); `handleCancel()` → setEditingId(null).
    Layout (return): `<div>` flex row minHeight 100vh background `var(--color-kreds-bg)`. `<ParentSidebar />`. `<main>` flex:1 flex column: `<ParentTopbar familyName={familyName} currentUserName={currentUserName} />` e um `<div>` flex row flex:1: à esquerda a task-list-area (flex:1, overflow-y auto, padding) contendo o botão "+ Nova tarefa" (onClick handleNewTask, trigger D-07), `<FilterChips familyChildren={familyChildren} active={filter} onChange={setFilter} />`, e o map de `filteredTasks` em `<ParentTaskCard key={task.id} task={task} justAdded={newTaskId === task.id} editing={editingId === task.id} onToggle={handleToggle} onEdit={handleEditTask} />`; se `filteredTasks.length === 0` renderizar empty state (heading "Nenhuma tarefa ainda" + body do UI-SPEC). À direita `<TaskFormPanel mode={formMode} formData={formData} onChange={setFormData} onSave={handleSave} onDelete={handleDelete} onCancel={handleCancel} familyChildren={familyChildren} />`. Marcar o botão "+ Nova tarefa" com aria-label/texto "+ Nova tarefa".
  </action>
  <verify>
    <automated>grep -q "export function ParentPanelView" src/components/parent/parent-panel-view.tsx && grep -q "familyChildren" src/components/parent/parent-panel-view.tsx && ! grep -q "children:" src/components/parent/parent-panel-view.tsx && grep -q "setNewTaskId" src/components/parent/parent-panel-view.tsx && grep -q "1200" src/components/parent/parent-panel-view.tsx && grep -q "'new'" src/components/parent/parent-panel-view.tsx && grep -q "TaskFormPanel" src/components/parent/parent-panel-view.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - parent-panel-view.tsx exporta `ParentPanelView`; usa prop `familyChildren` (não `children`)
    - Usa sentinela `editingId: string | 'new' | null` (sem boolean isCreating separado)
    - flashNew com setTimeout 1200ms controlando `newTaskId` (PTASK-09 / D-10)
    - Compõe ParentSidebar, ParentTopbar, FilterChips, ParentTaskCard e TaskFormPanel
    - Toggle independente do form (handleToggle não altera editingId)
    - `npx tsc --noEmit` sem erro
  </acceptance_criteria>
  <done>ParentPanelView orquestra o layout 3-colunas com CRUD otimista, sentinela editingId e flash kredsNew; compila.</done>
</task>

<task type="auto">
  <name>Task 2: Rota SSR /family/[familyId]/tasks + suite GREEN (PTASK-01,02; D-01,D-02)</name>
  <files>src/app/family/[familyId]/tasks/page.tsx</files>
  <read_first>
    - src/app/family/access/[familyId]/page.tsx (padrão SSR exato: await params, auth(), query childProfiles com and/eq, import relativo de auth)
    - src/components/parent/parent-panel-view.tsx (Task 1 — props de ParentPanelView)
    - src/lib/seed/parent-seed.ts (MOCK_PARENT_TASKS)
    - src/middleware.ts (confirmar que /family/* já cobre /family/[familyId]/tasks — NÃO alterar)
    - .planning/phases/05-parent-panel/05-RESEARCH.md (Code Example page.tsx linhas 410-451; Pitfall 1 await params; Pitfall 5 sem lookup de familyMemberships em dev; Open Question 1 query de families.name)
    - .planning/phases/05-parent-panel/05-PATTERNS.md (page.tsx linhas 28-91)
  </read_first>
  <action>
    Criar `src/app/family/[familyId]/tasks/page.tsx` (Server Component async, default export `ParentTasksPage`). Replicar o padrão de `access/[familyId]/page.tsx`:
    - Assinatura: `{ params }: { params: Promise<{ familyId: string }> }`; `const { familyId } = await params` (OBRIGATÓRIO await — Pitfall 1).
    - `const session = await auth()` (import relativo de `auth` no mesmo estilo do analog — ajustar a profundidade `../../../../../auth` para a nova rota; a rota tasks tem a mesma profundidade de segmentos que access, então o caminho relativo é equivalente; confirmar contando os segmentos). `if (!session) redirect('/login')`.
    - Query childProfiles incluindo `avatarPreset` (o analog não busca esse campo — adicionar): `db.select({ id, displayName, accentColor, avatarPreset }).from(childProfiles).where(and(eq(childProfiles.familyId, familyId), eq(childProfiles.active, true)))`.
    - Query opcional do nome da família para o breadcrumb (Open Question 1): `db.select({ name: families.name }).from(families).where(eq(families.id, familyId))` — usar o resultado ou fallback 'Família'. NÃO fazer lookup de `familyMemberships` (Pitfall 5 — banco de dev sem memberships; verificação de ownership é Fase 6).
    - Popular `assigned` do mock: como `MOCK_PARENT_TASKS` usa `assigned: []`, atribuir o primeiro child id (se houver) a algumas tarefas para que os filter chips e o filtro funcionem na demo — mapear MOCK_PARENT_TASKS atribuindo `children[0]?.id` (quando existir) a cada task, ou manter [] se não houver crianças.
    - Render: `<ParentPanelView familyId={familyId} familyName={familyName} currentUserName={session.user?.name ?? ''} familyChildren={children} initialTasks={tasksWithAssignees} />`. Usar prop `familyChildren` (NÃO `children`).
    Após criar, rodar a suite e levar a GREEN: `pnpm test -- tests/unit/parent-panel.test.tsx` deve passar (componente agora existe). Se algum teste falhar por mismatch de aria/label/texto entre o teste (Plano 05-01) e a implementação, ajustar a IMPLEMENTAÇÃO para satisfazer o teste (os testes são o contrato), exceto se o teste estiver factualmente incorreto vs. o UI-SPEC — nesse caso corrigir o teste e anotar no SUMMARY.
  </action>
  <verify>
    <automated>grep -q "const { familyId } = await params" src/app/family/[familyId]/tasks/page.tsx && grep -q "const session = await auth()" src/app/family/[familyId]/tasks/page.tsx && grep -q "if (!session) redirect('/login')" src/app/family/[familyId]/tasks/page.tsx && grep -q "ParentPanelView" src/app/family/[familyId]/tasks/page.tsx && grep -q "familyChildren=" src/app/family/[familyId]/tasks/page.tsx && ! grep -q "familyMemberships" src/app/family/[familyId]/tasks/page.tsx && pnpm test -- tests/unit/parent-panel.test.tsx 2>&1 | grep -Eq "passed|PASS|✓" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx usa `await params`, `auth()` e `redirect('/login')` sem sessão (D-01)
    - Query de childProfiles inclui `avatarPreset`; sem lookup de `familyMemberships` (Pitfall 5)
    - Renderiza `<ParentPanelView ... familyChildren={...} />` (não `children`)
    - `pnpm test -- tests/unit/parent-panel.test.tsx` passa em GREEN (PTASK-01..10)
    - `npx tsc --noEmit` sem erro; `pnpm build` compila a rota sem erro
  </acceptance_criteria>
  <done>Rota SSR funcional com auth + childProfiles; suite parent-panel.test.tsx GREEN cobrindo PTASK-01..10.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual — painel dos pais</name>
  <files>(nenhum — verificação visual, sem código)</files>
  <action>
    Checkpoint de verificação visual humana — NÃO há código a escrever nesta task. O executor pausa, sobe o app (`pnpm dev`) e apresenta ao usuário os passos de `<how-to-verify>` para validar visualmente o painel dos pais renderizado em /family/[familyId]/tasks. Aguardar o `<resume-signal>` ("approved" ou lista de problemas) antes de finalizar a fase. NÃO auto-aprovar.
  </action>
  <what-built>
    Painel desktop do responsável em /family/[familyId]/tasks: layout 3-colunas (sidebar 80px + main + form 336px), topbar com breadcrumb e badge, filter chips por criança, task cards com ícone de categoria/toggle/lápis, e form direito com criar/editar/excluir, stepper de recompensa, pills de recorrência, atribuição e aprovação. Mutações otimistas locais com flash kredsNew.
  </what-built>
  <how-to-verify>
    1. Rodar `pnpm dev` e garantir login do responsável (Zitadel) — se já logado, ir direto ao passo 2.
    2. Obter o familyId de dev: `PGPASSWORD=kreds_dev /opt/homebrew/Cellar/postgresql@16/16.14/bin/psql -h localhost -U kreds -d kreds_dev -tAc "SELECT id FROM families LIMIT 1;"` e visitar `http://localhost:3000/family/<familyId>/tasks`.
    3. Confirmar visualmente (PTASK-01): sidebar à esquerda (80px), área central, painel direito 336px sempre visível com placeholder "Selecione uma tarefa ou clique em + para criar".
    4. Confirmar topbar (PTASK-02): nome da família em verde no breadcrumb + badge com nome do usuário e avatar.
    5. Filter chips (PTASK-03): "Todas" + chip por criança; clicar um chip filtra e fica verde.
    6. Task card (PTASK-04/05): ícone de categoria colorido, toggle 42×24px (clicar desativa → card fica esmaecido), botão lápis. Toggle NÃO abre o form.
    7. Criar (PTASK-06/07/08/09): clicar "+ Nova tarefa"; preencher título, escolher categoria, stepper em R$ 0 mostra "Mordomia" → clicar + vira "R$ 1"; selecionar pills de recorrência + "Todos os dias"; atribuir uma criança; salvar → novo card aparece com glow verde kredsNew.
    8. Editar/Excluir (PTASK-10): clicar lápis de um card → form em modo edit mostra "Excluir tarefa"; em modo criar esse botão NÃO aparece. Excluir remove o card.
  </how-to-verify>
  <verify>
    <human-check>Usuário confirma os 8 passos de how-to-verify e responde "approved" ou lista problemas.</human-check>
  </verify>
  <resume-signal>Digite "approved" se tudo confere visualmente, ou descreva os problemas encontrados.</resume-signal>
  <done>Painel dos pais verificado visualmente pelo usuário; resposta "approved" recebida (ou problemas resolvidos).</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 05-04)

Novos símbolos — excluir da verificação de drift:
- `ParentPanelView` (src/components/parent/parent-panel-view.tsx) — props { familyId, familyName, currentUserName, familyChildren, initialTasks }
- `interface ParentPanelViewProps`
- Rota nova: `src/app/family/[familyId]/tasks/page.tsx` — default export `ParentTasksPage`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /family/[familyId]/tasks | request não autenticada cruza aqui; middleware + auth() guardam |
| familyId da URL → query | parâmetro de rota usado em WHERE de childProfiles/families |
| session next-auth → SSR | sessão do guardian valida acesso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Elevation of Privilege | IDOR — familyId da URL não verificado contra o guardian logado | accept | Middleware (/family/*) + auth() garantem sessão válida; verificação de ownership (requireActiveGuardian) é responsabilidade da Fase 6 (RESEARCH §Security; banco de dev sem memberships) — aceito para esta fase de UI com mock |
| T-05-08 | Spoofing | acesso sem sessão | mitigate | `auth()` no Server Component + `redirect('/login')` se !session; middleware já protege /family/* por cookie-check |
| T-05-09 | Tampering | familyId malicioso na query Drizzle | mitigate | Drizzle usa queries parametrizadas (`eq(childProfiles.familyId, familyId)`) — sem concatenação de SQL; sem risco de injection |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo instalado (RESEARCH §Package Legitimacy: zero novas instalações) |
</threat_model>

<verification>
- `npx tsc --noEmit` sem erros
- `pnpm test -- tests/unit/parent-panel.test.tsx` GREEN (PTASK-01..10)
- `pnpm build` compila a rota /family/[familyId]/tasks
- Checkpoint visual aprovado pelo usuário
- Middleware /family/* NÃO foi alterado (confirmar git diff em src/middleware.ts vazio)
</verification>

<success_criteria>
- Rota SSR funcional com auth e redirect (D-01/D-02)
- CRUD otimista completo funcionando na UI (D-09)
- Flash kredsNew ao criar/salvar (PTASK-09)
- Suite GREEN e checkpoint visual aprovado
</success_criteria>

<output>
Create `.planning/phases/05-parent-panel/05-04-SUMMARY.md` when done
</output>
