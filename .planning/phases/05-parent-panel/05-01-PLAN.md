---
phase: 05-parent-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema/index.ts
  - src/lib/seed/parent-seed.ts
  - tests/unit/parent-panel.test.tsx
autonomous: true
requirements: [PTASK-05, PTASK-07, PTASK-08]
must_haves:
  truths:
    - "Tabela task_templates no banco de dev tem as colunas category, days e approval"
    - "Existe um seed mock tipado (ParentTask[]) cobrindo as 5 categorias e estados ativo/inativo/mordomia"
    - "A suite de testes parent-panel.test.tsx existe e roda (RED inicial — componente ainda não implementado)"
  artifacts:
    - path: "src/lib/db/schema/index.ts"
      provides: "Colunas category, days, approval em taskTemplates"
      contains: "approval: boolean('approval')"
    - path: "src/lib/seed/parent-seed.ts"
      provides: "Interface ParentTask + MOCK_PARENT_TASKS + helpers de categoria/reward"
      exports: ["ParentTask", "MOCK_PARENT_TASKS", "CATEGORY_META", "rewardLabel"]
    - path: "tests/unit/parent-panel.test.tsx"
      provides: "Testes RED para PTASK-01..10"
      min_lines: 60
  key_links:
    - from: "src/lib/db/schema/index.ts"
      to: "PostgreSQL task_templates"
      via: "drizzle-kit push"
      pattern: "boolean\\('approval'\\)"
---

<objective>
Estabelecer a fundação de dados da Fase 5: adicionar os 3 campos novos (`category`, `days`, `approval`) ao schema Drizzle `taskTemplates` e aplicar no banco via `drizzle-kit push`; criar o seed mock tipado `parent-seed.ts` (análogo a `garden-seed.ts`); e criar a suite de testes `parent-panel.test.tsx` em estado RED cobrindo PTASK-01..10.

Purpose: Os componentes da UI (Waves 2-3) consomem a interface `ParentTask` e os helpers de categoria/reward. Definir o contrato de dados primeiro evita "scavenger hunt" nos planos seguintes. O schema push prepara o banco para a Fase 6.
Output: Schema atualizado + colunas no banco + `parent-seed.ts` + `parent-panel.test.tsx` (RED).
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-parent-panel/05-CONTEXT.md
@.planning/phases/05-parent-panel/05-RESEARCH.md
@.planning/phases/05-parent-panel/05-PATTERNS.md
@.planning/phases/05-parent-panel/05-UI-SPEC.md
@src/lib/seed/garden-seed.ts
@src/lib/db/schema/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar colunas category/days/approval ao schema taskTemplates</name>
  <files>src/lib/db/schema/index.ts</files>
  <read_first>
    - src/lib/db/schema/index.ts (estado atual de taskTemplates, linhas 176-203; bloco de imports linhas 1-15 — `text`, `boolean`, `jsonb` já importados; constraint `kreds_value_positive` linhas 198-201)
    - .planning/phases/05-parent-panel/05-RESEARCH.md (Pattern 3 e Pitfall 4)
  </read_first>
  <action>
    No objeto de colunas de `taskTemplates` (src/lib/db/schema/index.ts), após a coluna `isActive` (linha 190), adicionar três campos conforme D-03:
    - `category`: coluna `text('category')` — nullable por padrão, NÃO usar `.notNull()`.
    - `days`: coluna `jsonb('days').$type<string[]>()` — nullable por padrão, NÃO usar `.notNull()`. Armazena subset de ['D','S','T','Q','Q','S','S'].
    - `approval`: coluna `boolean('approval').notNull().default(false)`.
    Os imports `text`, `boolean` e `jsonb` já existem no bloco de import (linhas 3, 6, 8) — não adicionar imports novos. NÃO alterar o constraint `kreds_value_positive` (`kredsValue > 0`) — fica intacto nesta fase; a resolução do conflito com reward=0 (Mordomia) é responsabilidade da Fase 6. Manter todos os indexes e o objeto `(table) => ({...})` existentes sem alteração.
  </action>
  <verify>
    <automated>grep -v '^//' src/lib/db/schema/index.ts | grep -c "category: text('category')" | grep -qx 1 && grep -v '^//' src/lib/db/schema/index.ts | grep -c "approval: boolean('approval')" | grep -qx 1 && grep -q "days: jsonb('days')" src/lib/db/schema/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/db/schema/index.ts contém `category: text('category')` (sem `.notNull()`)
    - src/lib/db/schema/index.ts contém `days: jsonb('days').$type<string[]>()` (sem `.notNull()`)
    - src/lib/db/schema/index.ts contém `approval: boolean('approval').notNull().default(false)`
    - O constraint `kreds_value_positive` permanece inalterado no arquivo (`grep -q "kreds_value_positive"`)
    - `npx tsc --noEmit` não reporta erro novo em schema/index.ts
  </acceptance_criteria>
  <done>As 3 colunas existem no objeto taskTemplates do schema Drizzle com tipos e nullability corretos; constraint preservado.</done>
</task>

<task type="auto">
  <name>Task 2: Criar seed mock tipado parent-seed.ts</name>
  <files>src/lib/seed/parent-seed.ts</files>
  <read_first>
    - src/lib/seed/garden-seed.ts (padrão de interface tipada + constante MOCK exportada + Records de cor — linhas 1-21 e 143-153)
    - .planning/phases/05-parent-panel/05-UI-SPEC.md (Tabela de Categorias PTASK-05 linhas 101-109; Copywriting de reward linhas 221-222; labels de recorrência linhas 241-248)
    - .planning/phases/05-parent-panel/05-RESEARCH.md (Code Example "Seed Mock tipado" linhas 470-498)
  </read_first>
  <action>
    Criar `src/lib/seed/parent-seed.ts` (seed mock tipado, D-04) seguindo o padrão de `garden-seed.ts` (interface + constantes exportadas). Conteúdo:
    1. Interface `ParentTask` com campos exatos: `id: string`, `title: string`, `category: 'quarto' | 'higiene' | 'estudos' | 'casa' | 'espiritual'`, `reward: number` (inteiro R$; 0 = mordomia), `days: string[]` (subset de ['D','S','T','Q','Q','S','S']), `assigned: string[]` (ids de childProfile), `active: boolean`, `approval: boolean`.
    2. Type alias `Category = ParentTask['category']`.
    3. Constante `CATEGORY_META: Record<Category, { label: string; color: string; softBg: string }>` com os valores exatos da Tabela de Categorias do UI-SPEC (PTASK-05): quarto → label 'Quarto', color '#3B6E8F', softBg '#E4EDF2'; higiene → 'Higiene', '#2F8F8A', '#E1F0EE'; estudos → 'Estudos', '#B5623F', '#F4E7E0'; casa → 'Casa', '#8A6BB0', '#EEE8F3'; espiritual → 'Espiritual', '#3E6B4F', '#E7EFE8'.
    4. Constante `WEEKDAY_LABELS: string[] = ['D','S','T','Q','Q','S','S']` (PTASK-08) e `ALL_DAYS = [...WEEKDAY_LABELS]`.
    5. Helper `rewardLabel(reward: number): string` — retorna 'Mordomia' se reward === 0, senão `R$ ${reward}` (PTASK-07).
    6. Constante `MOCK_PARENT_TASKS: ParentTask[]` com 5+ tarefas cobrindo TODAS as 5 categorias, pelo menos uma com `reward: 0` (mordomia), pelo menos uma com `active: false` (inativa), pelo menos uma com `approval: true`, e variação de `days`. Usar `assigned: []` (a page.tsx popula com child ids reais; o mock base não depende de ids).
  </action>
  <verify>
    <automated>npx tsc --noEmit src/lib/seed/parent-seed.ts 2>/dev/null; node -e "const m=require('./src/lib/seed/parent-seed.ts')" 2>/dev/null; grep -q "export interface ParentTask" src/lib/seed/parent-seed.ts && grep -q "export const MOCK_PARENT_TASKS" src/lib/seed/parent-seed.ts && grep -q "rewardLabel" src/lib/seed/parent-seed.ts && grep -q "CATEGORY_META" src/lib/seed/parent-seed.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/seed/parent-seed.ts exporta `interface ParentTask` com os 8 campos especificados
    - Exporta `CATEGORY_META` cobrindo as 5 categorias com os hexs exatos do UI-SPEC (`grep -q "#3B6E8F"` ... `grep -q "#8A6BB0"`)
    - Exporta `rewardLabel` que retorna 'Mordomia' para 0
    - `MOCK_PARENT_TASKS` contém ≥5 itens; pelo menos um com `reward: 0`, um com `active: false`, um com `approval: true`
    - `npx tsc --noEmit` passa sem erro no arquivo
  </acceptance_criteria>
  <done>parent-seed.ts existe, tipado, com interface ParentTask + CATEGORY_META + helpers + MOCK_PARENT_TASKS cobrindo as 5 categorias e estados-chave.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar suite de testes RED parent-panel.test.tsx</name>
  <files>tests/unit/parent-panel.test.tsx</files>
  <read_first>
    - src/lib/seed/parent-seed.ts (interface ParentTask + MOCK_PARENT_TASKS — criado na Task 2)
    - .planning/phases/05-parent-panel/05-RESEARCH.md (Validation Architecture — Phase Requirements → Test Map linhas 595-606; quick run command linha 590)
    - .planning/phases/05-parent-panel/05-UI-SPEC.md (Interaction States + Copywriting Contract para asserts de texto/comportamento)
    - tests/setup.ts (se existir — para entender mocks globais como IntersectionObserver)
  </read_first>
  <behavior>
    - PTASK-01: ao renderizar ParentPanelView, existe um elemento com largura 80 (sidebar), um main, e um painel de 336 (form). Asserta presença de testids/roles.
    - PTASK-02: topbar mostra o nome da família (breadcrumb) e o nome do usuário (badge).
    - PTASK-03: filter chips renderizam "Todas" + um chip por criança em familyChildren.
    - PTASK-04: cada task card tem um toggle (role="switch") e um botão de editar (aria-label contém "Editar tarefa"); clicar no toggle NÃO entra em modo edit.
    - PTASK-05: as 5 categorias renderizam com a cor correspondente do CATEGORY_META.
    - PTASK-06: ao clicar "+ Nova tarefa" o painel direito mostra o form com campos título, categorias, recompensa, recorrência, atribuição, aprovação.
    - PTASK-07: stepper com reward=0 mostra "Mordomia"; clicar + incrementa para "R$ 1".
    - PTASK-08: clicar uma pill de recorrência alterna seu estado; "Todos os dias" seleciona os 7 dias.
    - PTASK-09: após criar/salvar tarefa, o card correspondente recebe `animation` contendo `kreds-new` (ou classe `animate-kreds-new`).
    - PTASK-10: botão "Excluir tarefa" NÃO aparece em modo create; aparece em modo edit.
  </behavior>
  <action>
    Criar `tests/unit/parent-panel.test.tsx` usando Vitest + @testing-library/react. Importar `ParentPanelView` de `@/components/parent/parent-panel-view` e `MOCK_PARENT_TASKS` de `@/lib/seed/parent-seed`. Definir um helper `renderPanel()` que renderiza `<ParentPanelView familyId="fam1" familyName="Família Teste" currentUserName="João" familyChildren={[{id:'c1',displayName:'Ana',accentColor:'#3E6B4F',avatarPreset:'sprout'},{id:'c2',displayName:'Beto',accentColor:'#B5623F',avatarPreset:'sprout'}]} initialTasks={MOCK_PARENT_TASKS} />`. Escrever um `describe('ParentPanelView — PTASK-01..10')` com um `it()` por requisito acima, com asserts de texto, role, aria e comportamento conforme a coluna `behavior`. Os testes DEVEM falhar inicialmente (RED) porque `parent-panel-view.tsx` ainda não existe — esse é o estado esperado deste plano. Usar queries acessíveis (`getByRole`, `getByLabelText`, `getByText`) em vez de testids onde possível; usar `data-testid` apenas para dimensões de layout (ex.: `data-testid="parent-sidebar"`). Para PTASK-09, asserir o atributo inline `style` ou className do card após a ação de criar.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/parent-panel.test.tsx 2>&1 | grep -Eq "Cannot find module|failed|FAIL|ParentPanelView" && echo "RED-OK (testes existem e falham porque componente ainda não existe)"</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/parent-panel.test.tsx existe e contém um `describe` com ≥10 blocos `it` (um por PTASK-01..10)
    - Cada `it` referencia o requisito no título (ex.: contém 'PTASK-07')
    - A suite roda e falha em estado RED (componente ParentPanelView ainda não existe) — saída do vitest indica módulo não encontrado ou asserts falhando, NÃO erro de sintaxe do teste
    - O arquivo importa de `@/lib/seed/parent-seed` (símbolos reais criados na Task 2)
  </acceptance_criteria>
  <done>parent-panel.test.tsx existe com 10 testes RED mapeando PTASK-01..10; importa o seed real; falha porque o componente ainda não foi implementado.</done>
</task>

</tasks>

<schema_push priority="blocking">

<task type="auto" gate="blocking">
  <name>Task 4: [BLOCKING] drizzle-kit push — aplicar colunas no banco</name>
  <files>(nenhum arquivo de código — aplica migração no PostgreSQL de dev)</files>
  <read_first>
    - src/lib/db/schema/index.ts (deve já conter as 3 colunas da Task 1 antes de rodar)
    - .planning/phases/05-parent-panel/05-RESEARCH.md (Environment Availability — DATABASE_URL e psql path linhas 568-578)
    - drizzle.config.ts (raiz — confirmar dialeto/conexão usados pelo push)
  </read_first>
  <action>
    APÓS a Task 1 concluída (schema com as 3 colunas), aplicar a migração no banco de dev com `pnpm db:push` (equivalente a `npx drizzle-kit push`). O `DATABASE_URL` de dev é `postgresql://kreds:kreds_dev@localhost:5432/kreds_dev`. Se o comando exigir prompt interativo de confirmação que não possa ser suprimido em ambiente não-TTY, marcar para intervenção manual e pausar. As colunas são nullable / com default → não deve haver risco de data loss prompt. Após o push, verificar com psql que as colunas existem. O binário psql está em `/opt/homebrew/Cellar/postgresql@16/16.14/bin/psql` (use `PGPASSWORD=kreds_dev`).
  </action>
  <verify>
    <automated>PGPASSWORD=kreds_dev /opt/homebrew/Cellar/postgresql@16/16.14/bin/psql -h localhost -U kreds -d kreds_dev -tAc "SELECT column_name FROM information_schema.columns WHERE table_name='task_templates' AND column_name IN ('category','days','approval');" | sort | tr '\n' ',' | grep -q "approval,category,days," && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - A tabela `task_templates` no banco de dev possui as colunas `category`, `days` e `approval` (confirmado via information_schema)
    - `approval` tem default false (`SELECT column_default ...` contém 'false')
    - Nenhuma coluna existente foi removida (drop) durante o push
  </acceptance_criteria>
  <done>Banco de dev tem as 3 colunas novas em task_templates; verificado via psql.</done>
</task>

</schema_push>

<artifacts_produced>
## Artifacts this phase produces (Plan 05-01)

Novos símbolos criados neste plano (excluir da verificação de drift):
- Colunas Drizzle: `taskTemplates.category`, `taskTemplates.days`, `taskTemplates.approval` (src/lib/db/schema/index.ts)
- Colunas SQL: `task_templates.category`, `task_templates.days`, `task_templates.approval`
- Arquivo novo: `src/lib/seed/parent-seed.ts`
  - `interface ParentTask`
  - `type Category`
  - `const CATEGORY_META`
  - `const WEEKDAY_LABELS`, `const ALL_DAYS`
  - `function rewardLabel`
  - `const MOCK_PARENT_TASKS`
- Arquivo novo: `tests/unit/parent-panel.test.tsx`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| código → banco de dev | `drizzle-kit push` altera o schema do PostgreSQL local |
| seed mock → UI | dados estáticos tipados consumidos pelos componentes (sem entrada de usuário ainda) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | drizzle-kit push (alteração de schema) | mitigate | Colunas nullable/com default — sem DROP de colunas; verificação via information_schema confirma ausência de data loss antes de prosseguir |
| T-05-02 | Denial of Service | push com prompt interativo travando CI/agente | accept | Banco de dev local; colunas seguras (nullable/default) tornam prompt improvável; fallback documentado (autonomous=false se travar) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo instalado nesta fase (RESEARCH §Package Legitimacy: zero novas instalações); sem checkpoint necessário |
</threat_model>

<verification>
- Schema Drizzle contém as 3 colunas com tipos corretos (Task 1)
- Banco de dev tem as 3 colunas aplicadas (Task 4)
- parent-seed.ts compila e exporta os símbolos do contrato (Task 2)
- parent-panel.test.tsx existe e está em RED (Task 3)
- `npx tsc --noEmit` sem erros novos
</verification>

<success_criteria>
- `category`, `days`, `approval` existem no schema E no banco de dev
- `ParentTask` + `MOCK_PARENT_TASKS` + `CATEGORY_META` + `rewardLabel` disponíveis para os planos seguintes
- Suite RED criada cobrindo PTASK-01..10
</success_criteria>

<output>
Create `.planning/phases/05-parent-panel/05-01-SUMMARY.md` when done
</output>
