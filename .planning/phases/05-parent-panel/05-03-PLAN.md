---
phase: 05-parent-panel
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/components/parent/task-form-panel.tsx
  - src/components/parent/reward-stepper.tsx
  - src/components/parent/recurrence-pills.tsx
  - src/components/parent/assignee-selector.tsx
  - src/components/parent/category-chips.tsx
autonomous: true
requirements: [PTASK-06, PTASK-07, PTASK-08, PTASK-10]
must_haves:
  truths:
    - "Em modo idle o painel mostra o placeholder elegante sem shift de layout"
    - "Form de criar/editar tem título, chips de categoria, stepper de recompensa, pills de recorrência, atribuição e aprovação"
    - "Stepper com reward=0 mostra 'Mordomia' em verde; > 0 mostra 'R$ X'"
    - "Pills de recorrência alternam; 'Todos os dias' seleciona os 7"
    - "Botão excluir aparece apenas em modo edit, nunca em create/idle"
  artifacts:
    - path: "src/components/parent/task-form-panel.tsx"
      provides: "Painel direito 336px com form create/edit/idle"
      exports: ["TaskFormPanel", "TaskFormData", "EMPTY_FORM", "taskToFormData"]
    - path: "src/components/parent/reward-stepper.tsx"
      provides: "Stepper ± com Mordomia/R$"
      exports: ["RewardStepper"]
    - path: "src/components/parent/recurrence-pills.tsx"
      provides: "Pills D/S/T/Q/Q/S/S + Todos os dias"
      exports: ["RecurrencePills"]
    - path: "src/components/parent/assignee-selector.tsx"
      provides: "Seletor de crianças atribuídas"
      exports: ["AssigneeSelector"]
    - path: "src/components/parent/category-chips.tsx"
      provides: "Chips de categoria no form"
      exports: ["CategoryChips"]
  key_links:
    - from: "src/components/parent/task-form-panel.tsx"
      to: "mode === 'edit'"
      via: "render condicional do DeleteButton"
      pattern: "mode === 'edit'"
    - from: "src/components/parent/reward-stepper.tsx"
      to: "rewardLabel"
      via: "Mordomia vs R$"
      pattern: "Mordomia"
---

<objective>
Construir o painel direito (336px) `TaskFormPanel` com seus estados `idle | create | edit` e os sub-componentes do form: `CategoryChips`, `RewardStepper` (PTASK-07), `RecurrencePills` (PTASK-08), `AssigneeSelector` e o `DeleteButton` condicional (PTASK-10). O `ApprovalToggle` reutiliza o `TaskToggle` do Plano 05-02.

Purpose: Completa o CRUD de UI (PTASK-06..10). O form é controlado: recebe `formData` + `onChange` + callbacks do `ParentPanelView` (Plano 04). Toda a lógica de estado da lista fica no pai; este painel apenas reflete e edita o `formData`.
Output: 5 componentes em `src/components/parent/`, incluindo o contrato `TaskFormData` consumido pelo Plano 04.
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-parent-panel/05-CONTEXT.md
@.planning/phases/05-parent-panel/05-PATTERNS.md
@.planning/phases/05-parent-panel/05-UI-SPEC.md
@src/components/tasks/tithe-card.tsx
@src/lib/seed/parent-seed.ts
@src/components/parent/task-toggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sub-componentes do form — CategoryChips, RewardStepper, RecurrencePills, AssigneeSelector (PTASK-06, PTASK-07, PTASK-08)</name>
  <files>src/components/parent/category-chips.tsx, src/components/parent/reward-stepper.tsx, src/components/parent/recurrence-pills.tsx, src/components/parent/assignee-selector.tsx</files>
  <read_first>
    - src/lib/seed/parent-seed.ts (CATEGORY_META, Category, rewardLabel, WEEKDAY_LABELS, ALL_DAYS — Plano 05-01)
    - src/components/tasks/tithe-card.tsx (padrão de botão e estilo inline do projeto)
    - .planning/phases/05-parent-panel/05-UI-SPEC.md (Component Inventory §Painel Direito linhas 144-154; RewardStepper linha 149; RecurrencePills linha 150; AssigneeSelector linha 151; Color §Accent itens 4,5,9; Copywriting linhas 221-224; Accessibility Notes linhas 376-383)
  </read_first>
  <action>
    Criar quatro Client Components controlados (cada um recebe valor + callback, sem estado interno de domínio).
    `CategoryChips` (category-chips.tsx), export `CategoryChips`, props `{ value: Category | null; onChange: (c: Category) => void }`. Renderiza um chip por categoria de `CATEGORY_META` (5 chips). Chip selecionado (`value === cat`): usa a cor da categoria como background/border (Accent item 5 do UI-SPEC). Não selecionado: bg `var(--color-kreds-card)`, border `#E2DECF`. `aria-pressed` por chip. Label do chip = `CATEGORY_META[cat].label`.
    `RewardStepper` (reward-stepper.tsx), export `RewardStepper`, props `{ value: number; onChange: (v: number) => void }`. Botões `−` e `+` 40×40px (`aria-label` "Diminuir recompensa"/"Aumentar recompensa") e valor central. Usar `rewardLabel(value)` de parent-seed: `value === 0` → texto "Mordomia" em verde (`var(--color-kreds-primary)`, fontSize 18, fontWeight 700); `value > 0` → "R$ {value}" em `#27372C`. `−` não desce abaixo de 0 (clamp em 0). `+` incrementa de 1.
    `RecurrencePills` (recurrence-pills.tsx), export `RecurrencePills`, props `{ value: string[]; onChange: (days: string[]) => void }`. Renderiza 7 pills com labels de `WEEKDAY_LABELS` (['D','S','T','Q','Q','S','S']) — usar índice como chave (labels repetem). Pill selecionada (índice presente em `value` via marcador de índice OU comparação por posição — usar array de índices selecionados internamente derivado de `value`): bg `#3E6B4F`, texto branco (Accent item 4); senão bg `var(--color-kreds-card)`, border `#E2DECF`. Toggle por clique. Link "Todos os dias" (texto verde) que chama `onChange(ALL_DAYS)`. `aria-pressed` por pill, fontSize 13 fontWeight 700. IMPORTANTE: como os labels D/S/T/Q/Q/S/S repetem, representar seleção por índice de dia (0-6), não por valor de string, para evitar colisão; o `value` array pode usar os 7 marcadores na ordem da semana.
    `AssigneeSelector` (assignee-selector.tsx), export `AssigneeSelector`, props `{ familyChildren: Array<{ id: string; displayName: string; accentColor: string; avatarPreset: string }>; value: string[]; onChange: (ids: string[]) => void }`. Lista as crianças como botões toggle (multi-seleção). Selecionado (`value.includes(child.id)`): bg `#EEF3EA`, border `#3E6B4F`, checkmark verde. Avatar 32px (inicial sobre accentColor). `aria-pressed` por criança. Clique alterna o id no array.
  </action>
  <verify>
    <automated>grep -q "export function CategoryChips" src/components/parent/category-chips.tsx && grep -q "export function RewardStepper" src/components/parent/reward-stepper.tsx && grep -q "Mordomia" src/components/parent/reward-stepper.tsx && grep -q "export function RecurrencePills" src/components/parent/recurrence-pills.tsx && grep -q "Todos os dias" src/components/parent/recurrence-pills.tsx && grep -q "export function AssigneeSelector" src/components/parent/assignee-selector.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - category-chips.tsx exporta `CategoryChips`, 5 chips com `aria-pressed`, selecionado usa cor da categoria
    - reward-stepper.tsx exporta `RewardStepper`; value=0 mostra "Mordomia" em `var(--color-kreds-primary)`; clamp em 0
    - recurrence-pills.tsx exporta `RecurrencePills`; 7 pills + link "Todos os dias" que seleciona todos
    - assignee-selector.tsx exporta `AssigneeSelector`; multi-seleção com `aria-pressed`
    - `npx tsc --noEmit` sem erro nos quatro arquivos
  </acceptance_criteria>
  <done>Os 4 sub-componentes controlados existem e compilam; stepper mostra Mordomia/R$; pills suportam "Todos os dias".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TaskFormPanel com estados idle/create/edit + DeleteButton condicional (PTASK-06, PTASK-10)</name>
  <files>src/components/parent/task-form-panel.tsx</files>
  <read_first>
    - src/components/tasks/tithe-card.tsx (padrão de botão CTA verde com gradiente e shadow-cta linhas 64-85)
    - src/lib/seed/parent-seed.ts (ParentTask, Category — para tipar TaskFormData e taskToFormData)
    - src/components/parent/task-toggle.tsx (TaskToggle — reutilizado como ApprovalToggle; Plano 05-02)
    - src/components/parent/category-chips.tsx, reward-stepper.tsx, recurrence-pills.tsx, assignee-selector.tsx (Task 1 deste plano)
    - .planning/phases/05-parent-panel/05-UI-SPEC.md (Component Inventory §Painel Direito linhas 144-154; Interaction States FormCTA/DeleteButton linhas 190-204; Copywriting linhas 210-232; Color destructive linha 87 e §Cores adicionais linhas 113-121; Page Structure linhas 284-296; Accessibility Notes linha 383)
    - .planning/phases/05-parent-panel/05-CONTEXT.md (D-05, D-06, D-10)
    - .planning/phases/05-parent-panel/05-PATTERNS.md (TaskFormPanel idle/CTA linhas 291-339)
  </read_first>
  <behavior>
    - mode='idle': renderiza placeholder "Selecione uma tarefa ou clique em + para criar" centralizado; largura fixa 336px; sem campos de form.
    - mode='create': header "Nova tarefa"; CTA label "Adicionar tarefa"; SEM botão excluir.
    - mode='edit': header "Editar tarefa" + botão X; CTA label "Salvar alterações"; botão "Excluir tarefa" visível.
    - CTA desabilitado quando nenhuma criança atribuída: label "Selecione uma criança", bg `#C2C9BC`, aria-disabled.
    - Clicar excluir chama onDelete; clicar X chama onCancel; clicar CTA chama onSave(formData).
  </behavior>
  <action>
    Criar `task-form-panel.tsx`. Exportar:
    1. `interface TaskFormData { title: string; category: Category | null; reward: number; days: string[]; assigned: string[]; approval: boolean }`.
    2. `const EMPTY_FORM: TaskFormData = { title: '', category: null, reward: 0, days: [], assigned: [], approval: false }`.
    3. `function taskToFormData(task: ParentTask): TaskFormData` — mapeia uma ParentTask para o formData (title, category, reward, days, assigned, approval).
    4. `TaskFormPanel` (Client Component), props `{ mode: 'idle' | 'create' | 'edit'; formData: TaskFormData; onChange: (data: TaskFormData) => void; onSave: () => void; onDelete: () => void; onCancel: () => void; familyChildren: Array<{ id: string; displayName: string; accentColor: string; avatarPreset: string }> }`.
    Container `<aside>` largura FIXA 336px (`flexShrink: 0`), padding 20px, borderRadius 20px, bg branco, boxShadow `0 16px 36px -26px rgba(40,55,45,.5)`, minHeight estável para evitar layout shift entre estados (D-05).
    Em `mode === 'idle'`: renderizar apenas o placeholder (heading "Nova tarefa" + body "Selecione uma tarefa ou clique em + para criar", muted, centralizado).
    Em create/edit: header dinâmico ("Nova tarefa" / "Editar tarefa" + botão X 30×30px `aria-label="Cancelar"` que chama onCancel só em edit/create) seguido do form:
    - TaskTitleInput: `<input>` 46px altura, borderRadius 12px, bg `var(--color-kreds-card)`; onChange atualiza `formData.title` via `onChange({ ...formData, title })`; focus border `#3E6B4F`.
    - `<CategoryChips value={formData.category} onChange={(c) => onChange({ ...formData, category: c })} />`.
    - `<RewardStepper value={formData.reward} onChange={(reward) => onChange({ ...formData, reward })} />`.
    - `<RecurrencePills value={formData.days} onChange={(days) => onChange({ ...formData, days })} />`.
    - `<AssigneeSelector familyChildren={familyChildren} value={formData.assigned} onChange={(assigned) => onChange({ ...formData, assigned })} />`.
    - ApprovalToggle: `<TaskToggle checked={formData.approval} onChange={() => onChange({ ...formData, approval: !formData.approval })} label="Requer aprovação" />` com label visível "Requer aprovação".
    - FormCTA: botão 44px borderRadius 13px. Habilitado quando `formData.assigned.length > 0`: bg `#3E6B4F`, shadow CTA, label = `mode === 'create' ? 'Adicionar tarefa' : 'Salvar alterações'`, onClick → onSave. Desabilitado quando `assigned.length === 0`: label "Selecione uma criança", bg `#C2C9BC`, `disabled` + `aria-disabled="true"`, cursor not-allowed.
    - DeleteButton (PTASK-10): renderizar SOMENTE quando `mode === 'edit'`. Botão 44px bg `#FBF1EC`, border `#E6CFC4`, texto `#B14A2E`, label "Excluir tarefa" + ícone lixeira SVG `aria-hidden`. onClick → onDelete. NUNCA renderizar em create ou idle (render condicional `{mode === 'edit' && (...)}`).
  </action>
  <verify>
    <automated>grep -q "export interface TaskFormData" src/components/parent/task-form-panel.tsx && grep -q "export const EMPTY_FORM" src/components/parent/task-form-panel.tsx && grep -q "export function taskToFormData" src/components/parent/task-form-panel.tsx && grep -q "export function TaskFormPanel" src/components/parent/task-form-panel.tsx && grep -q "mode === 'edit'" src/components/parent/task-form-panel.tsx && grep -q "Excluir tarefa" src/components/parent/task-form-panel.tsx && grep -q "Selecione uma tarefa ou clique em" src/components/parent/task-form-panel.tsx && grep -q "width: 336\|width:336" src/components/parent/task-form-panel.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - task-form-panel.tsx exporta `TaskFormData`, `EMPTY_FORM`, `taskToFormData`, `TaskFormPanel`
    - mode='idle' renderiza o placeholder exato "Selecione uma tarefa ou clique em + para criar"
    - DeleteButton só aparece com `mode === 'edit'` (`{mode === 'edit' &&` no source) — nunca em create/idle (PTASK-10)
    - CTA desabilitado mostra "Selecione uma criança" com `aria-disabled`
    - Painel tem largura fixa 336px com `flexShrink: 0`
    - `npx tsc --noEmit` sem erro
  </acceptance_criteria>
  <done>TaskFormPanel com idle/create/edit, CTA condicional e DeleteButton só-em-edit; exporta o contrato TaskFormData/EMPTY_FORM/taskToFormData; compila.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 05-03)

Componentes e contratos novos em src/components/parent/ — excluir da verificação de drift:
- `CategoryChips` (category-chips.tsx) — props { value, onChange }
- `RewardStepper` (reward-stepper.tsx) — props { value, onChange }
- `RecurrencePills` (recurrence-pills.tsx) — props { value, onChange }
- `AssigneeSelector` (assignee-selector.tsx) — props { familyChildren, value, onChange }
- `TaskFormPanel` (task-form-panel.tsx) — props { mode, formData, onChange, onSave, onDelete, onCancel, familyChildren }
- `interface TaskFormData`, `const EMPTY_FORM`, `function taskToFormData` (task-form-panel.tsx)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input do usuário (título da tarefa) | texto digitado no TaskTitleInput, mantido em estado local |
| formData (componente controlado) | pai (ParentPanelView) é a fonte de verdade; painel só edita via onChange |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | TaskTitleInput / render do título (XSS) | mitigate | Título renderizado via JSX/value de input controlado; React escapa; proibido `dangerouslySetInnerHTML`; sem persistência nesta fase (V5 ASVS: validação de comprimento no Plano 04 antes de salvar) |
| T-05-06 | Elevation of Privilege | exclusão de tarefa sem confirmação | accept | Estado mock local sem persistência; comportamento sem modal é decisão de design (UI-SPEC §Copywriting "remoção imediata") — risco aceitável em fase de UI |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo instalado (RESEARCH §Package Legitimacy: zero novas instalações) |
</threat_model>

<verification>
- `npx tsc --noEmit` sem erros nos 5 componentes
- grep confirma o contrato TaskFormData/EMPTY_FORM/taskToFormData e o render condicional do DeleteButton
- `pnpm lint` sem erros novos
</verification>

<success_criteria>
- TaskFormPanel funcional nos 3 modos (idle/create/edit) com largura fixa 336px (PTASK-06)
- Stepper Mordomia/R$ (PTASK-07) e pills com "Todos os dias" (PTASK-08)
- DeleteButton só em modo edit (PTASK-10)
- Contrato TaskFormData exportado para o Plano 04
</success_criteria>

<output>
Create `.planning/phases/05-parent-panel/05-03-SUMMARY.md` when done
</output>
