---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/sw.ts
autonomous: true
requirements: [DS-01, DS-02, DS-03, DS-04]
user_setup: []

must_haves:
  truths:
    - "O bloco @theme em globals.css define todos os 17 tokens de cor kreds (verde primário #3E6B4F incluso)"
    - "Os 15 keyframes kreds (kredsBreath..kredsSpin) estão definidos DENTRO do @theme e mapeados para tokens --animate-kreds-*"
    - "Tokens de radius (7), shadow (4) e a referência --font-sans para Plus Jakarta Sans existem no @theme"
    - "O stub src/app/sw.ts existe e satisfaz o swSrc do next.config.ts, evitando falha de build"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Fonte única de verdade dos design tokens (cores, tipografia, radius, shadow, animações)"
      contains: "@import \"tailwindcss\""
      min_lines: 110
    - path: "src/app/sw.ts"
      provides: "Service worker stub Serwist requerido pelo build"
      contains: "self.__SW_MANIFEST"
      min_lines: 8
  key_links:
    - from: "src/app/globals.css"
      to: "@tailwindcss/postcss"
      via: "@import \"tailwindcss\" processado pelo plugin PostCSS"
      pattern: "@import\\s+\"tailwindcss\""
    - from: "src/app/globals.css @theme --animate-kreds-*"
      to: "@keyframes kreds*"
      via: "token de animação referencia keyframe nomeado no mesmo bloco"
      pattern: "--animate-kreds-breath:\\s*kredsBreath"
    - from: "src/app/sw.ts"
      to: "next.config.ts swSrc"
      via: "path src/app/sw.ts resolvido pelo @serwist/next no build"
      pattern: "@serwist/next/worker"
---

<objective>
Criar a fonte única de verdade dos design tokens do Kreds em `src/app/globals.css` usando a diretiva `@theme` do Tailwind v4, e criar o stub mínimo do service worker `src/app/sw.ts` exigido pelo `next.config.ts`. Este plano cobre o coração de DS-01 (cores), DS-02 (referência de fonte), DS-03 (animações/keyframes) e DS-04 (radius/shadow/spacing).

Purpose: Todas as fases 2–6 consomem estes tokens como classes Tailwind (`bg-kreds-primary`, `text-kreds-text`, `animate-kreds-breath`, `rounded-card-md`, `shadow-card`). Sem este arquivo, nenhuma tela subsequente pode atingir fidelidade pixel-a-pixel com o design handoff. O `sw.ts` é incluído aqui porque o build inteiro quebra sem ele (o `next.config.ts` aponta `swSrc: 'src/app/sw.ts'`), e ele é independente dos demais arquivos React.

Output: `src/app/globals.css` (tokens completos) e `src/app/sw.ts` (stub Serwist).
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@design_handoff_kreds/README.md
@postcss.config.mjs
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar src/app/globals.css com todos os design tokens via @theme</name>
  <files>src/app/globals.css</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1 — bloco @theme completo, linhas 138-272; Anti-Patterns linhas 334-340)
    - .planning/phases/01-foundation/01-PATTERNS.md (estrutura obrigatória do arquivo, linhas 36-167)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Color linhas 84-145, Border-Radius linhas 148-169, Shadow linhas 171-182, Animation Contract linhas 184-228, Spacing Scale linhas 33-55)
    - design_handoff_kreds/README.md (Design Tokens linhas 246-312 — fonte autoritativa de valores hex)
    - postcss.config.mjs (confirma @tailwindcss/postcss ativo)
  </read_first>
  <action>
    Criar `src/app/globals.css` começando com a diretiva `@import "tailwindcss";` (NUNCA usar `@tailwind base/components/utilities` — proibido em v4). Em seguida, abrir um único bloco `@theme { ... }` contendo, nesta ordem:

    COR (DS-01) — declarar exatamente estes 17 tokens `--color-kreds-*` com os hex literais: `--color-kreds-primary: #3E6B4F`, `--color-kreds-primary-lt: #5A8A66`, `--color-kreds-soft: #E7EFE8`, `--color-kreds-hover: #EEF3EA`, `--color-kreds-bg: #F2F0E7`, `--color-kreds-card: #FBFAF5`, `--color-kreds-border: #ECE7DB`, `--color-kreds-border-alt: #E2DECF`, `--color-kreds-orange: #B5623F`, `--color-kreds-coin: #E3C57C`, `--color-kreds-gold: #9A7320`, `--color-kreds-text: #27372C`, `--color-kreds-muted: #7C8676`, `--color-kreds-hint: #9AA092`, `--color-kreds-error: #B14A2E`, `--color-kreds-rose: #C98AA0`, `--color-kreds-water: #6E9BA0`.

    TIPOGRAFIA (DS-02) — declarar `--font-sans: var(--font-plus-jakarta), system-ui, sans-serif;`. O `--font-plus-jakarta` será injetado pelo `layout.tsx` (Plano 02) via `next/font`. NÃO importar a fonte por `<link>` ou `@import url(...)`.

    BORDER-RADIUS (DS-04) — declarar exatamente estes 7 tokens: `--radius-app: 26px`, `--radius-card-lg: 20px`, `--radius-card-md: 18px`, `--radius-card-sm: 16px`, `--radius-input: 13px`, `--radius-chip: 10px`, `--radius-pill: 999px`.

    SOMBRAS (DS-04) — declarar exatamente estes 4 tokens: `--shadow-app: 0 40px 90px -36px rgba(40,55,45,.55)`, `--shadow-card: 0 16px 36px -26px rgba(40,55,45,.5)`, `--shadow-cta: 0 12px 24px -12px rgba(62,107,79,.6)`, `--shadow-device: 0 34px 70px -22px rgba(40,55,45,.5)`.

    ANIMAÇÕES (DS-03) — declarar exatamente estes 15 tokens `--animate-kreds-*` referenciando keyframes nomeados: `--animate-kreds-breath: kredsBreath 5s ease-in-out infinite`, `--animate-kreds-pop: kredsPop 0.6s ease`, `--animate-kreds-sun: kredsSun 5s ease-in-out infinite`, `--animate-kreds-drift1: kredsDrift1 16s ease-in-out alternate infinite`, `--animate-kreds-drift2: kredsDrift2 20s ease-in-out alternate infinite`, `--animate-kreds-flutter: kredsFlutter 3s ease-in-out infinite`, `--animate-kreds-fruit: kredsFruit 1.4s ease-in-out infinite`, `--animate-kreds-drop: kredsDrop 0.72s ease-in forwards`, `--animate-kreds-confetti: kredsConfetti 2.4s linear infinite`, `--animate-kreds-cele: kredsCele 0.5s cubic-bezier(.2,.85,.3,1.3)`, `--animate-kreds-bubble: kredsBubble 0.4s ease`, `--animate-kreds-sprout: kredsSprout 0.45s cubic-bezier(.2,.85,.3,1.3)`, `--animate-kreds-shake: kredsShake 0.5s cubic-bezier(.36,.07,.19,.97)`, `--animate-kreds-new: kredsNew 1.2s ease`, `--animate-kreds-spin: kredsSpin 0.7s linear infinite`.

    KEYFRAMES (DS-03) — definir os 15 blocos `@keyframes` DENTRO do bloco `@theme` (NUNCA fora — perde tree-shaking). Os percentuais/transforms exatos de cada keyframe (kredsBreath, kredsPop, kredsSun, kredsDrift1, kredsDrift2, kredsFlutter, kredsFruit, kredsDrop, kredsConfetti, kredsCele, kredsBubble, kredsSprout, kredsShake, kredsNew, kredsSpin) estão em 01-RESEARCH.md linhas 206-270 e 01-UI-SPEC.md linhas 213-227 — copiar os valores literais de lá, sem alterar.

    SPACING (DS-04) — NÃO redefinir `--spacing-*` com escala discreta. Em Tailwind v4 `--spacing` é um multiplicador único e a escala 4/8/16/24/32/48/64 (documentada na Spacing Scale do UI-SPEC) já é gerada pelo default (`p-1`=4px, `p-2`=8px, `p-4`=16px, etc.). Os valores fixos não-múltiplos-de-4 do handoff (38px, 62px, 80px, 64px, 336px, 316px) são tamanhos pontuais de componentes das fases 2–6, NÃO tokens de spacing — não criar tokens para eles aqui.

    Usar hex literal direto (não converter para oklch — Pitfall 3 do RESEARCH é apenas cosmético no editor).
  </action>
  <verify>
    <automated>test -f src/app/globals.css && grep -q '@import "tailwindcss"' src/app/globals.css && [ "$(grep -v '^[[:space:]]*/\*' src/app/globals.css | grep -c -- '--color-kreds-')" -ge 17 ] && [ "$(grep -c -- '--animate-kreds-' src/app/globals.css)" -ge 15 ] && [ "$(grep -c '@keyframes kreds' src/app/globals.css)" -ge 15 ] && grep -q -- '--radius-card-md:\s*18px' src/app/globals.css && grep -q -- '--shadow-card:' src/app/globals.css && grep -q -- '--font-sans:\s*var(--font-plus-jakarta)' src/app/globals.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo `src/app/globals.css` existe e começa com `@import "tailwindcss";`
    - Contém exatamente 17 declarações `--color-kreds-*`, incluindo `--color-kreds-primary: #3E6B4F`
    - Contém 15 tokens `--animate-kreds-*` E 15 blocos `@keyframes kreds*` correspondentes, todos dentro do bloco `@theme`
    - Contém os 7 tokens `--radius-*` (app/card-lg/card-md/card-sm/input/chip/pill) e os 4 tokens `--shadow-*` (app/card/cta/device) com os valores literais do handoff
    - Contém `--font-sans: var(--font-plus-jakarta), system-ui, sans-serif;`
    - NÃO contém `@tailwind base`, `@tailwind components`, `@tailwind utilities`, nem `@import url(` apontando para fontes
    - Nenhum `@keyframes` aparece fora do bloco `@theme`
  </acceptance_criteria>
  <done>globals.css existe com todos os tokens de cor, tipografia, radius, shadow e as 15 animações + keyframes dentro do @theme; o grep de verificação retorna OK.</done>
</task>

<task type="auto">
  <name>Task 2: Criar src/app/sw.ts (stub Serwist) para o build não quebrar</name>
  <files>src/app/sw.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3 — stub Serwist, linhas 313-332; Pitfall 2 linhas 369-377)
    - .planning/phases/01-foundation/01-PATTERNS.md (sw.ts, linhas 256-291)
    - next.config.ts (confirma swSrc: 'src/app/sw.ts' — linha 9)
  </read_first>
  <action>
    Criar `src/app/sw.ts` como stub mínimo do Serwist conforme Pattern 3 do RESEARCH. Importar `defaultCache` de `@serwist/next/worker` e `Serwist` de `serwist`. Instanciar `new Serwist({ precacheEntries: self.__SW_MANIFEST, skipWaiting: true, clientsClaim: true, runtimeCaching: defaultCache })` e chamar `serwist.addEventListeners()`.

    O global `self.__SW_MANIFEST` é injetado pelo compilador `@serwist/next` no build — em TypeScript puro ele não tem tipo. Para evitar erro de tipo em `strict: true` (tsconfig.json), adicionar no topo do arquivo a tripla-barra de referência de lib WebWorker e declarar o global: incluir `/// <reference lib="webworker" />` e uma declaração `declare const self: ServiceWorkerGlobalScope & { __SW_MANIFEST: (string | import("serwist").PrecacheEntry)[] };` ANTES dos imports não — coloque a `/// <reference>` na primeira linha e a `declare` após os imports. Isto satisfaz o `tsc` sem alterar runtime. Não criar arquivo `.d.ts` separado.

    Seguir o estilo de export/import do projeto (import no topo, sem barrel exports — ver Estilo de Export de Configs em 01-PATTERNS.md linhas 314-325). Este arquivo NÃO deve importar nada de `globals.css`, `layout.tsx` ou `page.tsx` — é independente.
  </action>
  <verify>
    <automated>test -f src/app/sw.ts && grep -q '@serwist/next/worker' src/app/sw.ts && grep -q 'self.__SW_MANIFEST' src/app/sw.ts && grep -q 'addEventListeners' src/app/sw.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo `src/app/sw.ts` existe
    - Importa `defaultCache` de `@serwist/next/worker` e `Serwist` de `serwist`
    - Instancia `new Serwist({ ... })` com `precacheEntries: self.__SW_MANIFEST`, `skipWaiting: true`, `clientsClaim: true`, `runtimeCaching: defaultCache`
    - Chama `serwist.addEventListeners()`
    - Contém `/// <reference lib="webworker" />` na primeira linha e uma `declare const self` tipando `__SW_MANIFEST`, evitando erro de tipo sob `strict: true`
    - Não importa nenhum dos outros arquivos da fase (globals.css/layout.tsx/page.tsx)
  </acceptance_criteria>
  <done>sw.ts existe como stub Serwist válido; o grep de verificação retorna OK e o arquivo está pronto para ser compilado pelo @serwist/next no Plano 02.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Símbolos criados neste plano (excluir da verificação de drift — são novos, não preexistentes):

**CSS custom properties (tokens @theme em src/app/globals.css):**
- Cores: `--color-kreds-primary`, `--color-kreds-primary-lt`, `--color-kreds-soft`, `--color-kreds-hover`, `--color-kreds-bg`, `--color-kreds-card`, `--color-kreds-border`, `--color-kreds-border-alt`, `--color-kreds-orange`, `--color-kreds-coin`, `--color-kreds-gold`, `--color-kreds-text`, `--color-kreds-muted`, `--color-kreds-hint`, `--color-kreds-error`, `--color-kreds-rose`, `--color-kreds-water`
- Tipografia: `--font-sans`
- Radius: `--radius-app`, `--radius-card-lg`, `--radius-card-md`, `--radius-card-sm`, `--radius-input`, `--radius-chip`, `--radius-pill`
- Shadow: `--shadow-app`, `--shadow-card`, `--shadow-cta`, `--shadow-device`
- Animação: `--animate-kreds-breath`, `--animate-kreds-pop`, `--animate-kreds-sun`, `--animate-kreds-drift1`, `--animate-kreds-drift2`, `--animate-kreds-flutter`, `--animate-kreds-fruit`, `--animate-kreds-drop`, `--animate-kreds-confetti`, `--animate-kreds-cele`, `--animate-kreds-bubble`, `--animate-kreds-sprout`, `--animate-kreds-shake`, `--animate-kreds-new`, `--animate-kreds-spin`

**Keyframes (@keyframes em src/app/globals.css):**
- `kredsBreath`, `kredsPop`, `kredsSun`, `kredsDrift1`, `kredsDrift2`, `kredsFlutter`, `kredsFruit`, `kredsDrop`, `kredsConfetti`, `kredsCele`, `kredsBubble`, `kredsSprout`, `kredsShake`, `kredsNew`, `kredsSpin`

**Classes Tailwind geradas automaticamente (consumíveis nas fases 2–6):**
- `bg-kreds-*`, `text-kreds-*`, `border-kreds-*` (uma por token de cor)
- `rounded-app`, `rounded-card-lg`, `rounded-card-md`, `rounded-card-sm`, `rounded-input`, `rounded-chip`, `rounded-pill`
- `shadow-app`, `shadow-card`, `shadow-cta`, `shadow-device`
- `animate-kreds-breath`, `animate-kreds-pop`, `animate-kreds-sun`, `animate-kreds-drift1`, `animate-kreds-drift2`, `animate-kreds-flutter`, `animate-kreds-fruit`, `animate-kreds-drop`, `animate-kreds-confetti`, `animate-kreds-cele`, `animate-kreds-bubble`, `animate-kreds-sprout`, `animate-kreds-shake`, `animate-kreds-new`, `animate-kreds-spin`
- `font-sans` (remapeada para Plus Jakarta Sans)

**Arquivos:**
- `src/app/globals.css`
- `src/app/sw.ts`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

Nenhuma. Esta fase produz apenas CSS estático (tokens) e um stub de service worker sem lógica de runtime própria. Não há entrada de usuário, formulários, autenticação, nem chamada de rede dinâmica. Não há fronteira de confiança cruzada nesta fase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-T | Tampering | src/app/sw.ts (service worker stub) | accept | Stub usa apenas `defaultCache` do `@serwist/next` (pacote oficial já instalado e auditado no RESEARCH). Sem runtime caching customizado ou interceptação de fetch sensível. Cache de assets estáticos não expõe superfície de adulteração nesta fase. |
| T-01-01-I | Information Disclosure | src/app/globals.css (tokens) | accept | Tokens de cor/tipografia/animação são públicos por natureza (CSS servido ao cliente). Nenhum segredo ou PII envolvido. |
| T-01-01-SC | Tampering | npm/pnpm installs | accept | Nenhum pacote novo é instalado nesta fase — `next`, `tailwindcss`, `@tailwindcss/postcss`, `@serwist/next` e `serwist` já existem em node_modules e foram auditados (RESEARCH.md Package Legitimacy Audit). Sem gate de legitimidade necessário. |

ASVS L1: fase é CSS/tokens apenas — categorias V2–V6 não se aplicam (confirmado em 01-RESEARCH.md Security Domain). Block-on: nenhuma ameaça de alta severidade identificada.
</threat_model>

<verification>
Após ambas as tasks:
- `test -f src/app/globals.css` e `test -f src/app/sw.ts` retornam sucesso
- `grep '@import "tailwindcss"' src/app/globals.css` encontra a diretiva v4
- `grep -c '@keyframes kreds' src/app/globals.css` ≥ 15
- `grep 'self.__SW_MANIFEST' src/app/sw.ts` encontra o manifest do service worker
- O build completo é validado no Plano 02 (que cria layout.tsx/page.tsx e roda `pnpm build`)
</verification>

<success_criteria>
- `src/app/globals.css` contém os 17 tokens de cor, 7 de radius, 4 de shadow, 15 animações + 15 keyframes e a referência `--font-sans`, todos dentro de um único `@theme` precedido por `@import "tailwindcss"`
- `src/app/sw.ts` existe como stub Serwist válido tipado, pronto para ser compilado pelo `@serwist/next`
- Nenhum anti-pattern presente (sem `tailwind.config.ts`, sem `@tailwind` directives, sem `@keyframes` fora do `@theme`, sem import de fonte via `<link>`)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
