---
phase: 12-m-dulo-de-fx
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema/fx.ts
  - src/lib/db/schema/index.ts
  - src/lib/auth/guardian-family.ts
  - src/modules/fx/schema.ts
  - src/modules/fx/queries.ts
autonomous: true
requirements:
  - FX-01
  - FX-04

must_haves:
  truths:
    - "Schema fx_purchase_orders existe com campos: id, familyId, poNumber, company, vendor, currency, foreignAmount, diRate, dueDate, createdAt, updatedAt"
    - "fxCurrencyEnum aceita apenas 'USD' e 'EUR'"
    - "resolveGuardianFamilyId(session) faz 2 queries DB e retorna familyId string"
    - "purchaseOrderSchema valida dueDate com z.string().date() (YYYY-MM-DD)"
    - "getPurchaseOrders filtra obrigatoriamente por familyId"
  artifacts:
    - path: "src/lib/db/schema/fx.ts"
      provides: "Drizzle schema — fxCurrencyEnum + fxPurchaseOrders table"
      exports:
        - fxCurrencyEnum
        - fxPurchaseOrders
    - path: "src/lib/db/schema/index.ts"
      provides: "Re-exporta fx.ts via export * from './fx'"
      contains: "export * from './fx'"
    - path: "src/lib/auth/guardian-family.ts"
      provides: "Helper server-only resolveGuardianFamilyId(session)"
      exports:
        - resolveGuardianFamilyId
    - path: "src/modules/fx/schema.ts"
      provides: "Zod purchaseOrderSchema para validação POST"
      exports:
        - purchaseOrderSchema
        - PurchaseOrderInput
    - path: "src/modules/fx/queries.ts"
      provides: "getPurchaseOrders server-only query com filtros opcionais"
      exports:
        - getPurchaseOrders
  key_links:
    - from: "src/lib/auth/guardian-family.ts"
      to: "src/lib/db/schema/index.ts"
      via: "import identities, familyMemberships from schema"
      pattern: "from '@/lib/db/schema'"
    - from: "src/modules/fx/queries.ts"
      to: "src/lib/db/schema/fx.ts"
      via: "import fxPurchaseOrders"
      pattern: "from '@/lib/db/schema/fx'"
    - from: "src/lib/db/schema/index.ts"
      to: "src/lib/db/schema/fx.ts"
      via: "export * from './fx'"
      pattern: "export \\* from './fx'"
---

<objective>
Criar toda a fundação backend da Fase 12: schema Drizzle, helper de resolução familyId, schema Zod de validação, e módulo de queries server-only.

Purpose: Estes artefatos são pré-requisitos para o drizzle-kit push (Plan 03) e para os Route Handlers (Plan 04). Nenhum arquivo de API ou UI pode existir sem eles.
Output: 5 arquivos — fx.ts (schema), guardian-family.ts (helper auth), schema.ts (Zod), queries.ts (DB queries), + atualização de index.ts.
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/hass/repos/github/comphass/kreds/.planning/ROADMAP.md
@/Users/hass/repos/github/comphass/kreds/.planning/phases/12-m-dulo-de-fx/12-RESEARCH.md
@/Users/hass/repos/github/comphass/kreds/src/lib/db/schema/index.ts
@/Users/hass/repos/github/comphass/kreds/src/lib/db/schema/ledger.ts
@/Users/hass/repos/github/comphass/kreds/src/lib/auth/child-guard.ts
@/Users/hass/repos/github/comphass/kreds/src/types/next-auth.d.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Schema Drizzle fx.ts + re-export em index.ts</name>
  <files>src/lib/db/schema/fx.ts, src/lib/db/schema/index.ts</files>
  <read_first>
    - /Users/hass/repos/github/comphass/kreds/src/lib/db/schema/index.ts (linha 205 — padrão export * from './ledger'; confirmar linha final antes de adicionar export * from './fx')
    - /Users/hass/repos/github/comphass/kreds/src/lib/db/schema/ledger.ts (padrão de tabela com index + enum)
    - /Users/hass/repos/github/comphass/kreds/.planning/phases/12-m-dulo-de-fx/12-RESEARCH.md (seção "Schema Drizzle: fx_purchase_orders" — copiar exatamente)
  </read_first>
  <action>
    Criar `src/lib/db/schema/fx.ts` com o conteúdo exato definido na RESEARCH.md (seção "Schema Drizzle: fx_purchase_orders"):

    Imports de `drizzle-orm/pg-core`: pgTable, uuid, text, timestamp, pgEnum, index, numeric.
    Import de `'./index'`: families (referência FK para familyId).

    ATENÇÃO: importar `families` de `'./index'` pode criar circular dependency. Verificar se `ledger.ts` usa o mesmo padrão — se sim, seguir. Se o padrão for `import { families } from '.'`, usar esse.

    Definir `fxCurrencyEnum = pgEnum('fx_currency', ['USD', 'EUR'])`.

    Definir `fxPurchaseOrders` com:
    - `id`: uuid defaultRandom primaryKey
    - `familyId`: uuid notNull references families.id
    - `poNumber`: text notNull
    - `company`: text notNull
    - `vendor`: text notNull
    - `currency`: fxCurrencyEnum('currency') notNull
    - `foreignAmount`: numeric('foreign_amount', { precision: 18, scale: 2 }) notNull (DRIZZLE RETORNA STRING — documentar em comentário)
    - `diRate`: numeric('di_rate', { precision: 18, scale: 6 }) notNull
    - `dueDate`: text('due_date') notNull (formato 'YYYY-MM-DD' — text evita timezone surprises, alinhado com taskCompletions.cycleStart)
    - `createdAt`: timestamp defaultNow notNull
    - `updatedAt`: timestamp defaultNow notNull
    - Índice: `familyIdIdx` = index('fx_purchase_orders_family_id_idx').on(table.familyId)

    Adicionar `export * from './fx'` ao final de `src/lib/db/schema/index.ts` (após a linha `export * from './ledger'`).
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && npx tsc --noEmit 2>&1 | grep -E "(fx\.ts|schema/index)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/db/schema/fx.ts` existe e exporta `fxCurrencyEnum` e `fxPurchaseOrders`
    - `src/lib/db/schema/index.ts` contém `export * from './fx'` na última linha relevante
    - `npx tsc --noEmit` não produz erros em `schema/fx.ts` nem em `schema/index.ts`
    - A tabela tem os campos: id, family_id, po_number, company, vendor, currency, foreign_amount (numeric), di_rate (numeric), due_date (text), created_at, updated_at
    - Comentário no arquivo documenta: "numeric() retorna string no TypeScript — usar toNumber() de @/modules/fx/calculations antes de calcular"
  </acceptance_criteria>
  <done>Schema fx.ts compilando sem erros TypeScript; index.ts re-exporta fx.ts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: helper guardian-family.ts + módulo fx/schema.ts + fx/queries.ts</name>
  <files>src/lib/auth/guardian-family.ts, src/modules/fx/schema.ts, src/modules/fx/queries.ts</files>
  <read_first>
    - /Users/hass/repos/github/comphass/kreds/src/lib/auth/child-guard.ts (padrão import 'server-only' na linha 1)
    - /Users/hass/repos/github/comphass/kreds/src/types/next-auth.d.ts (confirmar que session.user só tem: id, email, name, image, systemRoles — familyId NÃO existe)
    - /Users/hass/repos/github/comphass/kreds/.planning/phases/12-m-dulo-de-fx/12-RESEARCH.md (seção "Padrão Crítico: Resolução do familyId do Guardian" — código exato do helper; seção "Validação Zod" — purchaseOrderSchema)
    - /Users/hass/repos/github/comphass/kreds/tests/unit/family-authorization.test.ts (padrão DB lookup identityId → familyId — confirmar nomes de colunas)
  </read_first>
  <action>
    ARQUIVO 1 — `src/lib/auth/guardian-family.ts`:
    Primeira linha: `import 'server-only'`
    Imports: `{ db } from '@/lib/db'`, `{ identities, familyMemberships } from '@/lib/db/schema'`, `{ eq, and } from 'drizzle-orm'`, `type { Session } from 'next-auth'`.

    Exportar `async function resolveGuardianFamilyId(session: Session): Promise<string>`:
    - Extrair `zitadelSubject = session.user.id`
    - Guard: se !zitadelSubject, throw new Error('session.user.id ausente')
    - Query 1: `db.select({ id: identities.id }).from(identities).where(eq(identities.zitadelSubject, zitadelSubject)).limit(1)` → desestruturar `[identity]`
    - Guard: se !identity, throw new Error(`Identity não encontrada para zitadelSubject: ${zitadelSubject}`)
    - Query 2: `db.select({ familyId: familyMemberships.familyId }).from(familyMemberships).where(and(eq(familyMemberships.identityId, identity.id), eq(familyMemberships.role, 'guardian'))).limit(1)` → desestruturar `[membership]`
    - Guard: se !membership, throw new Error(`Membership guardian não encontrada para identityId: ${identity.id}`)
    - Retornar `membership.familyId`

    VERIFICAR: o campo da coluna `zitadelSubject` em `identities` — confirmar nome exato lendo schema/index.ts antes de escrever o helper.

    ARQUIVO 2 — `src/modules/fx/schema.ts`:
    Import apenas `{ z } from 'zod'` (sem drizzle, sem @/lib/db).
    Exportar `purchaseOrderSchema = z.object({...})` com:
    - `poNumber`: z.string().min(1).max(50)
    - `company`: z.string().min(1).max(100)
    - `vendor`: z.string().min(1).max(100)
    - `currency`: z.enum(['USD', 'EUR'])
    - `foreignAmount`: z.coerce.number().positive()
    - `diRate`: z.coerce.number().positive()
    - `dueDate`: z.string().date() — ATENÇÃO: usar `.date()` não `.datetime()` — valida YYYY-MM-DD (Zod v4 confirmado em runtime)
    Exportar `type PurchaseOrderInput = z.infer<typeof purchaseOrderSchema>`.

    ARQUIVO 3 — `src/modules/fx/queries.ts`:
    Primeira linha: `import 'server-only'`
    Imports: `{ db } from '@/lib/db'`, `{ fxPurchaseOrders } from '@/lib/db/schema/fx'`, `{ and, eq } from 'drizzle-orm'`.
    Exportar `async function getPurchaseOrders(opts: { familyId: string; company?: string; vendor?: string })`:
    - `db.select().from(fxPurchaseOrders).where(and(eq(fxPurchaseOrders.familyId, opts.familyId), opts.company ? eq(fxPurchaseOrders.company, opts.company) : undefined, opts.vendor ? eq(fxPurchaseOrders.vendor, opts.vendor) : undefined))`
    - Retorna a query diretamente (Promise<FxPurchaseOrder[]> inferido pelo Drizzle)
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && npx tsc --noEmit 2>&1 | grep -E "(guardian-family|fx/schema|fx/queries)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/auth/guardian-family.ts` inicia com `import 'server-only'`
    - `resolveGuardianFamilyId` é exportada e aceita `Session` de next-auth
    - `src/modules/fx/schema.ts` usa `z.string().date()` (não `.datetime()`) para dueDate
    - `src/modules/fx/queries.ts` inicia com `import 'server-only'`
    - `getPurchaseOrders` aceita `{ familyId: string; company?: string; vendor?: string }`
    - `npx tsc --noEmit` sem erros nos 3 arquivos novos
  </acceptance_criteria>
  <done>3 arquivos criados e compilando. helper auth, schema Zod, queries DB server-only — todos sem erros TypeScript.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session → familyId | Guardian não pode fornecer familyId via request — resolvido pelo helper via DB |
| client body → DB insert | purchaseOrderSchema zod valida antes de qualquer insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Spoofing | resolveGuardianFamilyId | mitigate | familyId NUNCA do body/query — derivado de session.user.id via 2 queries DB |
| T-12-02 | Tampering | purchaseOrderSchema | mitigate | z.string().date() + z.coerce.number().positive() — rejeita inputs maliciosos antes do insert |
| T-12-03 | Elevation of Privilege | getPurchaseOrders | mitigate | WHERE clause sempre inclui familyId derivado da sessão — nunca familyId do request |
| T-12-SC | Tampering | npm installs | accept | Nenhum pacote novo — todas as dependências já auditadas em fases anteriores |
</threat_model>

<verification>
`npx tsc --noEmit` sem erros em schema/fx.ts, guardian-family.ts, modules/fx/schema.ts, modules/fx/queries.ts.
</verification>

<success_criteria>
1. `src/lib/db/schema/fx.ts` exporta `fxCurrencyEnum` e `fxPurchaseOrders` com 11 campos
2. `src/lib/db/schema/index.ts` re-exporta fx.ts
3. `src/lib/auth/guardian-family.ts` implementa lookup de 2 queries, server-only
4. `src/modules/fx/schema.ts` usa z.string().date() para dueDate
5. `src/modules/fx/queries.ts` filtra sempre por familyId, server-only
6. `npx tsc --noEmit` sem erros nos 5 arquivos
</success_criteria>

<output>
Criar `.planning/phases/12-m-dulo-de-fx/12-02-SUMMARY.md` quando concluído.
</output>

## Artifacts this phase produces

| Symbol | File | Type | Consumed By |
|--------|------|------|-------------|
| `fxCurrencyEnum` | src/lib/db/schema/fx.ts | Drizzle pgEnum | Plan 03 (drizzle-kit), Plan 04 (routes) |
| `fxPurchaseOrders` | src/lib/db/schema/fx.ts | Drizzle table | Plan 03 (drizzle-kit), Plan 04 (routes), Plan 05 (page) |
| `resolveGuardianFamilyId` | src/lib/auth/guardian-family.ts | async function | Plan 04 (purchase-orders route), Plan 05 (page) |
| `purchaseOrderSchema` | src/modules/fx/schema.ts | Zod schema | Plan 04 (POST /api/fx/purchase-orders) |
| `PurchaseOrderInput` | src/modules/fx/schema.ts | TypeScript type | Plan 04 (POST body type) |
| `getPurchaseOrders` | src/modules/fx/queries.ts | async function | Plan 05 (SSR page) |
