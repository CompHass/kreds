---
phase: 12-m-dulo-de-fx
plan: "04"
type: execute
wave: 3
depends_on:
  - "12-02"
  - "12-03"
files_modified:
  - src/app/api/fx/rates/route.ts
  - src/app/api/fx/purchase-orders/route.ts
autonomous: true
requirements:
  - FX-01
  - FX-02

must_haves:
  truths:
    - "GET /api/fx/rates retorna { usd_brl: number, eur_brl: number, fetched_at: string, source: string }"
    - "GET /api/fx/rates retorna 401 sem sessão Guardian autenticada"
    - "GET /api/fx/purchase-orders retorna lista de POs filtrada pelo familyId da sessão"
    - "POST /api/fx/purchase-orders valida com Zod e insere no banco"
    - "familyId NUNCA aceito do body/query — sempre de resolveGuardianFamilyId(session)"
    - "URL open.er-api.com hardcoded no Route Handler — nunca aceita do cliente"
  artifacts:
    - path: "src/app/api/fx/rates/route.ts"
      provides: "Route Handler proxy GET /api/fx/rates → open.er-api.com"
      exports:
        - GET
    - path: "src/app/api/fx/purchase-orders/route.ts"
      provides: "Route Handlers GET e POST /api/fx/purchase-orders"
      exports:
        - GET
        - POST
  key_links:
    - from: "src/app/api/fx/rates/route.ts"
      to: "https://open.er-api.com/v6/latest/USD"
      via: "fetch server-side com revalidate 3600"
      pattern: "open.er-api.com"
    - from: "src/app/api/fx/purchase-orders/route.ts"
      to: "src/lib/auth/guardian-family.ts"
      via: "resolveGuardianFamilyId(session)"
      pattern: "resolveGuardianFamilyId"
    - from: "src/app/api/fx/purchase-orders/route.ts"
      to: "src/modules/fx/queries.ts"
      via: "getPurchaseOrders({ familyId })"
      pattern: "getPurchaseOrders"
---

<objective>
Criar os dois Route Handlers da API FX: proxy de cotação cambial e CRUD de Purchase Orders.

Purpose: FxAnalysisClient (Plan 05) chama `/api/fx/rates` via useEffect — sem este Route Handler, a cotação não carrega (CORS bloqueia fetch direto ao open.er-api.com). O route `/api/fx/purchase-orders` expõe GET e POST para gestão de POs.
Output: 2 Route Handlers autenticados — rates (proxy cambial) e purchase-orders (CRUD com Zod + resolveGuardianFamilyId).
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/hass/repos/github/comphass/kreds/.planning/ROADMAP.md
@/Users/hass/repos/github/comphass/kreds/.planning/phases/12-m-dulo-de-fx/12-RESEARCH.md
@/Users/hass/repos/github/comphass/kreds/src/lib/auth/guardian-family.ts
@/Users/hass/repos/github/comphass/kreds/src/modules/fx/queries.ts
@/Users/hass/repos/github/comphass/kreds/src/modules/fx/schema.ts
@/Users/hass/repos/github/comphass/kreds/src/lib/db/schema/fx.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Route Handler GET /api/fx/rates — proxy cambial</name>
  <files>src/app/api/fx/rates/route.ts</files>
  <read_first>
    - /Users/hass/repos/github/comphass/kreds/src/app/api/auth/[...nextauth]/route.ts (padrão de Route Handler — imports NextResponse, auth)
    - /Users/hass/repos/github/comphass/kreds/.planning/phases/12-m-dulo-de-fx/12-RESEARCH.md (seção "Route Handler: /api/fx/rates" — código exato)
  </read_first>
  <action>
    Criar diretório `src/app/api/fx/rates/` e arquivo `route.ts`.

    Imports: `{ NextResponse } from 'next/server'`, `{ auth } from '@/auth'`.

    Constante no topo do arquivo: `const ER_API_BASE = 'https://open.er-api.com/v6/latest'` — SSRF guard: URL hardcoded, nunca aceita do cliente.

    Exportar `async function GET()`:
    1. `const session = await auth()` — se !session, retornar `NextResponse.json({ error: 'Unauthorized' }, { status: 401 })`
    2. `Promise.all` de 2 fetches: `${ER_API_BASE}/USD` e `${ER_API_BASE}/EUR`, ambos com `{ next: { revalidate: 3600 } }` (cache 1 hora no Next.js)
    3. Se !usdRes.ok || !eurRes.ok → `NextResponse.json({ error: 'Falha ao buscar cotações' }, { status: 502 })`
    4. `Promise.all([usdRes.json(), eurRes.json()])` para parsear
    5. Retornar: `NextResponse.json({ usd_brl: usdData.rates['BRL'] as number, eur_brl: eurData.rates['BRL'] as number, fetched_at: new Date().toISOString(), source: 'open.er-api.com' })`
    6. Envolver em try/catch → catch retorna `NextResponse.json({ error: 'Erro interno ao buscar cotações' }, { status: 500 })`

    NOTA: `{ next: { revalidate: 3600 } }` é sintaxe estendida do Next.js para cache server-side. Não é fetch padrão — funciona somente em Server Components e Route Handlers (não em Client Components).
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && npx tsc --noEmit 2>&1 | grep "fx/rates" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/fx/rates/route.ts` existe e exporta `GET`
    - Contém `const ER_API_BASE = 'https://open.er-api.com/v6/latest'` (hardcoded — nunca do cliente)
    - Usa `{ next: { revalidate: 3600 } }` no fetch
    - Retorna 401 quando `!session`
    - Retorna 502 quando a API externa falha
    - `npx tsc --noEmit` sem erros em `src/app/api/fx/rates/route.ts`
  </acceptance_criteria>
  <done>Route Handler GET /api/fx/rates compilando; proxy para open.er-api.com com cache de 1h e guard 401.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Route Handler GET+POST /api/fx/purchase-orders</name>
  <files>src/app/api/fx/purchase-orders/route.ts</files>
  <read_first>
    - /Users/hass/repos/github/comphass/kreds/src/lib/auth/guardian-family.ts (importar resolveGuardianFamilyId daqui)
    - /Users/hass/repos/github/comphass/kreds/src/modules/fx/queries.ts (getPurchaseOrders)
    - /Users/hass/repos/github/comphass/kreds/src/modules/fx/schema.ts (purchaseOrderSchema)
    - /Users/hass/repos/github/comphass/kreds/src/lib/db/schema/fx.ts (fxPurchaseOrders para insert)
    - /Users/hass/repos/github/comphass/kreds/.planning/phases/12-m-dulo-de-fx/12-PATTERNS.md (seção "purchase-orders/route.ts" — padrão GET + POST)
  </read_first>
  <action>
    Criar diretório `src/app/api/fx/purchase-orders/` e arquivo `route.ts`.

    Imports: `{ NextRequest, NextResponse } from 'next/server'`, `{ auth } from '@/auth'`, `{ db } from '@/lib/db'`, `{ fxPurchaseOrders } from '@/lib/db/schema/fx'`, `{ getPurchaseOrders } from '@/modules/fx/queries'`, `{ purchaseOrderSchema } from '@/modules/fx/schema'`, `{ resolveGuardianFamilyId } from '@/lib/auth/guardian-family'`.

    Exportar `async function GET(req: NextRequest)`:
    1. `const session = await auth()` — se !session → 401
    2. `const familyId = await resolveGuardianFamilyId(session)` — familyId NUNCA do req (padrão T-04-04 / IDOR guard)
    3. Ler query params: `const { searchParams } = new URL(req.url)`, `const company = searchParams.get('company') ?? undefined`, `const vendor = searchParams.get('vendor') ?? undefined`
    4. `const rows = await getPurchaseOrders({ familyId, company, vendor })`
    5. Retornar `NextResponse.json(rows)`
    6. Envolver em try/catch → 500

    Exportar `async function POST(req: NextRequest)`:
    1. `const session = await auth()` — se !session → 401
    2. `const familyId = await resolveGuardianFamilyId(session)`
    3. `const body = await req.json()`
    4. `const parsed = purchaseOrderSchema.safeParse(body)` — se !parsed.success → `NextResponse.json({ error: parsed.error.flatten() }, { status: 400 })`
    5. `const [row] = await db.insert(fxPurchaseOrders).values({ familyId, poNumber: parsed.data.poNumber, company: parsed.data.company, vendor: parsed.data.vendor, currency: parsed.data.currency, foreignAmount: String(parsed.data.foreignAmount), diRate: String(parsed.data.diRate), dueDate: parsed.data.dueDate }).returning()`
    6. Retornar `NextResponse.json(row, { status: 201 })`
    7. Envolver em try/catch → 500

    ATENÇÃO: Drizzle espera `numeric` fields como string no insert (`.values({ foreignAmount: String(parsed.data.foreignAmount) })`). `z.coerce.number()` converte para number no parse — reverter para string para o insert Drizzle.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && npx tsc --noEmit 2>&1 | grep "fx/purchase-orders" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/fx/purchase-orders/route.ts` exporta `GET` e `POST`
    - GET usa `resolveGuardianFamilyId(session)` — nenhuma linha lê familyId de `req.url` ou `req.body`
    - POST chama `purchaseOrderSchema.safeParse(body)` antes de qualquer insert
    - `foreignAmount` e `diRate` são convertidos para `String()` antes do `db.insert()`
    - Ambos os handlers retornam 401 quando `!session`
    - `npx tsc --noEmit` sem erros em `purchase-orders/route.ts`
  </acceptance_criteria>
  <done>2 Route Handlers (GET + POST) em /api/fx/purchase-orders compilando; familyId sempre da sessão; Zod valida antes do insert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/fx/rates | Browser envia request autenticado; URL de destino (open.er-api.com) nunca é fornecida pelo cliente |
| browser → /api/fx/purchase-orders | Body do POST validado com Zod antes de qualquer operação DB |
| session → familyId | familyId derivado exclusivamente de session.user.id via DB lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Spoofing | GET /api/fx/purchase-orders | mitigate | auth() verifica sessão JWT; 401 sem token válido |
| T-12-06 | Tampering | SSRF via rates Route Handler | mitigate | ER_API_BASE = constante hardcoded — nunca aceita URL do cliente |
| T-12-07 | Tampering | POST body (injection via company/vendor) | mitigate | Zod z.string().max(100) + Drizzle queries parametrizadas (sem SQL raw) |
| T-12-08 | Elevation of Privilege | IDOR via familyId no GET | mitigate | resolveGuardianFamilyId(session) — familyId NUNCA de query params |
| T-12-SC | Tampering | npm installs | accept | Nenhum pacote novo neste plano |
</threat_model>

<verification>
1. `npx tsc --noEmit` sem erros nos 2 novos route.ts
2. Grep confirma SSRF guard: `grep -n "ER_API_BASE" src/app/api/fx/rates/route.ts` — URL hardcoded
3. Grep confirma IDOR guard: `grep -n "resolveGuardianFamilyId" src/app/api/fx/purchase-orders/route.ts` — usado em GET e POST
</verification>

<success_criteria>
1. GET /api/fx/rates retorna 401 sem sessão; 502 quando API externa falha; 200 com {usd_brl, eur_brl, fetched_at, source} quando autenticado
2. GET /api/fx/purchase-orders filtra por familyId da sessão; nunca do request
3. POST /api/fx/purchase-orders valida Zod, insere com .returning(), retorna 201
4. `npx tsc --noEmit` verde nos 2 arquivos
</success_criteria>

<output>
Criar `.planning/phases/12-m-dulo-de-fx/12-04-SUMMARY.md` quando concluído.
</output>

## Artifacts this phase produces

| Symbol | File | Type | Consumed By |
|--------|------|------|-------------|
| `GET /api/fx/rates` | src/app/api/fx/rates/route.ts | Route Handler | FxAnalysisClient (Plan 05, useEffect) |
| `GET /api/fx/purchase-orders` | src/app/api/fx/purchase-orders/route.ts | Route Handler | FxAnalysisClient (Plan 05, opcional) |
| `POST /api/fx/purchase-orders` | src/app/api/fx/purchase-orders/route.ts | Route Handler | Futuras fases (formulário de criação de PO) |
