---
phase: 12-m-dulo-de-fx
plan: "03"
type: execute
wave: 2
depends_on:
  - "12-02"
files_modified: []
autonomous: false
requirements:
  - FX-01

must_haves:
  truths:
    - "Tabela fx_purchase_orders existe no banco PostgreSQL de desenvolvimento"
    - "Enum fx_currency existe no banco PostgreSQL de desenvolvimento"
    - "INSERT de PO com currency='USD', foreignAmount='120000.00', diRate='5.200000' persiste corretamente"
  artifacts:
    - path: "banco PostgreSQL"
      provides: "Tabela fx_purchase_orders + enum fx_currency aplicados via drizzle-kit"
  key_links:
    - from: "src/lib/db/schema/fx.ts"
      to: "banco PostgreSQL"
      via: "drizzle-kit push"
      pattern: "fx_purchase_orders"
---

<objective>
Aplicar o schema Drizzle ao banco de dados de desenvolvimento e semear dados de teste para verificação visual.

Purpose: O schema `fx_purchase_orders` criado no Plan 02 só existe em TypeScript — precisa ser materializado no banco antes que qualquer Route Handler ou SSR page consiga fazer queries reais.
Output: Tabela `fx_purchase_orders` + enum `fx_currency` no banco PostgreSQL; 3 POs de seed (PO-001, PO-002, PO-003) para verificação visual.
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/hass/repos/github/comphass/kreds/.planning/ROADMAP.md
@/Users/hass/repos/github/comphass/kreds/.planning/phases/12-m-dulo-de-fx/12-RESEARCH.md
@/Users/hass/repos/github/comphass/kreds/src/lib/db/schema/fx.ts
@/Users/hass/repos/github/comphass/kreds/package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] drizzle-kit push — aplicar fx_purchase_orders ao banco</name>
  <files></files>
  <read_first>
    - /Users/hass/repos/github/comphass/kreds/package.json (confirmar script `db:push` = `drizzle-kit push`)
    - /Users/hass/repos/github/comphass/kreds/src/lib/db/schema/fx.ts (confirmar que o arquivo existe e está correto antes do push — dependência do Plan 02)
    - /Users/hass/repos/github/comphass/kreds/drizzle.config.ts (confirmar apontamento para schema correto e DATABASE_URL)
  </read_first>
  <action>
    Executar `pnpm db:push` no diretório `/Users/hass/repos/github/comphass/kreds`.

    O comando drizzle-kit push lê todos os arquivos em `src/lib/db/schema/` (incluindo o novo `fx.ts` via `export * from './fx'` em `index.ts`) e aplica as diferenças ao banco.

    Confirmar no output: "fx_purchase_orders" e "fx_currency" aparecem como "created" ou "applied".

    ATENÇÃO: se o comando pedir confirmação interativa ("Are you sure?"), responder "yes". O drizzle-kit push para desenvolvimento aceita confirmação automática com `--force` se necessário.

    Se o DATABASE_URL não estiver disponível no ambiente do agente, usar `pnpm db:push` com o .env carregado automaticamente pelo drizzle-kit (ele lê .env por padrão).
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && pnpm db:push 2>&1 | grep -E "(fx_purchase_orders|fx_currency|Applied|created|No changes)"</automated>
  </verify>
  <acceptance_criteria>
    - Comando `pnpm db:push` termina sem erro fatal
    - Output menciona `fx_purchase_orders` e `fx_currency` como criados ou já existentes
    - Nenhum "Error" ou "Cannot connect" no output
  </acceptance_criteria>
  <done>drizzle-kit push aplicou fx_purchase_orders e fx_currency ao banco de desenvolvimento.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [CHECKPOINT] Verificar schema no banco e inserir dados seed</name>
  <read_first>
    - /Users/hass/repos/github/comphass/kreds/.planning/phases/12-m-dulo-de-fx/12-RESEARCH.md (spec visual — PO-001/002/003 com valores exatos para seed)
  </read_first>
  <what-built>
    drizzle-kit push criou a tabela `fx_purchase_orders` e o enum `fx_currency` no banco PostgreSQL de desenvolvimento. Agora é necessário verificar a estrutura e inserir dados de seed para que o módulo FX tenha dados visíveis ao testar.
  </what-built>
  <how-to-verify>
    1. Abrir seu cliente PostgreSQL (psql, TablePlus, DBeaver, etc.)
    2. Conectar ao banco de desenvolvimento (mesmo DATABASE_URL do projeto)
    3. Verificar que a tabela existe: `\d fx_purchase_orders` ou `SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'fx_purchase_orders' ORDER BY ordinal_position;`
    4. Confirmar que o enum existe: `SELECT unnest(enum_range(NULL::fx_currency));` → deve retornar 'USD' e 'EUR'
    5. Inserir os 3 POs de seed. Você precisará de um `family_id` válido do banco — buscar com: `SELECT id FROM families LIMIT 1;`
    6. Com o family_id obtido, executar:
       ```sql
       INSERT INTO fx_purchase_orders (family_id, po_number, company, vendor, currency, foreign_amount, di_rate, due_date)
       VALUES
         ('<SEU_FAMILY_ID>', 'PO-001', 'Reditus', 'Fornecedor A', 'USD', 120000.00, 5.200000, '2026-06-28'),
         ('<SEU_FAMILY_ID>', 'PO-002', 'Reditus', 'Fornecedor B', 'USD', 102000.00, 5.090000, '2026-07-02'),
         ('<SEU_FAMILY_ID>', 'PO-003', 'Reditus', 'Fornecedor C', 'EUR', 20000.00, 6.020000, '2026-07-03');
       ```
    7. Confirmar inserção: `SELECT po_number, currency, foreign_amount, di_rate FROM fx_purchase_orders;` → deve retornar 3 linhas
  </how-to-verify>
  <resume-signal>Digite "aprovado" após confirmar as 3 linhas no banco, ou descreva qualquer problema encontrado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drizzle-kit → banco | drizzle-kit push aplica DDL — operação privilegiada, apenas em desenvolvimento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | drizzle-kit push | accept | Push em desenvolvimento — não produção; produção usa generate + migrate (mencionado em RESEARCH.md) |
| T-12-SC | Tampering | npm installs | accept | Nenhum pacote novo — drizzle-kit já instalado no projeto |
</threat_model>

<verification>
Tabela `fx_purchase_orders` existe no banco com 3 linhas de seed; enum `fx_currency` retorna 'USD' e 'EUR'.
</verification>

<success_criteria>
1. `pnpm db:push` termina sem erro
2. Tabela `fx_purchase_orders` existe no banco PostgreSQL de desenvolvimento
3. Enum `fx_currency` retorna valores 'USD' e 'EUR'
4. 3 POs de seed (PO-001, PO-002, PO-003) inseridos com values do spec visual
</success_criteria>

<output>
Criar `.planning/phases/12-m-dulo-de-fx/12-03-SUMMARY.md` quando concluído.
</output>

## Artifacts this phase produces

| Symbol | File | Type | Consumed By |
|--------|------|------|-------------|
| `fx_purchase_orders` (tabela DB) | banco PostgreSQL | DDL table | Plan 04 (Route Handlers), Plan 05 (SSR page) |
| `fx_currency` (enum DB) | banco PostgreSQL | DDL enum | Plan 04 (INSERT validação) |
| PO-001, PO-002, PO-003 (seed) | banco PostgreSQL | dados | Plan 05 (verificação visual checkpoint) |
