---
phase: 12-m-dulo-de-fx
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/modules/fx/calculations.ts
  - tests/unit/fx-calculations.test.ts
autonomous: true
requirements:
  - FX-04
  - FX-05

must_haves:
  truths:
    - "toNumber('5.20') retorna 5.20 (float, não 5)"
    - "getBrlAmount('120000', '5.20') retorna 624000"
    - "getCurrentValue('120000', 5.17) retorna 620400"
    - "getResult(624000, 620400) retorna 3600 (positivo)"
    - "getResult(519180, 527340) retorna -8160 (negativo)"
    - "getResultPct(3600, 624000) retorna ~0.5769 (positivo)"
    - "calculateTotals agrega corretamente linhas mistas USD/EUR"
  artifacts:
    - path: "src/modules/fx/calculations.ts"
      provides: "Funções puras de cálculo cambial — toNumber, getBrlAmount, getCurrentValue, getResult, getResultPct, calculateTotals"
      exports:
        - toNumber
        - getBrlAmount
        - getCurrentValue
        - getResult
        - getResultPct
        - calculateTotals
    - path: "tests/unit/fx-calculations.test.ts"
      provides: "Cobertura de teste unitário para FX-04 (todas as funções de cálculo)"
  key_links:
    - from: "tests/unit/fx-calculations.test.ts"
      to: "src/modules/fx/calculations.ts"
      via: "import direto"
      pattern: "from '../../src/modules/fx/calculations'"
---

<objective>
Criar o módulo de cálculos FX com TDD completo (RED → GREEN → REFACTOR).

Purpose: Drizzle retorna `numeric` como `string` — centralizar a conversão e os cálculos em funções puras testadas antes de qualquer UI ou API depender delas.
Output: `src/modules/fx/calculations.ts` (funções puras) + `tests/unit/fx-calculations.test.ts` (cobertura FX-04 completa).
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/hass/repos/github/comphass/kreds/.planning/PROJECT.md
@/Users/hass/repos/github/comphass/kreds/.planning/ROADMAP.md
@/Users/hass/repos/github/comphass/kreds/.planning/phases/12-m-dulo-de-fx/12-RESEARCH.md
@/Users/hass/repos/github/comphass/kreds/tests/unit/ledger-calculate.test.ts
@/Users/hass/repos/github/comphass/kreds/src/modules/ledger/calculate.ts
</context>

<feature>
  <name>Módulo de Cálculos FX — funções puras sem estado</name>
  <files>src/modules/fx/calculations.ts, tests/unit/fx-calculations.test.ts</files>
  <behavior>
    RED — escrever testes que falham primeiro:

    toNumber:
    - toNumber('5.20') → 5.20 (não 5 — parseFloat preserva decimais)
    - toNumber('5.1559') → 5.1559
    - toNumber(5.0) → 5.0 (number passthrough)
    - toNumber('0') → 0

    getBrlAmount (FX-04: BRL Amount = Foreign Amount × DI):
    - getBrlAmount('120000', '5.20') → 624000 (PO-001 do spec visual)
    - getBrlAmount('102000', '5.09') → 519180 (PO-002)
    - getBrlAmount('20000', '6.02') → 120400 (PO-003 EUR)

    getCurrentValue (FX-04: Current Value = Foreign Amount × Current Tax):
    - getCurrentValue('120000', 5.17) → 620400 (PO-001, rate 5.17)
    - getCurrentValue('102000', 5.17) → 527340 (PO-002, rate 5.17)
    - getCurrentValue('20000', 5.98) → 119600 (PO-003, EUR rate 5.98)

    getResult (FX-04: Result = BRL Amount − Current Value):
    - getResult(624000, 620400) → 3600 (positivo = favorável pagar agora)
    - getResult(519180, 527340) → -8160 (negativo = desfavorável)
    - getResult(120400, 119600) → 800

    getResultPct (FX-04: (%) = Result / BRL Amount × 100):
    - getResultPct(3600, 624000) → toBeCloseTo(0.5769, 2)
    - getResultPct(-8160, 519180) → toBeCloseTo(-1.5718, 2)
    - getResultPct(0, 0) → 0 (evitar divisão por zero)

    calculateTotals (FX-05: linha de totais no rodapé):
    - calculateTotals([{brlAmount:624000,currentValue:620400,result:3600},{brlAmount:519180,currentValue:527340,result:-8160},{brlAmount:120400,currentValue:119600,result:800}])
      → { totalBrl: 1263580, totalCurrent: 1267340, totalResult: -3760, totalPct: toBeCloseTo(-0.2975, 2) }

    GREEN — implementar o mínimo para passar:
    - toNumber: typeof === 'string' ? parseFloat(val) : val
    - getBrlAmount: toNumber(foreignAmount) * toNumber(diRate)
    - getCurrentValue: toNumber(foreignAmount) * currentRate
    - getResult: brlAmount - currentValue
    - getResultPct: brlAmount === 0 ? 0 : (result / brlAmount) * 100
    - calculateTotals: reduce sobre rows + getResultPct nos totais
  </behavior>
  <implementation>
    Criar `src/modules/` (não existe — primeiro módulo). Seguir o padrão de `src/modules/ledger/calculate.ts`:
    - Sem imports de DB, sem side effects, zero dependências externas
    - Exportar cada função individualmente (named exports)
    - Adicionar JSDoc comentando que Drizzle retorna `numeric` como string — necessidade do toNumber
    - Arquivo de teste usa padrão `tests/unit/ledger-calculate.test.ts`: import { describe, it, expect } from 'vitest' + import relativo `../../src/modules/fx/calculations`
    - Comando de teste: `pnpm test -- fx-calculations`
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: RED — testes fx-calculations falham (Wave 0 scaffolding)</name>
  <files>tests/unit/fx-calculations.test.ts</files>
  <read_first>
    - /Users/hass/repos/github/comphass/kreds/tests/unit/ledger-calculate.test.ts (padrão exato de test file)
    - /Users/hass/repos/github/comphass/kreds/vitest.config.ts (configuração — confirmar alias @/)
  </read_first>
  <action>
    Criar `tests/unit/fx-calculations.test.ts`. Importar de `../../src/modules/fx/calculations` (o arquivo ainda não existe — os testes devem falhar com "Cannot find module").

    Estrutura de describes: `fx calculations` > `toNumber`, `getBrlAmount`, `getCurrentValue`, `getResult`, `getResultPct`, `calculateTotals`.

    Usar `toBeCloseTo(expected, 2)` para todos os floats com decimais (evitar falsos negativos por precisão floating-point).

    Incluir todos os casos listados na `<behavior>` acima, mais um caso de edge: `getResultPct` com brlAmount=0 deve retornar 0.

    Após criar o arquivo, rodar: `cd /Users/hass/repos/github/comphass/kreds && pnpm test -- fx-calculations 2>&1 | tail -20`. Confirmar que a saída é "Cannot find module" ou erro de import — não "All tests passed".
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && pnpm test -- fx-calculations 2>&1 | grep -E "(FAIL|Cannot find module|Error)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/fx-calculations.test.ts` existe
    - O arquivo importa de `../../src/modules/fx/calculations`
    - `pnpm test -- fx-calculations` retorna erro de módulo não encontrado (RED confirmado)
    - Pelo menos 10 casos de teste cobrem: toNumber, getBrlAmount, getCurrentValue, getResult, getResultPct, calculateTotals
  </acceptance_criteria>
  <done>Testes escritos, RED confirmado — módulo importado não existe.</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN → REFACTOR — implementar calculations.ts e passar todos os testes</name>
  <files>src/modules/fx/calculations.ts</files>
  <read_first>
    - /Users/hass/repos/github/comphass/kreds/src/modules/ledger/calculate.ts (padrão de funções puras — DEVE existir; se não, criar o diretório src/modules/ledger/ também)
    - /Users/hass/repos/github/comphass/kreds/tests/unit/fx-calculations.test.ts (testes recém-criados na Task 1 — fonte da verdade)
    - /Users/hass/repos/github/comphass/kreds/.planning/phases/12-m-dulo-de-fx/12-RESEARCH.md (seção "Módulo de Cálculos FX" — assinaturas exatas)
  </read_first>
  <action>
    Criar `src/modules/fx/calculations.ts`. Não há `src/modules/` ainda — criar o diretório implicitamente ao criar o arquivo.

    Exportar com named exports:

    - `toNumber(val: string | number): number` — centraliza conversão de campo Drizzle numeric (string) para number via parseFloat. Motivo: `Number('')` silenciosamente retorna 0; `parseInt` perde decimais; `parseFloat` é o correto.
    - `getBrlAmount(foreignAmount: string | number, diRate: string | number): number` — chama toNumber em ambos antes de multiplicar
    - `getCurrentValue(foreignAmount: string | number, currentRate: number): number` — toNumber(foreignAmount) * currentRate (currentRate já é number da API)
    - `getResult(brlAmount: number, currentValue: number): number` — subtração simples
    - `getResultPct(result: number, brlAmount: number): number` — guard brlAmount === 0 → retorna 0; caso contrário (result / brlAmount) * 100
    - `calculateTotals(rows: Array<{brlAmount: number, currentValue: number, result: number}>): {totalBrl: number, totalCurrent: number, totalResult: number, totalPct: number}` — reduce + getResultPct nos totais

    Sem imports. Sem side effects. Adicionar JSDoc em `toNumber` explicando: "Drizzle retorna numeric() como string no TypeScript — centralizar conversão aqui para evitar concatenação de string em vez de soma."

    Após criar, rodar testes: `pnpm test -- fx-calculations`. Todos devem passar (GREEN).
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && pnpm test -- fx-calculations 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/modules/fx/calculations.ts` existe
    - Exports nomeados: `toNumber`, `getBrlAmount`, `getCurrentValue`, `getResult`, `getResultPct`, `calculateTotals`
    - `pnpm test -- fx-calculations` termina com 0 falhas, todos os casos GREEN
    - `getBrlAmount('120000', '5.20')` retorna exatamente 624000
    - `getResultPct(0, 0)` retorna 0 (sem divisão por zero)
    - Nenhum import de DB, next-auth, ou bibliotecas externas no arquivo
  </acceptance_criteria>
  <done>Suite fx-calculations 100% verde. `calculateTotals` com dados do spec visual retorna totalBrl=1263580, totalResult=-3760.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| funções puras → chamador | Dados de entrada vêm de campos Drizzle (string) ou API (number) — sem input de usuário neste módulo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo neste plano — todas as dependências (vitest) já existem no projeto |
</threat_model>

<verification>
`pnpm test -- fx-calculations` retorna 0 falhas com cobertura de: toNumber, getBrlAmount, getCurrentValue, getResult, getResultPct, calculateTotals.
</verification>

<success_criteria>
1. `tests/unit/fx-calculations.test.ts` existe com ≥ 10 casos de teste
2. `src/modules/fx/calculations.ts` exporta 6 funções nomeadas
3. Todos os cálculos do spec visual (PO-001/002/003) passam nos testes
4. Suite completa verde: `pnpm test -- fx-calculations`
</success_criteria>

<output>
Criar `.planning/phases/12-m-dulo-de-fx/12-01-SUMMARY.md` quando concluído.
</output>

## Artifacts this phase produces

| Symbol | File | Type | Consumed By |
|--------|------|------|-------------|
| `toNumber` | src/modules/fx/calculations.ts | exported function | FxAnalysisClient (Plan 05), tests |
| `getBrlAmount` | src/modules/fx/calculations.ts | exported function | FxAnalysisClient (Plan 05) |
| `getCurrentValue` | src/modules/fx/calculations.ts | exported function | FxAnalysisClient (Plan 05) |
| `getResult` | src/modules/fx/calculations.ts | exported function | FxAnalysisClient (Plan 05) |
| `getResultPct` | src/modules/fx/calculations.ts | exported function | FxAnalysisClient (Plan 05) |
| `calculateTotals` | src/modules/fx/calculations.ts | exported function | FxAnalysisClient (Plan 05) |
</output>
