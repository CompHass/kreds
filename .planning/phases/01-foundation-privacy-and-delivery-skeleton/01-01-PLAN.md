---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - .npmrc
  - .env.example
  - .gitignore
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/app/api/health/route.ts
  - src/lib/db/schema/index.ts
  - src/lib/env.ts
  - src/lib/logger.ts
autonomous: false
requirements:
  - FND-01

must_haves:
  truths:
    - "Developer can install dependencies after human package legitimacy approval"
    - "Developer can run pnpm dev and the app serves pages on localhost:3000"
    - "Health endpoint returns 200 with {status: 'ok'} JSON"
    - "Environment variables are represented by a Zod schema"
    - "Database schema defines a families table with id, name, timezone, created_at, updated_at columns"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next, react, drizzle-orm, pg, zod, pino scripts: dev/build/start/test/db:generate/db:migrate"
    - path: "next.config.ts"
      provides: "Next.js configuration with standalone output"
      exports: "output: 'standalone'"
    - path: "src/app/api/health/route.ts"
      provides: "Health check API endpoint"
      exports: "GET"
    - path: "src/lib/db/schema/index.ts"
      provides: "Drizzle schema definition"
      contains: "pgTable('families'"
    - path: "src/lib/env.ts"
      provides: "Zod environment validation"
      exports: "env"
  key_links:
    - from: "src/lib/env.ts"
      to: "package.json"
      via: "zod dependency"
      pattern: "import.*zod"
    - from: "src/app/api/health/route.ts"
      to: "next.config.ts"
      via: "Route Handler in App Router"
      pattern: "export async function GET"

user_setup:
  - service: npm registry
    why: "All packages flagged [ASSUMED] due to slopcheck unavailability — human must verify legitimacy before install"
    packages:
      - next@16.2.7 — verify at npmjs.com/package/next
      - react@19.2.7 — verify at npmjs.com/package/react
      - react-dom@19.2.7 — verify at npmjs.com/package/react-dom
      - typescript@6.0.3 — verify at npmjs.com/package/typescript
      - drizzle-orm@0.45.2 — verify at npmjs.com/package/drizzle-orm
      - drizzle-kit@0.31.10 — verify at npmjs.com/package/drizzle-kit
      - pg@8.21.0 — verify at npmjs.com/package/pg
      - zod@4.4.3 — verify at npmjs.com/package/zod
      - pino@10.3.1 — verify at npmjs.com/package/pino
      - @serwist/next@9.5.11 — verify at npmjs.com/package/@serwist/next
      - tailwindcss@4.3.0 — verify at npmjs.com/package/tailwindcss
      - vitest@4.1.8 — verify at npmjs.com/package/vitest
      - playwright@1.60.0 — verify at npmjs.com/package/playwright
      - @testcontainers/postgresql@12.0.1 — verify at npmjs.com/package/@testcontainers/postgresql
      - pg-boss@12.18.2 — verify at npmjs.com/package/pg-boss
      - react-hook-form@7.77.0 — verify at npmjs.com/package/react-hook-form
      - lucide-react — verify at npmjs.com/package/lucide-react
---

<objective>
Bootstrap the Kreds Next.js 16 TypeScript project scaffold, install dependencies only after package legitimacy approval, create the minimal Drizzle schema stub, and deliver a health check endpoint that proves the app shell starts and serves requests.

Purpose: Establish the runnable application foundation for FND-01 without requiring PostgreSQL or migration execution in this plan.
Output: package.json, TypeScript/Next/Tailwind config, environment/logging modules, app shell files, health route, families schema stub.
</objective>

<execution_context>
@/Users/hass/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hass/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-privacy-and-delivery-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-privacy-and-delivery-skeleton/01-PATTERNS.md
@.planning/phases/01-foundation-privacy-and-delivery-skeleton/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create base scaffold and source files</name>
  <read_first>
    - .planning/phases/01-foundation-privacy-and-delivery-skeleton/01-PATTERNS.md — all file patterns and code examples
    - .planning/phases/01-foundation-privacy-and-delivery-skeleton/01-RESEARCH.md — Standard Stack versions, Installation commands, Pitfalls
    - AGENTS.md — project conventions (English-only, Next.js/TypeScript/PostgreSQL/Drizzle stack)
  </read_first>
  <files>
    package.json, tsconfig.json, next.config.ts, postcss.config.mjs, .npmrc, .env.example, .gitignore,
    src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, src/app/api/health/route.ts,
    src/lib/db/schema/index.ts, src/lib/env.ts, src/lib/logger.ts
  </files>
  <action>
    Create the base project configuration and source files for the Kreds Next.js 16 bootstrap. Follow patterns from 01-PATTERNS.md exactly. Do NOT run pnpm install, npx tsc, db:generate, db:migrate, docker compose, or any command that requires installed dependencies in this task.

    Configuration files: package.json with name "kreds", private true, scripts dev/build/start/lint/test/test:e2e/db:generate/db:migrate/db:push/db:studio, and exact dependencies from 01-RESEARCH.md; tsconfig.json with ES2022, moduleResolution bundler, strict true, @/* path alias; next.config.ts with output: 'standalone'; postcss.config.mjs with @tailwindcss/postcss; .npmrc with public-hoist-pattern entries; .env.example with DATABASE_URL, NODE_ENV, NEXT_PUBLIC_APP_URL, ZITADEL_ISSUER; .gitignore with .next/, node_modules/, .env.local, .env*.local, test-results/, playwright-report/.

    Application source files: globals.css containing exactly @import "tailwindcss"; layout.tsx importing globals.css, metadata title/description, html lang="en", and SerwistProvider; page.tsx static Server Component with h1 "Kreds" and app description only; health route GET returning {status:'ok', version, timestamp}; schema/index.ts exporting families pgTable with uuid id, text name, varchar timezone default America/Sao_Paulo, createdAt, updatedAt; env.ts exporting Zod-validated env; logger.ts exporting Pino logger.
  </action>
  <acceptance_criteria>
    - package.json exists with all production and dev dependencies listed in 01-RESEARCH.md
    - package.json scripts include dev, build, start, lint, test, test:e2e, db:generate, db:migrate, db:push, db:studio
    - next.config.ts exports config with output: 'standalone'
    - src/app/globals.css contains `@import "tailwindcss";` and no @tailwind v3 directives
    - src/app/layout.tsx imports './globals.css' and wraps children in SerwistProvider
    - src/app/page.tsx renders static "Kreds" shell and does not query PostgreSQL yet
    - src/app/api/health/route.ts exports async GET returning JSON with status, version, timestamp fields
    - src/lib/db/schema/index.ts exports families pgTable with uuid id, text name, varchar timezone, timestamp createdAt, timestamp updatedAt columns
    - src/lib/env.ts exports env parsed from Zod schema with DATABASE_URL, NODE_ENV, NEXT_PUBLIC_APP_URL, ZITADEL_ISSUER fields
    - src/lib/logger.ts exports logger from pino
    - No dependency installation or TypeScript compilation has been attempted before package verification
  </acceptance_criteria>
  <verify>
    <automated>test -f package.json && test -f next.config.ts && test -f src/app/api/health/route.ts && test -f src/lib/db/schema/index.ts && grep -q "output: 'standalone'" next.config.ts && grep -q "@import \"tailwindcss\"" src/app/globals.css && grep -q "pgTable('families'" src/lib/db/schema/index.ts && echo "Base scaffold files verified without dependency execution"</automated>
  </verify>
  <done>
    Base scaffold files exist with correct content. No pnpm install, npx tsc, migration, or Docker commands have run before human package verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify package legitimacy on npmjs.com before install</name>
  <read_first>
    - .planning/phases/01-foundation-privacy-and-delivery-skeleton/01-RESEARCH.md — Package Legitimacy Audit table
    - package.json — verify all dependency names and versions match the audit table
  </read_first>
  <files>package.json</files>
  <action>
    All packages in package.json are tagged [ASSUMED] because slopcheck was unavailable at research time. Per GSD security rules, [ASSUMED] packages require a blocking human checkpoint before installation. The human must verify each package exists on npmjs.com with the expected version and is not a typosquat or malicious package: next, react, react-dom, typescript, drizzle-orm, drizzle-kit, pg, zod, pino, @serwist/next, tailwindcss, react-hook-form, lucide-react, vitest, playwright, @testcontainers/postgresql, testcontainers, pg-boss, @testing-library/react, @testing-library/jest-dom, vite-tsconfig-paths, @vitejs/plugin-react, jsdom, @tailwindcss/postcss, pino-pretty. After verification, the human types "approved" to proceed with pnpm install.
  </action>
  <acceptance_criteria>
    - Human has visited npmjs.com/package for each package listed above
    - Human confirms each package name, version, and publisher matches expected values
    - Human types "approved" to unblock package installation
  </acceptance_criteria>
  <verify>
    <automated>test -f package.json && grep -q '"next"' package.json && grep -q '"drizzle-orm"' package.json && echo "Package manifest ready for human legitimacy verification"</automated>
    <human-check>Visit npmjs.com/package/&lt;name&gt; for each package. Confirm package name, version, publisher, and download counts match expectations. Type "approved" when all verified.</human-check>
  </verify>
  <resume-signal>Type "approved" to proceed with pnpm install, or list any packages that look suspicious</resume-signal>
  <done>
    Human has verified all package names on npmjs.com and approved installation. pnpm install can proceed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Install dependencies and verify scaffold runtime</name>
  <read_first>
    - package.json — dependency list and scripts
    - .planning/phases/01-foundation-privacy-and-delivery-skeleton/01-RESEARCH.md — Pitfall 5 (pnpm + Next.js resolution)
  </read_first>
  <files>pnpm-lock.yaml, node_modules/</files>
  <action>
    Run corepack enable pnpm and pnpm install after the human package verification checkpoint is approved. Then run pnpm exec tsc --noEmit to verify TypeScript only after dependencies exist. Start pnpm dev in the background, wait for the server, curl /api/health and /, confirm /api/health returns status ok and / contains Kreds, then stop the dev server. If install fails with module resolution errors, check .npmrc for public-hoist-pattern entries per RESEARCH.md Pitfall 5.
  </action>
  <acceptance_criteria>
    - pnpm install exits 0 with no errors after human approval
    - pnpm-lock.yaml exists and is non-empty
    - pnpm exec tsc --noEmit exits 0 after dependencies are installed
    - curl http://localhost:3000/api/health returns HTTP 200 with JSON containing "status":"ok"
    - curl http://localhost:3000 returns HTML containing "Kreds"
    - Dev server process is stopped after verification
  </acceptance_criteria>
  <verify>
    <automated>corepack enable pnpm && pnpm install && pnpm exec tsc --noEmit && (pnpm dev &amp; DEV_PID=$!; sleep 8; curl -sf http://localhost:3000/api/health | grep -q '"status":"ok"'; curl -sf http://localhost:3000 | grep -q 'Kreds'; kill $DEV_PID) && echo "Install, TypeScript, and health verify complete"</automated>
  </verify>
  <done>
    Dependencies installed successfully. TypeScript compiles after install. Dev server starts and /api/health responds with 200 OK and {"status":"ok"}. Dev server stopped after verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API Route | Untrusted HTTP requests hit /api/health; response is static, no user input processed |
| Source → npm registry | Package installs from npmjs.com; [ASSUMED] packages require human verification |
| Server runtime → environment | DATABASE_URL and future secrets must remain server-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Environment variables | mitigate | Zod env validation in src/lib/env.ts; never prefix secrets with NEXT_PUBLIC_; only NEXT_PUBLIC_APP_URL is client-safe |
| T-01-02 | Information Disclosure | Database credentials in source control | mitigate | .gitignore includes .env.local and .env*.local; .env.example contains only placeholder values |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | Blocking human checkpoint verifies all [ASSUMED] packages on npmjs.com before pnpm install; no auto-approval |
</threat_model>

<verification>
1. Base file/content verification runs without installed dependencies.
2. Human package legitimacy checkpoint completes before install.
3. After install, `pnpm exec tsc --noEmit` exits 0.
4. Dev server health check returns 200 with {status: "ok"}.
</verification>

<success_criteria>
- [ ] All base configuration and source files created with correct content
- [ ] No TypeScript or dependency-dependent command runs before pnpm install
- [ ] All npm packages verified by human before install
- [ ] pnpm install completes successfully and creates pnpm-lock.yaml
- [ ] TypeScript compiles after dependencies are installed
- [ ] Dev server starts and /api/health responds with 200 OK
- [ ] Drizzle schema defines families table with id, name, timezone, created_at, updated_at
- [ ] Environment validation through Zod schema with DATABASE_URL, NODE_ENV, NEXT_PUBLIC_APP_URL, ZITADEL_ISSUER
- [ ] next.config.ts has output: 'standalone' for Docker build
</success_criteria>

<output>
Create `.planning/phases/01-foundation-privacy-and-delivery-skeleton/01-01-SUMMARY.md` when done
</output>
