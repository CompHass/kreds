---
phase: "11"
plan: "03"
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/components/ChildBottomNav.tsx
  - src/app/child/[childId]/dashboard/page.tsx
  - src/app/child/home/page.tsx
autonomous: true
requirements: [D-01, D-05, D-06]
must_haves:
  truths:
    - "ChildBottomNav renderiza 4 abas: Jardim, Tarefas, Sonhos, Saldo com rotas /child/[childId]/*"
    - "Aba ativa exibe background rgba(202,236,125,0.55) e color #4c6700 em pill shape"
    - "/child/[childId]/dashboard mostra avatar inicial, nome, badge de saldo e imagem do jardim"
    - "/child/home redireciona para /child/[session.childProfileId]/dashboard"
    - "requireChildSession() é chamado em dashboard; se session.childProfileId !== childId, redirect para /child/[session.childProfileId]/dashboard"
  artifacts:
    - path: "src/components/ChildBottomNav.tsx"
      provides: "Componente de navegação inferior do filho"
      exports: ["ChildBottomNav"]
    - path: "src/app/child/[childId]/dashboard/page.tsx"
      provides: "Página principal do filho com jardim e saldo"
      contains: "requireChildSession"
    - path: "src/app/child/home/page.tsx"
      provides: "Redirect para /child/[childId]/dashboard"
      contains: "redirect"
  key_links:
    - from: "src/app/child/[childId]/dashboard/page.tsx"
      to: "src/components/ChildBottomNav.tsx"
      via: "import ChildBottomNav"
      pattern: "ChildBottomNav"
    - from: "src/app/child/[childId]/dashboard/page.tsx"
      to: "src/lib/auth/child-guard.ts"
      via: "requireChildSession()"
      pattern: "requireChildSession"
---

<objective>
Criar o componente `ChildBottomNav`, a página `/child/[childId]/dashboard` e converter `/child/home` em redirect.

Purpose: Establece a shell de navegação do filho (D-05) e o dashboard principal com jardim e saldo (D-06). O `/child/home` existente precisa ser convertido em redirect (D-01) para que a URL canônica seja `/child/[childId]/dashboard`.

Output: ChildBottomNav reutilizável, dashboard do filho funcional, /child/home → redirect.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-CONTEXT.md
@.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-UI-SPEC.md
@src/components/BottomNav.tsx
@src/app/child/home/page.tsx
@src/lib/auth/child-guard.ts
@src/lib/db/schema/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar ChildBottomNav component</name>
  <files>src/components/ChildBottomNav.tsx</files>
  <read_first>
    - src/components/BottomNav.tsx — estrutura completa: NAV_STYLE, type NavItem, componente com Link, estilos inline de active/inactive. Copiar exatamente o mesmo CSS; apenas trocar tabs e tipos.
    - 11-UI-SPEC.md seção ChildBottomNav — tabs: jardim→/child/[childId]/dashboard, tarefas→/child/[childId]/tasks, sonhos→/child/[childId]/dreams, saldo→/child/[childId]/balance
  </read_first>
  <action>
    Criar src/components/ChildBottomNav.tsx como componente Client ('use client' NÃO necessário — usa Link de next/link apenas, sem useState/useEffect).

    O componente recebe props: { active: 'jardim' | 'tarefas' | 'sonhos' | 'saldo'; childId: string }.

    Copiar NAV_STYLE idêntico ao BottomNav.tsx (position fixed, bottom 0, background rgba(255,241,233,0.94), backdropFilter blur(20px), borderRadius 20px 20px 0 0, etc.).

    Items array (per D-05 e UI-SPEC):
    - { label: 'Jardim', href: `/child/${childId}/dashboard`, icon: '🪴', active: active === 'jardim' }
    - { label: 'Tarefas', href: `/child/${childId}/tasks`, icon: '📋', active: active === 'tarefas' }
    - { label: 'Sonhos', href: `/child/${childId}/dreams`, icon: '✨', active: active === 'sonhos' }
    - { label: 'Saldo', href: `/child/${childId}/balance`, icon: '🪙', active: active === 'saldo' }

    Estilos de active/inactive idênticos ao BottomNav:
    - Active: background rgba(202,236,125,0.55), color #4c6700, borderRadius 9999px, padding 6px 20px, fontWeight 700, fontSize 11px
    - Inactive: color #72796e, opacity 0.7, padding 6px 8px, fontWeight 600, fontSize 11px

    Como as hrefs contêm childId (string dinâmica), o arquivo DEVE ser marcado como 'use client' para evitar problemas de renderização SSR quando childId é parâmetro de route. Alternativamente, aceitar childId como prop e manter como Server Component. Usar a abordagem de prop (sem 'use client') já que não há interatividade.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && grep -c "ChildBottomNav\|jardim\|tarefas\|sonhos\|saldo" src/components/ChildBottomNav.tsx</automated>
  </verify>
  <done>
    src/components/ChildBottomNav.tsx existe, exporta ChildBottomNav, contém as 4 abas com as rotas corretas e estilos idênticos ao BottomNav.
  </done>
  <acceptance_criteria>
    - Exporta função ChildBottomNav({ active, childId }) com tipos corretos
    - 4 itens de nav: Jardim (🪴), Tarefas (📋), Sonhos (✨), Saldo (🪙)
    - Hrefs usam template literal com childId: /child/${childId}/dashboard etc.
    - Estilo active igual ao BottomNav: rgba(202,236,125,0.55) + #4c6700 + pill
    - NAV_STYLE position:fixed bottom:0 identico ao BottomNav
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Criar /child/[childId]/dashboard e converter /child/home em redirect</name>
  <files>src/app/child/[childId]/dashboard/page.tsx, src/app/child/home/page.tsx</files>
  <read_first>
    - src/app/child/home/page.tsx — estrutura existente completa: imports, requireChildSession(), consulta de childProfiles, ACCENT_CSS map, JSX. Reutilizar ACCENT_CSS e o padrão de query.
    - src/lib/auth/child-guard.ts — requireChildSession() retorna ChildSessionPayload { childProfileId, familyId, role }
    - src/lib/db/schema/index.ts — childProfiles campos: displayName, avatarPreset, accentColor
    - src/modules/ledger/queries.ts (se existir) — getBalance(childId, 'available')
    - src/modules/activity/cycle.ts — getCycleForDate(date, timezone) retorna { cycleStart, cycleEnd }
    - src/lib/db/tasks/queries.ts — getActiveTasksForFamily(familyId)
    - 11-UI-SPEC.md seção /child/[childId]/dashboard — layout: top bar + hero garden + quick links
  </read_first>
  <action>
    PARTE A — src/app/child/[childId]/dashboard/page.tsx (NOVO):

    Marcar export const dynamic = 'force-dynamic'.

    Props: { params: Promise<{ childId: string }> }

    Auth pattern (per D-06, D-12):
    1. const { childId } = await params
    2. const session = await requireChildSession()
    3. if (session.childProfileId !== childId) redirect(`/child/${session.childProfileId}/dashboard`)

    Queries em paralelo com Promise.all:
    - Perfil do filho: db.select({ displayName, avatarPreset, accentColor }).from(childProfiles).where(eq(childProfiles.id, session.childProfileId)).limit(1)
    - Saldo disponível: getBalance(session.childProfileId, 'available') de '@/modules/ledger/queries'
    - Família (timezone): db.select({ timezone }).from(families).innerJoin(childProfiles, eq(childProfiles.familyId, families.id)).where(eq(childProfiles.id, session.childProfileId)).limit(1)
    - Tarefas ativas do filho: getActiveTasksForFamily(session.familyId) filtrado por assignedChildId === childId

    Calcular taskCount = tarefas filtradas para este filho.

    Layout (per UI-SPEC):
    - Header fixo (top bar): avatar initial circle com accentColor + displayName à esquerda; badge "🪙 {balance}" em pill gold à direita
    - Hero section: card com imagem /garden-isometric.png em aspect-ratio 4/3, overlay "Meu Jardim" + "{taskCount} tarefa(s) ativa(s)"
    - Quick links: dois cards link — "Minhas Tarefas 📋" → /child/[childId]/tasks e "Meus Sonhos ✨" → /child/[childId]/dreams
    - ChildBottomNav com active='jardim' e childId={childId}
    - padding-bottom: 80px para não cobrir ChildBottomNav

    ACCENT_CSS map: moss→#3b6934, gold→#d2a501, sky→#0369a1, berry→#9333ea, clay→#c2410c, sage→#65a30d (igual ao child/home).

    PARTE B — src/app/child/home/page.tsx (MODIFICAR):
    Substituir todo o conteúdo da função ChildHomePage por:
    1. const session = await requireChildSession()
    2. redirect(`/child/${session.childProfileId}/dashboard`)

    Manter apenas os imports necessários: requireChildSession e redirect.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && grep -c "requireChildSession\|ChildBottomNav\|dashboard" src/app/child/\[childId\]/dashboard/page.tsx && grep -c "redirect" src/app/child/home/page.tsx</automated>
  </verify>
  <done>
    /child/[childId]/dashboard/page.tsx existe com requireChildSession, verificação de childId scope, queries de perfil/saldo/tarefas e ChildBottomNav. /child/home/page.tsx contém apenas o redirect para dashboard.
  </done>
  <acceptance_criteria>
    - dashboard/page.tsx: requireChildSession() chamado antes de qualquer query
    - dashboard/page.tsx: if (session.childProfileId !== childId) redirect(...)
    - dashboard/page.tsx: ChildBottomNav importado de @/components/ChildBottomNav com active='jardim'
    - dashboard/page.tsx: imagem /garden-isometric.png referenciada no JSX
    - child/home/page.tsx: conteúdo reduzido a requireChildSession() + redirect()
    - pnpm build não retorna erros de TypeScript nestas páginas
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /child/[childId]/dashboard | Cookie child-session necessário; childId verificado contra session.childProfileId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Spoofing | /child/[childId]/dashboard | mitigate | session.childProfileId !== childId → redirect; impede filho A de ver dashboard de filho B |
| T-11-08 | Information Disclosure | Balance na top bar | accept | Saldo próprio do filho — informação autorizada para o dono |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo |
</threat_model>

<verification>
- src/components/ChildBottomNav.tsx existe e exporta ChildBottomNav
- src/app/child/[childId]/dashboard/page.tsx existe com requireChildSession e verificação de scope
- src/app/child/home/page.tsx contém apenas redirect
- pnpm build sem erros de TypeScript
</verification>

<success_criteria>
ChildBottomNav renderiza 4 abas com estilos Sylvan. Dashboard do filho mostra jardim, saldo e tarefas com guard de sessão. /child/home redireciona automaticamente para o dashboard do filho autenticado.
</success_criteria>

<output>
Criar `.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-03-SUMMARY.md` quando concluído.
</output>

## Artifacts this phase produces

| Arquivo | Tipo | Descrição |
|---------|------|-----------|
| `src/components/ChildBottomNav.tsx` | Criado | Nav inferior do filho com 4 abas Sylvan (D-05) |
| `src/app/child/[childId]/dashboard/page.tsx` | Criado | Dashboard do filho com jardim + saldo + guard (D-06) |
| `src/app/child/home/page.tsx` | Modificado | Convertido em redirect para /child/[childId]/dashboard (D-01) |
