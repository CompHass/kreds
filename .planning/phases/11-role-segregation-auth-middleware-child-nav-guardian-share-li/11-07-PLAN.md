---
phase: "11"
plan: "07"
type: execute
wave: 4
depends_on: ["11-02", "11-03"]
files_modified:
  - src/app/(app)/guardian/[childId]/balance/page.tsx
autonomous: true
requirements: [D-11]
must_haves:
  truths:
    - "/guardian/[childId]/balance mostra saldo disponível e histórico de ledger do filho para o guardião autenticado"
    - "Page usa auth.ts auth() + requireAuthenticatedIdentity + requireActiveGuardian para verificar acesso"
    - "Guardião só pode ver o saldo de filhos da sua própria família"
    - "requireActiveGuardian verifica que o childId pertence à família do guardião antes de exibir dados"
  artifacts:
    - path: "src/app/(app)/guardian/[childId]/balance/page.tsx"
      provides: "View de saldo e histórico do filho para guardião autenticado"
      contains: "requireAuthenticatedIdentity, requireActiveGuardian, getBalance, getChildLedgerHistory"
  key_links:
    - from: "src/app/(app)/guardian/[childId]/balance/page.tsx"
      to: "auth.ts"
      via: "auth()"
      pattern: "await auth()"
    - from: "src/app/(app)/guardian/[childId]/balance/page.tsx"
      to: "src/modules/ledger/queries.ts"
      via: "getBalance, getChildLedgerHistory"
      pattern: "getBalance|getChildLedgerHistory"
---

<objective>
Criar a página `/guardian/[childId]/balance` que permite ao guardião ver o saldo e histórico de ledger de um filho específico usando a sessão NextAuth.

Purpose: D-11 exige que guardião veja o saldo do filho via uma rota própria (/guardian/*) em vez de acessar /child/* diretamente. Esta página é o destino do link "Ver saldo" adicionado em /family/children pelo plano 11-06.

Output: Página de view do guardião para saldo do filho com guard de session NextAuth e verificação de pertencimento à família.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-CONTEXT.md
@.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-RESEARCH.md
@src/lib/auth/authorization.ts
@src/app/(app)/guardian/[childId]/earning/page.tsx
@src/app/(app)/child/[childId]/history/page.tsx
@auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar /guardian/[childId]/balance com guard NextAuth e view de saldo</name>
  <files>src/app/(app)/guardian/[childId]/balance/page.tsx</files>
  <read_first>
    - src/app/(app)/guardian/[childId]/earning/page.tsx — padrão completo de auth guard do guardião: auth() de '../../../../auth'; requireCurrentFamilyContext com role check; requireChildInFamily. Usar mesmo padrão.
    - src/app/(app)/child/[childId]/history/page.tsx — padrão de getChildLedgerHistory, formatTimestamp, getChildLabel, translateAccountType — copiar funções de formatação inline.
    - src/lib/auth/authorization.ts — requireAuthenticatedIdentity(session), resolveKredsIdentityId(zitadelSub), requireActiveGuardian(identity, familyId)
    - src/modules/ledger/queries.ts — getBalance(childId, 'available'), getChildLedgerHistory(childId, familyId)
    - auth.ts — export { auth } — importar de caminho relativo (ver earning/page.tsx para profundidade correta)
  </read_first>
  <action>
    Criar src/app/(app)/guardian/[childId]/balance/page.tsx.

    export const dynamic = 'force-dynamic'.
    Props: { params: Promise&lt;{ childId: string }&gt; }

    Auth pattern (per D-11, mesmo padrão que earning/page.tsx):
    - const { childId } = await params
    - const session = await auth()  // import de '../../../../auth'
    - let identity; try { identity = requireAuthenticatedIdentity(session) } catch { redirect('/api/auth/signin') }
    - let kredsIdentityId; try { kredsIdentityId = await resolveKredsIdentityId(identity.zitadelSub) } catch { redirect('/family/onboarding') }
    - Buscar familyId da membership do guardião:
      const [membership] = await db.select({ familyId: schema.familyMemberships.familyId }).from(schema.familyMemberships).where(eq(schema.familyMemberships.identityId, kredsIdentityId)).limit(1)
      if (!membership) redirect('/family/onboarding')
      const familyId = membership.familyId
    - Verificar que childId pertence a esta família:
      const [childProfile] = await db.select({ id: schema.childProfiles.id, displayName: schema.childProfiles.displayName }).from(schema.childProfiles).where(and(eq(schema.childProfiles.id, childId), eq(schema.childProfiles.familyId, familyId))).limit(1)
      if (!childProfile) redirect('/family/children')

    Queries em paralelo:
    - const [available, firstfruits, rows] = await Promise.all([
        getBalance(childId, 'available'),
        getBalance(childId, 'firstfruits'),
        getChildLedgerHistory(childId, familyId),
      ])

    Layout (Sylvan design, guardian perspective):
    - Header: "Saldo de {childProfile.displayName}" com breadcrumb link "← Voltar" para /family/children
    - Hero glass card: saldo disponível em destaque + primícias como badge separado
    - Histórico de ledger: mesmo padrão de formatação do /(app)/child/[childId]/history/page.tsx
      - Funções helper inline: formatTimestamp, getGuardianLabel (variante do getChildLabel com linguagem de guardião), translateAccountType
    - Empty state: "Nenhum histórico registrado ainda."
    - Sem BottomNav (página do guardião — usar BottomNav do guardião se desejar, ou sem nav)
    - BottomNav do guardião: importar BottomNav de '@/components/BottomNav' com active='perfil'

    Importações necessárias:
    - import { auth } from '../../../../auth'
    - import { requireAuthenticatedIdentity, resolveKredsIdentityId } from '@/lib/auth/authorization'
    - import { db } from '@/lib/db'
    - import * as schema from '@/lib/db/schema'
    - import { eq, and } from 'drizzle-orm'
    - import { getBalance, getChildLedgerHistory } from '@/modules/ledger/queries'
    - import { redirect } from 'next/navigation'
    - import Link from 'next/link'
    - import { BottomNav } from '@/components/BottomNav'
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && grep -c "requireAuthenticatedIdentity\|getChildLedgerHistory\|familyId\|BottomNav" src/app/\(app\)/guardian/\[childId\]/balance/page.tsx</automated>
  </verify>
  <done>
    /guardian/[childId]/balance/page.tsx existe com auth guard NextAuth completo (identity → kredsIdentityId → membership → childProfile verification), getBalance e getChildLedgerHistory chamados com dados do filho, BottomNav do guardião.
  </done>
  <acceptance_criteria>
    - Arquivo existe em src/app/(app)/guardian/[childId]/balance/page.tsx
    - requireAuthenticatedIdentity chamado com sessão auth()
    - Verificação explícita que childProfile.familyId === familyId do guardião antes de exibir dados
    - getBalance e getChildLedgerHistory chamados com childId (não session.childProfileId)
    - BottomNav do guardião presente (não ChildBottomNav)
    - Redirecionamento para /family/children se childId não pertence à família do guardião
    - pnpm build sem erros TypeScript
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /guardian/[childId]/balance | NextAuth session necessário; childId verificado como membro da família do guardião |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-19 | Spoofing | /guardian/[childId]/balance | mitigate | auth() + requireAuthenticatedIdentity + verificação family membership |
| T-11-20 | Information Disclosure | Saldo de filho de outra família | mitigate | childProfile.familyId === membership.familyId verificado antes de exibir dados; redirect para /family/children se falhar |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado |
</threat_model>

<verification>
- Arquivo existe: ls src/app/(app)/guardian/[childId]/balance/page.tsx
- grep -c "requireAuthenticatedIdentity" src/app/(app)/guardian/[childId]/balance/page.tsx — retorna >= 1
- grep -c "getChildLedgerHistory" src/app/(app)/guardian/[childId]/balance/page.tsx — retorna >= 1
- pnpm build sem erros TypeScript
</verification>

<success_criteria>
Guardião autenticado pode ver saldo e histórico de ledger de um filho da sua família em /guardian/[childId]/balance. Acesso a filhos de outras famílias é bloqueado com redirect.
</success_criteria>

<output>
Criar `.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-07-SUMMARY.md` quando concluído.
</output>

## Artifacts this phase produces

| Arquivo | Tipo | Descrição |
|---------|------|-----------|
| `src/app/(app)/guardian/[childId]/balance/page.tsx` | Criado | View de saldo do filho para guardião autenticado (D-11) |
