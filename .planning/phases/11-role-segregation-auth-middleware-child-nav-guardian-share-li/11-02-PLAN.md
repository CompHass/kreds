---
phase: "11"
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/middleware.ts
autonomous: true
requirements: [D-03]
must_haves:
  truths:
    - "Requisições para /child/* sem cookie child-session são redirecionadas para /"
    - "Requisições para /family/* sem cookie NextAuth são redirecionadas para /api/auth/signin"
    - "Rotas públicas (/, /family/access/*, /api/auth/*, /api/child/*) passam sem verificação"
    - "O matcher exclui _next/static, _next/image, favicon.ico, arquivos .png, .svg, .ico e sw.js"
    - "Middleware não importa child-session.ts nem nenhum módulo com import 'server-only'"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Guard de rotas por papel (Edge Runtime)"
      contains: "jwtVerify, CHILD_SESSION_COOKIE, isPublicPath"
  key_links:
    - from: "src/middleware.ts"
      to: "process.env.CHILD_SESSION_SECRET"
      via: "new TextEncoder().encode()"
      pattern: "CHILD_SESSION_SECRET"
    - from: "src/middleware.ts"
      to: "/child/*"
      via: "pathname.startsWith('/child/')"
      pattern: "startsWith.*child"
---

<objective>
Criar `src/middleware.ts` que protege rotas `/child/*` com o cookie JWT customizado e rotas `/family/*` com a sessão NextAuth v5.

Purpose: Primeira linha de defesa de roteamento por papel (D-03). Sem este middleware, qualquer URL de child ou guardian é acessível sem autenticação.

Output: src/middleware.ts funcional no Edge Runtime.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-CONTEXT.md
@.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-RESEARCH.md
@src/lib/families/child-session.ts
@src/lib/auth/child-guard.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar src/middleware.ts com guard duplo Edge Runtime</name>
  <files>src/middleware.ts, tests/unit/middleware.test.ts</files>
  <read_first>
    - src/lib/families/child-session.ts — linha 1: import 'server-only'; linha 7: CHILD_SESSION_COOKIE = 'child-session'; linhas 20–24: ChildSessionPayload { childProfileId, familyId, role: 'child' }. NÃO importar este arquivo no middleware.
    - 11-RESEARCH.md Pattern 1 — código de referência do middleware com jwtVerify inline e cookie name switching HTTP/HTTPS
    - 11-RESEARCH.md Pitfall 1 — import 'server-only' causa erro Edge Runtime
    - 11-RESEARCH.md Pitfall 2 — NextAuth v5 cookie muda entre HTTP e HTTPS
    - 11-RESEARCH.md Pitfall 3 — matcher deve excluir assets estáticos e sw.js
  </read_first>
  <behavior>
    - Test 1: Requisição para /child/abc/dashboard sem cookie child-session → redirect para /
    - Test 2: Requisição para /child/abc/dashboard com cookie child-session válido (payload role='child') → NextResponse.next()
    - Test 3: Requisição para /child/abc/dashboard com cookie child-session inválido (assinatura errada) → redirect para /
    - Test 4: Requisição para /family/dashboard sem cookie authjs.session-token (HTTP) → redirect para /api/auth/signin
    - Test 5: Requisição para /family/dashboard com cookie authjs.session-token presente (HTTP) → NextResponse.next()
    - Test 6: Requisição para / (rota pública) sem cookies → NextResponse.next()
    - Test 7: Requisição para /family/access/abc123 sem cookies → NextResponse.next()
    - Test 8: Requisição para /api/auth/callback sem cookies → NextResponse.next()
    - Test 9: Requisição para /api/child/abc/tasks sem cookies → NextResponse.next()
  </behavior>
  <action>
    Criar tests/unit/middleware.test.ts primeiro (fase RED). Usar vi.mock para simular cookies e NextResponse. Os testes devem falhar inicialmente.

    Depois criar src/middleware.ts (fase GREEN):

    Importações permitidas (Edge Runtime):
    - import { NextRequest, NextResponse } from 'next/server'
    - import { jwtVerify } from 'jose'
    - NÃO importar nada de src/lib/* — todos têm import 'server-only' ou dependências Node.js

    Constantes:
    - CHILD_SESSION_COOKIE = 'child-session'
    - NEXTAUTH_COOKIE_DEV = 'authjs.session-token'
    - NEXTAUTH_COOKIE_PROD = '__Secure-authjs.session-token'
    - PUBLIC_PREFIXES: array com '/', '/api/auth', '/api/child', '/family/access'

    Função isPublicPath(pathname: string): boolean
    - Retorna true se pathname === '/' OU pathname.startsWith(prefixo + '/') para qualquer prefixo em PUBLIC_PREFIXES
    - ATENÇÃO: '/family/access/[familyId]' é público mas '/family/children' não é

    Função async middleware(request: NextRequest):
    1. Se isPublicPath(pathname) → return NextResponse.next()
    2. Se pathname.startsWith('/child/'):
       a. Ler token = request.cookies.get(CHILD_SESSION_COOKIE)?.value
       b. Se !token → redirect para new URL('/', request.url)
       c. Try: const secret = new TextEncoder().encode(process.env.CHILD_SESSION_SECRET)
          const { payload } = await jwtVerify(token, secret)
          if (payload.role !== 'child') throw new Error('not child')
          return NextResponse.next()
       d. Catch: redirect para new URL('/', request.url)
    3. Se pathname.startsWith('/family/'):
       a. isSecure = request.url.startsWith('https')
       b. cookieName = isSecure ? NEXTAUTH_COOKIE_PROD : NEXTAUTH_COOKIE_DEV
       c. Se request.cookies.get(cookieName)?.value ausente → redirect para new URL('/api/auth/signin', request.url)
       d. return NextResponse.next()
    4. Fallthrough: return NextResponse.next()

    Export config.matcher:
    - '/((?!_next/static|_next/image|favicon.ico|.*\\.png$|.*\\.svg$|.*\\.ico$|sw\\.js$).*)'

    Teste no arquivo tests/unit/middleware.test.ts:
    - Usar MockNextRequest helper que cria objetos com .nextUrl.pathname, .url, .cookies.get()
    - Mockar process.env.CHILD_SESSION_SECRET com valor de 32+ chars para testes
    - Para testes de token válido: criar JWT real com jose.SignJWT no beforeAll do teste
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && pnpm test tests/unit/middleware.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>
    Todos os 9 comportamentos testados passam. src/middleware.ts existe com export config.matcher. Arquivo não importa nenhum módulo de src/lib/*.
  </done>
  <acceptance_criteria>
    - pnpm test tests/unit/middleware.test.ts passa com todos os testes verdes
    - src/middleware.ts não contém nenhum import de src/lib/ (grep -c "from '@/lib" src/middleware.ts retorna 0)
    - export config.matcher inclui exclusão de sw.js e _next
    - Middleware verifica payload.role === 'child' antes de permitir rotas /child/*
    - Middleware verifica presença do cookie correto (dev vs prod) para /family/*
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /child/* | Cookie JWT assinado com HS256 necessário |
| Internet → /family/* | Cookie NextAuth necessário |
| Internet → Rotas públicas | Sem verificação necessária |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Spoofing | /child/* routes | mitigate | jwtVerify com CHILD_SESSION_SECRET; payload.role verificado |
| T-11-04 | Spoofing | /family/* routes | mitigate | Cookie NextAuth verificado por presença; Server Component faz verificação completa |
| T-11-05 | Tampering | child-session JWT | mitigate | HS256 assinatura verificada pelo jose — tokens adulterados rejeitados |
| T-11-06 | Elevation of Privilege | Public path bypass | mitigate | isPublicPath é restritivo: '/family/access' é público, '/family/children' não |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado |
</threat_model>

<verification>
- pnpm test tests/unit/middleware.test.ts — todos passam
- grep "from '@/lib" src/middleware.ts — retorna vazio (sem imports de server-only)
- pnpm build — sem erros de Edge Runtime relacionados ao middleware
</verification>

<success_criteria>
src/middleware.ts existe, passa nos testes unitários, não importa módulos Node.js incompatíveis com Edge Runtime, e protege corretamente /child/* e /family/* enquanto permite rotas públicas sem verificação.
</success_criteria>

<output>
Criar `.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-02-SUMMARY.md` quando concluído.
</output>

## Artifacts this phase produces

| Arquivo | Tipo | Descrição |
|---------|------|-----------|
| `src/middleware.ts` | Criado | Guard de roteamento por papel (Edge Runtime) |
| `tests/unit/middleware.test.ts` | Criado | Testes unitários cobrindo D-03 (9 comportamentos) |
