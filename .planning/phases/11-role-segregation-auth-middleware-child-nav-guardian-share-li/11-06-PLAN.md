---
phase: "11"
plan: "06"
type: execute
wave: 4
depends_on: ["11-02", "11-03"]
files_modified:
  - src/app/family/children/page.tsx
  - src/app/family/children/ShareLinkButton.tsx
  - src/app/(app)/child/[childId]/balance/page.tsx
  - src/app/(app)/child/[childId]/history/page.tsx
  - src/app/(app)/child/[childId]/new-goal/page.tsx
  - src/app/(app)/child/[childId]/profile/page.tsx
  - src/app/(app)/guardian/[childId]/earning/page.tsx
  - src/app/(app)/guardian/[childId]/adjustment/page.tsx
  - src/app/(app)/guardian/[childId]/history/page.tsx
  - src/lib/env.ts
autonomous: true
requirements: [D-04, D-11, D-12]
must_haves:
  truths:
    - "Cada card de filho em /family/children tem botão 'Compartilhar acesso' que copia URL para clipboard"
    - "URL copiada é ${NEXT_PUBLIC_APP_URL}/family/access/${familyId}"
    - "O link /child/${child.id}/balance foi removido de /family/children (substituído por /guardian/${child.id}/balance)"
    - "/(app)/child/[childId]/balance, /history, /new-goal e /profile chamam requireChildSession() + verificam session.childProfileId === childId"
    - "/(app)/guardian/[childId]/earning, /adjustment e /history chamam requireCurrentFamilyContext() com role=guardian"
    - "NEXT_PUBLIC_APP_URL em src/lib/env.ts tem .default('http://localhost:3000')"
  artifacts:
    - path: "src/app/family/children/ShareLinkButton.tsx"
      provides: "Botão cliente de cópia de link de acesso por filho"
      exports: ["ShareLinkButton"]
    - path: "src/app/family/children/page.tsx"
      provides: "Página de filhos com ShareLinkButton e link de guardião corrigido"
      contains: "ShareLinkButton, guardian/${child.id}/balance"
  key_links:
    - from: "src/app/family/children/ShareLinkButton.tsx"
      to: "navigator.clipboard.writeText"
      via: "onClick handler"
      pattern: "clipboard.writeText"
    - from: "src/app/(app)/child/[childId]/balance/page.tsx"
      to: "src/lib/auth/child-guard.ts"
      via: "requireChildSession()"
      pattern: "requireChildSession"
---

<objective>
Adicionar o botão "Compartilhar acesso" por filho em /family/children, corrigir o link de guardião, e adicionar guards de sessão corretos às páginas /(app)/*.

Purpose: Completa a segregação de papéis (D-04, D-11, D-12). Guardião não deve mais acessar rotas /child/* diretamente. Páginas /(app)/child/* precisam de child session. Páginas /(app)/guardian/* precisam de guardian session.

Output: ShareLinkButton funcional, link corrigido em /family/children, guards adicionados às 7 páginas /(app).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-CONTEXT.md
@.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-RESEARCH.md
@src/app/family/children/page.tsx
@src/lib/env.ts
@src/lib/auth/child-guard.ts
@src/lib/auth/authorization.ts
@src/app/(app)/child/[childId]/balance/page.tsx
@src/app/(app)/guardian/[childId]/earning/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar ShareLinkButton e atualizar /family/children</name>
  <files>src/app/family/children/ShareLinkButton.tsx, src/app/family/children/page.tsx, src/lib/env.ts</files>
  <read_first>
    - src/app/family/children/page.tsx — estrutura completa: imports, familyId, children.map loop (linhas 198–347); especificamente o bloco de links por filho (linhas 244–342). Remover o Link href={`/child/${child.id}/balance`} (linhas 293–308) e substituir por Link para /guardian/${child.id}/balance.
    - src/lib/env.ts — linha 5: NEXT_PUBLIC_APP_URL: z.string().url().optional(). Mudar para .default('http://localhost:3000')
    - 11-CONTEXT.md D-04 — URL format: ${NEXT_PUBLIC_APP_URL}/family/access/${familyId}; copy to clipboard com feedback "Copiado!" por 2s
    - 11-UI-SPEC.md seção "Guardian: Share Link on Child Card" — botão outlined pill "🔗 Compartilhar acesso"; URL text abaixo do botão (truncated)
  </read_first>
  <action>
    PASSO 1 — src/lib/env.ts:
    Alterar linha com NEXT_PUBLIC_APP_URL de:
      NEXT_PUBLIC_APP_URL: z.string().url().optional()
    Para:
      NEXT_PUBLIC_APP_URL: z.string().url().default('http://localhost:3000')

    PASSO 2 — src/app/family/children/ShareLinkButton.tsx:
    Marcar 'use client'.
    Props: { familyId: string; appUrl: string }

    const accessUrl = `${appUrl}/family/access/${familyId}`
    useState: copied = false

    async function handleCopy() {
      try {
        await navigator.clipboard.writeText(accessUrl)
        setCopied(true)
        setTimeout(() => setCopied(false), 2000)
      } catch {
        // fallback: selecionar texto do URL display
      }
    }

    Render:
    - div container com flexDirection column, gap 6px, alignItems flex-start
    - Botão "🔗 {copied ? 'Copiado!' : 'Compartilhar acesso'}" com estilo:
      - outlined pill: border '1px solid rgba(45,90,39,0.16)', background 'rgba(255,255,255,0.82)', color '#154212', borderRadius '99px', padding '4px 12px', fontSize '0.75rem', fontWeight 600, cursor pointer
      - Se copied: background 'rgba(59,105,52,0.12)', color '#3b6934', border '1px solid rgba(59,105,52,0.2)'
    - Parágrafo com URL truncada (max-width 180px, overflow hidden, textOverflow ellipsis, whiteSpace nowrap, fontSize '0.7rem', color '#72796e')

    PASSO 3 — src/app/family/children/page.tsx:
    a. Importar ShareLinkButton de './ShareLinkButton' no topo do arquivo
    b. No bloco de links por filho (dentro do children.map loop), realizar as seguintes mudanças:
       - REMOVER o Link existente com href={`/child/${child.id}/balance`} que tem label "Sonhos" (per D-11)
       - ADICIONAR no lugar (ou junto aos links de guardião): Link href={`/guardian/${child.id}/balance`} com label "Ver saldo" usando estilo verde similar
       - ADICIONAR após os links de ação: componente ShareLinkButton com familyId={familyId} e appUrl={process.env.NEXT_PUBLIC_APP_URL ?? 'http://localhost:3000'}
       (NEXT_PUBLIC_APP_URL é acessível em Server Components via process.env diretamente)
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && grep -c "ShareLinkButton\|guardian.*balance\|Compartilhar" src/app/family/children/page.tsx && grep -c "clipboard.writeText\|copied\|Copiado" src/app/family/children/ShareLinkButton.tsx</automated>
  </verify>
  <done>
    ShareLinkButton.tsx existe com 'use client', clipboard.writeText e feedback "Copiado!". family/children/page.tsx importa ShareLinkButton, não tem mais link /child/${child.id}/balance e tem link /guardian/${child.id}/balance. env.ts usa .default() para NEXT_PUBLIC_APP_URL.
  </done>
  <acceptance_criteria>
    - ShareLinkButton.tsx marcado 'use client' com navigator.clipboard.writeText
    - ShareLinkButton.tsx: button text muda para "Copiado!" por 2s após click
    - family/children/page.tsx: ShareLinkButton importado e usado dentro do children.map
    - family/children/page.tsx: grep para "/child/\${child.id}/balance" retorna 0 (link removido per D-11)
    - family/children/page.tsx: contém link para /guardian/${child.id}/balance
    - src/lib/env.ts: NEXT_PUBLIC_APP_URL usa .default('http://localhost:3000') em vez de .optional()
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Adicionar guards de sessão às páginas /(app)/child e /(app)/guardian</name>
  <files>
    src/app/(app)/child/[childId]/balance/page.tsx,
    src/app/(app)/child/[childId]/history/page.tsx,
    src/app/(app)/child/[childId]/new-goal/page.tsx,
    src/app/(app)/child/[childId]/profile/page.tsx,
    src/app/(app)/guardian/[childId]/earning/page.tsx,
    src/app/(app)/guardian/[childId]/adjustment/page.tsx,
    src/app/(app)/guardian/[childId]/history/page.tsx
  </files>
  <read_first>
    - src/app/(app)/child/[childId]/balance/page.tsx — atualmente usa requireCurrentFamilyContext + requireChildInFamily (linhas 1–19). Substituir por requireChildSession + verificação de childId scope.
    - src/app/(app)/child/[childId]/history/page.tsx — atualmente usa requireCurrentFamilyContext (linhas 1–9). Substituir por requireChildSession + verificação de childId.
    - src/app/(app)/guardian/[childId]/earning/page.tsx — atualmente usa requireCurrentFamilyContext + role check (linhas 18–23). Manter lógica existente — já tem guard correto.
    - src/lib/auth/child-guard.ts — requireChildSession(): Promise&lt;ChildSessionPayload&gt;; redireciona para '/' se sessão ausente
    - 11-RESEARCH.md Pitfall 4 — /(app) sem layout.tsx; cada page.tsx precisa do guard manualmente
    - 11-CONTEXT.md D-12 — requireChildSession + session.childProfileId === childId em todas as /(app)/child/*; requireCurrentFamilyContext com role=guardian em todas as /(app)/guardian/*
  </read_first>
  <action>
    PÁGINAS /(app)/child/[childId]/* — padrão a aplicar em TODAS as 4 páginas:

    Para cada página (balance, history, new-goal, profile):
    1. Adicionar import { requireChildSession } from '@/lib/auth/child-guard' se ausente
    2. Substituir o bloco try/catch com requireCurrentFamilyContext/requireChildInFamily por:
       const { childId } = await params
       const session = await requireChildSession()
       if (session.childProfileId !== childId) {
         redirect(`/child/${session.childProfileId}/[pagename]`)
       }
       const familyId = session.familyId
    3. Remover imports não utilizados (requireCurrentFamilyContext, requireChildInFamily) após a substituição
    4. Manter todas as queries existentes — apenas mudar a origem do familyId (agora vem de session.familyId)

    PÁGINAS /(app)/guardian/[childId]/* — padrão a aplicar em TODAS as 3 páginas:

    Para cada página (earning, adjustment, history):
    1. Verificar se requireCurrentFamilyContext já existe e tem role check — earning/page.tsx JÁ TEM o check correto (linhas 18–23)
    2. Se role check ausente: adicionar após requireCurrentFamilyContext:
       if (role !== 'guardian') redirect('/api/auth/signin')
    3. Verificar se requireChildInFamily está sendo chamado — se não, adicionar para confirmar que o childId pertence à família do guardião

    ATENÇÃO ao refatorar /(app)/child/[childId]/balance/page.tsx:
    - Esta página usa listGoals e getBalance que precisam de familyId como segundo parâmetro em alguns casos
    - Usar session.familyId em vez de familyId de requireCurrentFamilyContext
    - getBalance(childId, 'available') e getBalance(childId, 'firstfruits') não precisam de familyId
    - listGoals(childId, familyId) usa session.familyId
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && grep -l "requireCurrentFamilyContext" src/app/\(app\)/child/\[childId\]/*/page.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    Nenhuma página em /(app)/child/[childId]/* usa requireCurrentFamilyContext. Todas as 4 páginas do filho usam requireChildSession + verificação de childId. Todas as 3 páginas do guardião verificam role === 'guardian'.
  </done>
  <acceptance_criteria>
    - grep para "requireCurrentFamilyContext" em src/app/(app)/child/ retorna 0 resultados
    - Todas as 4 páginas /(app)/child/*: contêm "requireChildSession" e "session.childProfileId !== childId"
    - Todas as 3 páginas /(app)/guardian/*: contêm verificação de role 'guardian' (requireCurrentFamilyContext com role check ou requireActiveGuardian)
    - pnpm build sem erros TypeScript em nenhuma dessas 7 páginas
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /(app)/child/[childId]/* | Child session JWT necessário — estava aberto antes desta fase |
| Browser → /(app)/guardian/[childId]/* | NextAuth session com role guardian necessário |
| Guardian → /child/${id}/balance | Removido — substituído por /guardian/${id}/balance com NextAuth session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16 | Spoofing | /(app)/child/* sem guard | mitigate | requireChildSession adicionado a todas as 4 páginas do filho |
| T-11-17 | Elevation of Privilege | /child/[childId]/balance via guardião | mitigate | Link removido de /family/children; rota /child/* protegida pelo middleware |
| T-11-18 | Spoofing | clipboard URL | accept | NEXT_PUBLIC_APP_URL é variável de configuração, não segredo; URL copiada é apenas de acesso público |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado |
</threat_model>

<verification>
- grep -c "requireCurrentFamilyContext" src/app/(app)/child/[childId]/balance/page.tsx — retorna 0
- grep -c "requireChildSession" src/app/(app)/child/[childId]/balance/page.tsx — retorna >= 1
- grep -c "ShareLinkButton" src/app/family/children/page.tsx — retorna >= 1
- pnpm build sem erros TypeScript
</verification>

<success_criteria>
Guardião não pode mais acessar rotas /child/* diretamente via link em /family/children. Botão "Compartilhar acesso" copia URL de acesso do filho com feedback visual. Todas as páginas /(app)/child/* têm child session guard. Todas as páginas /(app)/guardian/* têm guardian session guard.
</success_criteria>

<output>
Criar `.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-06-SUMMARY.md` quando concluído.
</output>

## Artifacts this phase produces

| Arquivo | Tipo | Descrição |
|---------|------|-----------|
| `src/app/family/children/ShareLinkButton.tsx` | Criado | Botão cliente de cópia de link de acesso (D-04) |
| `src/app/family/children/page.tsx` | Modificado | ShareLinkButton adicionado; link /child/*/balance removido; /guardian/*/balance adicionado (D-11) |
| `src/lib/env.ts` | Modificado | NEXT_PUBLIC_APP_URL com .default() |
| `src/app/(app)/child/[childId]/balance/page.tsx` | Modificado | requireCurrentFamilyContext → requireChildSession (D-12) |
| `src/app/(app)/child/[childId]/history/page.tsx` | Modificado | requireCurrentFamilyContext → requireChildSession (D-12) |
| `src/app/(app)/child/[childId]/new-goal/page.tsx` | Modificado | Guard child session adicionado (D-12) |
| `src/app/(app)/child/[childId]/profile/page.tsx` | Modificado | Guard child session adicionado (D-12) |
| `src/app/(app)/guardian/[childId]/earning/page.tsx` | Modificado | Role guardian verificado (D-12) |
| `src/app/(app)/guardian/[childId]/adjustment/page.tsx` | Modificado | Role guardian verificado (D-12) |
| `src/app/(app)/guardian/[childId]/history/page.tsx` | Modificado | Role guardian verificado (D-12) |
