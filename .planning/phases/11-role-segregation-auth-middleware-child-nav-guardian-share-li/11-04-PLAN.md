---
phase: "11"
plan: "04"
type: execute
wave: 3
depends_on: ["11-01", "11-03"]
files_modified:
  - src/app/child/[childId]/tasks/page.tsx
  - src/app/child/[childId]/tasks/TaskToggleButton.tsx
  - src/app/api/child/[childId]/tasks/[taskId]/toggle/route.ts
autonomous: true
requirements: [D-07]
must_haves:
  truths:
    - "Página /child/[childId]/tasks lista apenas tarefas atribuídas ao filho autenticado no ciclo atual"
    - "Cada tarefa mostra título, valor em Kreds e botão de toggle com estado pending/completed"
    - "POST /api/child/[childId]/tasks/[taskId]/toggle faz upsert idempotente em task_completions usando onConflictDoUpdate"
    - "cycleStart computado como 'YYYY-MM-DD' usando getCycleForDate(new Date(), timezone).cycleStart.toISOString().split('T')[0]"
    - "requireChildSession() chamado na página E na API route; childId verificado em ambos"
  artifacts:
    - path: "src/app/child/[childId]/tasks/page.tsx"
      provides: "Lista de tarefas do filho para o ciclo atual"
      contains: "requireChildSession, ChildBottomNav"
    - path: "src/app/child/[childId]/tasks/TaskToggleButton.tsx"
      provides: "Botão cliente para marcar/desmarcar tarefa"
      exports: ["TaskToggleButton"]
    - path: "src/app/api/child/[childId]/tasks/[taskId]/toggle/route.ts"
      provides: "API route para toggle de task_completion"
      exports: ["POST"]
  key_links:
    - from: "src/app/child/[childId]/tasks/TaskToggleButton.tsx"
      to: "/api/child/[childId]/tasks/[taskId]/toggle"
      via: "fetch POST"
      pattern: "api/child.*tasks.*toggle"
    - from: "src/app/api/child/[childId]/tasks/[taskId]/toggle/route.ts"
      to: "schema.taskCompletions"
      via: "db.insert().onConflictDoUpdate()"
      pattern: "taskCompletions"
---

<objective>
Criar a página de tarefas do filho `/child/[childId]/tasks`, o botão cliente de toggle e a API route de toggle.

Purpose: Implementa D-07 — filho pode marcar/desmarcar tarefas do ciclo atual sem aprovação do guardião nesta fase. Tabela task_completions (criada no plano 11-01) registra o estado de cada tarefa.

Output: Página de tarefas funcional com guard de sessão, toggle otimista via API route, upsert idempotente na tabela task_completions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-CONTEXT.md
@.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-RESEARCH.md
@.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-UI-SPEC.md
@src/lib/db/schema/index.ts
@src/lib/auth/child-guard.ts
@src/lib/families/child-session.ts
@src/lib/db/tasks/queries.ts
@src/modules/activity/cycle.ts
@src/components/ChildBottomNav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar API route POST /api/child/[childId]/tasks/[taskId]/toggle</name>
  <files>src/app/api/child/[childId]/tasks/[taskId]/toggle/route.ts</files>
  <read_first>
    - src/lib/families/child-session.ts — exporta getChildSession(cookieStore): Promise&lt;ChildSessionPayload | null&gt;; CHILD_SESSION_COOKIE = 'child-session'; ChildSessionPayload { childProfileId, familyId, role }
    - src/lib/db/schema/index.ts — taskCompletions: campos id, taskTemplateId, childProfileId, cycleStart, completedAt, status, updatedAt. uniqueIndex unique_task_child_cycle em (taskTemplateId, childProfileId, cycleStart)
    - src/modules/activity/cycle.ts — getCycleForDate(date: Date, timezone: string): { cycleStart: Date, cycleEnd: Date }. Usar cycleStart.toISOString().split('T')[0] para obter 'YYYY-MM-DD'
    - src/app/(app)/child/[childId]/balance/GoalCard.tsx — padrão de fetch para API route do filho; auth via getChildSession na API
    - 11-RESEARCH.md Code Examples — "Upsert de task_completion (idempotente)" com onConflictDoUpdate
    - 11-RESEARCH.md Pitfall 5 — cycleStart como string ISO; usar .split('T')[0]
  </read_first>
  <action>
    Criar src/app/api/child/[childId]/tasks/[taskId]/toggle/route.ts.

    Importações: NextRequest, NextResponse de 'next/server'; cookies de 'next/headers'; getChildSession de '@/lib/families/child-session'; db de '@/lib/db'; schema de '@/lib/db/schema'; eq de 'drizzle-orm'; getCycleForDate de '@/modules/activity/cycle'; families, childProfiles de schema.

    Export async function POST(req, { params }):
    - const { childId, taskId } = await params
    - const cookieStore = await cookies()
    - const session = await getChildSession(cookieStore)
    - Se !session || session.childProfileId !== childId → return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

    Buscar family timezone para calcular ciclo correto:
    - const [familyRow] = await db.select({ timezone: schema.families.timezone }).from(schema.families).innerJoin(schema.childProfiles, eq(schema.childProfiles.familyId, schema.families.id)).where(eq(schema.childProfiles.id, session.childProfileId)).limit(1)
    - const timezone = familyRow?.timezone ?? 'America/Sao_Paulo'
    - const { cycleStart } = getCycleForDate(new Date(), timezone)
    - const cycleStartStr = cycleStart.toISOString().split('T')[0]  // 'YYYY-MM-DD'

    Ler body JSON: const { action } = await req.json().catch(() => ({}))
    - action pode ser 'complete' ou 'uncomplete'

    Se action === 'complete':
      await db.insert(schema.taskCompletions).values({
        taskTemplateId: taskId,
        childProfileId: session.childProfileId,
        cycleStart: cycleStartStr,
        status: 'completed',
        completedAt: new Date(),
      }).onConflictDoUpdate({
        target: [schema.taskCompletions.taskTemplateId, schema.taskCompletions.childProfileId, schema.taskCompletions.cycleStart],
        set: { status: 'completed', completedAt: new Date(), updatedAt: new Date() },
      })

    Se action === 'uncomplete':
      await db.insert(schema.taskCompletions).values({
        taskTemplateId: taskId,
        childProfileId: session.childProfileId,
        cycleStart: cycleStartStr,
        status: 'pending',
        completedAt: null,
      }).onConflictDoUpdate({
        target: [schema.taskCompletions.taskTemplateId, schema.taskCompletions.childProfileId, schema.taskCompletions.cycleStart],
        set: { status: 'pending', completedAt: null, updatedAt: new Date() },
      })

    Return NextResponse.json({ ok: true, status: action === 'complete' ? 'completed' : 'pending' })

    Wrap com try/catch geral → return NextResponse.json({ error: 'Internal error' }, { status: 500 }) on catch.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && grep -c "onConflictDoUpdate\|getChildSession\|cycleStart" src/app/api/child/\[childId\]/tasks/\[taskId\]/toggle/route.ts</automated>
  </verify>
  <done>
    route.ts existe com auth guard (getChildSession + childId check), cálculo de cycleStart como 'YYYY-MM-DD', upsert idempotente com onConflictDoUpdate para ambas as ações complete e uncomplete.
  </done>
  <acceptance_criteria>
    - Arquivo contém getChildSession e verifica session.childProfileId === childId
    - cycleStart calculado como cycleStart.toISOString().split('T')[0]
    - onConflictDoUpdate presente para target [taskTemplateId, childProfileId, cycleStart]
    - Retorna 401 se session ausente ou childId não corresponde
    - Retorna { ok: true, status: 'completed' | 'pending' } em sucesso
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Criar TaskToggleButton e página /child/[childId]/tasks</name>
  <files>src/app/child/[childId]/tasks/TaskToggleButton.tsx, src/app/child/[childId]/tasks/page.tsx</files>
  <read_first>
    - src/app/(app)/child/[childId]/balance/GoalCard.tsx — padrão 'use client', useState, useRouter().refresh(), fetch para API route, error state, loading state
    - src/components/ChildBottomNav.tsx — props { active, childId }; importar de '@/components/ChildBottomNav'
    - src/lib/db/tasks/queries.ts — getActiveTasksForFamily(familyId) retorna array com { id, title, kredsValue, assignedChildId, childName }
    - src/lib/db/schema/index.ts — taskCompletions: campos taskTemplateId, childProfileId, cycleStart, status
    - 11-UI-SPEC.md seção /child/[childId]/tasks — card por tarefa, botão toggle com estados pending/completed
    - src/modules/activity/cycle.ts — getCycleForDate para filtrar completions do ciclo atual
  </read_first>
  <action>
    PARTE A — src/app/child/[childId]/tasks/TaskToggleButton.tsx:
    Marcar 'use client'.
    Props: { taskId: string; childId: string; initialStatus: 'pending' | 'completed' }

    useState: status = initialStatus, loading = false, error = null.

    Função handleToggle():
    - setLoading(true)
    - const action = status === 'pending' ? 'complete' : 'uncomplete'
    - const res = await fetch(`/api/child/${childId}/tasks/${taskId}/toggle`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action }) })
    - Se res.ok: const data = await res.json(); setStatus(data.status)
    - Se !res.ok: setError('Erro ao atualizar. Tente novamente.')
    - finally: setLoading(false)

    Render:
    - Se status === 'pending': botão outlined "Marcar como feita" (border 1px solid #2d5a27, background transparente, color #2d5a27, borderRadius 9999px, padding 8px 16px)
    - Se status === 'completed': div com "✓ Feita" (background rgba(59,105,52,0.15), color #3b6934, borderRadius 9999px, padding 4px 12px) + botão link "Desmarcar" (color #72796e, font-size small, cursor pointer, border none, background none)
    - Se loading: desabilitar botão e mostrar '...'
    - Se error: parágrafo vermelho com mensagem

    PARTE B — src/app/child/[childId]/tasks/page.tsx:
    export const dynamic = 'force-dynamic'.
    Props: { params: Promise<{ childId: string }> }

    Auth:
    - const { childId } = await params
    - const session = await requireChildSession()
    - if (session.childProfileId !== childId) redirect(`/child/${session.childProfileId}/tasks`)

    Queries em paralelo:
    - Tarefas: getActiveTasksForFamily(session.familyId) — filtrar por assignedChildId === childId
    - Família timezone: db.select({ timezone: schema.families.timezone }).from(families).innerJoin(childProfiles, ...).where(eq(childProfiles.id, session.childProfileId)).limit(1)
    - cycleStartStr = getCycleForDate(new Date(), timezone).cycleStart.toISOString().split('T')[0]
    - Completions do ciclo: db.select({ taskTemplateId: schema.taskCompletions.taskTemplateId, status: schema.taskCompletions.status }).from(schema.taskCompletions).where(and(eq(schema.taskCompletions.childProfileId, session.childProfileId), eq(schema.taskCompletions.cycleStart, cycleStartStr)))

    Criar Map&lt;string, 'pending' | 'completed'&gt; de completions: completionMap.get(taskId) ?? 'pending'.

    Filtrar tasks: const childTasks = activeTasks.filter(t => t.assignedChildId === childId)

    Layout (per UI-SPEC, Sylvan design):
    - Header: "Minhas Tarefas" + subtítulo "Ciclo atual"
    - Se childTasks.length === 0: empty state card "Nenhuma tarefa ativa esta semana." com ícone 🌱
    - Lista: um card por tarefa com título, badge "🪙 {kredsValue} Kreds" (gold pill), TaskToggleButton
    - Padding-bottom 80px para ChildBottomNav
    - ChildBottomNav active="tarefas" childId={childId}
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && grep -c "requireChildSession\|TaskToggleButton\|ChildBottomNav" src/app/child/\[childId\]/tasks/page.tsx && grep -c "use client\|handleToggle\|initialStatus" src/app/child/\[childId\]/tasks/TaskToggleButton.tsx</automated>
  </verify>
  <done>
    tasks/page.tsx existe com requireChildSession, filtro de tarefas por assignedChildId, mapa de completions do ciclo e ChildBottomNav active="tarefas". TaskToggleButton.tsx é 'use client' com fetch para toggle API e estados pending/completed.
  </done>
  <acceptance_criteria>
    - TaskToggleButton.tsx marcado com 'use client' no topo
    - TaskToggleButton.tsx: fetch para /api/child/${childId}/tasks/${taskId}/toggle com { action: 'complete' | 'uncomplete' }
    - tasks/page.tsx: requireChildSession() chamado antes das queries
    - tasks/page.tsx: if (session.childProfileId !== childId) redirect(...)
    - tasks/page.tsx: completions filtradas por cycleStart === cycleStartStr (string 'YYYY-MM-DD')
    - tasks/page.tsx: ChildBottomNav importado com active='tarefas'
    - pnpm build sem erros de TypeScript nestas páginas
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/child/[childId]/tasks/[taskId]/toggle | Cookie child-session necessário; childId validado contra session |
| API route → task_completions DB | Upsert escoped por childProfileId da sessão — nunca por childId do URL diretamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Spoofing | POST /api/child/[childId]/tasks/[taskId]/toggle | mitigate | getChildSession + session.childProfileId === childId; retorna 401 se inválido |
| T-11-10 | Tampering | task_completions insert | mitigate | onConflictDoUpdate garante idempotência; unique constraint previne duplicatas |
| T-11-11 | Elevation of Privilege | taskId no URL | mitigate | API não usa taskId do URL diretamente para autorização — usa session.childProfileId como scope primário |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado |
</threat_model>

<verification>
- grep -c "onConflictDoUpdate" src/app/api/child/[childId]/tasks/[taskId]/toggle/route.ts — retorna >= 2
- grep -c "requireChildSession" src/app/child/[childId]/tasks/page.tsx — retorna >= 1
- pnpm build sem erros de TypeScript
</verification>

<success_criteria>
Filho pode ver tarefas do ciclo atual na página /child/[childId]/tasks, marcar como feita e desmarcar. API route persiste o estado em task_completions com upsert idempotente. Sessão verificada em ambas as camadas.
</success_criteria>

<output>
Criar `.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-04-SUMMARY.md` quando concluído.
</output>

## Artifacts this phase produces

| Arquivo | Tipo | Descrição |
|---------|------|-----------|
| `src/app/child/[childId]/tasks/page.tsx` | Criado | Página de tarefas do filho com guard e ciclo atual (D-07) |
| `src/app/child/[childId]/tasks/TaskToggleButton.tsx` | Criado | Botão cliente de toggle com fetch e estado otimista |
| `src/app/api/child/[childId]/tasks/[taskId]/toggle/route.ts` | Criado | API route de upsert idempotente em task_completions |
