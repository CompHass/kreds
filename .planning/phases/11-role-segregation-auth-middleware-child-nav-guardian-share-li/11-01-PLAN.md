---
phase: "11"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema/index.ts
autonomous: true
requirements: [D-07, D-10]
must_haves:
  truths:
    - "Tabela task_completions existe no banco com constraint unique (taskTemplateId, childProfileId, cycleStart)"
    - "Tabela donations existe no banco com check constraint amountKreds > 0"
    - "Enums task_completion_status e donation_status existem no schema Drizzle"
    - "pnpm db:generate executa sem erros após as alterações"
    - "pnpm db:migrate aplica as migrations ao banco PostgreSQL"
  artifacts:
    - path: "src/lib/db/schema/index.ts"
      provides: "Definição Drizzle de task_completions e donations"
      contains: "taskCompletions, donations, taskCompletionStatusEnum, donationStatusEnum"
    - path: "drizzle/"
      provides: "Arquivos SQL de migration gerados"
  key_links:
    - from: "src/lib/db/schema/index.ts"
      to: "PostgreSQL"
      via: "pnpm db:migrate"
      pattern: "taskCompletions|donations"
---

<objective>
Adicionar as tabelas `task_completions` e `donations` ao schema Drizzle e aplicar a migration ao banco PostgreSQL.

Purpose: As páginas de tarefas do filho (D-07) e doações (D-10) dependem dessas tabelas existirem no banco antes de qualquer código de UI ou API ser executado. Este plano é bloqueante — nenhum outro plano desta fase pode avançar sem as tabelas no banco.

Output: Schema Drizzle atualizado + migration SQL gerada + migration aplicada ao cluster PostgreSQL.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-CONTEXT.md
@.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-RESEARCH.md
@src/lib/db/schema/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar tabelas task_completions e donations ao schema Drizzle</name>
  <files>src/lib/db/schema/index.ts</files>
  <read_first>
    - src/lib/db/schema/index.ts — padrão de pgEnum, pgTable, index, uniqueIndex, check; ver taskTemplates (linhas 177–203) e wishlistGoals (linhas 208–229) como modelo de estrutura
    - src/modules/activity/cycle.ts — getCycleForDate retorna { cycleStart: Date, cycleEnd: Date }; cycleStart.toISOString().split('T')[0] produz 'YYYY-MM-DD'
  </read_first>
  <action>
    Ao final de src/lib/db/schema/index.ts, após a definição de `wishlistGoals`, adicionar:

    1. Enum `task_completion_status` com valores `['pending', 'completed']` — exportar como `taskCompletionStatusEnum`.

    2. Tabela `task_completions` (per D-07):
       - `id`: uuid().defaultRandom().primaryKey()
       - `taskTemplateId`: uuid('task_template_id').notNull().references(() => taskTemplates.id)
       - `childProfileId`: uuid('child_profile_id').notNull().references(() => childProfiles.id)
       - `cycleStart`: text('cycle_start').notNull() — armazena string 'YYYY-MM-DD'
       - `completedAt`: timestamp('completed_at') — nullable (não chamar .notNull())
       - `status`: taskCompletionStatusEnum('status').notNull().default('pending')
       - `createdAt`: timestamp('created_at').defaultNow().notNull()
       - `updatedAt`: timestamp('updated_at').defaultNow().notNull()
       - Índices/constraints no terceiro argumento:
         - `childIdIdx`: index('task_completions_child_id_idx').on(table.childProfileId)
         - `taskIdIdx`: index('task_completions_task_id_idx').on(table.taskTemplateId)
         - `uniqueTaskChildCycle`: uniqueIndex('unique_task_child_cycle').on(table.taskTemplateId, table.childProfileId, table.cycleStart)

    3. Enum `donation_status` com valores `['pending', 'approved', 'rejected']` — exportar como `donationStatusEnum`.

    4. Tabela `donations` (per D-10):
       - `id`: uuid().defaultRandom().primaryKey()
       - `familyId`: uuid('family_id').notNull().references(() => families.id)
       - `childProfileId`: uuid('child_profile_id').notNull().references(() => childProfiles.id)
       - `targetLabel`: text('target_label').notNull()
       - `amountKreds`: integer('amount_kreds').notNull()
       - `status`: donationStatusEnum('status').notNull().default('pending')
       - `requestedAt`: timestamp('requested_at').defaultNow().notNull()
       - `approvedAt`: timestamp('approved_at') — nullable
       - Índices/constraints:
         - `familyIdIdx`: index('donations_family_id_idx').on(table.familyId)
         - `childIdIdx`: index('donations_child_id_idx').on(table.childProfileId)
         - `amountCheck`: check('donation_amount_positive', sql`${table.amountKreds} > 0`)

    Importações necessárias já presentes em index.ts: `pgTable`, `uuid`, `text`, `timestamp`, `integer`, `pgEnum`, `index`, `uniqueIndex`, `check`, `sql` — não duplicar imports.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && grep -c "taskCompletions\|donations\|taskCompletionStatusEnum\|donationStatusEnum" src/lib/db/schema/index.ts</automated>
  </verify>
  <done>
    index.ts contém as definições de taskCompletionStatusEnum, taskCompletions, donationStatusEnum e donations com todos os campos, índices e constraints descritos acima. O grep retorna pelo menos 4.
  </done>
  <acceptance_criteria>
    - taskCompletionStatusEnum exportado com valores 'pending' | 'completed'
    - donationStatusEnum exportado com valores 'pending' | 'approved' | 'rejected'
    - taskCompletions.uniqueTaskChildCycle: uniqueIndex composto em (taskTemplateId, childProfileId, cycleStart)
    - donations.amountCheck: check constraint donation_amount_positive com amountKreds > 0
    - Todos os campos FK referenciam tabelas existentes (taskTemplates, childProfiles, families)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Gerar e aplicar migration Drizzle</name>
  <files>drizzle/</files>
  <read_first>
    - package.json — scripts: "db:generate": "drizzle-kit generate", "db:migrate": "drizzle-kit migrate"
    - drizzle.config.ts — confirmar caminho do schema e saída
  </read_first>
  <action>
    Executar em sequência (um falha → parar):

    1. `pnpm db:generate` — gera o arquivo SQL em drizzle/ com as novas tabelas.
    2. `pnpm db:migrate` — aplica a migration ao banco PostgreSQL (cluster k3s port-forwarded ou DATABASE_URL configurado).

    Este task é BLOQUEANTE: se qualquer comando falhar, não avançar para os próximos planos desta fase.

    Se `db:migrate` falhar com erro de conexão, verificar se o port-forward do cluster está ativo. Não modificar o schema para contornar erros de conexão — resolver a conectividade primeiro.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && pnpm db:generate 2>&1 | tail -5 && pnpm db:migrate 2>&1 | tail -5</automated>
  </verify>
  <done>
    pnpm db:generate concluiu sem erros e criou arquivo SQL em drizzle/. pnpm db:migrate concluiu sem erros e tabelas task_completions e donations existem no banco PostgreSQL.
  </done>
  <acceptance_criteria>
    - Arquivo SQL gerado em drizzle/ contém CREATE TABLE task_completions e CREATE TABLE donations
    - pnpm db:migrate retorna sucesso (sem "error" ou "failed" na saída)
    - Banco PostgreSQL aceita INSERT em task_completions e donations (verificável manualmente se necessário)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → PostgreSQL | Constraints de integridade aplicadas via migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | task_completions | mitigate | uniqueIndex unique_task_child_cycle previne múltiplas completions para o mesmo (task, child, cycle) |
| T-11-02 | Tampering | donations | mitigate | check constraint donation_amount_positive previne amountKreds <= 0 no banco |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase — todos os pacotes já auditados |
</threat_model>

<verification>
- pnpm db:generate executa sem erros
- pnpm db:migrate aplica migration sem erros
- src/lib/db/schema/index.ts exporta taskCompletions, donations, taskCompletionStatusEnum, donationStatusEnum
- uniqueIndex unique_task_child_cycle definido em taskCompletions
- check donation_amount_positive definido em donations
</verification>

<success_criteria>
Tabelas task_completions e donations existem no banco PostgreSQL com todas as constraints. Schema Drizzle exporta os novos types. Outros planos da fase 11 podem prosseguir.
</success_criteria>

<output>
Criar `.planning/phases/11-role-segregation-auth-middleware-child-nav-guardian-share-li/11-01-SUMMARY.md` quando concluído.
</output>

## Artifacts this phase produces

| Arquivo | Tipo | Descrição |
|---------|------|-----------|
| `src/lib/db/schema/index.ts` | Modificado | +taskCompletionStatusEnum, +taskCompletions, +donationStatusEnum, +donations |
| `drizzle/*.sql` | Gerado | Migration SQL com CREATE TABLE task_completions e donations |
