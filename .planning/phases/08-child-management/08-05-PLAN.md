---
phase: 08-child-management
plan: 05
type: execute
wave: 4
depends_on: ["08-02", "08-03", "08-04"]
files_modified:
  - src/components/parent/children-panel-view.tsx
  - src/app/family/[familyId]/children/page.tsx
  - tests/unit/children-panel.test.tsx
autonomous: false
requirements: [D-01, D-04, D-08, D-09, D-12, D-13, D-14, D-15]
must_haves:
  truths:
    - "The /family/[familyId]/children route is a Server Component page following the file-based pattern of /family/[familyId]/tasks (D-01)"
    - "Visiting /family/[familyId]/children lists the family's children (including deactivated ones)"
    - "Guardian can add a child via the side panel; the new card appears without a full reload"
    - "Guardian can reset a child's 4-digit PIN via the keypad panel"
    - "Guardian can reveal/hide a child's PIN when one has been set"
    - "Deactivate/reactivate goes through the confirmation dialog before mutating active"
    - "The /children drawer state is page-local and does not leak to /tasks (D-04)"
  artifacts:
    - path: "src/components/parent/children-panel-view.tsx"
      provides: "Client root orchestrating list + add + reset + reveal + deactivate"
      exports: ["ChildrenPanelView"]
    - path: "src/app/family/[familyId]/children/page.tsx"
      provides: "SSR page: familyId-scoped child query + view render"
      contains: "childProfiles"
  key_links:
    - from: "src/app/family/[familyId]/children/page.tsx"
      to: "childProfiles familyId query"
      via: "select where eq(familyId) — no active filter"
      pattern: "childProfiles"
    - from: "src/components/parent/children-panel-view.tsx"
      to: "src/app/actions/children.ts"
      via: "createChild/resetChildPin/revealChildPin/toggleChildActive calls"
      pattern: "children"
    - from: "src/components/parent/children-panel-view.tsx"
      to: "ConfirmDeactivateDialog"
      via: "confirmTargetId state gates dialog open"
      pattern: "ConfirmDeactivateDialog"
---

<objective>
Assemble the `/children` experience: the SSR `page.tsx` (familyId-scoped child query, no active filter so deactivated children still show) and the `ChildrenPanelView` client root that renders the sidebar/topbar/drawer (page-local per D-04), the child card list, the add-child panel, the PIN reset panel, and the confirmation dialog — wiring the Plan 03 Server Actions with the established optimistic + fire-and-forget pattern. Ends with a human-verify checkpoint covering the full flow and the /tasks refactor parity.

Purpose: This is the deliverable that satisfies all four Phase 8 ROADMAP success criteria (list, add, PIN, deactivate/reactivate). It composes the Plan 02 layout/sidebar, Plan 03 backend, and Plan 04 components.
Output: `children-panel-view.tsx`, `children/page.tsx`, an integration-shaped component test, and a visual checkpoint.
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-child-management/08-CONTEXT.md
@.planning/phases/08-child-management/08-RESEARCH.md
@.planning/phases/08-child-management/08-PATTERNS.md
@.planning/phases/08-child-management/08-UI-SPEC.md
@.planning/phases/08-child-management/08-02-SUMMARY.md
@.planning/phases/08-child-management/08-03-SUMMARY.md
@.planning/phases/08-child-management/08-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols and files this phase creates (exclude from drift/source-grounding checks):
- `src/components/parent/children-panel-view.tsx` — `ChildrenPanelView`
- `src/app/family/[familyId]/children/page.tsx` — `ChildrenPage` default export (Server Component)
- `tests/unit/children-panel.test.tsx` — new test file
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: children/page.tsx SSR query + ChildrenPanelView client root</name>
  <read_first>
    - src/app/family/[familyId]/tasks/page.tsx (SSR page template: await params, auth() for user fields, parallel queries, familyName fallback 'Família', prop-passing avoiding the reserved 'children' prop name — use initialChildren; import auth from `../../../../../auth` = 5 `../`)
    - src/components/parent/parent-panel-view.tsx (full file — the client-root template: useState root state, editingId sentinel null|'new'|'<id>', profileOpen page-local, optimistic + fire-and-forget handlers lines 72-84, JSX composition of ParentSidebar+ParentTopbar+main+GuardianProfileDrawer lines 182-330)
    - src/types/child.ts (ChildProfileView shape returned to the view)
    - src/app/actions/children.ts (createChild/resetChildPin/revealChildPin/toggleChildActive signatures)
    - src/components/parent/child-form-panel.tsx, child-card.tsx, child-pin-reset-panel.tsx, confirm-deactivate-dialog.tsx (the Plan 04 components + their prop contracts)
    - .planning/phases/08-child-management/08-PATTERNS.md (children-panel-view section lines 325-386 — imports, root state shape including confirmTargetId + revealedPins, handleConfirmDeactivate, JSX composition; children/page.tsx section lines 67-107)
    - .planning/phases/08-child-management/08-UI-SPEC.md (primary visual anchor = child card list line 28; empty-state copy lines 98-99)
  </read_first>
  <action>
    Create `src/app/family/[familyId]/children/page.tsx` as an async Server Component (default export `ChildrenPage`) — new file-based route `/family/[familyId]/children` following the same pattern as `/family/[familyId]/tasks/page.tsx` (D-01). Await `params` (Promise<{ familyId: string }>), call `const session = await auth()` (redirect handled by layout.tsx from Plan 02, but read user fields defensively with `session?.user?.name ?? ''`). Run parallel queries: (a) select `childProfiles` where `eq(childProfiles.familyId, familyId)` with NO `active` filter (deactivated children must render — D-14 toggle target), selecting id/displayName/ageYears/accentColor/active and computing `hasEncryptedPin` from `pinEncrypted` being non-null (map rows to `ChildProfileView[]`, never sending pinEncrypted/pinHash to the client); (b) `families.name` with fallback 'Família'. Pass to the view as `initialChildren` (NOT `children` — reserved prop name), plus familyId, familyName, currentUserName, guardianEmail.
    Create `src/components/parent/children-panel-view.tsx` as `'use client'` (clone ParentPanelView structure). Root state: `children` list (from initialChildren), `editingId: string | 'new' | null` (null=idle, 'new'=add form open), `profileOpen` (page-local — D-04), `confirmTargetId: string | null` (D-14 dialog target), `resetTargetId: string | null` (which child's PIN reset panel is open), `revealedPins: Record<string, string>` (client cache of already-decrypted PINs, D-12). Render: `ParentSidebar` with `familyId` + `activeRoute="children"` + `onOpenProfile`; `ParentTopbar`; a main area whose primary anchor is the ChildCard list (map children to `ChildCard`, empty-state copy "Nenhum filho cadastrado ainda" when list empty) plus a right-side panel showing either `ChildFormPanel` (when editingId==='new'), `ChildPinResetPanel` (when resetTargetId!==null), or an idle placeholder; a page-local `GuardianProfileDrawer`; and `ConfirmDeactivateDialog` gated by `open={confirmTargetId !== null}`.
    Handlers (mirror ParentPanelView optimistic + fire-and-forget): `handleAddChild(data)` awaits `createChild({ ...data, familyId })`, appends the returned real DB row to state (use the server UUID — Pitfall 6), closes the form; `handleResetPin(pin)` calls `resetChildPin(resetTargetId, familyId, pin)`, optimistically marks the child `hasEncryptedPin=true`, closes the panel; `handleToggleReveal(childId)` — if already in revealedPins remove it (hide), else `await revealChildPin(childId, familyId)` and store the result (skip if null); `handleConfirmDeactivate()` optimistically flips the target child's `active`, fires `toggleChildActive(confirmTargetId, familyId, !current.active).catch(console.error)`, clears confirmTargetId (Pitfall 3 — fire-and-forget matches convention). Deactivate/reactivate buttons on ChildCard set `confirmTargetId` (never mutate directly — D-14 requires the dialog).
  </action>
  <verify>
    <automated>docker compose exec -T app npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v node_modules | grep -E "children-panel-view.tsx|children/page.tsx" | grep -c "error TS" | grep -qx 0 && echo INTEGRATION_TS_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/family/[familyId]/children/page.tsx` queries `childProfiles` with `eq(childProfiles.familyId, familyId)` and has NO `eq(...active, true)` filter.
    - page.tsx passes `initialChildren` (not `children`) and never sends `pinEncrypted` or `pinHash` to the view (grep those tokens = 0 in the props mapping).
    - `grep -c "activeRoute=\"children\"" src/components/parent/children-panel-view.tsx` returns 1 (D-05 active state).
    - `grep -c "confirmTargetId" src/components/parent/children-panel-view.tsx` is >= 3 (dialog gated by it — D-14, no direct toggle).
    - `grep -c "createChild\|resetChildPin\|revealChildPin\|toggleChildActive" src/components/parent/children-panel-view.tsx` is >= 4 (all actions wired).
    - `grep -c "GuardianProfileDrawer" src/components/parent/children-panel-view.tsx` returns 1 (page-local drawer — D-04).
    - `tsc --noEmit` clean for both files.
  </acceptance_criteria>
  <done>/children lists children (deactivated included), add/reset/reveal/deactivate wired via Server Actions with optimistic UI, drawer page-local, deactivate gated by the dialog; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: ChildrenPanelView integration component test</name>
  <read_first>
    - tests/unit/parent-panel.test.tsx (integration-style component test conventions: mocking Server Actions, rendering the view with initial props, screen queries, waitFor for async state)
    - src/components/parent/children-panel-view.tsx (the component under test — props + handler behavior from Task 1)
    - .planning/phases/08-child-management/08-VALIDATION.md (Per-Task Verification Map — behaviors to cover: add-child submit, PIN dual-write path via UI, deactivate requires dialog)
  </read_first>
  <action>
    Create `tests/unit/children-panel.test.tsx` (jsdom). Mock `@/app/actions/children` (createChild returns a fake row with a real-looking UUID; revealChildPin returns '4321'; resetChildPin/toggleChildActive resolve void) and mock `next/navigation` useRouter. Render `<ChildrenPanelView familyId="fam-1" familyName="Família Teste" currentUserName="João" guardianEmail="j@x.com" initialChildren={[{ id:'c1', displayName:'Ana', ageYears:8, accentColor:'#3E6B4F', active:true, hasEncryptedPin:true }, { id:'c2', displayName:'Bia', ageYears:6, accentColor:'#B14A2E', active:true, hasEncryptedPin:false }]} />`. Assert: (1) both "Ana" and "Bia" cards render; (2) Bia's "Mostrar" button is disabled (hasEncryptedPin=false) and shows "PIN ainda não definido"; (3) clicking Ana's "Mostrar" calls revealChildPin and (waitFor) shows '4321'; (4) clicking Ana's "Desativar" opens ConfirmDeactivateDialog (title "Desativar Ana?") and does NOT call toggleChildActive until the dialog's confirm is clicked; (5) confirming the dialog calls toggleChildActive('c1','fam-1',false).
  </action>
  <verify>
    <automated>docker compose exec -T app npm test -- tests/unit/children-panel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/unit/children-panel.test.tsx` passes all five cases.
    - Test asserts `toggleChildActive` is NOT called before dialog confirm (D-14 invariant proven).
    - Test asserts the disabled Mostrar + "PIN ainda não definido" for the NULL-encrypted child (Pitfall 6).
  </acceptance_criteria>
  <done>Integration test proves list render, NULL-safe reveal gating, reveal flow, and dialog-gated deactivation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full /children module: sidebar navigation to /children, child list, add-child side panel (name/age/color), 4-digit PIN reset via keypad, PIN show/hide, and deactivate/reactivate with a confirmation dialog — plus the /tasks layout refactor (shared auth gate) which must look and behave identically to before.
  </what-built>
  <how-to-verify>
    Preconditions: ensure PIN_ENCRYPTION_KEY is set in the app container env and the app is running via `docker compose up` (kreds-app-1 on port 3000). Log in as a guardian and open a family, giving you a familyId in the URL.
    1. Visit http://localhost:3000/family/{familyId}/tasks — confirm sidebar/topbar/task list render exactly as before the refactor (no visual regression); the "Tarefas" icon is active (green bg #E7EFE8, stroke #3E6B4F).
    2. Click the "Crianças" (person) sidebar icon — confirm it navigates to /family/{familyId}/children and that icon is now the active one.
    3. Confirm the existing child (e.g. "Ana") appears as a card with an initial+color avatar and a masked PIN ••••.
    4. Click "Adicionar filho" flow: enter a name, age, pick a color via the native color picker, submit — the new child card appears immediately without a full page reload.
    5. On the new child, click "Redefinir PIN", enter a 4-digit PIN on the keypad — panel closes; then click "Mostrar" and confirm the PIN you just set is revealed; click "Ocultar" to hide it.
    6. On the pre-existing "Ana" (if her pinEncrypted is still NULL), confirm "Mostrar" is disabled and shows "PIN ainda não definido — use Redefinir PIN"; reset her PIN, then confirm "Mostrar" now works.
    7. Click "Desativar" on a child — confirm the confirmation dialog appears ("Desativar {nome}?"); click "Cancelar" and confirm nothing changes; click "Desativar" again and confirm — the child card shows the deactivated state and the button now reads "Reativar".
    8. Open the guardian profile drawer on /children, then navigate to /tasks — confirm the drawer is closed on /tasks (no cross-page state leak — D-04).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/behavioral issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | guardian actions from the /children view |
| server render → client props | SSR child list must not carry PIN material |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Information Disclosure | pinEncrypted/pinHash serialized to the client via SSR props | mitigate | page.tsx maps rows to ChildProfileView (only hasEncryptedPin boolean); plaintext arrives ONLY via explicit revealChildPin action; acceptance grep asserts no pin fields in props |
| T-08-15 | Elevation of Privilege | direct deactivate without confirmation | mitigate | ChildCard deactivate sets confirmTargetId; mutation only fires on dialog confirm (D-14); test asserts toggleChildActive not called pre-confirm |
| T-08-07 | Information Disclosure | drawer state leaking /children↔/tasks | mitigate | GuardianProfileDrawer + profileOpen declared inside ChildrenPanelView (page-local, D-04); checkpoint step 8 verifies |
| T-08-SC | Tampering | npm installs | accept | this plan installs no packages |
</threat_model>

<verification>
- `docker compose exec -T app npm test` (full unit+integration suite) green — includes all Phase 8 tests.
- `docker compose exec -T app npx tsc --noEmit` no new errors.
- `docker compose exec -T app npm run build` compiles.
- Human checkpoint approved (list/add/PIN/deactivate flow + /tasks parity + drawer isolation).
</verification>

<success_criteria>
- All four ROADMAP Phase 8 success criteria demonstrably met: (1) person icon opens child list; (2) add-child form; (3) set/reset 4-digit PIN; (4) deactivate/reactivate.
- Full suite green; drawer state page-local; /tasks unchanged.
- Human checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/08-child-management/08-05-SUMMARY.md` when done.
</output>
</content>
</invoke>
