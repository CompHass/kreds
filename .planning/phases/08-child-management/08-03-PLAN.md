---
phase: 08-child-management
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/types/child.ts
  - src/app/actions/children.ts
  - src/app/api/family/[familyId]/children/route.ts
  - src/app/api/family/[familyId]/children/[childId]/route.ts
  - tests/integration/child-pin-reset.test.ts
autonomous: true
requirements: [D-06, D-07, D-08, D-10, D-12, D-13, D-14, D-15]
must_haves:
  truths:
    - "createChild inserts a child with avatarPreset='initial' and returns the real DB row"
    - "resetChildPin writes pinHash (bcrypt) and pinEncrypted (AES-GCM) together in one UPDATE (D-13)"
    - "revealChildPin decrypts server-side and returns null when pinEncrypted IS NULL (Pitfall 6)"
    - "toggleChildActive flips active + sets/clears deactivatedAt, scoped by familyId (D-15)"
    - "every children Server Action and Route Handler is auth()-guarded and familyId-scoped"
  artifacts:
    - path: "src/types/child.ts"
      provides: "ChildProfileView type + CreateChildSchema / ResetPinSchema Zod schemas"
      exports: ["CreateChildSchema", "ResetPinSchema", "ChildProfileView"]
    - path: "src/app/actions/children.ts"
      provides: "createChild, resetChildPin, revealChildPin, toggleChildActive Server Actions"
      exports: ["createChild", "resetChildPin", "revealChildPin", "toggleChildActive"]
    - path: "src/app/api/family/[familyId]/children/route.ts"
      provides: "GET (list) / POST (create) Route Handlers"
      exports: ["GET", "POST"]
    - path: "src/app/api/family/[familyId]/children/[childId]/route.ts"
      provides: "PATCH Route Handler (reset-pin / toggle-active)"
      exports: ["PATCH"]
  key_links:
    - from: "src/app/actions/children.ts"
      to: "src/lib/crypto/pin-cipher.ts"
      via: "encryptPin/decryptPin import"
      pattern: "pin-cipher"
    - from: "src/app/actions/children.ts"
      to: "src/lib/families/child-pin.ts"
      via: "hashPin/validatePinFormat import"
      pattern: "child-pin"
    - from: "src/app/actions/children.ts"
      to: "childProfiles familyId scope"
      via: "and(eq(id), eq(familyId)) on every mutation"
      pattern: "familyId"
---

<objective>
Build the child-management backend: the `ChildProfileView` type + Zod schemas in `src/types/child.ts`, the four Server Actions (`createChild`, `resetChildPin`, `revealChildPin`, `toggleChildActive`) in `src/app/actions/children.ts`, and the Route Handlers under `src/app/api/family/[familyId]/children/`. Every action/handler is auth-guarded and familyId-scoped. `resetChildPin` dual-writes bcrypt `pinHash` + AES-GCM `pinEncrypted` (D-13); `revealChildPin` decrypts server-side and returns null for pre-existing NULL-encrypted rows (Pitfall 6).

Purpose: This is the contract layer consumed by the Plan 05 `ChildrenPanelView`. Defining these signatures and Zod schemas first lets Plan 04 UI components and Plan 05 integration build against fixed interfaces without a scavenger hunt.
Output: types, actions, route handlers, and the Wave 0 PIN-reset integration test.
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-child-management/08-CONTEXT.md
@.planning/phases/08-child-management/08-RESEARCH.md
@.planning/phases/08-child-management/08-PATTERNS.md
@.planning/phases/08-child-management/08-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols and files this phase creates (exclude from drift/source-grounding checks):
- `src/types/child.ts` — `ChildProfileView` interface, `CreateChildSchema`, `ResetPinSchema` (Zod)
- `src/app/actions/children.ts` — `createChild`, `resetChildPin`, `revealChildPin`, `toggleChildActive`
- `src/app/api/family/[familyId]/children/route.ts` — `GET`, `POST`
- `src/app/api/family/[familyId]/children/[childId]/route.ts` — `PATCH`
- `tests/integration/child-pin-reset.test.ts` — new integration test
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Child types + Zod schemas (contract for UI + actions + handlers)</name>
  <read_first>
    - src/types/task.ts (the single-source-of-truth pattern to mirror: ParentTask interface + CreateTaskSchema/UpdateTaskSchema Zod objects used by seed, route handlers, and actions)
    - src/lib/db/schema/index.ts (childProfiles columns after Plan 01: id, familyId, displayName, ageYears, avatarPreset, accentColor, active, deactivatedAt, pinHash, pinEncrypted — the view type must expose only what ChildCard needs)
    - .planning/phases/08-child-management/08-RESEARCH.md (Pattern 4 CreateChildSchema lines 328-332; V5 Input Validation security row line 544)
    - .planning/phases/08-child-management/08-UI-SPEC.md (Copywriting field labels line 109; ageYears bounds)
  </read_first>
  <action>
    Create `src/types/child.ts` (mirror the structure of `src/types/task.ts`). Export:
    - `interface ChildProfileView { id: string; displayName: string; ageYears: number; accentColor: string; active: boolean; hasEncryptedPin: boolean }` — the server-fetched, client-safe shape for ChildCard. `hasEncryptedPin` is derived server-side from `pinEncrypted !== null` (Pitfall 6 — never send the ciphertext or plaintext to the client at fetch time).
    - `const CreateChildSchema = z.object({ displayName: z.string().min(1, 'Nome obrigatório'), ageYears: z.number().int().min(1).max(18), accentColor: z.string().regex(/^#[0-9a-fA-F]{6}$/, 'Cor inválida') })` (D-06/D-07 — no avatarPreset field; accentColor is a 7-char hex).
    - `const ResetPinSchema = z.object({ pin: z.string().regex(/^\d{4}$/, 'PIN deve ter 4 dígitos') })` (D-10 — exactly 4 digits for this phase, distinct from the login validatePinFormat which allows 4-6).
    Do NOT include avatarPreset in CreateChildSchema (D-08 — it is a fixed server-injected value).
  </action>
  <verify>
    <automated>docker compose exec -T app npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v node_modules | grep -c "src/types/child.ts.*error TS" | grep -qx 0 && echo CHILD_TYPES_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface ChildProfileView\|export const CreateChildSchema\|export const ResetPinSchema" src/types/child.ts` returns 3.
    - `grep -c "avatarPreset" src/types/child.ts` returns 0 (D-08 — not a form field).
    - `grep -c "hasEncryptedPin" src/types/child.ts` returns 1.
    - CreateChildSchema accentColor uses the regex `/^#[0-9a-fA-F]{6}$/`; ResetPinSchema pin uses `/^\d{4}$/`.
    - `tsc --noEmit` reports no error in src/types/child.ts.
  </acceptance_criteria>
  <done>child.ts exports the view type + two Zod schemas; no avatarPreset field; 4-digit PIN schema distinct from login format.</done>
</task>

<task type="auto">
  <name>Task 2: children Server Actions (create / resetPin / revealPin / toggleActive)</name>
  <read_first>
    - src/app/actions/tasks.ts (full file — the exact Server Action conventions: 'use server', auth() guard throwing 'Unauthorized', `.returning()` for create, familyId-scoped `.where(and(eq(id), eq(familyId)))`, revalidatePath with concrete path, no try/catch inside actions)
    - src/lib/families/child-pin.ts (hashPin, validatePinFormat — bcrypt path, UNCHANGED per D-11)
    - src/lib/crypto/pin-cipher.ts (encryptPin, decryptPin from Plan 01)
    - src/types/child.ts (CreateChildSchema, ResetPinSchema, ChildProfileView from Task 1)
    - auth.ts location (repo root — from src/app/actions/children.ts use `../../../auth` = 3 `../`, same as tasks.ts line 12)
    - .planning/phases/08-child-management/08-PATTERNS.md (children.ts section lines 111-206 — exact action bodies for create/resetPin/revealPin/toggleActive)
  </read_first>
  <action>
    Create `src/app/actions/children.ts` starting with `'use server'`, importing `revalidatePath` from `next/cache`, `auth` from `../../../auth`, `db` from `@/lib/db`, `childProfiles` from `@/lib/db/schema`, `eq, and` from `drizzle-orm`, `hashPin, validatePinFormat` from `@/lib/families/child-pin`, `encryptPin, decryptPin` from `@/lib/crypto/pin-cipher`, and `CreateChildSchema` from `@/types/child`. Export four actions (all begin with `const session = await auth(); if (!session) throw new Error('Unauthorized')`):
    - `createChild(data: z.infer<typeof CreateChildSchema> & { familyId: string })` → `CreateChildSchema.parse(data)`, insert with `avatarPreset: 'initial'` fixed (D-08), `.returning()` the row; `revalidatePath(\`/family/${data.familyId}/children\`)`; return the created row.
    - `resetChildPin(childId: string, familyId: string, pin: string)` → `if (!/^\d{4}$/.test(pin)) throw new Error('Invalid PIN format')`; compute `const pinHash = await hashPin(pin)` and `const pinEncrypted = encryptPin(pin)`; a single `db.update(childProfiles).set({ pinHash, pinEncrypted, updatedAt: new Date() }).where(and(eq(childProfiles.id, childId), eq(childProfiles.familyId, familyId)))` (D-13 — both written together); revalidatePath.
    - `revealChildPin(childId: string, familyId: string): Promise<string | null>` → select `pinEncrypted` scoped by `and(eq(id), eq(familyId))`, limit 1; `if (!child?.pinEncrypted) return null` (Pitfall 6); else `return decryptPin(child.pinEncrypted)`. No revalidatePath (read-only).
    - `toggleChildActive(childId: string, familyId: string, active: boolean): Promise<void>` → update `{ active, deactivatedAt: active ? null : new Date(), updatedAt: new Date() }` scoped by `and(eq(id), eq(familyId))` (D-14/D-15 — no session revocation, no child-guard change); revalidatePath.
    Follow tasks.ts: no try/catch inside actions (errors propagate to the client's fire-and-forget `.catch()`). NEVER read pinEncrypted for authentication (D-11) and NEVER trust a client-supplied familyId without pairing it with childId in the `.where`.
  </action>
  <verify>
    <automated>docker compose exec -T app npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v node_modules | grep -c "children.ts.*error TS" | grep -qx 0 && echo ACTIONS_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function createChild\|export async function resetChildPin\|export async function revealChildPin\|export async function toggleChildActive" src/app/actions/children.ts` returns 4.
    - `grep -c "await auth()" src/app/actions/children.ts` is >= 4 (every action guarded).
    - `grep -c "and(eq(childProfiles.id" src/app/actions/children.ts` is >= 3 (create excepted — it inserts).
    - resetChildPin sets BOTH `pinHash` and `pinEncrypted` in one `.set({...})` (grep line contains both identifiers).
    - `grep -c "avatarPreset: 'initial'" src/app/actions/children.ts` returns 1 (D-08).
    - revealChildPin returns null on falsy pinEncrypted (grep for `return null`).
    - No occurrence of `pinEncrypted` inside any auth/verify code path (this file never authenticates).
  </acceptance_criteria>
  <done>Four familyId-scoped, auth-guarded Server Actions; resetChildPin dual-writes; revealChildPin null-safe; toggleChildActive soft-deactivates.</done>
</task>

<task type="auto">
  <name>Task 3: children Route Handlers + Wave 0 PIN-reset integration test</name>
  <read_first>
    - src/app/api/family/[familyId]/tasks/route.ts (full file — exact Route Handler conventions: await params, auth() → 401, req.json() try/catch → 400, safeParse → 400 with flatten(), familyId injected from URL never body, 201 on create; import auth from `../../../../../../auth` = 6 `../`)
    - src/types/child.ts (CreateChildSchema, ResetPinSchema)
    - src/app/actions/children.ts (resetChildPin, toggleChildActive — the PATCH handler can call these or replicate their DB writes; prefer calling the domain writes directly with familyId from URL params)
    - tests/integration/family-child-profiles.test.ts (Testcontainers + drizzle migrate pattern to mirror for the new test: PostgreSqlContainer('postgres:18-alpine'), migrate from './drizzle', beforeAll/afterAll)
    - .planning/phases/08-child-management/08-PATTERNS.md (route handler section lines 210-270; child-pin-reset test requirements)
    - .planning/phases/08-child-management/08-VALIDATION.md (child-pin-reset.test.ts Wave 0 requirement line 60 — verify pinHash + pinEncrypted written atomically AND pre-existing NULL rows handled)
  </read_first>
  <action>
    Create `src/app/api/family/[familyId]/children/route.ts` with `GET` (auth() → 401; select all childProfiles where `eq(familyId)` — NO active filter, deactivated children must appear per D-14 toggle target; return JSON) and `POST` (auth() → 401; parse body with try/catch → 400; `CreateChildSchema.safeParse` → 400 with `result.error.flatten()`; insert with `{ ...result.data, familyId, avatarPreset: 'initial' }` injecting familyId from URL params never body; return created row with status 201). Import `auth` from `../../../../../../auth`.
    Create `src/app/api/family/[familyId]/children/[childId]/route.ts` with a `PATCH` handler: await `params` (Promise<{ familyId: string; childId: string }>), auth() → 401, parse body → 400. The body carries an `action` discriminator: `{ action: 'reset-pin', pin }` (validate with `ResetPinSchema`, then dual-write pinHash+pinEncrypted scoped by `and(eq(id), eq(familyId))`) or `{ action: 'toggle-active', active }` (update active + deactivatedAt scoped by familyId). Reject unknown action with 400. familyId + childId both come from URL params (never body) for T-08-05 isolation. Return 200 with the updated row (reset-pin returns row WITHOUT pin fields).
    Create `tests/integration/child-pin-reset.test.ts` (mirror family-child-profiles.test.ts infra: PostgreSqlContainer postgres:18-alpine, drizzle migrate from './drizzle', beforeAll 60s). Set `process.env.PIN_ENCRYPTION_KEY` to a valid base64 32-byte value (Buffer.alloc(32,7)) before importing the actions/cipher. Tests: (1) after resetChildPin, the row's pinHash is a bcrypt hash (starts with `$2`) AND pinEncrypted is non-null and decrypts back to the PIN via decryptPin; (2) a child inserted with pinEncrypted=NULL causes revealChildPin to return null (Pitfall 6); (3) resetChildPin on a childId belonging to another familyId does NOT modify the row (familyId isolation — T-08-05).
  </action>
  <verify>
    <automated>docker compose exec -T app npm test -- tests/integration/child-pin-reset.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/family/[familyId]/children/route.ts` exports `GET` and `POST`; GET has NO `eq(...active, true)` filter (deactivated children listed).
    - `src/app/api/family/[familyId]/children/[childId]/route.ts` exports `PATCH` handling `reset-pin` and `toggle-active` actions, rejecting unknown actions with 400.
    - Both handlers inject familyId (and childId) from URL params, never from the request body (grep confirms `familyId` comes from `await params`).
    - `npm test -- tests/integration/child-pin-reset.test.ts` passes all three cases (dual-write, NULL-safe reveal, cross-family isolation).
  </acceptance_criteria>
  <done>GET/POST + PATCH handlers live and familyId-isolated; PIN-reset integration test green proving dual-write, null-safe reveal, and cross-family isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | guardian-submitted child data + PIN reset requests |
| client → Route Handler | JSON body for list/create/patch under /api/family/[familyId]/children |
| server → DB | dual-write of bcrypt hash + AES-GCM ciphertext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Elevation of Privilege | cross-family child access (reveal/reset/toggle Family B's child) | mitigate | every mutation/read scoped by `and(eq(childProfiles.id, childId), eq(childProfiles.familyId, familyId))`; familyId from session/URL never body (mirrors T-06-15); integration test asserts cross-family reset is a no-op |
| T-08-08 | Spoofing | unauthenticated action/handler call | mitigate | `await auth()` guard on every Server Action (throw Unauthorized) and Route Handler (401) |
| T-08-09 | Information Disclosure | plaintext PIN leaking to client on list fetch | mitigate | ChildProfileView exposes only hasEncryptedPin boolean; plaintext returned ONLY by explicit revealChildPin call |
| T-08-10 | Tampering | client-supplied familyId in request body | mitigate | familyId injected from URL params in Route Handlers; body familyId ignored |
| T-08-11 | Repudiation/auth-confusion | pinEncrypted used for authentication | mitigate | children.ts never imports child-guard/verifyPin; auth path stays bcrypt-only (D-11) |
| T-08-SC | Tampering | npm installs | accept | this plan installs no packages |
</threat_model>

<verification>
- `docker compose exec -T app npm test -- tests/integration/child-pin-reset.test.ts` green.
- `docker compose exec -T app npx tsc --noEmit` reports no new errors.
- `grep` acceptance criteria confirm familyId isolation and dual-write.
</verification>

<success_criteria>
- child.ts contract, four Server Actions, and two Route Handler files exist.
- resetChildPin dual-writes bcrypt + AES-GCM; revealChildPin null-safe; toggleChildActive soft-deactivates.
- All auth-guarded and familyId-scoped; PIN-reset integration test proves it.
</success_criteria>

<output>
Create `.planning/phases/08-child-management/08-03-SUMMARY.md` when done.
</output>
