---
phase: 08-child-management
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - package.json
  - src/components/parent/child-form-panel.tsx
  - src/components/parent/child-card.tsx
  - src/components/parent/child-pin-reset-panel.tsx
  - src/components/parent/confirm-deactivate-dialog.tsx
  - tests/unit/child-form-panel.test.tsx
  - tests/unit/confirm-deactivate-dialog.test.tsx
autonomous: true
requirements: [D-06, D-07, D-08, D-09, D-10, D-12, D-14]
must_haves:
  truths:
    - "ChildFormPanel validates displayName/ageYears/accentColor via RHF+Zod and calls onSave with valid data"
    - "ChildCard renders an initial+accentColor avatar, masked PIN, and reveal/reset/deactivate controls"
    - "ChildCard disables the Mostrar button and shows 'PIN ainda não definido' when hasEncryptedPin is false"
    - "ConfirmDeactivateDialog (Radix AlertDialog) opens per open prop, calls onConfirm on Action, closes on Cancel/ESC"
    - "ChildPinResetPanel reuses the 3×4 NumericKeypad and emits a 4-digit PIN"
  artifacts:
    - path: "src/components/parent/child-form-panel.tsx"
      provides: "Add-child side panel (RHF+Zod), cloned from TaskFormPanel shell"
      exports: ["ChildFormPanel", "ChildFormData", "EMPTY_CHILD_FORM"]
    - path: "src/components/parent/child-card.tsx"
      provides: "Frame C list-row: avatar, masked PIN, reveal/reset/deactivate"
      exports: ["ChildCard"]
    - path: "src/components/parent/confirm-deactivate-dialog.tsx"
      provides: "First Radix AlertDialog in project (D-14)"
      exports: ["ConfirmDeactivateDialog"]
    - path: "src/components/parent/child-pin-reset-panel.tsx"
      provides: "Non-fullscreen keypad panel emitting a 4-digit PIN (D-10)"
      exports: ["ChildPinResetPanel"]
  key_links:
    - from: "src/components/parent/confirm-deactivate-dialog.tsx"
      to: "radix-ui AlertDialog"
      via: "import { AlertDialog } from 'radix-ui'"
      pattern: "AlertDialog"
    - from: "src/components/parent/child-pin-reset-panel.tsx"
      to: "src/components/auth/numeric-keypad.tsx"
      via: "NumericKeypad reuse"
      pattern: "NumericKeypad"
---

<objective>
Build the four new Phase 8 UI components against the Plan 03 contract: `ChildFormPanel` (add-child side panel using React Hook Form + Zod, cloned from `TaskFormPanel`'s shell), `ChildCard` (Frame C list-row with initial avatar, masked PIN, reveal/reset/deactivate controls), `ChildPinResetPanel` (reuses the existing `NumericKeypad` non-fullscreen, emits a 4-digit PIN), and `ConfirmDeactivateDialog` (the project's first Radix `AlertDialog`, D-14). Install the `radix-ui` unified package.

Purpose: These are the presentational pieces Plan 05 wires into `ChildrenPanelView`. Each is a leaf component with props defined by the Plan 03 types, so Plan 05 assembles them without re-deriving shapes.
Output: `radix-ui` installed, four components, and two component tests.
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-child-management/08-CONTEXT.md
@.planning/phases/08-child-management/08-RESEARCH.md
@.planning/phases/08-child-management/08-PATTERNS.md
@.planning/phases/08-child-management/08-UI-SPEC.md
@.planning/phases/08-child-management/08-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols and files this phase creates (exclude from drift/source-grounding checks):
- `src/components/parent/child-form-panel.tsx` — `ChildFormPanel`, `interface ChildFormData`, `EMPTY_CHILD_FORM`
- `src/components/parent/child-card.tsx` — `ChildCard`
- `src/components/parent/child-pin-reset-panel.tsx` — `ChildPinResetPanel`
- `src/components/parent/confirm-deactivate-dialog.tsx` — `ConfirmDeactivateDialog`
- `package.json` — new dependency `radix-ui`
- `tests/unit/child-form-panel.test.tsx`, `tests/unit/confirm-deactivate-dialog.test.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install radix-ui + ConfirmDeactivateDialog (first modal) + dialog test</name>
  <read_first>
    - .planning/phases/08-child-management/08-RESEARCH.md (Pattern 3 lines 252-316 — complete AlertDialog component; Pitfall 3 lines 377-381 — Action fires onConfirm then closes, consistent with fire-and-forget; Package Legitimacy Audit lines 63-73 — radix-ui verified OK/Approved, 9.05M weekly, no postinstall)
    - .planning/phases/08-child-management/08-PATTERNS.md (confirm-deactivate-dialog section lines 482-519 — same component, ready to use)
    - .planning/phases/08-child-management/08-UI-SPEC.md (Color line 78 — destructive #B14A2E; overlay rgba(39,55,44,.25); content radius 16 padding 24 width 360; Copywriting dialog titles/bodies lines 102-107)
    - src/components/parent/guardian-profile-drawer.tsx (backdrop rgba(39,55,44,.25) reference for overlay match)
  </read_first>
  <action>
    Run `docker compose exec app npm install radix-ui` (unified package v1.6.1 — VERIFIED OK in the Package Legitimacy Audit; 9.05M weekly downloads, no postinstall script, so no blocking-human legitimacy checkpoint is required per the research audit).
    Create `src/components/parent/confirm-deactivate-dialog.tsx` as `'use client'` importing `{ AlertDialog } from 'radix-ui'`. Implement exactly 08-RESEARCH.md Pattern 3 with props `{ open: boolean; childName: string; willDeactivate: boolean; onConfirm: () => void; onOpenChange: (open: boolean) => void }`. Controlled `AlertDialog.Root open={open} onOpenChange={onOpenChange}`, Portal + Overlay (rgba(39,55,44,.25), zIndex 60) + Content (white, borderRadius 16, padding 24, width 360, centered via transform translate(-50%,-50%), zIndex 61). Title/Description text driven by `willDeactivate` (UI-SPEC copy: "Desativar {childName}?" / "{childName} não conseguirá fazer login até ser reativado(a)." vs "Reativar {childName}?" / "{childName} poderá fazer login novamente."). Cancel button ("Cancelar") via `AlertDialog.Cancel asChild`; Action button via `AlertDialog.Action asChild` with `onClick={onConfirm}`, label "Desativar" (color #B14A2E when willDeactivate) or "Reativar" (color #3E6B4F when reactivating).
    Create `tests/unit/confirm-deactivate-dialog.test.tsx` (jsdom): (1) with `open={false}` the title is not in the document; (2) with `open={true} willDeactivate={true} childName="Ana"` the title "Desativar Ana?" renders; (3) clicking the "Desativar" button calls onConfirm; (4) clicking "Cancelar" does NOT call onConfirm; (5) pressing ESC (fireEvent.keyDown Escape) triggers onOpenChange(false).
  </action>
  <verify>
    <automated>docker compose exec -T app npm test -- tests/unit/confirm-deactivate-dialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\"radix-ui\"" package.json` returns 1 (dependency added).
    - `grep -c "import { AlertDialog } from 'radix-ui'" src/components/parent/confirm-deactivate-dialog.tsx` returns 1.
    - `grep -c "willDeactivate" src/components/parent/confirm-deactivate-dialog.tsx` is >= 3 (drives title, body, button label/color).
    - `grep -c "#B14A2E" src/components/parent/confirm-deactivate-dialog.tsx` returns 1 (destructive color for Desativar).
    - `npm test -- tests/unit/confirm-deactivate-dialog.test.tsx` passes all five cases.
  </acceptance_criteria>
  <done>radix-ui installed; ConfirmDeactivateDialog renders per open prop, drives copy/color by willDeactivate, confirm/cancel/ESC behave; dialog test green.</done>
</task>

<task type="auto">
  <name>Task 2: ChildFormPanel (RHF + Zod, cloned TaskFormPanel shell) + form test</name>
  <read_first>
    - src/components/parent/task-form-panel.tsx (full file — clone containerStyle lines 74-85 verbatim: width 336, padding 20, borderRadius 20, boxShadow '0 16px 36px -26px rgba(40,55,45,.5)', minHeight 400; header + X cancel button lines 138-180; CTA gradient button lines 293-314; DO NOT copy the DeleteButton block lines 316-361 — no delete in this form)
    - src/types/child.ts (CreateChildSchema — use directly with zodResolver; ChildFormData mirrors its fields)
    - .planning/phases/08-child-management/08-RESEARCH.md (Pattern 4 lines 318-345 — RHF + zodResolver + <input type="color"> register pattern)
    - .planning/phases/08-child-management/08-PATTERNS.md (child-form-panel section lines 390-441 — ChildFormData/EMPTY_CHILD_FORM contract, RHF deviation, no delete button)
    - .planning/phases/08-child-management/08-UI-SPEC.md (Typography label 13px/600 lines 61,65,109; field labels "Nome"/"Idade"/"Cor"; CTA "Adicionar filho" line 92; 20px padding exception line 47)
  </read_first>
  <action>
    Create `src/components/parent/child-form-panel.tsx` as `'use client'`. Export `interface ChildFormData { displayName: string; ageYears: number; accentColor: string }` and `const EMPTY_CHILD_FORM: ChildFormData = { displayName: '', ageYears: 6, accentColor: '#3E6B4F' }`. Export `ChildFormPanel` with props `{ mode: 'idle' | 'create'; onSave: (data: ChildFormData) => void; onCancel: () => void }` (no edit mode — D-06 specifies no update-name flow; no familyChildren prop). Clone the TaskFormPanel container shell verbatim (336px width, 20px padding — UI-SPEC exception, do NOT round to 16/24; borderRadius 20; the same boxShadow; minHeight 400). Idle mode shows the centered placeholder (header "Adicionar filho", body per UI-SPEC empty-state guidance). Create mode uses `useForm({ resolver: zodResolver(CreateChildSchema), defaultValues: EMPTY_CHILD_FORM })`: a text input `register('displayName')` labelled "Nome" (13px/600 label style matching TaskFormPanel's "Título"), a number input `register('ageYears', { valueAsNumber: true })` labelled "Idade", and `<input type="color" {...register('accentColor')} />` labelled "Cor". Render field errors from `formState.errors`. The CTA button ("Adicionar filho", green gradient matching TaskFormPanel CTA) is `type="submit"` wired via `handleSubmit(onSave)`. Include the X cancel button (calls onCancel) matching TaskFormPanel's header X. Do NOT include a delete button.
    Create `tests/unit/child-form-panel.test.tsx` (jsdom): (1) submitting with empty displayName shows the "Nome obrigatório" error and does NOT call onSave; (2) submitting with valid displayName/ageYears/accentColor calls onSave with those values; (3) ageYears above 18 shows a validation error (bounds); (4) idle mode renders the "Adicionar filho" placeholder header without form inputs.
  </action>
  <verify>
    <automated>docker compose exec -T app npm test -- tests/unit/child-form-panel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "zodResolver\|useForm" src/components/parent/child-form-panel.tsx` is >= 2 (RHF wired).
    - `grep -c "type=\"color\"" src/components/parent/child-form-panel.tsx` returns 1 (D-07 native color picker).
    - `grep -c "Excluir\|onDelete\|mode === 'edit'" src/components/parent/child-form-panel.tsx` returns 0 (no delete/edit — D-06).
    - `grep -c "padding: 20" src/components/parent/child-form-panel.tsx` returns 1 (cloned shell padding — UI-SPEC exception preserved).
    - `grep -c "export interface ChildFormData\|export const EMPTY_CHILD_FORM\|export function ChildFormPanel" src/components/parent/child-form-panel.tsx` returns 3.
    - `npm test -- tests/unit/child-form-panel.test.tsx` passes all four cases.
  </acceptance_criteria>
  <done>ChildFormPanel validates via RHF+Zod, uses native color input, has no delete/edit, clones the 336/20px shell; form test green.</done>
</task>

<task type="auto">
  <name>Task 3: ChildCard (Frame C list-row) + ChildPinResetPanel (keypad reuse)</name>
  <read_first>
    - src/components/parent/parent-topbar.tsx (avatar-by-initial + accentColor gradient pattern to reuse for the 52×52px avatar)
    - src/components/auth/numeric-keypad.tsx (full file — NumericKeypad props `{ onDigit: (d: string) => void; onBackspace: () => void }`, 3×4 grid, 64px buttons; reuse as-is, do NOT redesign)
    - src/types/child.ts (ChildProfileView — the row shape ChildCard receives, including hasEncryptedPin)
    - .planning/phases/08-child-management/08-PATTERNS.md (child-card section lines 444-470 — avatar pattern, masked/reveal PIN display, hasEncryptedPin gating; child-pin-reset-panel section lines 474-478)
    - .planning/phases/08-child-management/08-UI-SPEC.md (Component Inventory ChildCard line 139 avatar 52×52, radius 24; ChildPinResetPanel line 140 reuse keypad button styling; Copywriting reveal "Mostrar"/"Ocultar", "Redefinir PIN", "PIN ainda não definido" lines 94-101,113-114)
  </read_first>
  <action>
    Create `src/components/parent/child-card.tsx` as `'use client'`. Export `ChildCard` with props `{ child: ChildProfileView; revealedPin: string | null; onToggleReveal: () => void; onResetPin: () => void; onToggleActive: () => void }`. Render a full-width list-row (borderRadius 24 per Frame C, bg matching an existing token — use `var(--color-kreds-card)` or `#FBFAF5`) containing: a 52×52px circular avatar with `linear-gradient(135deg, ${child.accentColor} 0%, ${child.accentColor}CC 100%)` and the uppercased first initial (D-08); the displayName + ageYears; a PIN region showing `revealedPin ?? '••••'` with a "Mostrar"/"Ocultar" button (label = revealedPin ? 'Ocultar' : 'Mostrar') that is `disabled` when `!child.hasEncryptedPin` and, in that disabled state, shows the muted text "PIN ainda não definido — use Redefinir PIN" (Pitfall 6 / UI-SPEC copy); a "Redefinir PIN" button (calls onResetPin); and a "Desativar"/"Reativar" button (label depends on child.active) calling onToggleActive. Do NOT render "Ver atividade" (deferred to Phase 9). Deactivated children should be visually distinguished (e.g. reduced opacity) but still fully rendered.
    Create `src/components/parent/child-pin-reset-panel.tsx` as `'use client'`, exporting `ChildPinResetPanel` with props `{ childName: string; onSubmit: (pin: string) => void; onCancel: () => void }`. Use `useState('')` for the entered digits; render the existing `NumericKeypad` (import from `@/components/auth/numeric-keypad`) wired so `onDigit` appends a digit (capped at 4) and `onBackspace` removes the last; show 4 masked/filled indicators for progress. When the 4th digit is entered, call `onSubmit(pin)` (D-10 — 4-digit PIN). Include a header ("Redefinir PIN de {childName}") and a cancel affordance calling onCancel. Reuse the panel shell dimensions consistent with TaskFormPanel/ChildFormPanel (336px). Do NOT redesign the keypad buttons.
  </action>
  <verify>
    <automated>docker compose exec -T app npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v node_modules | grep -E "child-card.tsx|child-pin-reset-panel.tsx" | grep -c "error TS" | grep -qx 0 && echo CARD_KEYPAD_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hasEncryptedPin" src/components/parent/child-card.tsx` is >= 1 and used to `disabled` the Mostrar button.
    - `grep -c "PIN ainda não definido" src/components/parent/child-card.tsx` returns 1 (Pitfall 6 copy).
    - `grep -c "Ver atividade" src/components/parent/child-card.tsx` returns 0 (deferred to Phase 9).
    - `grep -c "Redefinir PIN\|Desativar\|Reativar\|Mostrar" src/components/parent/child-card.tsx` is >= 4 (all controls present).
    - `grep -c "NumericKeypad" src/components/parent/child-pin-reset-panel.tsx` is >= 1 (keypad reused, not rebuilt).
    - `tsc --noEmit` reports no error for child-card.tsx or child-pin-reset-panel.tsx.
  </acceptance_criteria>
  <done>ChildCard renders Frame C row with reveal/reset/deactivate and NULL-safe PIN gating; ChildPinResetPanel reuses NumericKeypad and emits a 4-digit PIN; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client input → validation | add-child form fields validated by Zod before submit |
| npm registry → build | radix-ui package install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Tampering | invalid add-child input reaching the server | mitigate | RHF+zodResolver(CreateChildSchema) blocks submit; server re-validates (Plan 03) — defense in depth |
| T-08-13 | Information Disclosure | attempting PIN reveal for a child with no encrypted PIN | mitigate | ChildCard disables Mostrar when hasEncryptedPin is false; shows explanatory copy (Pitfall 6) |
| T-08-SC | Tampering | radix-ui npm install | accept | radix-ui VERIFIED OK in 08-RESEARCH.md Package Legitimacy Audit (9.05M weekly downloads, official radix-ui/primitives repo, no postinstall) — not [ASSUMED]/[SUS], so auto-approvable per research; no blocking-human checkpoint required |
</threat_model>

<verification>
- `docker compose exec -T app npm test -- tests/unit/confirm-deactivate-dialog.test.tsx tests/unit/child-form-panel.test.tsx` green.
- `docker compose exec -T app npx tsc --noEmit` reports no new errors for the four new components.
- `grep "\"radix-ui\"" package.json` confirms install.
</verification>

<success_criteria>
- radix-ui installed; ConfirmDeactivateDialog, ChildFormPanel, ChildCard, ChildPinResetPanel exist.
- Form validates via RHF+Zod; dialog opens/confirms/cancels/ESC; card gates reveal on hasEncryptedPin; keypad reused.
- Two component tests green.
</success_criteria>

<output>
Create `.planning/phases/08-child-management/08-04-SUMMARY.md` when done.
</output>
