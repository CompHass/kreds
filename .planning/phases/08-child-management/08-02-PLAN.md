---
phase: 08-child-management
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/app/family/[familyId]/layout.tsx
  - src/app/family/[familyId]/tasks/page.tsx
  - src/components/parent/parent-sidebar.tsx
  - src/components/parent/parent-panel-view.tsx
  - tests/unit/parent-sidebar.test.tsx
autonomous: true
requirements: [D-02, D-03, D-05]
must_haves:
  truths:
    - "A shared Server Component layout.tsx performs the auth() gate for both /tasks and /children"
    - "The 'Crianças' sidebar icon navigates to /family/[familyId]/children and shows active styling when on that route"
    - "The 'Tarefas' sidebar icon active styling is now conditional on the current route, not hardcoded"
    - "/tasks route renders identically to before the refactor (no behavior change)"
  artifacts:
    - path: "src/app/family/[familyId]/layout.tsx"
      provides: "Shared auth-gate Server Component wrapping /tasks and /children"
      contains: "redirect('/login')"
    - path: "src/components/parent/parent-sidebar.tsx"
      provides: "Sidebar with route-aware nav for Tarefas/Crianças icons"
      contains: "activeRoute"
  key_links:
    - from: "src/components/parent/parent-sidebar.tsx"
      to: "/family/[familyId]/children"
      via: "useRouter().push on Crianças icon click"
      pattern: "children"
    - from: "src/app/family/[familyId]/layout.tsx"
      to: "auth()"
      via: "session gate + redirect"
      pattern: "redirect"
---

<objective>
Extract the shared auth gate into `src/app/family/[familyId]/layout.tsx` (D-02/D-03) and make `ParentSidebar` route-aware so the "Crianças" icon navigates to `/children` and reflects active state (D-05). Refactor `/tasks/page.tsx` to rely on the shared layout for the auth gate without changing any data query, prop shape, or `ParentPanelView` behavior.

Purpose: The new `/children` page (Plan 05) needs the same sidebar + auth gate as `/tasks`; centralizing the auth gate avoids duplicating it, while keeping `ParentSidebar`/`ParentTopbar`/`GuardianProfileDrawer` rendered per-page (D-04) so drawer state never leaks across routes (Pitfall 1).
Output: `layout.tsx`, route-aware `parent-sidebar.tsx`, unchanged-behavior `/tasks`, and the first `parent-sidebar` test.
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-child-management/08-CONTEXT.md
@.planning/phases/08-child-management/08-RESEARCH.md
@.planning/phases/08-child-management/08-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols and files this phase creates (exclude from drift/source-grounding checks):
- `src/app/family/[familyId]/layout.tsx` — `FamilyLayout` default export (Server Component auth gate)
- `src/components/parent/parent-sidebar.tsx` — new props `familyId: string`, `activeRoute: 'tasks' | 'children'`
- `tests/unit/parent-sidebar.test.tsx` — new test file
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Shared layout.tsx auth gate + de-duplicate /tasks page auth</name>
  <read_first>
    - src/app/family/[familyId]/tasks/page.tsx (current auth gate lines 22-24: `const session = await auth(); if (!session) redirect('/login')`; params is a Promise — `await params` at line 20; do NOT change the queries lines 28-48 or the ParentPanelView props lines 64-73)
    - auth.ts (at project ROOT — import path from layout.tsx one level up from tasks/ so use `../../../../auth` = 4 `../`; verify by counting: layout.tsx is at src/app/family/[familyId]/, auth.ts at repo root)
    - .planning/phases/08-child-management/08-PATTERNS.md (layout.tsx section lines 29-55 — resolved ambiguity: layout stays minimal auth-gate only, does NOT instantiate ParentSidebar/ParentTopbar with shared state; Pitfall 1 lines 365-369)
    - .planning/phases/08-child-management/08-RESEARCH.md (Open Question 1 resolution lines 474-477 — "shared" means shared auth check, sidebar rendered per-page)
  </read_first>
  <action>
    Create `src/app/family/[familyId]/layout.tsx` as an async Server Component `FamilyLayout({ children, params }: { children: React.ReactNode; params: Promise<{ familyId: string }> })`. Import `redirect` from `next/navigation` and `auth` from the repo-root `auth.ts` (relative path with four `../` from `src/app/family/[familyId]/`). Await `params`, call `const session = await auth()`, `if (!session) redirect('/login')`, and return `<>{children}</>`. Do NOT render ParentSidebar/ParentTopbar/GuardianProfileDrawer here (D-04 / Pitfall 1 — those stay in each page's client view to avoid cross-route drawer-state leakage).
    Refactor `src/app/family/[familyId]/tasks/page.tsx`: remove its own `const session = await auth(); if (!session) redirect('/login')` gate (now performed by layout.tsx) — BUT the page still needs `session` for `session.user?.name`/`session.user?.email` props. Keep the `const session = await auth()` call for reading user fields, but rely on the layout for the redirect; if `session` could still be null at the props line, guard with `session?.user?.name ?? ''` (already the existing pattern at lines 68-69). Leave the queries (lines 28-48), familyName fallback, mappedTasks mapping, and the entire ParentPanelView invocation (props, prop names including `familyChildren`) byte-for-byte unchanged.
  </action>
  <verify>
    <automated>docker compose exec -T app npm test -- tests/unit/parent-panel.test.tsx && docker compose exec -T app npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "node_modules" | grep -c "error TS" | grep -qx 0 && echo TS_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/family/[familyId]/layout.tsx` exists, default-exports an async `FamilyLayout`, and contains `redirect('/login')`.
    - `grep -c "import { auth }" src/app/family/[familyId]/layout.tsx` returns 1 and the import path resolves to the repo-root auth.ts.
    - `grep -c "ParentSidebar\|ParentTopbar\|GuardianProfileDrawer" src/app/family/[familyId]/layout.tsx` returns 0 (D-04 — no chrome in layout).
    - `/tasks/page.tsx` queries and ParentPanelView props are unchanged (the `familyChildren` prop name and the two parallel-query calls remain).
    - Existing `tests/unit/parent-panel.test.tsx` still passes; `tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>layout.tsx performs the shared auth gate; /tasks relies on it without behavior change; typecheck + parent-panel test green.</done>
</task>

<task type="auto">
  <name>Task 2: Route-aware ParentSidebar (Crianças nav + conditional active) + first sidebar test</name>
  <read_first>
    - src/components/parent/parent-sidebar.tsx (full file; current "Crianças" button lines 109-139 has no onClick and hardcoded stroke #9AA092; "Tarefas" button lines 76-107 is hardcoded active with bg #E7EFE8 + stroke #3E6B4F — this active styling must become conditional)
    - src/components/parent/parent-panel-view.tsx (lines 192-195 — where ParentSidebar is rendered; must pass the new familyId + activeRoute="tasks" props after this change)
    - .planning/phases/08-child-management/08-PATTERNS.md (parent-sidebar modify section lines 274-321 — exact target styling and the new props ParentSidebarProps { guardianInitial, onOpenProfile, familyId, activeRoute })
    - .planning/phases/08-child-management/08-UI-SPEC.md (Component Inventory row for ParentSidebar line 142 — active bg #E7EFE8, stroke #3E6B4F; inactive stroke #9AA092)
    - tests/unit/child-tasks.test.tsx (an existing component test for jsdom conventions: render + screen queries)
  </read_first>
  <action>
    Modify `src/components/parent/parent-sidebar.tsx`: add `'use client'` is already present; import `useRouter` from `next/navigation`. Extend `ParentSidebarProps` with `familyId: string` and `activeRoute: 'tasks' | 'children'`. Inside the component, `const router = useRouter()`.
    For the "Tarefas" button (lines 76-107): make `background` conditional — `activeRoute === 'tasks' ? '#E7EFE8' : 'none'` — and the svg `stroke` conditional — `activeRoute === 'tasks' ? '#3E6B4F' : '#9AA092'`; add `onClick={() => router.push(\`/family/${familyId}/tasks\`)}`.
    For the "Crianças" button (lines 109-139): add `onClick={() => router.push(\`/family/${familyId}/children\`)}`; make `background` conditional `activeRoute === 'children' ? '#E7EFE8' : 'none'` and svg `stroke` `activeRoute === 'children' ? '#3E6B4F' : '#9AA092'`. Keep `aria-label="Crianças"` exactly (UI-SPEC — do not change).
    Update the ParentSidebar render call in `src/components/parent/parent-panel-view.tsx` (lines 192-195) to pass `familyId={_familyId}` and `activeRoute="tasks"`.
    Create `tests/unit/parent-sidebar.test.tsx` (jsdom) that mocks `next/navigation`'s `useRouter` (push spy), renders `<ParentSidebar guardianInitial="A" onOpenProfile={vi.fn()} familyId="fam-1" activeRoute="children" />`, and asserts: (1) clicking the button with `aria-label="Crianças"` calls `push('/family/fam-1/children')`; (2) with `activeRoute="children"` the Crianças svg stroke is `#3E6B4F`; (3) with `activeRoute="tasks"` clicking `aria-label="Tarefas"` calls `push('/family/fam-1/tasks')`.
  </action>
  <verify>
    <automated>docker compose exec -T app npm test -- tests/unit/parent-sidebar.test.tsx tests/unit/parent-panel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "activeRoute" src/components/parent/parent-sidebar.tsx` is >= 4 (prop declared + used on both buttons' bg and stroke).
    - `grep -c "router.push" src/components/parent/parent-sidebar.tsx` returns 2 (Tarefas + Crianças navigation).
    - `grep -c "aria-label=\"Crianças\"" src/components/parent/parent-sidebar.tsx` returns 1 (label preserved).
    - `parent-panel-view.tsx` passes `activeRoute="tasks"` and `familyId` to ParentSidebar.
    - `npm test -- tests/unit/parent-sidebar.test.tsx` passes all three cases; `parent-panel.test.tsx` still green.
  </acceptance_criteria>
  <done>Crianças icon navigates to /children and shows active state; Tarefas active state is route-conditional; parent-panel unaffected; sidebar test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server route | unauthenticated request to /family/[familyId]/* must be redirected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Elevation of Privilege | /family/[familyId]/* without session | mitigate | layout.tsx auth() gate redirects to /login before any child data renders (mirrors T-05-08) |
| T-08-07 | Information Disclosure | drawer state leaking across /tasks↔/children | mitigate | GuardianProfileDrawer + profileOpen kept page-local, NOT lifted to layout (D-04, Pitfall 1) |
| T-08-SC | Tampering | npm installs | accept | this plan installs no packages |
</threat_model>

<verification>
- `docker compose exec -T app npm test -- tests/unit/parent-sidebar.test.tsx tests/unit/parent-panel.test.tsx` green.
- `docker compose exec -T app npx tsc --noEmit` reports no new errors.
- Manual (checkpoint deferred to Plan 05): /tasks renders identically post-refactor.
</verification>

<success_criteria>
- Shared layout.tsx auth gate in place; /tasks behavior unchanged.
- Crianças icon navigates to /children with correct active styling.
- Tarefas active state route-conditional.
- First parent-sidebar test green.
</success_criteria>

<output>
Create `.planning/phases/08-child-management/08-02-SUMMARY.md` when done.
</output>
