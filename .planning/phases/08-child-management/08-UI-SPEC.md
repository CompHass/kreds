---
phase: 8
slug: child-management
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-01
---

# Phase 8 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — project uses hand-authored design tokens (Tailwind v4 CSS-first `@theme` block in `src/app/globals.css`), not shadcn. Confirmed absent: no `components.json`, no `tailwind.config.*` file. |
| Preset | not applicable |
| Component library | none (no Radix/base-ui in use yet). Phase 8 introduces the project's **first** Radix usage: `radix-ui` unified package (v1.6.1) for `AlertDialog` only (D-14 confirm dialog). Source: 08-RESEARCH.md Standard Stack. |
| Icon library | none — inline SVG per component (see `parent-sidebar.tsx`, `parent-task-card.tsx` for the established pattern: `stroke` matches text color, `strokeWidth: 1.8-2`, `aria-hidden="true"` on every icon) |
| Font | Plus Jakarta Sans, weights 400/500/600/700/800, via `--font-sans: var(--font-plus-jakarta), system-ui, sans-serif` (DS-02, already configured, do not re-declare) |

**Note on styling approach:** This codebase does NOT use Tailwind utility classes in component markup. Every existing parent-panel component (`task-form-panel.tsx`, `parent-sidebar.tsx`, `parent-task-card.tsx`, `guardian-profile-drawer.tsx`, `task-toggle.tsx`) uses **inline `style={{}}` objects** referencing CSS custom properties declared in `globals.css` `@theme` (e.g. `var(--color-kreds-primary)`, `var(--shadow-cta)`, `var(--radius-pill)`). Phase 8 components MUST follow this exact pattern — do not introduce Tailwind classes or a new styling paradigm for this phase alone.

**Primary visual anchor:** On the `/children` screen, the child card list (`ChildCard` list within `ChildrenPanelView`) is the primary visual anchor — it occupies the main content area and is visible by default. `ChildFormPanel` is secondary/on-demand: it renders only in the add-child (create) flow and does not compete with the card list for default visual attention.

---

## Spacing Scale

Declared values (multiples of 4, matching values actually observed in `task-form-panel.tsx`/`parent-task-card.tsx`):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Label-to-input gap, badge internal gap |
| sm | 8px | Icon-to-text gap, badge row gap, card list gap |
| md | 16px | Card internal padding, form field vertical gap, panel section gap |
| lg | 24px | Drawer padding (`GuardianProfileDrawer`), dialog padding (new `ConfirmDeactivateDialog`) |
| xl | 32px | not used this phase |
| 2xl | 48px | not used this phase |
| 3xl | 64px | not used this phase |

Exceptions:
- **Form panel padding — 20px** (`TaskFormPanel` container root, verified in source at `src/components/parent/task-form-panel.tsx:77` — `padding: 20`). `ChildFormPanel` clones this container verbatim, so it inherits the existing 20px padding exactly. This is NOT a new value chosen for Phase 8 — it is an already-shipped component's padding being preserved during cloning. Do not "round" this to 16px or 24px; doing so would create a visible layout mismatch between `TaskFormPanel` and `ChildFormPanel`, which must remain visually identical containers.
- Icon-only touch targets: sidebar nav buttons 44×44px (`ParentSidebar` pattern, unchanged — "Crianças" icon reuses this exact size per D-05)
- Toggle/switch component: 42×24px (existing `TaskToggle` — NOT reused for D-14 per CONTEXT.md, deactivate/reactivate requires a confirmation dialog, not an instant toggle)
- Avatar sizes are non-4px-grid by design-handoff spec: 52×52px (Frame C child card avatar, D-08), 64px (drawer avatar, existing), 38px (sidebar profile button, existing) — these are locked visual constants from the design handoff, not part of the spacing scale

---

## Typography

Pre-populated from existing component inspection (`task-form-panel.tsx`, `parent-task-card.tsx`, `guardian-profile-drawer.tsx`) — Phase 8 introduces zero new sizes/weights, reuses the established set exactly:

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 500 (regular-weight body text: form labels, descriptions, PIN-hidden dots) — also reused for primary button/CTA label text (`"Adicionar filho"`, `"Redefinir PIN"`, `"Desativar"`) at the same weight 500, keeping the CTA label on the Body size token rather than introducing a 5th size | 1.5 |
| Label | 13px | 500 (field labels: "Título", "Categoria", drawer section labels) — also reused for small badge text (PIN masked dots row context, `ParentTaskCard`-style reward badge), keeping badges on the Label size token rather than introducing a 6th size | 1.3 |
| Heading | 18px | 700 (panel/drawer headers: "Nova tarefa", "Editar tarefa" → phase 8 equivalents "Adicionar filho", "Perfil do responsável") | 1.2 |
| Display | 24px | 700 (dialog title in `ConfirmDeactivateDialog` — new this phase, matches `guardian-profile-drawer.tsx` avatar-adjacent name text size scaled up for dialog emphasis) | 1.2 |

Only 4 sizes used (14/13/18/24px), only 2 weights used across the entire phase (500/700). Body and Label both sit at weight 500 (legible, non-heading weight — appropriate for a Christian family finance app's body copy, form labels, and CTA text), while Heading and Display both sit at weight 700 (reserved exclusively for headers and the dialog title). Do not introduce a 5th font size or a 3rd distinct weight for this phase. `TaskFormPanel`'s original CTA button and `ParentTaskCard`'s original badge (both pre-existing components, unchanged) keep their own historical sizing; this remap applies to Phase 8's new components (`ChildFormPanel`, `ChildCard`, `ChildPinResetPanel`) only.

---

## Color

Pre-populated from `src/app/globals.css` `@theme` tokens — Phase 8 introduces **zero new color tokens**. Reuse exactly:

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#F2F0E7` (`--color-kreds-bg`) | Page background behind the children list and shared layout |
| Secondary (30%) | `#FBFAF5` (`--color-kreds-card`) / `#ffffff` | Child cards, form panel (`ChildFormPanel`), sidebar, topbar |
| Accent (10%) | `#3E6B4F` (`--color-kreds-primary`) | RESERVED for: primary CTA buttons ("Adicionar filho" submit, "Salvar PIN"), active nav state ("Crianças" icon active background `#E7EFE8` + stroke `#3E6B4F`), `AlertDialog.Action` "Reativar" confirm button, focus ring on inputs |
| Destructive | `#B14A2E` (`--color-kreds-error`) | RESERVED for: "Desativar" confirm button inside `ConfirmDeactivateDialog` only (matches existing `DeleteButton` pattern in `task-form-panel.tsx`: text `#B14A2E`, bg `#FBF1EC`, border `#E6CFC4`) |

Accent reserved for: primary form submit CTA, active sidebar icon state, dialog "confirm" action when the action is non-destructive (reactivate) or Radix `AlertDialog.Action` default green. Never apply `--color-kreds-primary` to purely decorative elements or secondary buttons (Cancel buttons use `--color-kreds-muted` `#7C8676` text on `--color-kreds-card` background, matching `TaskFormPanel`'s Cancel/X button style).

Additional existing tokens Phase 8 will use as-is (not new): `--color-kreds-muted` (#7C8676, secondary text), `--color-kreds-border` (#ECE7DB), `--color-kreds-border-alt` (#E2DECF, input borders), `--color-kreds-hover` (#EEF3EA, hover/active backgrounds), `--color-kreds-text` (#27372C, primary text).

---

## Copywriting Contract

Pre-populated from CONTEXT.md `Claude's Discretion` section (exact button text was explicitly left to this agent) plus D-06/D-09/D-10/D-12/D-13/D-14/D-15 constraints:

| Element | Copy |
|---------|------|
| Primary CTA (add child) | "Adicionar filho" (idle/create state), matching `TaskFormPanel`'s "Adicionar tarefa" pattern exactly |
| Primary CTA (edit child, if applicable) | "Salvar alterações" — reused verbatim from `TaskFormPanel` edit mode |
| Reset PIN CTA | "Redefinir PIN" — locked term from design handoff Frame C, do not paraphrase |
| Reveal PIN toggle | "Mostrar" / "Ocultar" (toggles based on state) — locked term from design handoff Frame C ("Mostrar/Ocultar") |
| Deactivate CTA (child card) | "Desativar" |
| Reactivate CTA (child card) | "Reativar" |
| Empty state heading (no children yet) | "Nenhum filho cadastrado ainda" |
| Empty state body | "Adicione o primeiro filho da família para começar a atribuir tarefas e acompanhar o jardim." + CTA "Adicionar filho" (same button, no separate empty-state button needed — panel is always visible per D-09 idle-state pattern from `TaskFormPanel`) |
| Error state (generic form/save failure) | "Não foi possível salvar. Tente novamente." — matches project's terse existing error tone (no existing error-copy component found elsewhere to diverge from; this is a sensible default) |
| Error state (PIN not yet set — pre-Phase-8 child, `pinEncrypted IS NULL`) | "PIN ainda não definido — use Redefinir PIN" (per 08-RESEARCH.md Pitfall 6 — mandatory copy for this exact edge case, disable "Mostrar" button in this state) |
| Deactivate confirmation dialog title | "Desativar {childName}?" |
| Deactivate confirmation dialog body | "{childName} não conseguirá fazer login até ser reativado(a)." |
| Reactivate confirmation dialog title | "Reativar {childName}?" |
| Reactivate confirmation dialog body | "{childName} poderá fazer login novamente." |
| Deactivate confirmation dialog Cancel button | "Cancelar" |
| Deactivate confirmation dialog Confirm button | "Desativar" (destructive color) / "Reativar" (primary color) — label matches the action being confirmed |
| Sidebar "Crianças" icon aria-label | "Crianças" (already exists verbatim in `parent-sidebar.tsx` line 111 — do not change) |
| Add-child form field labels | "Nome" (displayName), "Idade" (ageYears), "Cor" (accentColor) — 13px/600 label style matching `TaskFormPanel`'s "Título"/"Categoria" labels |
| Add-child form title | "Adicionar filho" (panel header, matches idle-state "Nova tarefa" → "create" pattern) |

Destructive actions in this phase:
1. **Desativar filho** — confirmation required via `ConfirmDeactivateDialog` (D-14, first modal in project). Confirmation copy above. Uses `--color-kreds-error` (#B14A2E) on the confirm button, matching `DeleteButton`'s existing destructive-action color pattern.
2. **Reativar filho** — technically not destructive but shares the same dialog component (per 08-RESEARCH.md Pattern 3) with `willDeactivate: false`; confirm button uses `--color-kreds-primary` (green), NOT the destructive color, since reactivating restores access rather than removing it.

No other destructive actions in this phase (no delete-child action — only deactivate/reactivate per Roadmap Phase 8 success criteria).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized for this project | not applicable |
| radix-ui (official npm package, not a shadcn registry) | `AlertDialog` primitive only, imported directly via `npm install radix-ui` per 08-RESEARCH.md | not applicable — this is a direct npm package install of an official, actively-maintained primitives library (9.05M weekly downloads, verified via `npm view` in 08-RESEARCH.md Package Legitimacy Audit), NOT a shadcn third-party block/registry fetch. The registry vetting gate (`npx shadcn view {block} --registry {url}`) applies only to shadcn CLI block registries and does not apply here since no shadcn CLI or third-party shadcn registry is involved in this phase. |

No third-party shadcn registries declared for this phase. Registry vetting gate not triggered.

---

## Component Inventory (Phase-Specific)

Additive detail beyond the template — for planner/executor traceability against 08-RESEARCH.md's Recommended Project Structure:

| Component | Pattern Source | Visual Notes |
|-----------|----------------|--------------|
| `ChildrenPanelView` (new) | Clone of `parent-panel-view.tsx` state shape | Root client component, `--color-kreds-bg` page background |
| `ChildFormPanel` (new) | Clone of `task-form-panel.tsx` (336px width, 20px padding, `--radius-card-lg` 20px, `--shadow-card`) | idle/create states only (no edit mode per D-06 — no update-name flow specified); RHF + Zod per 08-RESEARCH.md Pattern 4 |
| `ChildCard` (new) | Design handoff Frame C, adapted from single-card to list-row | 420px card width per handoff → adapt to list context (full-width row within main content area, `border-radius: 24px` per handoff, bg `#F6F5F1` — note this is a handoff-specific bg, distinct from `--color-kreds-card`; confirm against `--color-kreds-hover` #EEF3EA as closest existing token if exact hex is not tokenized) |
| `ChildPinResetPanel` (new) | Adapts `numeric-keypad.tsx` (CAUTH-01, 3×4 grid, 62px buttons) to non-fullscreen desktop panel context | Reuse keypad button styling (`border-radius: 50%`, `bg #FBFAF5`, `box-shadow: 0 3px 0 #E6E1D4`) at existing size — do not redesign the keypad, only its container/placement |
| `ConfirmDeactivateDialog` (new) | Radix `AlertDialog`, project's first modal | Overlay `rgba(39,55,44,.25)` (matches `GuardianProfileDrawer` backdrop exactly), content `border-radius: 16px`, `padding: 24px`, `width: 360px`, centered |
| `ParentSidebar` (modified) | D-05 — "Crianças" icon gains href + active state | Active state reuses exact existing pattern from "Tarefas" icon: bg `#E7EFE8`, stroke `#3E6B4F` (vs. inactive stroke `#9AA092`, no bg) |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
