---
phase: 03-child-garden
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/components/garden/water-drops.tsx
  - src/components/garden/harvest-button.tsx
  - src/components/garden/confetti-field.tsx
  - src/components/garden/celebration-overlay.tsx
  - src/components/garden/garden-view.tsx
  - src/app/(child)/child/[childId]/garden/page.tsx
autonomous: true
requirements: [GARD-05, GARD-08, GARD-10]
must_haves:
  truths:
    - "Ao concluir tarefa, 5 drops (kredsDrop) disparam, planta faz pop (kredsPop) e tracker de agua avanca"
    - "Botao 'Colher Frutos' laranja (kredsFruit) aparece somente quando todas as tarefas estao concluidas"
    - "Ao colher, overlay com 20 confetes (kredsConfetti), card de versiculo biblico e botao 'Voltar ao jardim'"
    - "Ao fechar overlay, jardim mantem ultimo estado visual (plant-d, tarefas marcadas, botao colher some)"
    - "Pagina /child/[childId]/garden busca versiculo aleatorio do banco e renderiza o jardim"
  artifacts:
    - path: "src/components/garden/garden-view.tsx"
      provides: "Client Component raiz com estado e handlers (GARD-05, GARD-08, GARD-10)"
      contains: "export function GardenView"
    - path: "src/components/garden/water-drops.tsx"
      provides: "5 drops kredsDrop remontados por key (GARD-05)"
      contains: "export function WaterDrops"
    - path: "src/components/garden/harvest-button.tsx"
      provides: "Botao Colher Frutos kredsFruit (GARD-08)"
      contains: "export function HarvestButton"
    - path: "src/components/garden/celebration-overlay.tsx"
      provides: "Overlay com confetes + versiculo + voltar (GARD-10)"
      contains: "export function CelebrationOverlay"
    - path: "src/components/garden/confetti-field.tsx"
      provides: "20 confetes array estatico (GARD-10)"
      contains: "ConfettiField"
    - path: "src/app/(child)/child/[childId]/garden/page.tsx"
      provides: "Server Component: query versiculo RANDOM + render GardenView"
      contains: "GardenPage"
  key_links:
    - from: "src/app/(child)/child/[childId]/garden/page.tsx"
      to: "bibleVerses (Drizzle)"
      via: "db.select from bibleVerses orderBy sql RANDOM limit 1"
      pattern: "RANDOM"
    - from: "src/components/garden/garden-view.tsx"
      to: "src/components/garden/garden-hero.tsx"
      via: "compoe GardenHero + WaterDrops + HarvestButton + CelebrationOverlay"
      pattern: "GardenHero"
    - from: "src/components/garden/garden-view.tsx"
      to: "handleTaskComplete"
      via: "incrementa waterTick + setShowPop"
      pattern: "setWaterTick"
---

<objective>
Completar a Fase 3 com a camada interativa e a pagina servidor: WaterDrops (GARD-05, remontado por key), HarvestButton (GARD-08, kredsFruit), ConfettiField + CelebrationOverlay (GARD-10, 20 confetes array estatico + card de versiculo + botao voltar), GardenView (Client Component raiz que orquestra estado de tarefas/agua/colheita e compoe GardenHero do Plano 02), e a pagina Server Component /child/[childId]/garden que busca um versiculo aleatorio do banco e passa seed + verse para o GardenView.

Purpose: Esta e a camada que conecta tudo — transforma os componentes passivos do Plano 02 em uma experiencia interativa completa, consome a tabela bible_verses do Plano 01, e entrega a rota final navegavel (D-04). Fecha o loop GARD-05/08/10.
Output: 6 arquivos; testes garden-view.test.tsx e garden-celebration.test.tsx verdes; rota /child/[childId]/garden funcional.
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-child-garden/03-CONTEXT.md
@.planning/phases/03-child-garden/03-RESEARCH.md
@.planning/phases/03-child-garden/03-PATTERNS.md
@.planning/phases/03-child-garden/03-UI-SPEC.md
@src/lib/seed/garden-seed.ts
@src/components/garden/garden-hero.tsx
@src/app/(child)/child/[childId]/login/page.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (novos simbolos — nao tratar como drift)

Criados neste plano:
- WaterDrops() — water-drops.tsx (5 divs kredsDrop com delays escalonados; remontado via key no GardenView)
- HarvestButton com props visible e onHarvest — harvest-button.tsx
- ConfettiField() + constante CONFETTI_ITEMS (array estatico de 20) — confetti-field.tsx
- CelebrationOverlay com props visible, verse, onClose — celebration-overlay.tsx (verse: reference + text ou null)
- GardenView com props childId, seed, verse — garden-view.tsx (Client Component raiz; estado: tasks, waterTick, showPop, harvested, showOverlay; handlers handleTaskComplete, handleHarvest, handleCloseOverlay)
- GardenPage — page.tsx (Server Component async; default export)

Consome do Plano 01: bibleVerses, GardenSeed, getPlantStage, getBubbleText, constantes SEED_*.
Consome do Plano 02: GardenHeader, GardenHero (e indiretamente todos os componentes passivos).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WaterDrops + HarvestButton + ConfettiField + CelebrationOverlay</name>
  <files>src/components/garden/water-drops.tsx, src/components/garden/harvest-button.tsx, src/components/garden/confetti-field.tsx, src/components/garden/celebration-overlay.tsx</files>
  <behavior>
    - WaterDrops: renderiza 5 divs com animacao kredsDrop e delays 0/80/160/240/320ms
    - HarvestButton: visible true renderiza botao "Colher Frutos" com aria-label; visible false retorna null
    - CelebrationOverlay: visible true com verse mock mostra o texto do versiculo, a referencia e o botao "Voltar ao jardim"; clicar o botao chama onClose (garden-celebration.test.tsx)
    - ConfettiField: renderiza 20 elementos com aria-hidden e animacao kredsConfetti
  </behavior>
  <read_first>
    - src/components/auth/spinner-button.tsx (padrao de botao com props visibilidade/disabled, min-height touch target)
    - src/components/auth/gate-lock.tsx (padrao de fixed/absolute overlay; divs mapeados com CSS var via style)
    - src/components/auth/pin-dot.tsx (CSS var animation via style prop)
    - .planning/phases/03-child-garden/03-UI-SPEC.md secao Overlay de Celebracao (medidas, card versiculo, botao voltar), secao Animation Contract (delays de WaterDrop, array de confetes), secao Color (botao laranja gradiente C77F52 para B5623F), secao Accessibility Notes (role dialog, aria-modal)
    - .planning/phases/03-child-garden/03-RESEARCH.md Code Examples Pattern 4 (CONFETTI_ITEMS array estatico — copiar formula exata: left, delay, color, size), secao Anti-Patterns (sem Math.random no render)
    - .planning/phases/03-child-garden/03-PATTERNS.md (assignments water-drops, harvest-button, confetti-field, celebration-overlay)
    - src/app/globals.css (animacoes kreds-drop, kreds-fruit, kreds-confetti, kreds-cele)
    - tests/unit/garden-celebration.test.tsx (contrato GARD-10)
  </read_first>
  <action>
    water-drops.tsx: exportar WaterDrops sem args. 5 divs position absolute espalhados horizontalmente sobre a planta, cada um com style aplicando a variavel de animacao kreds-drop e animationDelay igual a i vezes 80 ms, aria-hidden. Sem estado interno — o replay e feito pelo GardenView via key igual a waterTick. Sem 'use client' necessario (apenas style/markup).

    harvest-button.tsx: exportar HarvestButton com props visible boolean e onHarvest funcao. Quando nao visible, retornar null. Quando visible: button com onClick igual a onHarvest, texto "Colher Frutos", gradiente linear de C77F52 para B5623F, borderRadius 999px, minHeight 44px (WCAG touch target), color branco peso 700, animacao kreds-fruit, aria-label "Colher os frutos do jardim". Precisa de 'use client' (onClick handler).

    confetti-field.tsx: definir constante CONFETTI_ITEMS FORA do componente (array de 20 com left, delay, color, size calculados deterministicamente — usar a formula do RESEARCH.md Pattern 4: left igual a 5 + (i*4.75) modulo 90 com sufixo de porcentagem, delay igual a i*100 ms, color igual ao indice i modulo 5 do array de cores 3E6B4F E3C57C C98AA0 6E9BA0 B5623F, size igual a 8 + (i modulo 4) vezes 3). Exportar ConfettiField que mapeia CONFETTI_ITEMS em 20 divs position absolute top menos 30px, borderRadius 2, background c.color, width e height c.size, animacao kreds-confetti, animationDelay c.delay, aria-hidden. NAO usar Math.random no render (evita hydration mismatch). Sem 'use client' (estatico).

    celebration-overlay.tsx: exportar CelebrationOverlay com props visible boolean, verse objeto com reference e text ou null, onClose funcao. Quando nao visible, retornar null. Container position fixed inset 0 zIndex 50, background rgba(244,241,232,.98), role "dialog" aria-modal "true". Renderizar ConfettiField. Card do versiculo: bg var(--color-kreds-card), borderRadius 20, boxShadow var(--shadow-card), padding 24, animacao kreds-cele — titulo "Parabens! Voce colheu seu jardim!" (Display 24/700), texto do versiculo (verse text, Body 14/500 var(--color-kreds-text)), referencia (verse reference, Label 12/700 var(--color-kreds-muted)). Botao "Voltar ao jardim" onClick igual a onClose, 52px altura, borderRadius 13, bg var(--color-kreds-primary), texto branco 700, boxShadow var(--shadow-cta) — unico elemento focavel. Tratar verse null graciosamente (nao quebrar). Precisa de 'use client' (onClick).
  </action>
  <verify>
    <automated>pnpm test tests/unit/garden-celebration.test.tsx 2>&1 | grep -qE "passed|PASS"</automated>
  </verify>
  <acceptance_criteria>
    - water-drops.tsx exporta WaterDrops; 5 divs com a animacao kreds-drop e delays 0/80/160/240/320ms
    - harvest-button.tsx exporta HarvestButton; null quando nao visible; gradiente C77F52 para B5623F, animacao kreds-fruit, aria-label, minHeight 44px, diretiva 'use client'
    - confetti-field.tsx define CONFETTI_ITEMS fora do componente; NAO contem Math.random; 20 itens; aria-hidden
    - celebration-overlay.tsx exporta CelebrationOverlay; role dialog aria-modal true; mostra verse text, verse reference e botao "Voltar ao jardim" que chama onClose; trata verse null
    - pnpm test tests/unit/garden-celebration.test.tsx passa (GARD-10)
    - grep Math.random em src/components/garden/confetti-field.tsx retorna vazio
  </acceptance_criteria>
  <done>Drops, botao colher, confetes e overlay de celebracao implementados (GARD-05 parcial, GARD-08, GARD-10).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GardenView (Client Component raiz com estado e interacao)</name>
  <files>src/components/garden/garden-view.tsx</files>
  <behavior>
    - GardenView monta com seed; renderiza GardenHeader e GardenHero
    - handleTaskComplete(taskId): marca tarefa done, incrementa waterTick (remonta WaterDrops), ativa showPop por aprox 650ms, recalcula stage/waterCount/canHarvest via getPlantStage
    - HarvestButton oculto ate todas tarefas done; visivel quando canHarvest true (garden-view.test.tsx GARD-08)
    - handleHarvest: abre overlay (showOverlay true, harvested true) e CelebrationOverlay fica visivel; handleCloseOverlay: overlay some mas jardim mantem plant-d e tarefas marcadas (D-10), botao colher some (garden-view.test.tsx GARD-05)
  </behavior>
  <read_first>
    - src/components/auth/pin-screen.tsx (padrao Client Component com useState; handler com setTimeout para animacao; layout container)
    - src/lib/seed/garden-seed.ts (GardenSeed, getPlantStage, getBubbleText)
    - src/components/garden/garden-header.tsx, src/components/garden/garden-hero.tsx (componentes a compor)
    - src/components/garden/water-drops.tsx, src/components/garden/harvest-button.tsx, src/components/garden/celebration-overlay.tsx (criados na Task 1)
    - .planning/phases/03-child-garden/03-RESEARCH.md Pattern 3 (key igual a waterTick para replay), Code Examples (handleTaskComplete: setWaterTick, setShowPop, setTimeout 650ms)
    - .planning/phases/03-child-garden/03-CONTEXT.md D-09 (colher sem POST), D-10 (manter estado ao fechar)
    - .planning/phases/03-child-garden/03-UI-SPEC.md secao Layout (container max-width 392px, padding-bottom 80px reservado), secao Hero (HarvestButton substitui tracker quando canHarvest)
    - tests/unit/garden-view.test.tsx (contrato GARD-05, GARD-08)
  </read_first>
  <action>
    Exportar GardenView com props childId string, seed GardenSeed, verse objeto reference/text ou null — com diretiva 'use client'.

    Estado (useState): tasks inicializado de seed.tasks; waterTick number 0; showPop bool false; harvested bool inicializado de seed.harvested; showOverlay bool false.

    Derivados (recalculados no render): doneCount igual a contagem de tasks com done true; stage igual a getPlantStage(doneCount, tasks.length); waterCount igual a doneCount; canHarvest igual a doneCount igual ao total de tasks E nao harvested; hasPending igual a doneCount menor que total de tasks; bubbleText igual a getBubbleText do seed com tasks e harvested atuais.

    Handlers:
    - handleTaskComplete(taskId): setTasks marcando a tarefa como done; setWaterTick incrementando; setShowPop true; setTimeout para setShowPop false apos 650ms.
    - handleHarvest(): setHarvested true e setShowOverlay true.
    - handleCloseOverlay(): setShowOverlay false APENAS. NAO resetar tasks nem harvested (D-10 — manter ultimo estado). Como harvested permanece true, canHarvest fica false (botao colher some) e a planta permanece em plant-d.

    Layout: container minHeight 100vh, background var(--color-kreds-bg), maxWidth 392px, centralizado (margin auto), paddingBottom 80 (reservado para bottom nav Fase 4 — D-05). Renderizar GardenHeader com name seed.childName, initial seed.initial, coins seed.coins. Renderizar GardenHero com stage, season seed.season, waterCount, titheDone seed.titheDone, canHarvest, droop hasPending, pop showPop, showBubble igual a nao harvested, bubbleText — passando como children: WaterDrops com key igual a waterTick, e HarvestButton com visible canHarvest e onHarvest handleHarvest. Apos o hero, renderizar a lista de tarefas com um controle clicavel por tarefa chamando handleTaskComplete (UI minima — os task cards completos sao escopo da Fase 4; aqui basta disparar GARD-05). Renderizar CelebrationOverlay com visible showOverlay, verse, onClose handleCloseOverlay.
  </action>
  <verify>
    <automated>pnpm test tests/unit/garden-view.test.tsx 2>&1 | grep -qE "passed|PASS"</automated>
  </verify>
  <acceptance_criteria>
    - garden-view.tsx exporta GardenView e tem diretiva 'use client'
    - Contem setWaterTick e usa key igual a waterTick para remontar WaterDrops (replay)
    - handleCloseOverlay nao reseta tasks nem harvested (apenas showOverlay false) — D-10
    - canHarvest verdadeiro somente quando todas tarefas done e nao harvested (HarvestButton visivel) — GARD-08
    - handleTaskComplete usa setTimeout 650ms para resetar showPop
    - pnpm test tests/unit/garden-view.test.tsx passa (GARD-05, GARD-08)
  </acceptance_criteria>
  <done>GardenView orquestra estado e interacao do jardim; testes GARD-05/08 verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Pagina Server Component /child/[childId]/garden</name>
  <files>src/app/(child)/child/[childId]/garden/page.tsx</files>
  <read_first>
    - src/app/(child)/child/[childId]/login/page.tsx (padrao EXATO de Server Component async com params Promise, await params, query Drizzle, import db e schema)
    - src/lib/db/index.ts (export db)
    - src/lib/db/schema/index.ts (export bibleVerses — criado no Plano 01)
    - src/lib/seed/garden-seed.ts (constantes SEED_* — escolher SEED_STAGE_C como padrao por ser o estado visualmente mais rico, RESEARCH.md Open Question 2)
    - src/components/garden/garden-view.tsx (GardenView — criado na Task 2)
    - .planning/phases/03-child-garden/03-RESEARCH.md Pattern 1 (Server Component busca versiculo), Pitfall 4 (ORDER BY RANDOM sintaxe Drizzle: import sql de drizzle-orm, orderBy com sql RANDOM)
    - .planning/phases/03-child-garden/03-CONTEXT.md D-04 (URL canonica), D-08 (selecao aleatoria)
  </read_first>
  <action>
    Criar src/app/(child)/child/[childId]/garden/page.tsx como Server Component async (seguir padrao exato de login/page.tsx). Imports: db de @/lib/db, bibleVerses de @/lib/db/schema, sql de drizzle-orm, GardenView de @/components/garden/garden-view, SEED_STAGE_C de @/lib/seed/garden-seed.

    Assinatura: export default async function GardenPage com params Promise de childId. Resolver const childId via await params.

    Query versiculo aleatorio (D-08): const resultado igual a await db.select().from(bibleVerses) com orderBy usando sql RANDOM e limit 1; extrair o primeiro elemento como verse. Mapear para o shape esperado pelo GardenView (reference e text) ou null se vazio (tratar verse undefined com null fallback).

    Renderizar GardenView com childId, seed SEED_STAGE_C (estado padrao visualmente rico — RESEARCH.md Open Question 2), verse. NAO adicionar guard de childId scope (RESEARCH.md Security Domain — escopo de childId validado na Fase 6; dados sao seed mockado). NAO modificar pin-screen.tsx (D-03 ja implementado — Pitfall 1). NAO criar Server Action de colheita (D-09).
  </action>
  <verify>
    <automated>test -f "src/app/(child)/child/[childId]/garden/page.tsx" && grep -q "GardenPage" "src/app/(child)/child/[childId]/garden/page.tsx" && grep -q "RANDOM" "src/app/(child)/child/[childId]/garden/page.tsx" && pnpm test 2>&1 | grep -qE "passed|PASS"</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx existe em src/app/(child)/child/[childId]/garden/ e exporta default async function GardenPage
    - Resolve params via await params (padrao Next.js 16 — params e Promise)
    - Consulta bibleVerses com orderBy sql RANDOM e limit 1 (D-08); trata resultado vazio como verse null
    - Renderiza GardenView com childId, seed SEED_STAGE_C e verse
    - NAO modifica pin-screen.tsx; NAO adiciona Server Action de colheita; NAO adiciona guard de childId scope
    - pnpm test (suite completa) passa — todos os 7 arquivos garden verdes
  </acceptance_criteria>
  <done>Rota /child/[childId]/garden funcional buscando versiculo aleatorio e renderizando o jardim completo (D-04, GARD-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser para /child/[childId]/garden | Rota so acessivel com JWT child-session valido (middleware Fase 2) |
| Server Component para banco (bible_verses) | Query Drizzle server-side; texto biblico publico (sem PII) |
| Client (GardenView) para estado local | Colheita e estado local (D-09 — sem POST ao backend nesta fase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | Acesso direto a /child/[id]/garden sem sessao | mitigate | Middleware /child/* ja verifica JWT child-session e redireciona para / — testado em tests/unit/middleware.test.ts; nenhum codigo novo |
| T-03-06 | Spoofing | childId na URL diferente do JWT | accept | Dados sao seed mockado (nao dados reais por-crianca); RESEARCH.md Security Domain define validateChildSessionScope para Fase 6 |
| T-03-07 | Information Disclosure | Versiculo retornado ao cliente | accept | Conteudo biblico publico; sem dado sensivel |
| T-03-08 | Tampering | Estado de colheita manipulado no cliente | accept | Sem efeito no backend nesta fase (D-09 — colheita visual; POST real e ledger na Fase 6) |
</threat_model>

<verification>
- pnpm test (suite completa) verde — 7 arquivos garden passam
- Rota /child/[childId]/garden existe e renderiza GardenView com versiculo do banco
- grep RANDOM em page.tsx confirma query aleatoria (D-08)
- Manual (browser): login crianca, portao, jardim; concluir tarefa dispara drops+pop+tracker; com todas done aparece "Colher Frutos"; clicar abre overlay com confetes e versiculo; "Voltar ao jardim" mantem plant-d
</verification>

<success_criteria>
- GARD-05: drops + pop + avanco de tracker ao concluir tarefa
- GARD-08: botao Colher Frutos visivel somente com todas tarefas done
- GARD-10: overlay com 20 confetes + versiculo real do banco + botao voltar
- Rota /child/[childId]/garden navegavel (D-04)
- Suite completa verde
</success_criteria>

<output>
Create `.planning/phases/03-child-garden/03-03-SUMMARY.md` when done
</output>
