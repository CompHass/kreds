---
phase: 03-child-garden
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/garden/garden-header.tsx
  - src/components/garden/garden-hero.tsx
  - src/components/garden/plant-stage.tsx
  - src/components/garden/water-tracker.tsx
  - src/components/garden/season-badge.tsx
  - src/components/garden/speech-bubble.tsx
  - src/components/garden/decorative-flowers.tsx
  - src/components/garden/harvest-glow.tsx
autonomous: true
requirements: [GARD-01, GARD-02, GARD-03, GARD-04, GARD-06, GARD-07, GARD-09]
must_haves:
  truths:
    - "Header exibe avatar com inicial, nome da criança e badge de moedas com ícone SVG coin"
    - "Hero 316px renderiza céu gradiente, sol animado (kredsSun) e 2 nuvens (kredsDrift1/2)"
    - "Planta exibida no estágio correto (plant-a→d) com droop quando há tarefas pendentes"
    - "WaterTracker mostra N dots azuis (#6E9BA0) para filled=N, restantes brancos semi-transparentes"
    - "SeasonBadge exibe nome da estação com dot colorido correto"
    - "SpeechBubble mostra texto contextual com animação kredsBubble"
    - "DecorativeFlowers (SVG rosa) visíveis quando titheDone, HarvestGlow visível quando canHarvest"
  artifacts:
    - path: "src/components/garden/garden-header.tsx"
      provides: "Header com avatar, nome, coins (GARD-01)"
      contains: "export function GardenHeader"
    - path: "src/components/garden/garden-hero.tsx"
      provides: "Container hero 316px com céu/sol/nuvens/chão e composição dos filhos (GARD-02)"
      contains: "export function GardenHero"
    - path: "src/components/garden/plant-stage.tsx"
      provides: "img plant-[a-d] com droop/pop (GARD-03)"
      contains: "export function PlantStage"
    - path: "src/components/garden/water-tracker.tsx"
      provides: "4 dots água (GARD-04)"
      contains: "export function WaterTracker"
    - path: "src/components/garden/season-badge.tsx"
      provides: "Pill estação com dot (GARD-06)"
      contains: "export function SeasonBadge"
    - path: "src/components/garden/speech-bubble.tsx"
      provides: "Bubble contextual kredsBubble (GARD-07)"
      contains: "export function SpeechBubble"
    - path: "src/components/garden/decorative-flowers.tsx"
      provides: "Flores SVG dízimo (GARD-09)"
      contains: "export function DecorativeFlowers"
    - path: "src/components/garden/harvest-glow.tsx"
      provides: "Glow radial amarelo quando canHarvest"
      contains: "export function HarvestGlow"
  key_links:
    - from: "src/components/garden/plant-stage.tsx"
      to: "/garden/plant-[stage].png"
      via: "img src"
      pattern: "garden/plant-"
    - from: "src/components/garden/season-badge.tsx"
      to: "SEASON_DOT_COLORS"
      via: "import de garden-seed"
      pattern: "SEASON_DOT_COLORS"
---

<objective>
Construir os 8 componentes de exibição passiva (sem estado interativo) do jardim: GardenHeader (GARD-01), GardenHero como container 316px que compõe sol/nuvens/chão/morros (GARD-02), PlantStage com img por estágio + droop (GARD-03), WaterTracker (GARD-04), SeasonBadge (GARD-06), SpeechBubble (GARD-07), DecorativeFlowers (GARD-09) e HarvestGlow. Todos usam tokens e animações CSS já existentes em globals.css aplicadas via `style` prop.

Purpose: Estes componentes são os blocos visuais que o GardenView (Plano 03) compõe. São puramente presentacionais — recebem props e renderizam, sem `useState`. Isolá-los aqui permite testá-los unitariamente e mantém o GardenView focado em estado/interação.
Output: 8 componentes em src/components/garden/, fazendo os testes de garden-header.test.tsx, garden-hero.test.tsx e garden-season.test.ts passarem (GREEN).
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-child-garden/03-PATTERNS.md
@.planning/phases/03-child-garden/03-UI-SPEC.md
@.planning/phases/03-child-garden/03-RESEARCH.md
@src/lib/seed/garden-seed.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (novos símbolos — não tratar como drift)

Criados neste plano (todos em `src/components/garden/`):
- `GardenHeader({ name, initial, coins })` — garden-header.tsx
- `GardenHero({ stage, season, waterCount, titheDone, canHarvest, droop, showBubble, bubbleText, children? })` — garden-hero.tsx (container que compõe sol/nuvens/morros/chão + PlantStage, WaterTracker, SeasonBadge, SpeechBubble, DecorativeFlowers, HarvestGlow)
- `PlantStage({ stage, droop, pop })` — plant-stage.tsx
- `WaterTracker({ filled })` — water-tracker.tsx
- `SeasonBadge({ season })` — season-badge.tsx
- `SpeechBubble({ text, visible })` — speech-bubble.tsx
- `DecorativeFlowers({ visible })` — decorative-flowers.tsx
- `HarvestGlow({ visible })` — harvest-glow.tsx

Consome símbolos do Plano 01: `GardenSeed`, `SEASON_DOT_COLORS`, tipos de estágio.
Consome assets do Plano 01: `/garden/plant-[a-d].png`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GardenHeader + WaterTracker + SeasonBadge</name>
  <files>src/components/garden/garden-header.tsx, src/components/garden/water-tracker.tsx, src/components/garden/season-badge.tsx</files>
  <behavior>
    - GardenHeader: renderiza inicial dentro de avatar, o nome da criança e o valor de coins (texto) — verificável no DOM (garden-header.test.tsx)
    - WaterTracker: para filled=3 renderiza 4 dots, 3 com cor de água e 1 vazio; aria-label "Tracker de água: 3 de 4 tarefas concluídas" (garden-hero.test.tsx)
    - SeasonBadge: para season='verao' renderiza label "Verão" e um dot com cor #E3C57C (garden-season.test.ts)
  </behavior>
  <read_first>
    - src/components/auth/profile-card.tsx (padrão de avatar gradiente + inicial; padrão de pill/chip com borda)
    - src/components/auth/pin-dots.tsx e src/components/auth/pin-dot.tsx (padrão de N dots com prop booleana)
    - src/lib/seed/garden-seed.ts (SEASON_DOT_COLORS, tipo de estação, GardenSeed)
    - .planning/phases/03-child-garden/03-UI-SPEC.md §Header, §Color (paleta), §Season Dot Colors
    - .planning/phases/03-child-garden/03-PATTERNS.md (assignments de garden-header, water-tracker, season-badge)
    - tests/unit/garden-header.test.tsx, tests/unit/garden-hero.test.tsx, tests/unit/garden-season.test.ts (contratos que estes componentes devem satisfazer)
  </read_first>
  <action>
    `garden-header.tsx`: exportar `GardenHeader({ name, initial, coins }: { name: string; initial: string; coins: number })`. Avatar `46×46px` com `borderRadius: 15px`, gradiente `linear-gradient(135deg, #5A8A66 0%, #3E6B4F 100%)`, inicial branca peso 700. Nome em Display (24px/700, letterSpacing -0.01em, `var(--color-kreds-text)`). Badge de coins: pill `bg var(--color-kreds-card)`, `border 1px solid var(--color-kreds-border)`, contendo SVG inline de moeda preenchido com `var(--color-kreds-coin)` (#E3C57C, aria-hidden) e o valor formatado com `color: var(--color-kreds-gold)` peso 700. Sem 'use client' (componente estático).

    `water-tracker.tsx`: exportar `WaterTracker({ filled }: { filled: number })`. Render 4 dots `8×8px borderRadius 50%`, `display:flex gap:6`. Dot i recebe `background: i < filled ? 'var(--color-kreds-water)' : 'rgba(255,255,255,.35)'`. `aria-label={\`Tracker de água: ${filled} de 4 tarefas concluídas\`}` no container. Sem 'use client'.

    `season-badge.tsx`: exportar `SeasonBadge({ season }: { season: 'primavera'|'verao'|'outono'|'inverno' })`. Pill `bg var(--color-kreds-card)`, `border 1px solid var(--color-kreds-border)`, `borderRadius var(--radius-pill)`, `padding 4px 10px`, `display:flex gap:6 alignItems:center`. Dot `8×8px borderRadius 50%` com `background: SEASON_DOT_COLORS[season]` (importado de garden-seed). Label em Label (12px/700) com capitalização correta (Primavera/Verão/Outono/Inverno). Sem 'use client'.
  </action>
  <verify>
    <automated>pnpm test tests/unit/garden-header.test.tsx tests/unit/garden-season.test.ts 2>&1 | grep -qE "passed|PASS|✓" && grep -q "export function GardenHeader" src/components/garden/garden-header.tsx && grep -q "export function WaterTracker" src/components/garden/water-tracker.tsx && grep -q "export function SeasonBadge" src/components/garden/season-badge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test tests/unit/garden-header.test.tsx` passa (GARD-01)
    - `pnpm test tests/unit/garden-season.test.ts` passa (GARD-06)
    - garden-header.tsx exporta GardenHeader; avatar usa borderRadius 15px e gradiente #5A8A66→#3E6B4F
    - water-tracker.tsx: WaterTracker com filled=N renderiza N dots com var(--color-kreds-water) e (4-N) com rgba(255,255,255,.35); container tem aria-label "Tracker de água: N de 4 tarefas concluídas"
    - season-badge.tsx importa SEASON_DOT_COLORS de garden-seed e renderiza dot com a cor da estação
    - SVG do coin tem aria-hidden e usa var(--color-kreds-coin)
  </acceptance_criteria>
  <done>Header, tracker de água e badge de estação implementados e testados (GARD-01, GARD-04, GARD-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlantStage + DecorativeFlowers + SpeechBubble + HarvestGlow</name>
  <files>src/components/garden/plant-stage.tsx, src/components/garden/decorative-flowers.tsx, src/components/garden/speech-bubble.tsx, src/components/garden/harvest-glow.tsx</files>
  <behavior>
    - PlantStage: stage='c' renderiza `<img>` com src `/garden/plant-c.png` e alt descritivo; droop=true aplica rotate(-2.5deg); pop=true aplica animação kredsPop
    - DecorativeFlowers: visible=true renderiza SVG de flores (aria-hidden); visible=false não renderiza/oculta (garden-hero.test.tsx)
    - SpeechBubble: visible=true mostra o texto passado em props com animação kredsBubble
    - HarvestGlow: visible=true tem opacity 1; visible=false opacity 0
  </behavior>
  <read_first>
    - src/components/auth/pin-dot.tsx (padrão de animação CSS var via style prop; SVG inline com aria-hidden)
    - src/components/auth/gate-lock.tsx (padrão de overlay absoluto condicional, CSS var via style)
    - src/lib/seed/garden-seed.ts (tipo de stage 'a'|'b'|'c'|'d')
    - .planning/phases/03-child-garden/03-UI-SPEC.md §Hero (planta, droop, glow), §Estágios da Planta, §Color (rosa dízimo #C98AA0), §Animation Contract
    - .planning/phases/03-child-garden/03-PATTERNS.md (assignments de plant-stage, decorative-flowers, speech-bubble, harvest-glow; Padrão de img simples não next/image)
    - src/app/globals.css (animações --animate-kreds-pop, --animate-kreds-bubble; tokens de cor)
    - tests/unit/garden-hero.test.tsx (contrato de DecorativeFlowers)
  </read_first>
  <action>
    `plant-stage.tsx`: exportar `PlantStage({ stage, droop, pop }: { stage: 'a'|'b'|'c'|'d'; droop: boolean; pop: boolean })`. Usar `<img src={\`/garden/plant-${stage}.png\`} alt={\`Planta no estágio ${stage}\`} />` (NÃO next/image — projeto usa img simples). `position:absolute bottom:30 left:'50%'`, `transform: \`translateX(-50%) ${droop ? 'rotate(-2.5deg)' : ''}\``, `transformOrigin:'50% 94%'`, `filter:'drop-shadow(0 4px 8px rgba(0,0,0,.15))'`. Quando `pop`, adicionar `animation: 'var(--animate-kreds-pop)'` ao style. Sem 'use client'.

    `decorative-flowers.tsx`: exportar `DecorativeFlowers({ visible }: { visible: boolean })`. SVG inline de flores com `fill` rosa `var(--color-kreds-rose)` (#C98AA0), `aria-hidden="true"`, `position:absolute`. Quando `!visible`, retornar null (ou display:none). Sem 'use client'.

    `speech-bubble.tsx`: exportar `SpeechBubble({ text, visible }: { text: string; visible: boolean })`. `position:absolute bottom:60`, centralizado horizontalmente (left 50% translateX -50%), `bg var(--color-kreds-card)`, `borderRadius var(--radius-card-md)`, `padding` adequado, texto em Body (14px/500, `var(--color-kreds-text)`). Quando visible, aplicar `animation: 'var(--animate-kreds-bubble)'`; quando `!visible` retornar null. Sem 'use client'.

    `harvest-glow.tsx`: exportar `HarvestGlow({ visible }: { visible: boolean })`. `position:absolute`, `pointerEvents:'none'`, `borderRadius:'50%'`, `background:'radial-gradient(circle, rgba(227,197,124,.4) 0%, transparent 70%)'`, `opacity: visible ? 1 : 0`, `transition:'opacity 0.4s ease'`, posicionado ao redor da planta. Sem 'use client'.
  </action>
  <verify>
    <automated>pnpm test tests/unit/garden-hero.test.tsx 2>&1 | grep -qE "passed|PASS|✓" && grep -q "export function PlantStage" src/components/garden/plant-stage.tsx && grep -q "garden/plant-" src/components/garden/plant-stage.tsx && grep -q "export function DecorativeFlowers" src/components/garden/decorative-flowers.tsx && grep -q "export function SpeechBubble" src/components/garden/speech-bubble.tsx && grep -q "export function HarvestGlow" src/components/garden/harvest-glow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - plant-stage.tsx exporta PlantStage; usa `<img src={...plant-${stage}.png}` (não next/image); droop aplica rotate(-2.5deg) com transformOrigin 50% 94%; pop aplica var(--animate-kreds-pop)
    - decorative-flowers.tsx exporta DecorativeFlowers; SVG usa var(--color-kreds-rose) e aria-hidden; oculto quando visible=false
    - speech-bubble.tsx exporta SpeechBubble; aplica var(--animate-kreds-bubble) quando visible
    - harvest-glow.tsx exporta HarvestGlow; radial-gradient amarelo; opacity 1↔0 por visible
    - `pnpm test tests/unit/garden-hero.test.tsx` passa na parte de DecorativeFlowers (GARD-09)
    - Nenhum componente importa next/image
  </acceptance_criteria>
  <done>Planta, flores de dízimo, speech bubble e glow de colheita implementados (GARD-03, GARD-07, GARD-09).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GardenHero container que compõe todos os elementos</name>
  <files>src/components/garden/garden-hero.tsx</files>
  <behavior>
    - GardenHero renderiza container de 316px com sol (animação kredsSun) e 2 nuvens (kredsDrift1/drift2) presentes no DOM
    - Compõe PlantStage no estágio recebido, WaterTracker com waterCount, SeasonBadge, SpeechBubble (texto contextual), DecorativeFlowers (visible=titheDone) e HarvestGlow (visible=canHarvest)
    - garden-hero.test.tsx passa para GARD-02, GARD-04, GARD-09
  </behavior>
  <read_first>
    - src/components/auth/pin-screen.tsx (seção hero da planta linhas 116-149: padrão de animação CSS var via style, SVG com aria-hidden, container relative)
    - src/components/garden/plant-stage.tsx, water-tracker.tsx, season-badge.tsx, speech-bubble.tsx, decorative-flowers.tsx, harvest-glow.tsx (componentes a compor — criados nas Tasks 1-2 deste plano)
    - .planning/phases/03-child-garden/03-UI-SPEC.md §Hero Garden (medidas: container 316px borderRadius 28px; sol 58px top16 right24; nuvens; morros 260px; chão 52px gradiente #AFC289→#96AB71 borderRadius 999px 999px 0 0; posições absolutas de cada elemento), §Color §paleta específica do jardim
    - .planning/phases/03-child-garden/03-PATTERNS.md (assignment garden-hero)
    - src/app/globals.css (--animate-kreds-sun, --animate-kreds-drift1, --animate-kreds-drift2)
    - tests/unit/garden-hero.test.tsx (contrato)
  </read_first>
  <action>
    Exportar `GardenHero` recebendo props: `stage: 'a'|'b'|'c'|'d'`, `season`, `waterCount: number` (0-4), `titheDone: boolean`, `canHarvest: boolean`, `droop: boolean`, `showBubble: boolean`, `bubbleText: string`, e `children?: ReactNode` (slot para WaterDrops/HarvestButton/PlantStage com pop injetados pelo GardenView do Plano 03 — usar children para permitir override do PlantStage com pop, OU expor prop `pop` e renderizar PlantStage internamente; escolher renderizar PlantStage internamente com prop `pop` opcional e aceitar `children` como overlay extra).

    Container: `position:relative`, `height:316px`, `borderRadius:28px`, `overflow:hidden`, céu via gradiente `linear-gradient(#CFE0D8, #DCE6CC, #CCD8AF)`. Filhos absolute:
    - Sol: círculo 58px `top:16 right:24`, cor sol, `animation:'var(--animate-kreds-sun)'`, aria-hidden.
    - 2 nuvens: pills brancas, `animation:'var(--animate-kreds-drift1)'` e `'var(--animate-kreds-drift2)'`, aria-hidden.
    - 2 morros: círculos 260px `#BBCB9E`/`#A9BA8B` nos cantos inferiores, aria-hidden.
    - Chão: `height:52px` `bottom:0`, gradiente `#AFC289→#96AB71`, `borderRadius:'999px 999px 0 0'`.
    - `<SeasonBadge season={season} />` em `position:absolute top:12 left:12`.
    - `<WaterTracker filled={waterCount} />` em `position:absolute top:12 right:12` (oculto quando canHarvest — ver nota de posicionamento do UI-SPEC; quando canHarvest, o slot do HarvestButton via children ocupa essa posição).
    - `<HarvestGlow visible={canHarvest} />` ao redor da planta.
    - `<PlantStage stage={stage} droop={droop} pop={pop ?? false} />` centralizado embaixo.
    - `<DecorativeFlowers visible={titheDone} />`.
    - `<SpeechBubble text={bubbleText} visible={showBubble} />`.
    - `{children}` renderizado por último (overlay para WaterDrops/HarvestButton do Plano 03).
    Sem 'use client' (componente de composição estático; estado fica no GardenView).
  </action>
  <verify>
    <automated>pnpm test tests/unit/garden-hero.test.tsx 2>&1 | grep -qE "passed|PASS|✓" && grep -q "export function GardenHero" src/components/garden/garden-hero.tsx && grep -q "var(--animate-kreds-sun)" src/components/garden/garden-hero.tsx && grep -q "var(--animate-kreds-drift1)" src/components/garden/garden-hero.tsx</automated>
  </verify>
  <acceptance_criteria>
    - garden-hero.tsx exporta GardenHero; container height 316px borderRadius 28px overflow hidden
    - Sol usa var(--animate-kreds-sun); nuvens usam var(--animate-kreds-drift1) e var(--animate-kreds-drift2)
    - Compõe PlantStage, WaterTracker, SeasonBadge, SpeechBubble, DecorativeFlowers, HarvestGlow com as props corretas
    - Aceita `children` como slot de overlay para o Plano 03
    - `pnpm test tests/unit/garden-hero.test.tsx` passa (GARD-02, GARD-04, GARD-09)
  </acceptance_criteria>
  <done>Hero do jardim compondo todos os elementos visuais; testes GARD-02/04/09 verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → componentes garden | Componentes só renderizados dentro de /child/[id]/garden (protegido por middleware child-session) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | Props dos componentes (nome/coins da criança) | accept | Dados são seed mockado nesta fase (não dados reais por-criança); página protegida por middleware; risco nulo |
| T-03-05 | Tampering | Assets PNG servidos de public/ | accept | Imagens estáticas públicas (planta decorativa); sem conteúdo sensível |
| T-03-01 | Elevation of Privilege | Acesso à rota sem sessão | mitigate | Middleware /child/* já implementado (Fase 2); nenhum código novo |
</threat_model>

<verification>
- `pnpm test tests/unit/garden-header.test.tsx tests/unit/garden-hero.test.tsx tests/unit/garden-season.test.ts` → verde
- `ls src/components/garden/*.tsx` → 8 componentes deste plano
- Nenhum import de next/image em src/components/garden/
</verification>

<success_criteria>
- 8 componentes presentacionais implementados
- Testes GARD-01, GARD-02, GARD-04, GARD-06, GARD-09 verdes
- Animações aplicadas via var(--animate-kreds-*) por style prop (não classes Tailwind)
</success_criteria>

<output>
Create `.planning/phases/03-child-garden/03-02-SUMMARY.md` when done
</output>
