---
phase: 03-child-garden
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema/index.ts
  - drizzle/seed/bible-verses.sql
  - src/lib/seed/garden-seed.ts
  - public/garden/plant-a.png
  - public/garden/plant-b.png
  - public/garden/plant-c.png
  - public/garden/plant-d.png
  - tests/unit/garden-stage.test.ts
  - tests/unit/garden-season.test.ts
  - tests/unit/garden-bubble.test.ts
  - tests/unit/garden-header.test.tsx
  - tests/unit/garden-hero.test.tsx
  - tests/unit/garden-view.test.tsx
  - tests/unit/garden-celebration.test.tsx
autonomous: true
requirements: [GARD-03, GARD-06, GARD-07, GARD-10]
must_haves:
  truths:
    - "D-07: Tabela bible_verses existe no banco com 7 versículos inseridos via migração Drizzle + seed SQL"
    - "D-06: Versículo bíblico servido da tabela bible_verses (sem hardcode)"
    - "D-01: Dados são seed mockado — constantes SEED_* cobrem todos os estados do jardim (stage a–d, harvested, tithe). Nenhuma chamada real ao backend."
    - "D-02: Estrutura das constantes permite testar cada estado sem mudar código — getPlantStage(doneCount, total) recebe params e retorna 'a'|'b'|'c'|'d'"
    - "getPlantStage mapeia doneCount→stage corretamente (0→a, 1→b, 2-3→c, 4→d)"
    - "Os 4 PNGs de planta estão servíveis em /garden/plant-[a-d].png"
    - "Os 7 arquivos de teste de jardim existem e rodam (falhando por componente ausente — RED)"
  artifacts:
    - path: "src/lib/db/schema/index.ts"
      provides: "Tabela bibleVerses (Drizzle pgTable)"
      contains: "export const bibleVerses"
    - path: "src/lib/seed/garden-seed.ts"
      provides: "Constantes de seed + getPlantStage + textos de speech bubble + cores de estação"
      exports: ["GardenSeed", "GardenTask", "SEED_STAGE_A", "SEED_STAGE_B", "SEED_STAGE_C", "SEED_STAGE_D", "SEED_HARVESTED", "SEED_TITHE", "getPlantStage", "getBubbleText", "SEASON_DOT_COLORS"]
    - path: "drizzle/seed/bible-verses.sql"
      provides: "INSERT de 7 versículos sobre mordomia/colheita/generosidade"
      contains: "INSERT INTO bible_verses"
    - path: "public/garden/plant-a.png"
      provides: "Asset estágio inicial da planta"
    - path: "tests/unit/garden-stage.test.ts"
      provides: "Teste de getPlantStage (GARD-03)"
      contains: "getPlantStage"
  key_links:
    - from: "src/lib/db/schema/index.ts"
      to: "drizzle/0008_*.sql"
      via: "drizzle-kit push aplica bibleVerses no banco"
      pattern: "bible_verses"
    - from: "drizzle/seed/bible-verses.sql"
      to: "bible_verses table"
      via: "INSERT após push"
      pattern: "INSERT INTO bible_verses"
---

<objective>
Estabelecer toda a infraestrutura de dados e validação da Fase 3: tabela `bible_verses` no banco (migração + push + seed de 7 versículos), constantes de seed mockado do jardim em TypeScript (cobrindo todos os estados visuais), funções puras de lógica (`getPlantStage`, `getBubbleText`, cores de estação), os 4 assets PNG da planta em `public/garden/`, e os 7 arquivos de teste unitário exigidos pelo VALIDATION.md (Nyquist — escritos antes dos componentes, em estado RED).

Purpose: Plano 02 e 03 dependem do seed TS, das funções puras e dos assets PNG para construir e testar os componentes. O versículo do banco é consumido pelo overlay de celebração (GARD-10). Os test stubs garantem feedback automático contínuo durante toda a fase.
Output: Schema atualizado e aplicado no banco, seed SQL+TS, 4 PNGs servíveis, 7 arquivos de teste rodando.
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-child-garden/03-CONTEXT.md
@.planning/phases/03-child-garden/03-RESEARCH.md
@.planning/phases/03-child-garden/03-PATTERNS.md
@.planning/phases/03-child-garden/03-UI-SPEC.md
@.planning/phases/03-child-garden/03-VALIDATION.md
</context>

<artifacts_produced>
## Artifacts this phase produces (novos símbolos — não tratar como drift)

Criados neste plano:
- `bibleVerses` — Drizzle pgTable export em `src/lib/db/schema/index.ts` (colunas: `id` uuid PK, `reference` text, `text` text, `createdAt` timestamp)
- Tabela SQL `bible_verses` (via drizzle-kit push)
- `GardenSeed`, `GardenTask` — interfaces TypeScript em `src/lib/seed/garden-seed.ts`
- `SEED_STAGE_A`, `SEED_STAGE_B`, `SEED_STAGE_C`, `SEED_STAGE_D`, `SEED_HARVESTED`, `SEED_TITHE` — constantes de seed
- `getPlantStage(doneCount: number, totalTasks: number): 'a'|'b'|'c'|'d'` — função pura
- `getBubbleText(seed: GardenSeed): string` — função pura (textos de speech bubble por estado, GARD-07)
- `SEASON_DOT_COLORS` — record estação→hex (GARD-06)
- Arquivos de teste: `tests/unit/garden-stage.test.ts`, `garden-season.test.ts`, `garden-bubble.test.ts`, `garden-header.test.tsx`, `garden-hero.test.tsx`, `garden-view.test.tsx`, `garden-celebration.test.tsx`
- Diretório `public/garden/` com `plant-a.png`, `plant-b.png`, `plant-c.png`, `plant-d.png`
- Diretório `drizzle/seed/` com `bible-verses.sql`
- Migração gerada `drizzle/0008_*.sql`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Schema bible_verses + seed SQL de 7 versículos</name>
  <files>src/lib/db/schema/index.ts, drizzle/seed/bible-verses.sql</files>
  <read_first>
    - src/lib/db/schema/index.ts (estado atual — imports linhas 1-15 já incluem pgTable, uuid, text, timestamp, sql; padrão de tabela simples `donations` no final do arquivo)
    - drizzle/0007_tidy_cerise.sql (padrão de migração gerada e de SQL com gen_random_uuid())
    - .planning/phases/03-child-garden/03-UI-SPEC.md §Bible Verses — Seed (7 versículos exatos com referência e texto)
  </read_first>
  <action>
    Adicionar ao FINAL de `src/lib/db/schema/index.ts` (após o export `donations`) a tabela `bibleVerses` via `pgTable('bible_verses', {...})` com colunas: `id` = `uuid('id').defaultRandom().primaryKey()`, `reference` = `text('reference').notNull()`, `text` = `text('text').notNull()`, `createdAt` = `timestamp('created_at').defaultNow().notNull()`. Sem FK, sem índices (tabela pequena standalone). Reusar os imports já existentes no topo do arquivo — NÃO adicionar novos imports (pgTable, uuid, text, timestamp já importados).

    Criar `drizzle/seed/bible-verses.sql` com um único statement `INSERT INTO bible_verses (id, reference, text) VALUES (...)` contendo exatamente os 7 versículos do UI-SPEC §Bible Verses Seed (Colossenses 3:23, Provérbios 3:9, 2 Coríntios 9:7, Lucas 6:38, Provérbios 11:24, Gálatas 6:9, Mateus 6:20), usando `gen_random_uuid()` para cada `id` (padrão de todas as migrations do projeto). Texto em PT-BR exatamente como no UI-SPEC. Tornar o seed idempotente: prefixar com `DELETE FROM bible_verses;` para permitir re-execução sem duplicar.
  </action>
  <verify>
    <automated>grep -q "export const bibleVerses" src/lib/db/schema/index.ts && grep -c "INSERT INTO bible_verses" drizzle/seed/bible-verses.sql | grep -q 1 && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -v "garden\|celebration\|water\|plant\|season\|speech\|harvest\|confetti\|decorative" | grep -c "error TS" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/db/schema/index.ts` contém `export const bibleVerses = pgTable('bible_verses'`
    - `bibleVerses` define as 4 colunas: id (uuid PK defaultRandom), reference (text notNull), text (text notNull), createdAt (timestamp defaultNow notNull)
    - `drizzle/seed/bible-verses.sql` contém um `INSERT INTO bible_verses` com 7 tuplas de VALUES, cada uma usando `gen_random_uuid()`
    - As 7 referências bíblicas do UI-SPEC aparecem literalmente no arquivo SQL (`grep -c "Colossenses\|Provérbios\|Coríntios\|Lucas\|Gálatas\|Mateus" drizzle/seed/bible-verses.sql` retorna 7)
    - Nenhum import novo adicionado ao topo de schema/index.ts (linhas 1-15 inalteradas)
  </acceptance_criteria>
  <done>Tabela bibleVerses no schema e seed SQL de 7 versículos prontos para push e aplicação.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: Aplicar schema no banco (drizzle-kit push) + rodar seed</name>
  <files>drizzle/0008_bible_verses.sql</files>
  <read_first>
    - drizzle.config.ts (schema path = ./src/lib/db/schema/index.ts, out = ./drizzle, dialect postgresql)
    - package.json §scripts (db:generate, db:push, db:migrate)
    - drizzle/0007_tidy_cerise.sql (próxima migração será 0008_*)
  </read_first>
  <action>
    Esta é uma tarefa [BLOCKING] obrigatória — a fase NÃO pode passar na verificação sem a tabela existir no banco real (build/type-check passam sem o push porque os tipos vêm do schema TS, não do banco — falso positivo).

    Passo 1: rodar `pnpm db:generate` para gerar `drizzle/0008_*.sql` automaticamente a partir do schema atualizado (drizzle-kit detecta a nova tabela `bible_verses`). NÃO escrever SQL de DDL manualmente.

    Passo 2: aplicar no banco com `pnpm db:push` (drizzle-kit push é não-interativo por padrão; para tabela nova sem conflito não há prompts). Se `db:push` exigir prompt interativo não-suprimível, parar e sinalizar intervenção manual.

    Passo 3: rodar o seed dos versículos via psql: `psql "$DATABASE_URL" -f drizzle/seed/bible-verses.sql`. Confirmar com `psql "$DATABASE_URL" -c "SELECT count(*) FROM bible_verses;"` retornando 7.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT count(*) FROM bible_verses;" | grep -q "^7$"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `drizzle/0008_*.sql` foi gerado por drizzle-kit (não escrito manualmente) e contém `CREATE TABLE "bible_verses"`
    - `psql "$DATABASE_URL" -c "\d bible_verses"` lista a tabela com colunas id, reference, text, created_at
    - `psql "$DATABASE_URL" -tAc "SELECT count(*) FROM bible_verses;"` retorna exatamente `7`
    - `psql "$DATABASE_URL" -tAc "SELECT count(*) FROM bible_verses WHERE reference = 'Colossenses 3:23';"` retorna `1`
  </acceptance_criteria>
  <done>Tabela bible_verses criada no banco e populada com 7 versículos; query RANDOM() do Plano 03 funcionará.</done>
</task>

<task type="auto">
  <name>Task 3: Seed TS do jardim + funções puras + 7 test stubs (Nyquist Wave 0)</name>
  <files>src/lib/seed/garden-seed.ts, public/garden/plant-a.png, public/garden/plant-b.png, public/garden/plant-c.png, public/garden/plant-d.png, tests/unit/garden-stage.test.ts, tests/unit/garden-season.test.ts, tests/unit/garden-bubble.test.ts, tests/unit/garden-header.test.tsx, tests/unit/garden-hero.test.tsx, tests/unit/garden-view.test.tsx, tests/unit/garden-celebration.test.tsx</files>
  <read_first>
    - .planning/phases/03-child-garden/03-RESEARCH.md §Code Examples (estrutura completa de GardenSeed, GardenTask, SEED_STAGE_A..SEED_HARVESTED, getPlantStage)
    - .planning/phases/03-child-garden/03-UI-SPEC.md §Speech Bubble Textos Contextuais (6 estados) e §Season Dot Colors (4 estações) e §Seed Data Contract
    - .planning/phases/03-child-garden/03-VALIDATION.md §Per-Task Verification Map e §Wave 0 Requirements (mapeamento de qual req cada arquivo de teste cobre)
    - tests/unit/ledger-calculate.test.ts (padrão exato de teste de função pura — import + describe/it/expect)
    - tests/unit/child-session-guard.test.ts (padrão de cabeçalho de teste)
  </read_first>
  <action>
    Criar `src/lib/seed/garden-seed.ts` exportando: interfaces `GardenTask` ({ id, title, emoji, done }) e `GardenSeed` ({ childName, initial, coins, tasks: GardenTask[], titheDone, harvested, season }); as 6 constantes `SEED_STAGE_A` (0 done), `SEED_STAGE_B` (1 done), `SEED_STAGE_C` (3 done), `SEED_STAGE_D` (4 done, harvested false), `SEED_HARVESTED` (4 done, harvested true), `SEED_TITHE` (4 done, titheDone true) — usar os 4 BASE_TASKS do RESEARCH.md (arrumar cama, estudar 30min, ajudar na cozinha, ler a Bíblia); `getPlantStage(doneCount, totalTasks)` com regra: 0→'a', 1→'b', <totalTasks→'c', senão 'd'; `getBubbleText(seed)` retornando o texto do UI-SPEC por estado (harvested→texto pós-colheita; titheDone→texto dízimo; senão por stage a/b/c/d); `SEASON_DOT_COLORS` = { primavera:'#5A8A66', verao:'#E3C57C', outono:'#B5623F', inverno:'#6E9BA0' }.

    Copiar os 4 PNGs de `design_handoff_kreds/garden/` para `public/garden/` (criar o diretório): `cp design_handoff_kreds/garden/plant-{a,b,c,d}.png public/garden/`.

    Criar os 7 arquivos de teste do VALIDATION.md. Vitest usa jsdom por padrão (vitest.config.ts), então testes de componente (.tsx) NÃO precisam de diretiva de environment. Os testes de função pura (.ts) importam de `../../src/lib/seed/garden-seed`. Como os componentes ainda não existem (Plano 02/03), os testes de componente devem importar do caminho-alvo do componente e estar em estado RED (falham por módulo ausente) — isso é o esperado para Wave 0 (Nyquist).
    - `garden-stage.test.ts`: GARD-03 — assert `getPlantStage(0,4)==='a'`, `(1,4)==='b'`, `(2,4)==='c'`, `(3,4)==='c'`, `(4,4)==='d'`.
    - `garden-season.test.ts`: GARD-06 — assert `SEASON_DOT_COLORS.primavera==='#5A8A66'` e que existem 4 estações; importar SeasonBadge de `../../src/components/garden/season-badge` e renderizar label.
    - `garden-bubble.test.ts`: GARD-07 — assert `getBubbleText` retorna texto correto para SEED_STAGE_A, SEED_STAGE_D, SEED_TITHE, SEED_HARVESTED.
    - `garden-header.test.tsx`: GARD-01 — render GardenHeader (de `../../src/components/garden/garden-header`) e assert nome, inicial e coins no DOM.
    - `garden-hero.test.tsx`: GARD-02, GARD-04, GARD-09 — render GardenHero/WaterTracker/DecorativeFlowers; assert tracker mostra N dots para filled=N e flores presentes quando titheDone.
    - `garden-view.test.tsx`: GARD-05, GARD-08 — render GardenView (de `../../src/components/garden/garden-view`); assert HarvestButton oculto quando nem todas tarefas done e visível quando canHarvest; assert clicar tarefa avança tracker de água.
    - `garden-celebration.test.tsx`: GARD-10 — render CelebrationOverlay com verse mock; assert texto do versículo, referência e botão "Voltar ao jardim" presentes quando visible.
    NÃO usar flag de watch em nenhum teste. NÃO incluir fenced code completo de implementação nos componentes — testes apenas.
  </action>
  <verify>
    <automated>test -f public/garden/plant-a.png && test -f public/garden/plant-d.png && grep -q "export function getPlantStage" src/lib/seed/garden-seed.ts && grep -q "export function getBubbleText" src/lib/seed/garden-seed.ts && pnpm test tests/unit/garden-stage.test.ts tests/unit/garden-season.test.ts tests/unit/garden-bubble.test.ts 2>&1 | grep -qE "passed|PASS|✓"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/seed/garden-seed.ts` exporta GardenSeed, GardenTask, SEED_STAGE_A..SEED_TITHE (6 constantes), getPlantStage, getBubbleText, SEASON_DOT_COLORS
    - `getPlantStage(0,4)` retorna 'a', `getPlantStage(4,4)` retorna 'd' (verificável via `pnpm test tests/unit/garden-stage.test.ts` verde)
    - `pnpm test tests/unit/garden-bubble.test.ts` e `tests/unit/garden-season.test.ts` passam (funções puras já implementadas)
    - Os 4 arquivos existem: `public/garden/plant-a.png`, `plant-b.png`, `plant-c.png`, `plant-d.png`
    - Todos os 7 arquivos de teste existem em `tests/unit/garden-*.test.*`
    - Testes de componente (.tsx) estão em RED (falham por componente ausente) — esperado para Wave 0; testes de função pura (.ts) estão verdes
    - Nenhum arquivo de teste contém flag `--watch` ou `vitest watch`
  </acceptance_criteria>
  <done>Seed TS + funções puras testadas (verde), assets PNG servíveis, e os 7 stubs de teste prontos para guiar Plano 02/03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /child/[childId]/garden | Rota só acessível com JWT child-session válido (middleware já implementado na Fase 2) |
| Server Component → banco (bible_verses) | Query Drizzle roda server-side; tabela sem dados sensíveis (texto bíblico público) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | Acesso direto a /child/[id]/garden sem sessão | mitigate | Middleware `/child/*` já verifica JWT child-session e redireciona para `/` — testado em tests/unit/middleware.test.ts; nenhum código novo necessário |
| T-03-02 | Information Disclosure | Conteúdo de bible_verses | accept | Tabela contém apenas texto bíblico público (sem PII, sem dado por-criança); risco nulo |
| T-03-03 | Tampering | drizzle-kit push / seed SQL | accept | Sem novos pacotes npm instalados nesta fase (audit de legitimidade vazio em RESEARCH.md); push aplica apenas DDL gerado pelo schema versionado |
</threat_model>

<verification>
- `pnpm test tests/unit/garden-stage.test.ts tests/unit/garden-season.test.ts tests/unit/garden-bubble.test.ts` → verde (funções puras)
- `psql "$DATABASE_URL" -tAc "SELECT count(*) FROM bible_verses;"` → 7
- `ls public/garden/plant-*.png` → 4 arquivos
- `ls tests/unit/garden-*.test.*` → 7 arquivos
</verification>

<success_criteria>
- Tabela bible_verses existe no banco com 7 versículos
- Seed TS exporta 6 constantes + getPlantStage + getBubbleText + SEASON_DOT_COLORS
- 4 PNGs em public/garden/
- 7 arquivos de teste criados; funções puras verdes, componentes RED (esperado)
</success_criteria>

<output>
Create `.planning/phases/03-child-garden/03-01-SUMMARY.md` when done
</output>
