---
phase: 04-child-tasks
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - src/components/garden/garden-view.tsx
autonomous: false
requirements: [CTASK-01, CTASK-02, CTASK-03, CTASK-04, CTASK-05]
must_haves:
  truths:
    - "Toda a Fase 4 é integrada na página única /child/[childId]/garden — sem nova rota /tasks criada (D-01)"
    - "GardenView eleva titheDone para useState(seed.titheDone) e tem handler handleTithe() que seta titheDone=true (CTASK-03, D-11)"
    - "GardenHero recebe titheDone={titheDone} (state), não seed.titheDone — clicar Plantar ativa DecorativeFlowers no hero (Pitfall 1)"
    - "O stub inline de task buttons (linhas ~108-170) é substituído por uma lista de <TaskCard> dentro de #section-tasks (CTASK-01, CTASK-02)"
    - "TitheCard, SavingsCard e BottomNav são renderizados; SavingsCard fica dentro de #section-savings; o hero fica dentro de #section-garden (CTASK-03, CTASK-04, CTASK-05)"
    - "O wrapper raiz mantém paddingBottom: 80 para não ocultar conteúdo atrás do BottomNav (Pitfall 4)"
    - "A suíte completa (npm run test) passa GREEN, incluindo garden-view.test.tsx"
  artifacts:
    - path: "src/components/garden/garden-view.tsx"
      provides: "GardenView integrado com os 4 componentes Fase 4, titheDone interativo e anchors de seção"
      contains: "const [titheDone, setTitheDone]"
  key_links:
    - from: "src/components/garden/garden-view.tsx"
      to: "src/components/tasks/task-card.tsx"
      via: "named import + tasks.map(TaskCard)"
      pattern: "import \\{ TaskCard \\}"
    - from: "src/components/garden/garden-view.tsx"
      to: "DecorativeFlowers (hero)"
      via: "titheDone state passado a GardenHero"
      pattern: "titheDone=\\{titheDone\\}"
---

<objective>
Integrar os 4 componentes da Fase 4 no `GardenView` (componente raiz client existente). Esta é a Wave 3 — depende de TaskCard/TitheCard/SavingsCard (Plano 04-02) e BottomNav (Plano 04-03) já existirem. As mudanças: (1) elevar `titheDone` de `seed.titheDone` read-only para `useState` interativo + handler `handleTithe()` (D-02, D-11); (2) passar `titheDone` (state) ao GardenHero em vez de `seed.titheDone` (Pitfall 1); (3) substituir o stub inline de task buttons (linhas ~108-170) por uma lista de `<TaskCard>`; (4) renderizar `<TitheCard>`, `<SavingsCard>` e `<BottomNav>`; (5) adicionar os anchors de seção `#section-garden`, `#section-tasks`, `#section-savings`; (6) garantir que `paddingBottom: 80` permanece (Pitfall 4). Termina com um checkpoint de verificação visual.

Purpose: Esta é a costura final que conecta todos os componentes ao estado central e torna a página de jardim completa (CTASK-01..05 todos visíveis e funcionais). É o único plano que modifica `garden-view.tsx`, portanto não pode rodar em paralelo com nada que toque esse arquivo. A verificação visual confirma o que os testes unitários não cobrem: layout, fixação do nav, animações e o fluxo "Plantar → flores no hero".
Output: `garden-view.tsx` modificado; suíte completa GREEN; checkpoint visual aprovado.
</objective>

<artifacts_produced>
## Artifacts this phase produces

Símbolos e mudanças produzidas por este plano:

- `titheDone` / `setTitheDone` — novo `useState` em `GardenView` (substitui leitura read-only de `seed.titheDone`)
- `handleTithe` — novo handler em `GardenView` que seta `titheDone = true`
- Imports novos em `garden-view.tsx`: `TaskCard`, `TitheCard`, `SavingsCard`, `BottomNav` de `@/components/tasks/*`
- Anchors DOM novos: `id="section-garden"`, `id="section-tasks"`, `id="section-savings"` (consumidos pelo IntersectionObserver do BottomNav)
- Remoção: stub inline de `<button>` de task (linhas ~108-170 do estado atual)
</artifacts_produced>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-child-tasks/04-CONTEXT.md
@.planning/phases/04-child-tasks/04-RESEARCH.md
@.planning/phases/04-child-tasks/04-PATTERNS.md
@.planning/phases/04-child-tasks/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrar componentes Fase 4 no GardenView (titheDone state, TaskCard list, cards, BottomNav, anchors)</name>
  <behavior>
    - garden-view.test.tsx continua passando: HarvestButton oculto em SEED_STAGE_C, visível em SEED_STAGE_D; clicar em "Ler a Bíblia" avança o tracker de água para 4 (a tarefa pendente ainda deve ser clicável via TaskCard).
    - Após a integração, a suíte completa (`npm run test`) passa GREEN (garden-view.test.tsx + child-tasks.test.tsx + bottom-nav.test.tsx + demais).
    - O fluxo de dízimo: o `titheDone` é state; clicar Plantar (via TitheCard) seta o state e o GardenHero recebe o novo valor.
  </behavior>
  <read_first>
    - src/components/garden/garden-view.tsx (ARQUIVO ALVO — ler inteiro: useState block linhas 30-35, handlers 45-63, GardenHero linha 88-105 com titheDone={seed.titheDone} na linha 92, stub de tasks linhas 108-170, paddingBottom: 80 na linha 72)
    - src/components/tasks/task-card.tsx (props: { task, onComplete } — criado no Plano 04-02)
    - src/components/tasks/tithe-card.tsx (props: { done, onPlant } — criado no Plano 04-02)
    - src/components/tasks/savings-card.tsx (props: { savings, goal } — criado no Plano 04-02)
    - src/components/tasks/bottom-nav.tsx (sem props; observa #section-garden/#section-tasks/#section-savings — criado no Plano 04-03)
    - src/components/garden/garden-hero.tsx (confirma a prop titheDone consumida e passada a DecorativeFlowers)
    - .planning/phases/04-child-tasks/04-PATTERNS.md (seção `src/components/garden/garden-view.tsx` — diffs exatos de cada mudança)
    - .planning/phases/04-child-tasks/04-UI-SPEC.md (seção "Page Layout Extension" + "GardenView State Extension" — anchors e ordem das seções)
    - .planning/phases/04-child-tasks/04-RESEARCH.md (Pitfall 1: titheDone state; Pitfall 4: paddingBottom 80; Pitfall 5: re-click)
    - tests/unit/garden-view.test.tsx (contrato existente que NÃO pode quebrar)
  </read_first>
  <files>src/components/garden/garden-view.tsx</files>
  <action>
    Modificar `src/components/garden/garden-view.tsx` (manter `'use client'` e tudo o que já funciona da Fase 3). Mudanças:

    1. IMPORTS (após os imports de garden/* existentes, linhas 5-9): adicionar
       `import { TaskCard } from '@/components/tasks/task-card'`
       `import { TitheCard } from '@/components/tasks/tithe-card'`
       `import { SavingsCard } from '@/components/tasks/savings-card'`
       `import { BottomNav } from '@/components/tasks/bottom-nav'`

    2. STATE (no bloco de useState, após linha 35): adicionar `const [titheDone, setTitheDone] = useState(seed.titheDone)` (Pitfall 1 / D-02).

    3. HANDLER (após `handleHarvest`, ~linha 58): adicionar `function handleTithe() { setTitheDone(true) }` (D-11, CTASK-03). Não fazer fetch/POST (D-12).

    4. GARDEN HERO (linha 92): trocar `titheDone={seed.titheDone}` por `titheDone={titheDone}` (Pitfall 1 — usar state, não seed).

    5. SECTION ANCHORS:
       - Envolver o bloco do hero (o `<div style={{ padding: '0 16px' }}>` que contém `<GardenHero>`, linhas ~87-106) com `id="section-garden"` (adicionar o id ao div existente do hero OU envolver header+hero). Garantir que o IntersectionObserver do BottomNav encontre `#section-garden`.

    6. SUBSTITUIR O STUB DE TASKS (linhas ~108-170): remover TODO o `<div>` que mapeia `<button>` inline por task (incluindo o checkbox visual de 22px, opacity, line-through). Substituir por:
       ```
       <ul id="section-tasks" aria-label="Lista de tarefas" style={{ listStyle: 'none', margin: 0, padding: '0 16px', display: 'flex', flexDirection: 'column', gap: 8 }}>
         {tasks.map((task) => (
           <li key={task.id}>
             <TaskCard task={task} onComplete={handleTaskComplete} />
           </li>
         ))}
       </ul>
       ```
       (UI-SPEC Accessibility: lista é `<ul aria-label="Lista de tarefas">` com `<li>` por item. NÃO chamar handleTaskComplete em re-click — TaskCard já guarda `!task.done`, Pitfall 5.)

    7. NOVOS CARDS + NAV (após a lista de tarefas, antes do CelebrationOverlay): adicionar
       ```
       <TitheCard done={titheDone} onPlant={handleTithe} />
       <div id="section-savings">
         <SavingsCard savings={seed.savings} goal={seed.goal} />
       </div>
       <BottomNav />
       ```
       (D-10/D-13/D-03 — ordem: tasks → tithe → savings → nav.)

    8. PADDING: confirmar que o wrapper raiz mantém `paddingBottom: 80` (linha 72 — NÃO remover, Pitfall 4). Opcional: aumentar o `gap` entre seções para 32 conforme UI-SPEC "Section gap" se o layout exigir — manter dentro do design tokens existentes.

    Não alterar `handleTaskComplete`, `handleHarvest`, `handleCloseOverlay`, nem a lógica de derivados (doneCount, stage, canHarvest). Não alterar GardenHeader, WaterDrops, HarvestButton, CelebrationOverlay.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && npx tsc --noEmit && npm run test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `garden-view.tsx` contém `import { TaskCard }`, `import { TitheCard }`, `import { SavingsCard }`, `import { BottomNav }`
    - `garden-view.tsx` contém `const [titheDone, setTitheDone] = useState(seed.titheDone)` e `function handleTithe()`
    - `grep -c 'titheDone={titheDone}' src/components/garden/garden-view.tsx` >= 1 (e `grep -c 'titheDone={seed.titheDone}'` == 0)
    - `garden-view.tsx` contém `id="section-garden"`, `id="section-tasks"`, `id="section-savings"`
    - `garden-view.tsx` contém `<TaskCard`, `<TitheCard`, `<SavingsCard`, `<BottomNav`
    - O stub antigo foi removido: `grep -c 'line-through' src/components/garden/garden-view.tsx` == 0 e o `<button>` inline de task não existe mais (a renderização de task é via `<TaskCard>`)
    - `garden-view.tsx` mantém `paddingBottom: 80`
    - `npx tsc --noEmit` sai com código 0
    - `npm run test` passa 100% GREEN (toda a suíte: garden-view, child-tasks, bottom-nav e demais)
  </acceptance_criteria>
  <done>GardenView integra os 4 componentes, titheDone é interativo, anchors de seção presentes, stub removido, paddingBottom preservado; suíte completa GREEN; tsc verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A página `/child/[childId]/garden` agora exibe, em scroll vertical: hero do jardim (Fase 3) → lista de TaskCards → TitheCard (Dízimo) → SavingsCard (Cofrinho) → BottomNav fixo. Todos os 4 componentes da Fase 4 (CTASK-01..05) estão integrados ao GardenView com estado central. As verificações automatizadas (tsc + suíte completa de testes) já passaram nas tasks anteriores; este checkpoint confirma o que os testes unitários não cobrem: layout visual, fixação do nav, animações e o fluxo "Plantar → flores no hero".
  </what-built>
  <how-to-verify>
    1. Rodar o servidor de desenvolvimento: `cd /Users/hass/repos/github/comphass/kreds && npm run dev`.
    2. Autenticar como criança e abrir `/child/[childId]/garden` (use o fluxo de PIN da Fase 2; o seed mockado é SEED_STAGE_C por padrão na garden page).
    3. TASK CARDS (CTASK-01/02): confirmar que tarefas pendentes têm fundo branco e tarefas concluídas têm fundo verde suave (#EEF3EA). Clicar no check (círculo 38×38) de uma tarefa pendente — deve marcar (bg verde #3E6B4F + check branco), disparar os water drops + pop na planta, e a tarefa concluída NÃO deve responder a novo clique.
    4. DÍZIMO (CTASK-03): clicar no botão "Plantar" (gradiente rosa). O botão deve mudar para "Feito ✓" (bg #B07E91, desabilitado) E as flores decorativas devem aparecer no hero do jardim (DecorativeFlowers). Este é o teste-chave do Pitfall 1.
    5. COFRINHO (CTASK-04): confirmar que o card exibe "Cofrinho", "R$ 25" e "Meta: R$ 100", e que a progress bar anima da esquerda (de 0 até 25%) ao carregar/scrollar até ela.
    6. BOTTOM NAV (CTASK-05): confirmar a barra fixa de 80px no rodapé com 4 ícones. Ao scrollar a página, o ícone ativo (verde #3E6B4F) deve mudar conforme a seção visível (Jardim → Tarefas → Cofrinho). Clicar num ícone deve fazer scroll suave até a seção. "Doar" não deve responder (placeholder). Confirmar que o BottomNav NÃO oculta o conteúdo final do SavingsCard (paddingBottom 80).
  </how-to-verify>
  <resume-signal>Digite "approved" se todos os 6 pontos funcionam, ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passa
- `npm run test` passa 100% GREEN (suíte completa: todos os arquivos de teste, incluindo os GREEN das fases anteriores)
- `garden-view.tsx`: titheDone é useState; GardenHero recebe `titheDone={titheDone}` (não seed); stub de task removido; TaskCard/TitheCard/SavingsCard/BottomNav renderizados; anchors de seção presentes; paddingBottom 80 mantido
- Checkpoint visual aprovado: layout, toggle de tarefa + drops, Plantar → flores no hero, progress bar anima, bottom nav active state + scroll anchor
</verification>

<success_criteria>
- CTASK-01..05 todos integrados e visíveis na página de jardim ✓
- titheDone interativo (Pitfall 1 resolvido — flores aparecem ao Plantar) ✓
- Stub inline de tasks removido e substituído por TaskCard (State of the Art) ✓
- paddingBottom 80 preservado (Pitfall 4) ✓
- Suíte completa GREEN; tsc verde; verificação visual aprovada
</success_criteria>

<output>
Create `.planning/phases/04-child-tasks/04-04-SUMMARY.md` when done
</output>
