---
phase: 04-child-tasks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seed/garden-seed.ts
  - tests/setup.ts
  - tests/unit/child-tasks.test.tsx
  - tests/unit/bottom-nav.test.tsx
autonomous: true
requirements: [CTASK-01, CTASK-02, CTASK-03, CTASK-04, CTASK-05]
must_haves:
  truths:
    - "A interface GardenSeed expõe os campos savings e goal"
    - "Todas as constantes de seed têm savings: 25 e goal: 100"
    - "Os testes de TaskCard, TitheCard, SavingsCard e BottomNav existem e descrevem o comportamento esperado (RED até os componentes serem implementados)"
    - "IntersectionObserver está mockado globalmente no ambiente jsdom"
  artifacts:
    - path: "src/lib/seed/garden-seed.ts"
      provides: "Interface GardenSeed estendida com savings/goal (D-15)"
      contains: "savings: number"
    - path: "tests/unit/child-tasks.test.tsx"
      provides: "Testes RED para TaskCard (CTASK-01/02), TitheCard (CTASK-03), SavingsCard (CTASK-04)"
      min_lines: 60
    - path: "tests/unit/bottom-nav.test.tsx"
      provides: "Testes RED para BottomNav (CTASK-05)"
      min_lines: 25
    - path: "tests/setup.ts"
      provides: "Mock global de IntersectionObserver para jsdom"
      contains: "IntersectionObserver"
  key_links:
    - from: "tests/unit/child-tasks.test.tsx"
      to: "src/components/tasks/task-card.tsx"
      via: "named import TaskCard"
      pattern: "from '.*tasks/task-card'"
    - from: "src/lib/seed/garden-seed.ts"
      to: "SavingsCard / GardenView"
      via: "savings/goal fields consumed downstream"
      pattern: "savings: number"
---

<objective>
Estabelecer a fundação de dados e o scaffolding de testes (Wave 0) para a Fase 4. Este plano estende a interface `GardenSeed` com os campos financeiros `savings`/`goal` (D-15), cria os arquivos de teste falhando (RED) que descrevem o contrato de comportamento de todos os 4 novos componentes, e adiciona o mock de `IntersectionObserver` necessário para o ambiente jsdom.

Purpose: Os testes RED definem o contrato que os planos 02, 03 e 04 devem satisfazer. A extensão de `GardenSeed` é a fundação compartilhada que `SavingsCard` (Plano 03) e `GardenView` (Plano 04) consomem. Fazer isso primeiro evita que executores downstream tenham que adivinhar a forma dos dados ou o comportamento esperado.
Output: `garden-seed.ts` estendido, `tests/setup.ts` com mock de IntersectionObserver, e dois arquivos de teste RED (`child-tasks.test.tsx`, `bottom-nav.test.tsx`).
</objective>

<artifacts_produced>
## Artifacts this phase produces

Símbolos e arquivos criados/modificados por este plano (a serem excluídos de drift verification):

- Campos novos na interface `GardenSeed`: `savings: number`, `goal: number` (em `src/lib/seed/garden-seed.ts`)
- Mock global `IntersectionObserver` em `tests/setup.ts`
- Arquivo novo `tests/unit/child-tasks.test.tsx`
- Arquivo novo `tests/unit/bottom-nav.test.tsx`
- Imports antecipados (RED) de: `TaskCard` (`@/components/tasks/task-card`), `TitheCard` (`@/components/tasks/tithe-card`), `SavingsCard` (`@/components/tasks/savings-card`), `BottomNav` (`@/components/tasks/bottom-nav`) — esses componentes só existem após Planos 02/03.
</artifacts_produced>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-child-tasks/04-CONTEXT.md
@.planning/phases/04-child-tasks/04-RESEARCH.md
@.planning/phases/04-child-tasks/04-PATTERNS.md
@.planning/phases/04-child-tasks/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender interface GardenSeed com savings e goal (D-15)</name>
  <read_first>
    - src/lib/seed/garden-seed.ts (estado atual: interface GardenSeed nas linhas 11-19; constantes SEED_STAGE_A/B/C/D, SEED_HARVESTED, SEED_TITHE nas linhas 29-92)
    - .planning/phases/04-child-tasks/04-UI-SPEC.md (seção "GardenSeed Interface Extension")
  </read_first>
  <files>src/lib/seed/garden-seed.ts</files>
  <action>
    Adicionar dois campos à interface `GardenSeed` (após `season`): `savings: number` e `goal: number`. Adicionar a cada uma das SEIS constantes existentes (`SEED_STAGE_A`, `SEED_STAGE_B`, `SEED_STAGE_C`, `SEED_STAGE_D`, `SEED_HARVESTED`, `SEED_TITHE`) os valores `savings: 25` e `goal: 100` (D-15). Não alterar nenhum outro campo das constantes nem as funções auxiliares (`getPlantStage`, `getBubbleText`, `SEASON_DOT_COLORS`). Manter os comentários de cabeçalho existentes.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && npx tsc --noEmit && grep -c 'savings: 25' src/lib/seed/garden-seed.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/seed/garden-seed.ts` contém `savings: number` e `goal: number` na interface GardenSeed
    - `grep -c 'savings: 25' src/lib/seed/garden-seed.ts` retorna 6 (uma por constante)
    - `grep -c 'goal: 100' src/lib/seed/garden-seed.ts` retorna 6
    - `npx tsc --noEmit` sai com código 0 (nenhum erro de tipo introduzido)
  </acceptance_criteria>
  <done>Interface GardenSeed expõe savings/goal e todas as 6 constantes de seed têm savings: 25, goal: 100; tsc passa.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar mock global de IntersectionObserver ao setup de testes</name>
  <read_first>
    - tests/setup.ts (estado atual: apenas `import '@testing-library/jest-dom/vitest'`)
    - vitest.config.ts (confirma `globals: true` — `vi` disponível globalmente)
    - .planning/phases/04-child-tasks/04-RESEARCH.md (Pitfall 2: IntersectionObserver não existe no jsdom)
  </read_first>
  <files>tests/setup.ts</files>
  <action>
    Adicionar ao `tests/setup.ts` (após o import de jest-dom) um mock global de `IntersectionObserver`. O mock deve ser uma classe com métodos `observe`, `unobserve` e `disconnect` como `vi.fn()`, e um construtor que armazena a `callback` (tipo `IntersectionObserverCallback`) em `public callback` para permitir invocação manual nos testes do BottomNav. Atribuir a `globalThis.IntersectionObserver` com cast `as unknown as typeof IntersectionObserver`. Como `vitest.config.ts` tem `globals: true`, `vi` está disponível globalmente sem import. Não remover o import existente de jest-dom.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && grep -c 'IntersectionObserver' tests/setup.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tests/setup.ts` contém `globalThis.IntersectionObserver`
    - `tests/setup.ts` contém os métodos `observe`, `unobserve`, `disconnect`
    - `tests/setup.ts` mantém o `import '@testing-library/jest-dom/vitest'`
    - `npx tsc --noEmit` sai com código 0
  </acceptance_criteria>
  <done>Mock de IntersectionObserver disponível globalmente no ambiente de teste; jest-dom preservado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar testes RED para os 4 componentes da Fase 4</name>
  <behavior>
    child-tasks.test.tsx:
    - TaskCard (CTASK-01): renderiza título e emoji da tarefa; tarefa pendente NÃO tem o atributo `aria-pressed="true"`; tarefa concluída tem o checkmark/estado marcado (verificar via aria-pressed ou aria-label "Tarefa concluída").
    - TaskCard (CTASK-02): clicar no check de uma tarefa pendente chama `onComplete` com `task.id`; clicar em tarefa já concluída NÃO chama `onComplete` (botão disabled / pointer-events none).
    - TitheCard (CTASK-03): renderiza título "Dízimo"; com `done=false` o botão exibe "Plantar" e clicar chama `onPlant`; com `done=true` o botão exibe "Feito ✓" e está desabilitado (aria-disabled).
    - SavingsCard (CTASK-04): renderiza "Cofrinho", o valor salvo "R$ 25" e a meta "R$ 100"; existe um elemento `role="progressbar"` com `aria-valuenow={savings}`, `aria-valuemax={goal}`, `aria-valuemin={0}`.
    bottom-nav.test.tsx:
    - BottomNav (CTASK-05): renderiza 4 botões com labels acessíveis "Jardim", "Tarefas", "Cofrinho", "Doar"; "Jardim" está ativo por padrão (aria-current="page"); o item "Doar" tem `aria-disabled="true"`.
  </behavior>
  <read_first>
    - tests/unit/garden-view.test.tsx (analog exato: padrão de imports relativos `../../src/`, describe em português, render/screen/fireEvent)
    - tests/unit/garden-header.test.tsx (analog: testes simples de render + toBeInTheDocument)
    - .planning/phases/04-child-tasks/04-PATTERNS.md (seções `tests/unit/child-tasks.test.tsx` e `tests/unit/bottom-nav.test.tsx` — padrões de teste e cobertura)
    - .planning/phases/04-child-tasks/04-UI-SPEC.md (seções Component Inventory e Accessibility — contratos de aria-label, role, labels de copy)
    - src/lib/seed/garden-seed.ts (após Task 1 — usar SEED_STAGE_C ou BASE_TASKS para construir props de tarefa; savings/goal disponíveis)
  </read_first>
  <files>tests/unit/child-tasks.test.tsx, tests/unit/bottom-nav.test.tsx</files>
  <action>
    Criar `tests/unit/child-tasks.test.tsx` cobrindo TaskCard (CTASK-01, CTASK-02), TitheCard (CTASK-03) e SavingsCard (CTASK-04), e `tests/unit/bottom-nav.test.tsx` cobrindo BottomNav (CTASK-05), conforme o bloco `<behavior>` acima. Seguir o padrão do analog `garden-view.test.tsx`: `import { describe, it, expect, vi } from 'vitest'`, `import { render, screen, fireEvent } from '@testing-library/react'`, imports de componentes via caminho relativo `../../src/components/tasks/...`. Descrições de `describe`/`it` em português. Os imports de `TaskCard`, `TitheCard`, `SavingsCard`, `BottomNav` apontam para componentes que ainda NÃO existem (Planos 02/03) — os testes DEVEM falhar nesta fase (RED). Para props de tarefa, importar `SEED_STAGE_C` de `../../src/lib/seed/garden-seed` e derivar uma tarefa pendente e uma concluída, ou montar objetos `GardenTask` literais (`{ id, title, emoji, done }`). Usar handlers `vi.fn()` para `onComplete`/`onPlant`. Assertions via `screen.getByText`, `screen.getByRole`, `screen.getByLabelText`. Não inventar APIs além das definidas no UI-SPEC (props: TaskCard `{task, onComplete}`, TitheCard `{done, onPlant}`, SavingsCard `{savings, goal}`, BottomNav sem props).
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && npm run test -- tests/unit/child-tasks.test.tsx tests/unit/bottom-nav.test.tsx 2>&1 | grep -E 'Cannot find module|failed|FAIL' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/child-tasks.test.tsx` existe e contém blocos describe para TaskCard, TitheCard e SavingsCard
    - `tests/unit/bottom-nav.test.tsx` existe e contém describe para BottomNav
    - `grep -c "from '../../src/components/tasks/task-card'" tests/unit/child-tasks.test.tsx` >= 1
    - Rodar os testes resulta em FALHA por módulo não encontrado (componentes ainda não implementados) — estado RED esperado
    - As assertions referenciam os textos exatos do UI-SPEC: "Dízimo", "Plantar", "Feito ✓", "Cofrinho", "Jardim", "Tarefas", "Doar"
  </acceptance_criteria>
  <done>Dois arquivos de teste criados descrevendo o contrato completo de CTASK-01..05; testes falham por componentes ausentes (RED), confirmando o contrato antes da implementação.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passa (interface estendida e mock tipados corretamente)
- `garden-seed.ts` tem exatamente 6 ocorrências de `savings: 25` e 6 de `goal: 100`
- `tests/setup.ts` tem mock de IntersectionObserver com observe/unobserve/disconnect
- Os dois arquivos de teste existem e falham com "Cannot find module" para os componentes de `src/components/tasks/` (RED esperado)
</verification>

<success_criteria>
- Interface GardenSeed expõe savings/goal; todas as constantes atualizadas (D-15)
- Mock de IntersectionObserver disponível globalmente (resolve Pitfall 2 do RESEARCH)
- Contrato de comportamento de CTASK-01..05 codificado em testes RED
- tsc verde; testes em estado RED documentado
</success_criteria>

<output>
Create `.planning/phases/04-child-tasks/04-01-SUMMARY.md` when done
</output>
