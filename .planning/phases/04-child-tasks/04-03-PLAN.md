---
phase: 04-child-tasks
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/tasks/bottom-nav.tsx
autonomous: true
requirements: [CTASK-05]
must_haves:
  truths:
    - "BottomNav é um <nav> fixo de 80px com fundo rgba(248,247,242,.93), backdrop-filter blur(8px) e border-top 1px solid #E7E2D6 (CTASK-05, D-03/D-04)"
    - "BottomNav renderiza 4 itens com labels acessíveis: Jardim, Tarefas, Cofrinho, Doar (CTASK-05, D-03)"
    - "Por padrão (no load) 'Jardim' está ativo (aria-current=page) com cor #3E6B4F; itens inativos usam #9AA092 (D-06)"
    - "'Doar' tem aria-disabled=true e onClick no-op (D-05)"
    - "IntersectionObserver observa #section-garden/#section-tasks/#section-savings com cleanup disconnect (D-06)"
    - "Os testes de bottom-nav.test.tsx passam (GREEN)"
  artifacts:
    - path: "src/components/tasks/bottom-nav.tsx"
      provides: "Componente BottomNav fixo com IntersectionObserver e scroll anchor (CTASK-05)"
      contains: "export function BottomNav"
  key_links:
    - from: "src/components/tasks/bottom-nav.tsx"
      to: "DOM sections #section-garden/#section-tasks/#section-savings"
      via: "IntersectionObserver.observe + scrollIntoView"
      pattern: "getElementById\\('section-"
    - from: "tests/unit/bottom-nav.test.tsx"
      to: "src/components/tasks/bottom-nav.tsx"
      via: "named import BottomNav (RED → GREEN)"
      pattern: "from '.*tasks/bottom-nav'"
---

<objective>
Implementar o `BottomNav` (CTASK-05): barra de navegação fixa de 80px no rodapé com 4 ícones (Jardim, Tarefas, Cofrinho, Doar). O ícone ativo é determinado por `IntersectionObserver` observando as seções `#section-garden`, `#section-tasks` e `#section-savings`; "Jardim" é ativo por padrão. Clicar num item (exceto "Doar") faz `scrollIntoView({ behavior: 'smooth' })` na seção correspondente. "Doar" é placeholder desabilitado (sem rota nesta fase). Este componente satisfaz o contrato dos testes RED de `tests/unit/bottom-nav.test.tsx` (criados no Plano 04-01), tornando-os GREEN.

Purpose: BottomNav é o único componente da fase que usa Web API de browser (IntersectionObserver) e é o que exige o mock jsdom criado no Plano 04-01. Mantê-lo em arquivo exclusivo (sem overlap com TaskCard/TitheCard/SavingsCard do Plano 04-02) permite execução paralela na Wave 2. O Plano 04-04 (integração) depende deste componente existir e fornecerá os `id` de seção no GardenView.
Output: Novo arquivo `src/components/tasks/bottom-nav.tsx`. Após este plano, `bottom-nav.test.tsx` passa GREEN.
</objective>

<artifacts_produced>
## Artifacts this phase produces

Símbolos e arquivos criados por este plano:

- `BottomNav` — `export function BottomNav()` (sem props) em `src/components/tasks/bottom-nav.tsx`
- IDs de seção consumidos (DOM): `section-garden`, `section-tasks`, `section-savings` — estes anchors serão ADICIONADOS ao GardenView no Plano 04-04 (BottomNav apenas os referencia via getElementById, com guards para ausência)
</artifacts_produced>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-child-tasks/04-CONTEXT.md
@.planning/phases/04-child-tasks/04-RESEARCH.md
@.planning/phases/04-child-tasks/04-PATTERNS.md
@.planning/phases/04-child-tasks/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar BottomNav fixo com 4 itens e estados acessíveis (CTASK-05)</name>
  <behavior>
    - CTASK-05: BottomNav renderiza 4 botões com labels acessíveis "Jardim", "Tarefas", "Cofrinho", "Doar".
    - CTASK-05: Por padrão "Jardim" está ativo — `aria-current="page"` e cor `#3E6B4F`; os demais usam `#9AA092`.
    - CTASK-05: "Doar" tem `aria-disabled="true"`, `tabIndex={-1}` e onClick no-op (sem scroll, sem rota).
    - O elemento raiz é `<nav aria-label="Navegação principal">` com `position: fixed`, `bottom: 0`, `height: 80`.
  </behavior>
  <read_first>
    - src/components/garden/water-drops.tsx (analog: componente client sem props externas; padrão de useEffect)
    - src/components/garden/garden-view.tsx (confirma que o wrapper raiz usa flex/column SEM transform — position:fixed seguro; e que `paddingBottom: 80` já existe na linha 72)
    - .planning/phases/04-child-tasks/04-UI-SPEC.md (seção "BottomNav" — specs de posição, cor, 4 itens, ícones SVG, z-index 40, max-width 392)
    - .planning/phases/04-child-tasks/04-PATTERNS.md (seção `src/components/tasks/bottom-nav.tsx` — core pattern)
    - .planning/phases/04-child-tasks/04-RESEARCH.md (Pattern 1: IntersectionObserver com useEffect + cleanup; Anti-Patterns)
    - tests/unit/bottom-nav.test.tsx (contrato RED de BottomNav)
  </read_first>
  <files>src/components/tasks/bottom-nav.tsx</files>
  <action>
    Criar `src/components/tasks/bottom-nav.tsx` como client component (`'use client'`). Importar `import { useEffect, useState } from 'react'`. Sem props. Definir `type Section = 'garden' | 'tasks' | 'savings'` e `const [active, setActive] = useState<Section>('garden')` (D-06: Jardim ativo por padrão).

    Renderizar `<nav aria-label="Navegação principal">` com estilo (D-04 / UI-SPEC): `position: 'fixed'`, `bottom: 0`, `left: 0`, `right: 0`, `height: 80`, `background: 'rgba(248,247,242,0.93)'`, `backdropFilter: 'blur(8px)'`, `WebkitBackdropFilter: 'blur(8px)'`, `borderTop: '1px solid #E7E2D6'`, `display: 'flex'`, `alignItems: 'center'`, `justifyContent: 'space-around'`, `zIndex: 40` (abaixo do CelebrationOverlay), `maxWidth: 392`, `margin: '0 auto'`.

    Definir um array de 4 itens com `{ key, label, section?, disabled? }`:
    - `{ key: 'garden', label: 'Jardim', section: 'section-garden' }`
    - `{ key: 'tasks', label: 'Tarefas', section: 'section-tasks' }`
    - `{ key: 'savings', label: 'Cofrinho', section: 'section-savings' }`
    - `{ key: 'donate', label: 'Doar', disabled: true }`

    Cada item é um `<button>` (com `<svg>` de ícone 24×24 viewBox, strokeWidth 1.5, e `<span>` do label):
    - Cor do item (ativo vs inativo): item é ativo quando `active === item.key` (mapear 'donate' nunca ativo). Ativo: stroke/color `#3E6B4F`, label `fontWeight: 700`. Inativo: stroke/color `#9AA092`, label `fontWeight: 600`. Label `fontSize: 12`.
    - `aria-label={item.label}`
    - Item ativo (não-disabled): `aria-current="page"`.
    - Itens navegáveis: `onClick={() => item.section && document.getElementById(item.section)?.scrollIntoView({ behavior: 'smooth', block: 'start' })}` (D-05).
    - Item "Doar": `disabled` visual via `aria-disabled="true"`, `tabIndex={-1}`, `onClick` no-op (sem scroll, sem href), `cursor: 'default'`. Visualmente idêntico a inativo (#9AA092).

    Ícones SVG inline (Claude's Discretion / UI-SPEC "Icon spec", todos 24×24 viewBox, strokeWidth 1.5, `stroke="currentColor"` para herdar a cor ativa/inativa): Jardim = folha/planta; Tarefas = checklist; Cofrinho = porquinho/moeda; Doar = mão-coração/presente. Usar `stroke="currentColor"` e setar `color` no botão para que o estado ativo/inativo controle a cor do ícone.

    IntersectionObserver (D-06 / Pattern 1, dentro de `useEffect` com deps `[]`):
    - Buscar `document.getElementById('section-garden' | 'section-tasks' | 'section-savings')`.
    - Construir um `Map<Element, Section>` apenas com os elementos que existem (guard `if (el) map.set(...)`).
    - `const observer = new IntersectionObserver(callback, { threshold: [0, 0.1] })`. No callback, para cada `entry.isIntersecting`, `setActive(map.get(entry.target))`.
    - `map.forEach((_, el) => observer.observe(el))`.
    - `return () => observer.disconnect()` (cleanup obrigatório — Anti-Pattern #2 do RESEARCH).
    - NÃO recriar o observer em re-render (deps `[]`). NÃO esquecer guards: as seções podem não existir no ambiente de teste (jsdom) — getElementById retorna null e o Map fica vazio; o componente deve renderizar normalmente com "Jardim" ativo por padrão.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && npx tsc --noEmit && npm run test -- tests/unit/bottom-nav.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/tasks/bottom-nav.tsx` existe e contém `export function BottomNav`
    - Arquivo contém os literais `Jardim`, `Tarefas`, `Cofrinho`, `Doar`
    - Arquivo contém `position: 'fixed'`, `height: 80`, `'rgba(248,247,242,0.93)'`, `'blur(8px)'`, `'1px solid #E7E2D6'`
    - Arquivo contém `aria-current`, `aria-disabled`, `'#3E6B4F'`, `'#9AA092'`
    - Arquivo contém `IntersectionObserver`, `getElementById('section-garden')`, `observer.disconnect()`, `scrollIntoView`
    - `npx tsc --noEmit` sai com código 0
    - `npm run test -- tests/unit/bottom-nav.test.tsx` passa 100% GREEN
  </acceptance_criteria>
  <done>BottomNav implementa CTASK-05 (4 itens, fixo 80px, blur, active via IntersectionObserver com cleanup, Doar aria-disabled); bottom-nav.test.tsx GREEN; tsc verde.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passa
- `npm run test -- tests/unit/bottom-nav.test.tsx` passa 100% GREEN (usa o mock de IntersectionObserver do Plano 04-01)
- BottomNav é `position: fixed` 80px com blur e border-top corretos (D-04)
- "Jardim" ativo por padrão; "Doar" aria-disabled; cores ativo #3E6B4F / inativo #9AA092 (D-06)
- IntersectionObserver tem cleanup `disconnect()` e deps `[]` (sem recriação por re-render)
</verification>

<success_criteria>
- CTASK-05 satisfeito: 4 ícones, fixo 80px, scroll anchor, active state via IntersectionObserver ✓
- Guards para seções ausentes (jsdom) — componente renderiza com Jardim ativo sem crashar ✓
- Segue padrão Fase 1-3: 'use client', inline style, SVG inline, sem dependências novas
- bottom-nav.test.tsx GREEN; tsc verde
</success_criteria>

<output>
Create `.planning/phases/04-child-tasks/04-03-SUMMARY.md` when done
</output>
