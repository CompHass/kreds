---
phase: 04-child-tasks
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/tasks/task-card.tsx
  - src/components/tasks/tithe-card.tsx
  - src/components/tasks/savings-card.tsx
autonomous: true
requirements: [CTASK-01, CTASK-02, CTASK-03, CTASK-04]
must_haves:
  truths:
    - "TaskCard pendente renderiza com fundo branco; concluída renderiza com fundo #EEF3EA (CTASK-01)"
    - "Clicar no check de uma tarefa pendente chama onComplete(task.id); tarefa concluída não dispara onComplete (CTASK-02)"
    - "TitheCard exibe título 'Dízimo' e botão 'Plantar'; após done=true o botão exibe 'Feito ✓' e fica desabilitado (CTASK-03)"
    - "SavingsCard exibe 'Cofrinho', 'R$ 25' e 'R$ 100', com role=progressbar / aria-valuenow=savings / aria-valuemax=goal (CTASK-04)"
    - "Os testes de child-tasks.test.tsx referentes a TaskCard, TitheCard e SavingsCard passam (GREEN)"
  artifacts:
    - path: "src/components/tasks/task-card.tsx"
      provides: "Componente TaskCard com toggle visual e check button (CTASK-01, CTASK-02)"
      contains: "export function TaskCard"
    - path: "src/components/tasks/tithe-card.tsx"
      provides: "Componente TitheCard com botão Plantar / estado Feito (CTASK-03)"
      contains: "export function TitheCard"
    - path: "src/components/tasks/savings-card.tsx"
      provides: "Componente SavingsCard com progress bar animada (CTASK-04)"
      contains: "export function SavingsCard"
  key_links:
    - from: "src/components/tasks/task-card.tsx"
      to: "GardenView handleTaskComplete"
      via: "prop onComplete(task.id) chamada no click"
      pattern: "onComplete\\(task\\.id\\)"
    - from: "tests/unit/child-tasks.test.tsx"
      to: "src/components/tasks/task-card.tsx"
      via: "named import TaskCard (RED → GREEN)"
      pattern: "from '.*tasks/task-card'"
---

<objective>
Implementar os três componentes de exibição/interação da seção de tarefas: `TaskCard` (CTASK-01, CTASK-02), `TitheCard` (CTASK-03) e `SavingsCard` (CTASK-04). Esses componentes satisfazem o contrato definido pelos testes RED de `tests/unit/child-tasks.test.tsx` (criados no Plano 04-01), tornando-os GREEN. Nenhum gerencia estado próprio — TaskCard e TitheCard recebem estado + handler do GardenView por props; SavingsCard é display-only com uma única animação CSS de progress bar no mount.

Purpose: Esta é a Wave 2 de implementação dos componentes "filhos". Separar dos componentes em arquivos exclusivos (sem overlap com BottomNav no Plano 04-03) permite execução paralela. O Plano 04-04 (integração no GardenView) depende destes três componentes existirem.
Output: Três novos arquivos em `src/components/tasks/`. Após este plano, o subconjunto de testes de child-tasks.test.tsx que cobre TaskCard/TitheCard/SavingsCard passa (GREEN).
</objective>

<artifacts_produced>
## Artifacts this phase produces

Símbolos e arquivos criados por este plano:

- `TaskCard` — `export function TaskCard({ task, onComplete }: TaskCardProps)` em `src/components/tasks/task-card.tsx`
- `TitheCard` — `export function TitheCard({ done, onPlant }: TitheCardProps)` em `src/components/tasks/tithe-card.tsx`
- `SavingsCard` — `export function SavingsCard({ savings, goal }: SavingsCardProps)` em `src/components/tasks/savings-card.tsx`
- Interfaces de props: `TaskCardProps`, `TitheCardProps`, `SavingsCardProps`
</artifacts_produced>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-child-tasks/04-CONTEXT.md
@.planning/phases/04-child-tasks/04-RESEARCH.md
@.planning/phases/04-child-tasks/04-PATTERNS.md
@.planning/phases/04-child-tasks/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar TaskCard com toggle visual e check button (CTASK-01, CTASK-02)</name>
  <behavior>
    - CTASK-01: TaskCard com `task.done=false` tem fundo `#ffffff` e título cor `#27372C`; com `task.done=true` tem fundo `#EEF3EA`, borda `#D6E2CC` e título cor `#4E6E3E`.
    - CTASK-01: O título e o emoji da tarefa são renderizados (`screen.getByText(task.title)`).
    - CTASK-02: Clicar no check button de uma tarefa pendente chama `onComplete` com `task.id`.
    - CTASK-02: Tarefa já concluída NÃO chama `onComplete` ao clicar (botão `disabled`).
    - CTASK-02: Estado marcado expõe `aria-pressed="true"` (ou `aria-label` "Tarefa concluída: {title}"); pendente expõe `aria-pressed="false"` (ou `aria-label` "Marcar tarefa: {title}").
  </behavior>
  <read_first>
    - src/components/garden/garden-view.tsx (linhas 117–169: stub inline de task button que está sendo extraído — fonte do padrão de estrutura, opacity/line-through são do STUB e NÃO devem ser portados; usar o design completo do UI-SPEC)
    - src/components/garden/harvest-button.tsx (padrão de botão de ação: 'use client', inline style, minHeight 44)
    - src/lib/seed/garden-seed.ts (tipo `GardenTask` — `{ id, title, emoji, done }`)
    - .planning/phases/04-child-tasks/04-UI-SPEC.md (seção "TaskCard" e "Accessibility" — specs exatas de cor, tamanho, aria)
    - .planning/phases/04-child-tasks/04-PATTERNS.md (seção `src/components/tasks/task-card.tsx` — pattern de referência)
    - tests/unit/child-tasks.test.tsx (contrato RED que TaskCard deve satisfazer)
  </read_first>
  <files>src/components/tasks/task-card.tsx</files>
  <action>
    Criar `src/components/tasks/task-card.tsx` como client component (`'use client'` na linha 1). Importar o tipo: `import type { GardenTask } from '@/lib/seed/garden-seed'`. Definir `interface TaskCardProps { task: GardenTask; onComplete: (taskId: string) => void }` e exportar `export function TaskCard({ task, onComplete }: TaskCardProps)`.

    Renderizar um `<button>` com:
    - `onClick={() => !task.done && onComplete(task.id)}` (Pitfall 5: não disparar segundo click em tarefa concluída)
    - `disabled={task.done}`
    - `aria-pressed={task.done}`
    - `aria-label={task.done ? \`Tarefa concluída: ${task.title}\` : \`Marcar tarefa: ${task.title}\`}`
    - Estilo do card (D-07 / UI-SPEC): `display: 'flex'`, `alignItems: 'center'`, `gap: 12`, `padding: '12px 13px'`, `borderRadius: 18`, `width: '100%'`, `textAlign: 'left'`, `cursor: task.done ? 'default' : 'pointer'`, `transition: 'background .3s ease, border-color .3s ease'`.
    - `background: task.done ? '#EEF3EA' : '#ffffff'`
    - `border: \`1px solid ${task.done ? '#D6E2CC' : '#EDE9DF'}\``
    - `animation: task.done ? 'var(--animate-kreds-new)' : undefined` (glow kredsNew ao concluir — UI-SPEC Animations)

    Dentro do button, um `<span aria-hidden="true">` para o check button 38×38px (D-08): `width: 38, height: 38, borderRadius: '50%', flexShrink: 0, display: flex, alignItems/justifyContent: center`. Desmarcado: `border: '2px solid #D7DBCC'`, `background: '#ffffff'`. Marcado: `border: 'none'`, `background: '#3E6B4F'`. Quando `task.done`, renderizar dentro do span o SVG checkmark branco: `<svg width="16" height="12" viewBox="0 0 16 12" fill="none"><path d="M1 6L6 11L15 1" stroke="#fff" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" /></svg>`.

    Depois: `<span style={{ fontSize: 20 }}>{task.emoji}</span>` e `<span>` com o título: `fontSize: 15, fontWeight: 600, lineHeight: 1.4, color: task.done ? '#4E6E3E' : '#27372C'`.

    NÃO portar `opacity: 0.6` nem `textDecoration: line-through` do stub — esses não estão no UI-SPEC. NÃO criar useState interno (Anti-Pattern: estado vive no GardenView). Texto secundário (sub) é Claude's Discretion (D-09) — omitir nesta fase (UI-SPEC default: omit).
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && npx tsc --noEmit && npm run test -- tests/unit/child-tasks.test.tsx 2>&1 | grep -iE 'TaskCard|task-card' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/tasks/task-card.tsx` existe e contém `export function TaskCard`
    - `grep -c "background: task.done ? '#EEF3EA' : '#ffffff'" src/components/tasks/task-card.tsx` >= 1 (ou equivalente literal com as cores #EEF3EA e #ffffff)
    - `grep -c "onComplete(task.id)" src/components/tasks/task-card.tsx` >= 1
    - `grep -c 'aria-pressed' src/components/tasks/task-card.tsx` >= 1
    - Arquivo contém os literais `38` (check button), `'#3E6B4F'` (marcado), `'#D7DBCC'` (borda desmarcado), `transition: 'background .3s ease, border-color .3s ease'`
    - `npx tsc --noEmit` sai com código 0
    - Os testes de TaskCard em `child-tasks.test.tsx` passam (não mais "Cannot find module" para task-card)
  </acceptance_criteria>
  <done>TaskCard implementa CTASK-01 e CTASK-02 com cores/tamanhos exatos do UI-SPEC, sem estado interno; testes de TaskCard GREEN; tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar TitheCard com botão Plantar / estado Feito (CTASK-03)</name>
  <behavior>
    - CTASK-03: Com `done=false`, TitheCard renderiza título "Dízimo" e botão com label "Plantar"; clicar no botão chama `onPlant`.
    - CTASK-03: Com `done=true`, o botão exibe "Feito ✓", está desabilitado e clicar NÃO chama `onPlant`.
    - O botão expõe `aria-label="Plantar dízimo"` (default) e `aria-label="Dízimo plantado"` + `aria-disabled="true"` após done.
  </behavior>
  <read_first>
    - src/components/garden/harvest-button.tsx (analog: botão de ação com gradiente, borderRadius 999, minHeight 44, cor branca, fontWeight 700)
    - .planning/phases/04-child-tasks/04-UI-SPEC.md (seção "TitheCard" + "Copywriting Contract" + "Accessibility")
    - .planning/phases/04-child-tasks/04-PATTERNS.md (seção `src/components/tasks/tithe-card.tsx`)
    - tests/unit/child-tasks.test.tsx (contrato RED de TitheCard)
  </read_first>
  <files>src/components/tasks/tithe-card.tsx</files>
  <action>
    Criar `src/components/tasks/tithe-card.tsx` como client component (`'use client'`). Definir `interface TitheCardProps { done: boolean; onPlant: () => void }` e exportar `export function TitheCard({ done, onPlant }: TitheCardProps)`.

    Estrutura (D-10, D-11 / UI-SPEC "TitheCard"): um wrapper `<div style={{ padding: '0 16px' }}>` contendo um card `<div>` com `background: 'var(--color-kreds-card)'` (#FBFAF5), `border: '1px solid var(--color-kreds-border)'` (#ECE7DB), `borderRadius: 'var(--radius-card-md)'` (18px), `padding: 16`, `display: 'flex'`, `alignItems: 'center'`, `justifyContent: 'space-between'`.

    Lado esquerdo: `<div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>` com ícone de flor (SVG inline ~40×40px de 4 pétalas: pétalas `#C98AA0`, centro `#E3C57C` — usar `<circle>` simples seguindo o padrão de `decorative-flowers.tsx`; emoji 🌸 também aceitável conforme D-10) e título `<span style={{ fontSize: 18, fontWeight: 700, letterSpacing: '-0.01em', color: '#27372C' }}>Dízimo</span>`.

    Botão "Plantar" (D-11):
    - `onClick={() => !done && onPlant()}`
    - `disabled={done}`
    - `aria-disabled={done}`
    - `aria-label={done ? 'Dízimo plantado' : 'Plantar dízimo'}`
    - Estilo: `border: 'none'`, `borderRadius: 'var(--radius-card-sm)'` (16px), `height: 44`, `padding: '10px 20px'`, `color: '#ffffff'`, `fontWeight: 700`, `fontSize: 15`, `cursor: done ? 'default' : 'pointer'`, `boxShadow: 'var(--shadow-cta)'`, `transition: 'background .3s ease'`.
    - `background: done ? '#B07E91' : 'linear-gradient(135deg, #C98AA0 0%, #A55E76 100%)'`
    - Label: `{done ? 'Feito ✓' : 'Plantar'}`

    NÃO criar useState interno — `done` vem do GardenView (estado elevado no Plano 04-04). Não fazer POST/fetch (D-12: visual apenas).
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && npx tsc --noEmit && npm run test -- tests/unit/child-tasks.test.tsx 2>&1 | grep -iE 'TitheCard|tithe-card|Dízimo' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/tasks/tithe-card.tsx` existe e contém `export function TitheCard`
    - Arquivo contém os literais `Dízimo`, `Plantar`, `Feito ✓`
    - Arquivo contém `linear-gradient(135deg, #C98AA0 0%, #A55E76 100%)` e `'#B07E91'`
    - `grep -c "!done && onPlant()" src/components/tasks/tithe-card.tsx` >= 1
    - `npx tsc --noEmit` sai com código 0
    - Os testes de TitheCard em `child-tasks.test.tsx` passam
  </acceptance_criteria>
  <done>TitheCard implementa CTASK-03 com gradiente rosa, estado "Feito ✓" desabilitado e aria-label corretos; testes GREEN; tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar SavingsCard com progress bar animada (CTASK-04)</name>
  <behavior>
    - CTASK-04: SavingsCard renderiza título "Cofrinho", o valor salvo "R$ 25" e a meta "R$ 100".
    - CTASK-04: Existe um elemento `role="progressbar"` com `aria-valuenow={savings}`, `aria-valuemax={goal}`, `aria-valuemin={0}`.
    - A progress bar anima a largura de 0% até `(savings/goal)*100`% no mount (CSS transition width .6s).
  </behavior>
  <read_first>
    - src/components/garden/decorative-flowers.tsx (analog: componente display-only, SVG inline, sem handler)
    - .planning/phases/04-child-tasks/04-RESEARCH.md (Pitfall 3: progress bar não anima a partir de 0 — usar double requestAnimationFrame)
    - .planning/phases/04-child-tasks/04-UI-SPEC.md (seção "SavingsCard" — cores, labels, progress bar specs)
    - .planning/phases/04-child-tasks/04-PATTERNS.md (seção `src/components/tasks/savings-card.tsx`)
    - tests/unit/child-tasks.test.tsx (contrato RED de SavingsCard)
  </read_first>
  <files>src/components/tasks/savings-card.tsx</files>
  <action>
    Criar `src/components/tasks/savings-card.tsx` como client component (`'use client'`). Importar `import { useState, useEffect } from 'react'`. Definir `interface SavingsCardProps { savings: number; goal: number }` e exportar `export function SavingsCard({ savings, goal }: SavingsCardProps)`.

    Animação on-mount (Pitfall 3): `const [barWidth, setBarWidth] = useState(0)`; `const targetWidth = Math.min(100, (savings / goal) * 100)`; dentro de `useEffect`, usar double `requestAnimationFrame` para setar `setBarWidth(targetWidth)` após o primeiro paint. Dependência: `[targetWidth]`.

    Estrutura (D-13, D-14 / UI-SPEC "SavingsCard"): wrapper `<div style={{ padding: '0 16px' }}>` com `role="region"` `aria-label="Cofrinho"` no card interno. Card interno `<div>`: `background: '#EEF3EA'`, `border: '1px solid #D6E2CC'`, `borderRadius: 18`, `padding: 16`.

    Conteúdo:
    - Título `<h3 style={{ fontSize: 18, fontWeight: 700, letterSpacing: '-0.01em', color: '#27372C', margin: 0 }}>Cofrinho</h3>` (ou span — manter heading semântico)
    - Label da meta: `<span style={{ fontSize: 12, fontWeight: 600, color: '#7C8676' }}>Meta: R$ {goal}</span>`
    - Valor salvo: `<span style={{ fontSize: 20, fontWeight: 700, color: '#3E6B4F' }}>R$ {savings}</span>`
    - Track da progress bar `<div>` com `role="progressbar"`, `aria-valuenow={savings}`, `aria-valuemin={0}`, `aria-valuemax={goal}`, `aria-label={\`Cofrinho: R$ ${savings} de R$ ${goal}\`}`, e estilo: `height: 12`, `borderRadius: 'var(--radius-pill)'` (999px), `background: '#D6E2CC'`, `overflow: 'hidden'`.
    - Fill interno `<div>`: `height: '100%'`, `borderRadius: 'var(--radius-pill)'`, `background: 'linear-gradient(90deg, #5A8A66 0%, #3E6B4F 100%)'`, `width: \`${barWidth}%\``, `transition: 'width .6s cubic-bezier(.2,.8,.3,1)'`.

    Os labels "R$ 25" e "R$ 100" devem renderizar como texto plano contínuo (testes usam `getByText('R$ 25')` / `getByText(/100/)`) — garantir que `R$ {savings}` produza "R$ 25" (com espaço único) no DOM. Componente é display-only — sem handler de click.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && npx tsc --noEmit && npm run test -- tests/unit/child-tasks.test.tsx 2>&1 | grep -iE 'SavingsCard|savings-card|Cofrinho|progressbar' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/tasks/savings-card.tsx` existe e contém `export function SavingsCard`
    - Arquivo contém `role="progressbar"`, `aria-valuenow={savings}`, `aria-valuemax={goal}`, `aria-valuemin={0}`
    - Arquivo contém `Cofrinho`, `R$ ` (label), `linear-gradient(90deg, #5A8A66 0%, #3E6B4F 100%)`, `transition: 'width .6s cubic-bezier(.2,.8,.3,1)'`
    - Arquivo contém `requestAnimationFrame` (double rAF) para animação on-mount
    - `npx tsc --noEmit` sai com código 0
    - Os testes de SavingsCard em `child-tasks.test.tsx` passam; o arquivo inteiro `child-tasks.test.tsx` passa GREEN (TaskCard + TitheCard + SavingsCard implementados após Tasks 1-3)
  </acceptance_criteria>
  <done>SavingsCard implementa CTASK-04 com progress bar animada on-mount e role=progressbar acessível; child-tasks.test.tsx inteiro GREEN; tsc verde.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passa (3 componentes tipados corretamente)
- `npm run test -- tests/unit/child-tasks.test.tsx` passa 100% GREEN (TaskCard, TitheCard, SavingsCard implementados)
- Cores e dimensões exatas do UI-SPEC presentes em cada arquivo (#EEF3EA, #3E6B4F, gradiente rosa, gradiente verde, 38px check, 12px bar)
- Nenhum componente cria useState próprio para o estado de domínio (task.done / done vêm por props); apenas SavingsCard usa useState local para a animação da barra
</verification>

<success_criteria>
- TaskCard: CTASK-01 (visual pendente/concluído) + CTASK-02 (check 38×38 toggle, onComplete) ✓
- TitheCard: CTASK-03 (Plantar gradiente rosa → Feito ✓) ✓
- SavingsCard: CTASK-04 (meta, valor, progress bar animada role=progressbar) ✓
- Os 3 componentes seguem o padrão Fase 1-3: 'use client', inline style com CSS vars, props por cima
- child-tasks.test.tsx GREEN; tsc verde
</success_criteria>

<output>
Create `.planning/phases/04-child-tasks/04-02-SUMMARY.md` when done
</output>
