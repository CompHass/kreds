---
phase: 02-family-access-tenancy-roles-and-profiles
plan: 10
type: execute
wave: 6
depends_on: []
files_modified:
  - src/lib/db/schema/index.ts
  - src/lib/families/child-profiles.ts
  - src/app/family/children/page.tsx
  - src/lib/env.ts
  - tests/unit/child-pin-management.test.ts
autonomous: false
gap_closure: false
requirements:
  - FAM-03
  - FAM-07

must_haves:
  truths:
    - "child_profiles table has pin_hash (text, nullable) and last_accessed_at (timestamp, nullable) columns after migration per D-31 and D-33."
    - "Guardian can set or update a 4–6 digit numeric PIN for a child profile per D-28."
    - "PIN is stored only as bcrypt hash — never as plaintext or reversible encoding per D-31."
    - "CHILD_SESSION_SECRET env var is validated in src/lib/env.ts for use by later auth endpoint per D-30."
    - "createChildProfile and updateChildProfile commands accept optional pin parameter and hash it before storage per D-28."
  artifacts:
    - path: "src/lib/db/schema/index.ts"
      provides: "child_profiles table with pin_hash and last_accessed_at columns"
      contains: "pin_hash"
    - path: "src/lib/families/child-profiles.ts"
      provides: "PIN hashing logic in createChildProfile and updateChildProfile"
      exports: ["createChildProfile", "updateChildProfile", "setChildPin"]
    - path: "src/app/family/children/page.tsx"
      provides: "PIN input field in child creation and edit form"
    - path: "src/lib/env.ts"
      provides: "CHILD_SESSION_SECRET validation"
      contains: "CHILD_SESSION_SECRET"
    - path: "tests/unit/child-pin-management.test.ts"
      provides: "Unit tests for PIN validation and hashing behavior"
  key_links:
    - from: "src/lib/families/child-profiles.ts"
      to: "src/lib/db/schema/index.ts"
      via: "childProfiles.pin_hash column"
      pattern: "pinHash"
    - from: "src/app/family/children/page.tsx"
      to: "src/lib/families/child-profiles.ts"
      via: "createChildProfile / updateChildProfile with pin"
      pattern: "pin.*[0-9]{4,6}"

user_setup:
  - service: kreds-child-auth
    why: "JWT signing secret for child session tokens — separate from AUTH_SECRET to scope child session signing independently"
    env_vars:
      - name: CHILD_SESSION_SECRET
        source: "Generate a random 32+ character secret: openssl rand -base64 32"
---

<objective>
Add PIN authentication foundation to child profiles: schema migration, bcrypt hashing in commands, PIN input in guardian UI, and env var validation.

Purpose: D-28 through D-33 require children to authenticate via a guardian-set PIN. Before the auth endpoint can be built (plan 02-11), the schema must have the PIN hash column and the guardian commands must accept and hash PINs.

Output: Drizzle migration adding pin_hash + last_accessed_at to child_profiles; updated createChildProfile/updateChildProfile/setChildPin commands with bcrypt; PIN input in /family/children form; CHILD_SESSION_SECRET in env validation.
</objective>

## Phase Goal

**As a** parent/guardian, **I want to** set a PIN for my child so that **so that** my child can independently access the app through the guardian-generated link without using a ZITADEL account.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-CONTEXT.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-RESEARCH.md
@src/lib/db/schema/index.ts
@src/lib/families/child-profiles.ts
@src/lib/env.ts
@src/app/family/children/page.tsx
</context>

## Source Coverage Audit

| Source Item | Coverage |
|---|---|
| GOAL: child PIN auth foundation | Covered by schema migration + PIN hashing commands |
| REQ FAM-03: parent manages child profiles | Covered by PIN set/update in createChildProfile/updateChildProfile |
| REQ FAM-07: guardian reviews audit trail | PIN set events written to family_audit_events via createAuditEvent |
| CONTEXT D-28: guardian defines PIN | Covered by PIN input in /family/children form + setChildPin command |
| CONTEXT D-31: pin_hash column, bcrypt, never plaintext | Covered by schema migration + bcrypt hash before storage |
| CONTEXT D-33: last_accessed_at column | Covered by schema migration (column nullable, updated on login in plan 02-11) |
| CONTEXT D-30: CHILD_SESSION_SECRET env var | Covered by env.ts validation addition |

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Auditoria de pacotes — bcryptjs e @types/bcryptjs</name>
  <what-built>
    Os planos 02-10, 02-11 e 02-12 requerem dois novos pacotes npm:
    - bcryptjs (runtime) — hashing de PIN com bcrypt
    - @types/bcryptjs (devDependency) — tipos TypeScript para bcryptjs
    - jose (runtime) — assinatura e verificação de JWT para child session

    Nenhum desses pacotes está em package.json. Verifique a legitimidade antes de instalar:

    1. https://www.npmjs.com/package/bcryptjs
       - Esperado: escopo `null` (não-scoped), maintainer histórico `nicktindall` ou `dcodeio`, downloads semanais > 1M, repositório apontando para github.com/dcodeio/bcrypt.js ou similar
    2. https://www.npmjs.com/package/@types/bcryptjs
       - Esperado: publicado por @types (DefinitelyTyped), repositório apontando para DefinitelyTyped/DefinitelyTyped
    3. https://www.npmjs.com/package/jose
       - Esperado: maintainer `panva` (Filip Skokan), downloads semanais > 5M, repositório github.com/panva/jose

    Se algum dos três estiver suspeito (nome parecido mas diferente, maintainer desconhecido, zero downloads), NÃO instale e reporte.
  </what-built>
  <how-to-verify>
    Acesse cada URL acima no navegador e confirme:
    - bcryptjs: versão >= 2.4.3, maintainer dcodeio ou nicktindall
    - @types/bcryptjs: publicado sob @types scope
    - jose: versão >= 6.x, maintainer panva, repositório panva/jose
  </how-to-verify>
  <resume-signal>Digite "aprovado" se todos os três pacotes forem legítimos, ou descreva a suspeita encontrada</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Schema migration — pin_hash e last_accessed_at em child_profiles</name>
  <files>src/lib/db/schema/index.ts</files>
  <read_first>
    - src/lib/db/schema/index.ts (linhas 54-73) — definição atual de childProfiles: id, familyId, displayName, ageYears, avatarPreset, accentColor, identityId, active, deactivatedAt, createdAt, updatedAt. NÃO tem pin_hash nem last_accessed_at.
    - drizzle/0004_flimsy_norman_osborn.sql — último migration gerado; usar como referência do padrão de nomenclatura de migrations.
  </read_first>
  <behavior>
    - Após `pnpm db:generate`, um novo arquivo SQL deve ser criado em drizzle/ contendo `ALTER TABLE "child_profiles" ADD COLUMN "pin_hash" text;` e `ALTER TABLE "child_profiles" ADD COLUMN "last_accessed_at" timestamp;`
    - Ambas as colunas são nullable (sem NOT NULL constraint) para compatibilidade com linhas existentes
    - O schema TypeScript exporta os novos campos: `pinHash: text('pin_hash')` e `lastAccessedAt: timestamp('last_accessed_at')`
  </behavior>
  <action>
    Modificar a definição de `childProfiles` em src/lib/db/schema/index.ts para adicionar duas colunas após `deactivatedAt`:

    Coluna 1: `pinHash: text('pin_hash')` — sem `.notNull()`, sem default. Representa o hash bcrypt do PIN guardião (D-31). Nomeado `pin_hash` no banco.

    Coluna 2: `lastAccessedAt: timestamp('last_accessed_at')` — sem `.notNull()`, sem default. Atualizado pelo child auth endpoint em cada login bem-sucedido (D-33). Nomeado `last_accessed_at` no banco.

    Após editar o schema, executar `pnpm db:generate` para gerar o arquivo de migration Drizzle.

    NÃO executar `pnpm db:migrate` — deixar para o guardião executar manualmente ou para CI. Apenas gerar o arquivo SQL.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && grep -c 'pinHash\|lastAccessedAt' src/lib/db/schema/index.ts</automated>
  </verify>
  <done>src/lib/db/schema/index.ts contém pinHash e lastAccessedAt em childProfiles. Um novo arquivo SQL existe em drizzle/ com os dois ALTER TABLE statements. pnpm build passa sem erros de tipo.</done>
  <acceptance_criteria>
    - src/lib/db/schema/index.ts contém `pinHash: text('pin_hash')` em childProfiles (sem .notNull())
    - src/lib/db/schema/index.ts contém `lastAccessedAt: timestamp('last_accessed_at')` em childProfiles (sem .notNull())
    - `pnpm db:generate` produz novo migration SQL com `ADD COLUMN "pin_hash" text` e `ADD COLUMN "last_accessed_at" timestamp`
    - `pnpm build` passa sem novos erros de TypeScript
    - Colunas existentes (id, familyId, displayName, ageYears, avatarPreset, accentColor, active, deactivatedAt) não são modificadas
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instalar bcryptjs + @types/bcryptjs + jose, validar env, adicionar PIN commands</name>
  <files>
    src/lib/env.ts,
    src/lib/families/child-profiles.ts,
    tests/unit/child-pin-management.test.ts
  </files>
  <read_first>
    - src/lib/env.ts — padrão Zod atual: DATABASE_URL, NODE_ENV, AUTH_SECRET, AUTH_ZITADEL_ID, AUTH_ZITADEL_SECRET, AUTH_ZITADEL_ISSUER. Adicionar CHILD_SESSION_SECRET seguindo o mesmo padrão.
    - src/lib/families/child-profiles.ts — assinaturas de CreateChildProfileInput (linhas 9-17) e UpdateChildProfileVisualsInput (linhas 19-26). Ambas precisam de campo opcional `pin?: string`.
    - src/lib/families/audit.ts — assinatura de createAuditEvent: recebe CreateAuditEventInput com familyId, actorIdentityId, eventType, subjectType, subjectId, summary, metadata opcional.
    - src/lib/db/schema/index.ts — após Task 1: childProfiles agora tem pinHash e lastAccessedAt.
    - tests/unit/glossary.test.ts — padrão de testes unitários vitest: `import { describe, it, expect } from 'vitest'`, relative imports
  </read_first>
  <behavior>
    Testes em tests/unit/child-pin-management.test.ts:
    - Test 1: validatePinFormat('1234') retorna true (4 dígitos)
    - Test 2: validatePinFormat('123456') retorna true (6 dígitos)
    - Test 3: validatePinFormat('123') retorna false (< 4 dígitos)
    - Test 4: validatePinFormat('1234567') retorna false (> 6 dígitos)
    - Test 5: validatePinFormat('abcd') retorna false (não numérico)
    - Test 6: validatePinFormat('12 34') retorna false (contém espaço)
    - Test 7: hashPin('1234') retorna string começando com '$2' (bcrypt hash)
    - Test 8: verifyPin('1234', hash) retorna true com hash gerado por hashPin('1234')
    - Test 9: verifyPin('5678', hash) retorna false com hash de '1234'
  </behavior>
  <action>
    PASSO 1 — Instalar dependências (após aprovação no checkpoint Task 0):
    Executar: `pnpm add bcryptjs jose` e `pnpm add -D @types/bcryptjs`

    PASSO 2 — Adicionar CHILD_SESSION_SECRET ao env schema em src/lib/env.ts:
    Adicionar `CHILD_SESSION_SECRET: z.string().min(32)` ao objeto envSchema existente, seguindo o padrão dos outros segredos (`AUTH_SECRET: z.string().min(1)`). O valor mínimo de 32 caracteres garante entropia adequada para assinar JWTs.

    PASSO 3 — Criar src/lib/families/child-pin.ts com funções exportadas:
    - `validatePinFormat(pin: string): boolean` — valida que pin é string de 4-6 dígitos numéricos (regex `/^\d{4,6}$/`). Exportar.
    - `hashPin(pin: string): Promise<string>` — chama `bcrypt.hash(pin, 12)`. Exportar.
    - `verifyPin(pin: string, hash: string): Promise<boolean>` — chama `bcrypt.compare(pin, hash)`. Exportar.

    PASSO 4 — Atualizar src/lib/families/child-profiles.ts:
    a) Adicionar campo opcional `pin?: string` em CreateChildProfileInput.
    b) Adicionar campo opcional `pin?: string` em UpdateChildProfileVisualsInput.
    c) Exportar nova função `setChildPin(childProfileId: string, familyId: string, guardianIdentityId: string, pin: string): Promise<void>` que:
       - Valida que guardianIdentityId é guardian ativo da family (mesmo padrão já existente na função updateChildProfile)
       - Chama validatePinFormat(pin) — lança erro se inválido: 'PIN deve ter 4 a 6 dígitos numéricos'
       - Chama hashPin(pin) de src/lib/families/child-pin.ts
       - Executa UPDATE child_profiles SET pin_hash = $hash, updated_at = now() WHERE id = $childProfileId AND family_id = $familyId em transação
       - Escreve audit event: eventType 'child_profile.pin_set', summary 'PIN definido para o perfil', metadata {} (sem incluir o hash no metadata)
    d) Em createChildProfile: se input.pin está presente, chamar hashPin(input.pin) e incluir pinHash no INSERT; senão pinHash fica null.
    e) Em updateChildProfile: se input.pin está presente, chamar setChildPin internamente dentro da transação existente (ou chamar hashPin diretamente e incluir no UPDATE).

    PASSO 5 — Criar tests/unit/child-pin-management.test.ts com os 9 testes descritos em <behavior>. Importar validatePinFormat, hashPin, verifyPin de '../../src/lib/families/child-pin'.

    REGRAS DE SEGURANÇA (OBRIGATÓRIAS):
    - NUNCA logar o PIN raw em nenhum contexto
    - NUNCA incluir o pinHash no metadata do audit event
    - NUNCA retornar pinHash em nenhum tipo ChildProfile (o tipo existente não inclui pinHash e não deve incluir)
    - Usar bcrypt cost factor 12 (não menos)
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && pnpm vitest run tests/unit/child-pin-management.test.ts</automated>
  </verify>
  <done>9/9 testes de PIN passam. pnpm build passa. CHILD_SESSION_SECRET está em env.ts. setChildPin exportado de child-profiles.ts. pinHash jamais exposto no tipo ChildProfile.</done>
  <acceptance_criteria>
    - pnpm add bcryptjs jose && pnpm add -D @types/bcryptjs executado com sucesso (pacotes em package.json)
    - src/lib/env.ts contém `CHILD_SESSION_SECRET: z.string().min(32)` no envSchema
    - src/lib/families/child-pin.ts existe e exporta validatePinFormat, hashPin, verifyPin
    - validatePinFormat usa regex `/^\d{4,6}$/`
    - hashPin usa bcrypt com salt rounds = 12
    - tests/unit/child-pin-management.test.ts: 9/9 testes passam
    - src/lib/families/child-profiles.ts: CreateChildProfileInput e UpdateChildProfileVisualsInput têm `pin?: string`
    - src/lib/families/child-profiles.ts exporta setChildPin
    - O tipo ChildProfile (retornado por listActiveChildProfiles) NÃO inclui pinHash
    - pnpm build passa sem erros de TypeScript
    - `grep -c 'CHILD_SESSION_SECRET' src/lib/env.ts` retorna >= 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: PIN input na UI de criação e edição de filho (/family/children)</name>
  <files>src/app/family/children/page.tsx, src/app/family/children/ChildrenForm.tsx</files>
  <read_first>
    - src/app/family/children/page.tsx — página server component atual: usa listActiveChildProfiles, ChildrenForm. Verificar onde ChildrenForm recebe props e onde o form action de criação envia dados.
    - src/app/api/families/children/route.ts — rota de criação: verificar campos atuais aceitos no body do POST e como createChildProfile é chamado.
    - src/lib/families/child-profiles.ts (após Task 2) — CreateChildProfileInput agora tem campo `pin?: string`.
  </read_first>
  <action>
    Adicionar campo PIN à UI de criação de filho e à rota de criação. O PIN é opcional para manter backward-compat com perfis existentes (será obrigatório apenas no novo fluxo de acesso da criança).

    PARTE A — Adicionar campo PIN ao ChildrenForm.tsx (ou ao formulário inline em page.tsx se não existir arquivo separado):
    - Campo `<input type="password" name="pin" ...>` com label "PIN da criança (opcional)"
    - Placeholder: "4 a 6 dígitos"
    - maxLength={6}, minLength={4}, inputMode="numeric", pattern="\d{4,6}"
    - Estilo inline coerente com os outros campos do formulário (background card, border Sylvan, padding)
    - Texto auxiliar abaixo do campo: "A criança usará este PIN para acessar o app."

    PARTE B — Atualizar src/app/api/families/children/route.ts (rota POST existente):
    - Ler campo `pin` do formData (opcional: `formData.get('pin') as string | null`)
    - Se pin não vazio, passar `pin: pin` para createChildProfile
    - Se pin vazio ou ausente, não passar (deixar undefined)
    - Retornar 400 se pin fornecido mas validatePinFormat retornar false: `{ error: 'PIN deve ter 4 a 6 dígitos numéricos' }`

    NÃO adicionar campo PIN em formulários de edição neste plano — a edição de PIN é tratada via setChildPin separadamente. O formulário de criação é suficiente para o MVP vertical slice.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Campo PIN aparece em /family/children no formulário de criação. POST /api/families/children aceita `pin` opcional e retorna 400 para PIN com formato inválido. pnpm build passa.</done>
  <acceptance_criteria>
    - src/app/family/children/page.tsx ou ChildrenForm.tsx contém `type="password"` e `name="pin"` com `inputMode="numeric"`
    - src/app/api/families/children/route.ts lê campo `pin` do formData e passa para createChildProfile
    - src/app/api/families/children/route.ts retorna 400 com `{ error: 'PIN deve ter 4 a 6 dígitos numéricos' }` para PIN inválido
    - pnpm build sem erros de tipo
    - O campo é marcado como opcional na UI (label "opcional" ou equivalente)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guardian browser → POST /api/families/children | PIN raw entra na requisição; deve ser hashado antes de qualquer escrita em banco |
| DB child_profiles.pin_hash | Hash bcrypt — leitura da coluna não deve ser exposta em respostas de API ou tipos ChildProfile |
| src/lib/families/child-pin.ts | Única superfície que toca o PIN raw; deve ser server-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | Information Disclosure | ChildProfile type | mitigate | pinHash never included in ChildProfile return type; listActiveChildProfiles SELECT does not include pin_hash column |
| T-02-10-02 | Tampering | createChildProfile input | mitigate | validatePinFormat gate before hashPin; bcrypt cost factor 12 minimum |
| T-02-10-03 | Information Disclosure | audit event metadata | mitigate | child_profile.pin_set event metadata is empty {}; hash never logged or stored in metadata |
| T-02-10-04 | Elevation of Privilege | setChildPin command | mitigate | Guardian active membership check before any PIN update — same pattern as updateChildProfile |
| T-02-10-SC | Tampering | npm installs (bcryptjs, jose, @types/bcryptjs) | mitigate | Blocking human checkpoint (Task 0) verifies all three packages before install |
</threat_model>

<verification>
<automated>cd /Users/hass/repos/github/comphass/kreds && pnpm vitest run tests/unit/child-pin-management.test.ts && pnpm build</automated>
<human-check>Abrir /family/children no navegador e confirmar que o campo PIN aparece no formulário de criação de filho com label "PIN da criança (opcional)" e inputMode numeric.</human-check>
</verification>

<success_criteria>
- child_profiles schema tem pin_hash (nullable) e last_accessed_at (nullable)
- Drizzle migration SQL gerado com os dois ALTER TABLE
- bcryptjs instalado; hashPin usa cost factor 12
- CHILD_SESSION_SECRET em env.ts com min(32)
- setChildPin exportado de child-profiles.ts; audit event escrito sem o hash
- ChildProfile type não expõe pinHash
- 9/9 testes unitários de PIN passam
- Campo PIN opcional em /family/children (formulário de criação)
</success_criteria>

## Artifacts this phase produces

| Symbol | File | Type |
|--------|------|------|
| `validatePinFormat` | `src/lib/families/child-pin.ts` | exported function |
| `hashPin` | `src/lib/families/child-pin.ts` | exported async function |
| `verifyPin` | `src/lib/families/child-pin.ts` | exported async function |
| `setChildPin` | `src/lib/families/child-profiles.ts` | exported async function |
| `pinHash` column | `src/lib/db/schema/index.ts` (childProfiles) | Drizzle column definition |
| `lastAccessedAt` column | `src/lib/db/schema/index.ts` (childProfiles) | Drizzle column definition |
| `CHILD_SESSION_SECRET` | `src/lib/env.ts` | env var validation |
| Drizzle migration SQL | `drizzle/` (new file) | DB migration |

<output>
Create `.planning/phases/02-family-access-tenancy-roles-and-profiles/02-10-SUMMARY.md` when done
</output>
