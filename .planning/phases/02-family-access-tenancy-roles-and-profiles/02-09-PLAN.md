---
phase: 02-family-access-tenancy-roles-and-profiles
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/family-access.spec.ts
  - tests/e2e/auth-screen.spec.ts
  - scripts/test-unauthenticated-decline.sh
autonomous: true
gap_closure: true
requirements:
  - FAM-01
  - FAM-02
  - FAM-03
  - FAM-04
  - FAM-05
  - FAM-06
  - FAM-07

must_haves:
  truths:
    - "Testes E2E automatizados cobrem a tela de autenticação Sylvan (gradiente, símbolo 🧺, botão ZITADEL)"
    - "Teste automatizado verifica que POST unauthenticated com action=decline retorna 401 sem live ZITADEL"
    - "Testes E2E existentes em family-access.spec.ts são corrigidos para rotas que realmente existem"
    - "Script curl documenta e verifica o comportamento de declínio não autenticado"
  artifacts:
    - path: "tests/e2e/auth-screen.spec.ts"
      provides: "Testes Playwright para landing screen Sylvan e comportamento de redirecionamento"
    - path: "tests/e2e/family-access.spec.ts"
      provides: "Testes E2E de controle de acesso corrigidos para as rotas reais implementadas na fase 02"
    - path: "scripts/test-unauthenticated-decline.sh"
      provides: "Script curl que testa POST /api/families/invitations com action=decline sem auth e verifica resposta 401"
  key_links:
    - from: "tests/e2e/auth-screen.spec.ts"
      to: "src/app/page.tsx"
      via: "Playwright page.goto('/')"
      pattern: "page\\.goto.*'/'|request\\.get.*'/'"
    - from: "scripts/test-unauthenticated-decline.sh"
      to: "src/app/api/families/invitations/route.ts"
      via: "curl POST /api/families/invitations"
      pattern: "api/families/invitations"
---

<objective>
Automate the three human-verification items from VERIFICATION.md as much as technically possible without live ZITADEL credentials. Items 1 and 2 (live ZITADEL session) cannot be fully automated but get Playwright scaffolds with skip annotations and clear instructions. Item 3 (unauthenticated decline returns 401) can be fully automated as a curl test against a running server without any ZITADEL credentials.

Purpose: Convert human-verification items into executable tests so future re-runs do not require manual repetition. Fix the existing family-access.spec.ts which tests routes that were restructured in Phase 02 (e.g., /api/families/[id] does not exist — the actual route is /api/families). Add a new auth-screen.spec.ts that validates the Sylvan landing page renders correctly without authentication.

Output: tests/e2e/auth-screen.spec.ts (new), tests/e2e/family-access.spec.ts (updated), scripts/test-unauthenticated-decline.sh (new)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-VERIFICATION.md
@tests/e2e/family-access.spec.ts
@src/app/page.tsx
@src/app/api/families/invitations/route.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes E2E da tela de autenticação Sylvan</name>
  <files>tests/e2e/auth-screen.spec.ts</files>
  <read_first>
    - src/app/page.tsx — LandingScreen: renderiza quando session?.user?.id é falsy. Contém: texto "Kreds", "Mordomia cristã para famílias", "Acesso familiar", "Entrar com ZITADEL", Link href="/api/auth/signin". OnboardingScreen: renderiza quando identityId é null. Contém: "Bem-vindo ao Kreds", Link href="/family/onboarding"
    - playwright.config.ts — baseURL: process.env.NEXT_PUBLIC_APP_URL || 'http://localhost:3000', testDir: './tests/e2e'
    - 02-CONTEXT.md D-23: landing screen unauthenticated: gradiente Sylvan, símbolo 🧺, wordmark "Kreds", subtitle "Mordomia cristã para famílias", botão "Entrar com ZITADEL" (pill.primary). D-24: routing rules página /
  </read_first>
  <action>
    Criar tests/e2e/auth-screen.spec.ts com os seguintes testes Playwright usando `import { test, expect } from '@playwright/test'`:

    describe block: 'Auth screen — Sylvan landing (FAM-01, UI-01)'

    Teste 1 — 'landing page renders Sylvan branding when unauthenticated':
    - page.goto('/')
    - Verificar que o body contém texto "Kreds"
    - Verificar que o body contém texto "Mordomia cristã para famílias"
    - Verificar que o body contém texto "Entrar com ZITADEL"
    - Verificar que não contém texto "families registered" (regressão: proof-of-concept da fase 1 foi removido)
    - Verificar que não contém texto "famílias cadastradas" no sentido de contador (contém "Acesso exclusivo para famílias cadastradas" está OK — verificar ausência de padrão /\d+ famílias/)

    Teste 2 — 'sign in button links to /api/auth/signin':
    - page.goto('/')
    - Localizar o link com texto "Entrar com ZITADEL" usando page.getByRole('link', { name: /Entrar com ZITADEL/i })
    - Verificar que o href do link inclui '/api/auth/signin'

    Teste 3 — 'unauthenticated visitor does not see family dashboard content':
    - page.goto('/family/children')
    - Verificar que a URL final NÃO é /family/children (deve ter redirecionado para / ou /api/auth/signin)
    - OU: verificar que o body não contém conteúdo do dashboard de crianças como "children" junto com conteúdo sensível

    Teste 4 — 'POST to /api/families returns 401 when unauthenticated' (usando request, não page):
    - request.post('/api/families', { data: { name: 'Test', timezone: 'America/Sao_Paulo' } })
    - Verificar status === 401

    Teste 5 — LIVE ZITADEL (skip annotation):
    - test.skip(true, 'Requires live ZITADEL credentials — run manually per VERIFICATION.md item 1')
    - Comentário descrevendo o que deveria ser verificado: session.user.id === ZITADEL sub após login

    Teste 6 — POST-CREATION ROUTING (skip annotation):
    - test.skip(true, 'Requires live ZITADEL session and database family — run manually per VERIFICATION.md item 2')
    - Comentário: após criar família, /family/children carrega normalmente

    Todos os testes que dependem de servidor local devem usar o padrão request ou page da config do Playwright (sem hardcode de porta).
    Usar `test.describe` para agrupar.
    Incluir comentário no topo: `// Gap closure: covers VERIFICATION.md human_verification items 1 (partial), 2 (partial), and landing screen UI-01`
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/kreds && npx tsc --noEmit tests/e2e/auth-screen.spec.ts 2>&1 | head -20 || echo "TypeScript check done"</automated>
  </verify>
  <done>tests/e2e/auth-screen.spec.ts existe com 6 testes (4 executáveis + 2 com skip). Sem erros de TypeScript. Testes 1-4 podem ser executados com pnpm test:e2e quando servidor está rodando.</done>
  <acceptance_criteria>
    - Arquivo existe em tests/e2e/auth-screen.spec.ts
    - Contém exatamente 6 testes: 4 auto-executáveis + 2 com test.skip(true, ...)
    - Testes auto-executáveis verificam: "Kreds" text, "Entrar com ZITADEL" link href, redirecionamento de /family/children sem auth, POST /api/families retorna 401
    - Os 2 skip tests têm mensagem clara explicando que requerem ZITADEL ao vivo
    - Comentário no topo referencia VERIFICATION.md
    - Sem imports não utilizados
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Correção dos testes E2E existentes e script de declínio não autenticado</name>
  <files>tests/e2e/family-access.spec.ts, scripts/test-unauthenticated-decline.sh</files>
  <read_first>
    - tests/e2e/family-access.spec.ts atual — testa rotas que não existem: /api/families/any-uuid (GET), /api/families/any-uuid/children (POST/GET), /api/families/any-uuid/invitations (POST), /api/families/invitations/invalid-token-12345 (GET), /api/families/any-uuid/audit (GET). As rotas reais implementadas são: /api/families (GET/POST), /api/families/children (POST), /api/families/children/deactivate (POST), /api/families/invitations (POST com action param)
    - src/app/api/families/invitations/route.ts — decline case: chama requireAuthenticatedIdentity(session); se não autenticado retorna 401. Body: { action: 'decline', token: '<token>' }
    - playwright.config.ts — baseURL, retries config
  </read_first>
  <action>
    PARTE A — Atualizar tests/e2e/family-access.spec.ts:

    Reescrever o arquivo mantendo os objetivos de teste mas corrigindo as rotas para as que realmente existem na fase 02:

    describe 'Family access — unauthenticated denial (FAM-01, FAM-05)':
    - Teste: GET /api/families retorna 401 quando não autenticado (MANTIDO — rota existe)
    - Teste: POST /api/families retorna 401 quando não autenticado (MANTIDO — rota existe)
    - REMOVER: GET /api/families/any-uuid (rota não existe — era teste da fase 1)
    - MANTER adaptado: GET /api/families não retorna 200 com lista de famílias (rota existe, lógica preservada)

    describe 'Family access — child profile routes (FAM-03)':
    - CORRIGIR: POST /api/families/children retorna 401 quando não autenticado (era /api/families/any-uuid/children)
    - REMOVER: GET /api/families/any-uuid/children (não existe — página server-rendered, não API GET)

    describe 'Family access — invitation routes (FAM-02)':
    - CORRIGIR: POST /api/families/invitations com action=create retorna 401 quando não autenticado (era /api/families/any-uuid/invitations)
    - CORRIGIR: POST /api/families/invitations com action=decline retorna 401 quando não autenticado — GAP CLOSURE: verifica CR-03 fix em runtime
    - REMOVER: GET /api/families/invitations/invalid-token-12345 (rota API GET não existe — página server-rendered)

    describe 'Family access — audit routes (FAM-07)':
    - REMOVER: GET /api/families/any-uuid/audit (rota API não existe — página server-rendered, não Route Handler)
    - ADICIONAR: page.goto('/family/audit') sem auth deve redirecionar (não mostrar dados)

    describe 'Family access — homepage smoke (FAM-01)':
    - MANTER: homepage não mostra contagem de famílias

    Cada teste deve ter comentário inline indicando qual requisito cobre (FAM-01, FAM-02, etc.).

    PARTE B — Criar scripts/test-unauthenticated-decline.sh:

    Script bash que testa o item 3 de verificação humana (CR-03 fix) usando curl:
    1. Shebang: #!/usr/bin/env bash
    2. Definir BASE_URL="${NEXT_PUBLIC_APP_URL:-http://localhost:3000}"
    3. Exibir: "Testando: POST ${BASE_URL}/api/families/invitations com action=decline sem autenticação..."
    4. Executar curl: curl -s -o /dev/null -w "%{http_code}" -X POST "${BASE_URL}/api/families/invitations" -H "Content-Type: application/json" -d '{"action":"decline","token":"fake-token-for-auth-test"}'
    5. Capturar HTTP status code
    6. Se status == 401: exibir em verde "PASS: retornou 401 Unauthorized — CR-03 fix confirmado em runtime"
    7. Se status != 401: exibir em vermelho "FAIL: esperado 401, recebido ${status}. CR-03 pode não estar funcionando corretamente."
    8. Exibir instrução: "Para rodar: certifique-se que o servidor está rodando com 'pnpm dev' e execute: bash scripts/test-unauthenticated-decline.sh"
    chmod +x scripts/test-unauthenticated-decline.sh
  </action>
  <verify>
    <automated>bash -n /Users/hass/repos/github/comphass/kreds/tests/e2e/family-access.spec.ts 2>/dev/null || npx tsc --noEmit /Users/hass/repos/github/comphass/kreds/tests/e2e/family-access.spec.ts 2>&1 | head -10; bash -n /Users/hass/repos/github/comphass/kreds/scripts/test-unauthenticated-decline.sh && echo "Script syntax OK"</automated>
  </verify>
  <done>family-access.spec.ts corrigido sem referências a rotas inexistentes. scripts/test-unauthenticated-decline.sh existe, é executável, e tem sintaxe bash válida. Os testes de declínio não autenticado cobrem o item 3 de VERIFICATION.md.</done>
  <acceptance_criteria>
    - tests/e2e/family-access.spec.ts não contém mais referências a: /api/families/any-uuid (GET), /api/families/any-uuid/children, /api/families/any-uuid/invitations, /api/families/invitations/invalid-token-12345, /api/families/any-uuid/audit
    - family-access.spec.ts contém teste para POST /api/families/invitations com action=decline retornando 401 (gap closure item 3)
    - scripts/test-unauthenticated-decline.sh existe com chmod +x e sintaxe bash válida
    - Script exibe PASS em verde quando recebe 401 e FAIL em vermelho caso contrário
    - Script é auto-descritivo com instrução de uso
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → local app | Playwright/curl faz requests sem cookies de auth — simula visitante não autenticado |
| scripts/test-unauthenticated-decline.sh → /api/families/invitations | Envia token fake para confirmar que auth guard funciona antes de qualquer token check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-GC03 | Elevation of Privilege | /api/families/invitations decline | mitigate | Test explicitly verifies 401 response for unauthenticated decline — confirms CR-03 fix at runtime |
| T-02-GC04 | Information Disclosure | E2E test assertions | accept | Tests use page.textContent assertions that don't persist data; low risk |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — uses existing Playwright and bash |
</threat_model>

<verification>
- tests/e2e/auth-screen.spec.ts: 6 testes presentes, 2 com skip, 4 auto-executáveis
- tests/e2e/family-access.spec.ts: sem referências a rotas inexistentes (/api/families/any-uuid, etc.)
- scripts/test-unauthenticated-decline.sh: sintaxe bash válida, retorna PASS quando recebe 401
- Com servidor rodando: bash scripts/test-unauthenticated-decline.sh deve retornar PASS (401) para declínio sem auth
</verification>

<success_criteria>
- tests/e2e/auth-screen.spec.ts cobre tela Sylvan, botão ZITADEL, redirecionamento sem auth, e POST 401
- tests/e2e/family-access.spec.ts corrigido para rotas reais da fase 02
- scripts/test-unauthenticated-decline.sh automatiza o item 3 de VERIFICATION.md (CR-03 em runtime)
- Todos os testes auto-executáveis passam com servidor em execução sem credenciais ZITADEL reais
</success_criteria>

<output>
Create `.planning/phases/02-family-access-tenancy-roles-and-profiles/02-09-SUMMARY.md` when done
</output>
