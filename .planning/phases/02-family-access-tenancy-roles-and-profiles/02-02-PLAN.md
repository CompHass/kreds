---
phase: 02-family-access-tenancy-roles-and-profiles
plan: 2
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - auth.ts
  - src/app/api/auth/[...nextauth]/route.ts
  - src/lib/env.ts
requirements: [FAM-01]
autonomous: false
user_setup:
  - service: zitadel
    why: "Guardian authentication through HassLab ZITADEL for FAM-01"
    env_vars:
      - name: AUTH_SECRET
        source: "Generate locally with Auth.js secret tooling or password manager"
      - name: AUTH_ZITADEL_ID
        source: "ZITADEL console application client ID"
      - name: AUTH_ZITADEL_SECRET
        source: "ZITADEL console application client secret"
      - name: AUTH_ZITADEL_ISSUER
        source: "https://auth.hasslab.pro"
    dashboard_config:
      - task: "Register local redirect URI /api/auth/callback/zitadel if live OIDC verification is attempted"
        location: "ZITADEL console application settings"
must_haves:
  truths:
    - "Parent can authenticate through ZITADEL before family creation per D-01."
    - "Auth config uses stable ZITADEL subject and verified-email gate when claim is present."
  artifacts:
    - path: "auth.ts"
      provides: "Auth.js ZITADEL configuration"
      exports: ["handlers", "auth", "signIn", "signOut"]
    - path: "src/app/api/auth/[...nextauth]/route.ts"
      provides: "App Router Auth.js GET/POST handlers"
    - path: "src/lib/env.ts"
      provides: "Validated Auth.js/ZITADEL env vars"
  key_links:
    - from: "src/app/api/auth/[...nextauth]/route.ts"
      to: "auth.ts"
      via: "exported handlers"
      pattern: "GET|POST"
---

<objective>
Add the ZITADEL/Auth.js authentication foundation only.

Purpose: Split auth/env work away from schema, domain commands, API, and UI so the plan stays within context budget.
Output: Auth package pin, env validation, Auth.js config, and auth route.
</objective>

<execution_context>
@/Users/hass/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hass/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-CONTEXT.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-RESEARCH.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-PATTERNS.md
</context>

## Source Coverage Audit

| Source Item | Coverage |
|---|---|
| GOAL/REQ FAM-01 authentication | Covered by Auth.js/ZITADEL route and env validation. |
| RESEARCH resolved ZITADEL config | Covered by selected env names and issuer. |
| CONTEXT D-01 | Covered by auth-before-family onboarding prerequisite; child data not collected here. |

<tasks>

<task type="auto">
  <name>Task 1: Install Auth.js and validate ZITADEL env</name>
  <files>package.json, pnpm-lock.yaml, src/lib/env.ts</files>
  <action>Install and pin `next-auth@5.0.0-beta.31` only; research marks it slopcheck OK. Extend `src/lib/env.ts` using the existing Zod object pattern from `02-PATTERNS.md` / `src/lib/env.ts` for `AUTH_SECRET`, `AUTH_ZITADEL_ID`, `AUTH_ZITADEL_SECRET`, and `AUTH_ZITADEL_ISSUER` defaulting to `https://auth.hasslab.pro`. Do not install `@auth/drizzle-adapter` or `openid-client` in this plan.</action>
  <verify><automated>pnpm install --lockfile-only && pnpm test -- tests/unit/family-authorization.test.ts</automated></verify>
  <done>Auth dependency and env keys are present without introducing unplanned packages.</done>
</task>

<task type="auto">
  <name>Task 2: Add Auth.js ZITADEL route</name>
  <files>auth.ts, src/app/api/auth/[...nextauth]/route.ts</files>
  <action>Create root `auth.ts` as a small config export following the `src/lib/env.ts`/`src/lib/logger.ts` export style mapped in `02-PATTERNS.md`. Configure the Zitadel provider from validated env values, preserve the ZITADEL `sub` for downstream local identity mapping, and reject sign-in when `email_verified` is explicitly false. Create `src/app/api/auth/[...nextauth]/route.ts` using the App Router route export pattern from `src/app/api/health/route.ts`: export `GET` and `POST` from `handlers`.</action>
  <verify><automated>pnpm build</automated></verify>
  <done>Auth route compiles and exposes Auth.js handlers for ZITADEL-backed guardian sessions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Confirm ZITADEL local app config for live login</name>
  <files>auth.ts, src/app/api/auth/[...nextauth]/route.ts, src/lib/env.ts</files>
  <action>Pause only to verify external ZITADEL console/client setup that the agent cannot access. Do not ask the human to implement code; code and automated build checks are already handled by Tasks 1-2.</action>
  <verify><automated>pnpm build</automated></verify>
  <what-built>Auth.js/ZITADEL configuration and local route handlers.</what-built>
  <how-to-verify>Confirm the ZITADEL application has callback `/api/auth/callback/zitadel` for the local app URL and provide env vars if live login should be tested. If credentials are unavailable, execution may continue with automated boundary tests and the summary must record live OIDC as not verified.</how-to-verify>
  <resume-signal>Type "approved" with env availability, or state that live OIDC verification is unavailable.</resume-signal>
  <done>ZITADEL live-login availability is recorded, and build still passes for auth configuration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser to Auth.js | Untrusted redirects/cookies enter auth boundary. |
| Auth.js to ZITADEL | External IdP assertions become local session claims. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Spoofing | High | `auth.ts` | mitigate | Validate Auth.js ZITADEL session, preserve stable `sub`, reject unverified email when claim is false. |
| T-02-SC | Tampering | High | npm install | mitigate | Install only `next-auth@5.0.0-beta.31` from research package note; no unvetted auth packages. |
</threat_model>

<verification>
<automated>pnpm build</automated>
<human-check>Live ZITADEL login only if user provides configured client env vars.</human-check>
</verification>

<success_criteria>
- Auth.js ZITADEL route compiles.
- Env keys are validated.
- No schema, domain, onboarding UI, or family API work is mixed into this plan.
</success_criteria>

<output>
Create `.planning/phases/02-family-access-tenancy-roles-and-profiles/02-02-SUMMARY.md` when done.
</output>
