---
phase: 02-family-access-tenancy-roles-and-profiles
plan: 3
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/lib/db/schema/index.ts
  - drizzle/0002_family_access.sql
requirements: [FAM-01, FAM-02, FAM-03, FAM-04, FAM-05, FAM-06, FAM-07]
autonomous: false
must_haves:
  truths:
    - "Kreds stores family roles by family_id independent of ZITADEL claims per D-14, D-15, D-16."
    - "Schema supports invitations, child profiles, explicit consent, and sanitized audit events."
    - "Drizzle migration is generated and `pnpm db:push` runs before dependent integration/build verification."
  artifacts:
    - path: "src/lib/db/schema/index.ts"
      provides: "Identity, membership, invitation, child profile, consent, and audit schema"
      contains: "family_memberships"
    - path: "drizzle/0002_family_access.sql"
      provides: "Generated Phase 02 migration"
  key_links:
    - from: "src/lib/db/schema/index.ts"
      to: "tests/integration/family-tenancy.test.ts"
      via: "migration-backed schema assertions"
      pattern: "family_memberships|family_audit_events"
---

<objective>
Create and push the Phase 02 data model before any domain/API/UI code depends on it.

Purpose: Fix schema verification order and isolate schema work from auth/domain/UI implementation.
Output: Drizzle schema and migration for family access, tenancy, roles, profiles, invitations, consents, and audit.
</objective>

<execution_context>
@/Users/hass/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hass/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-CONTEXT.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-RESEARCH.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-PATTERNS.md
@src/lib/db/schema/index.ts
@drizzle.config.ts
</context>

## Source Coverage Audit

| Source Item | Coverage |
|---|---|
| GOAL isolated family accounts with roles/profiles/audit | Covered by family_id schema and indexes. |
| REQ FAM-01 through FAM-07 | Schema foundation covers identity, memberships, invitations, children, avatars, audit. |
| RESEARCH data model and RLS resolution | Covered by app-authorized family_id schema; RLS is not implemented in Phase 02 per resolved research. |
| CONTEXT D-02, D-05-D-17, D-19-D-22 | Covered by table columns/enums/constraints; deferred child login and avatar progression excluded. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Drizzle schema for family tenancy</name>
  <files>src/lib/db/schema/index.ts</files>
  <behavior>
    - Test 1: schema exports identities, memberships, invitations, child profiles, consents, and audit events.
    - Test 2: fixed role/status values reject invalid family roles and invitation states.
    - Test 3: all family-scoped tables include indexed `family_id`.
  </behavior>
  <action>Extend the existing schema barrel rather than creating a parallel tenancy schema, following the `pgTable` camelCase-to-snake_case pattern from `src/lib/db/schema/index.ts` mapped in `02-PATTERNS.md`. Add Kreds identities keyed by `zitadel_subject`, family memberships with role `guardian`/`child`, guardian invitations with hashed-token lifecycle states, child profiles with age years, avatar preset, accent color, nullable future identity link, parental consent evidence, and family audit events with sanitized metadata. Include constraints/indexes for duplicate active memberships, duplicate pending invitations by normalized email, child profiles requiring a family, and family_id filtering. Do not add PostgreSQL RLS in this phase per resolved research.</action>
  <verify><automated>pnpm test -- tests/integration/family-tenancy.test.ts</automated></verify>
  <done>Schema expresses all Phase 02 family access entities and fixed role/status values.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Generate and push Drizzle migration immediately after schema edits</name>
  <files>drizzle/0002_family_access.sql, src/lib/db/schema/index.ts</files>
  <action>Run `pnpm db:generate` immediately after Task 1, inspect the generated migration for Phase 02 tables/indexes, then run `pnpm db:push` before any integration/build verification that depends on new schema. If `pnpm db:push` prompts or cannot connect, stop and request the port-forward/database approval; do not mark downstream verification complete based only on build/typecheck.</action>
  <verify><automated>pnpm db:generate && pnpm db:push</automated></verify>
  <what-built>Phase 02 database schema and generated Drizzle migration.</what-built>
  <how-to-verify>Confirm `pnpm db:push` completed against the intended PostgreSQL database after reviewing the generated migration.</how-to-verify>
  <resume-signal>Type "approved" after schema push succeeds, or paste the exact database/prompt error.</resume-signal>
  <done>Database schema is pushed before domain/API/UI verification proceeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Domain commands to PostgreSQL | Untrusted family-scoped data must be constrained by schema and family_id indexes. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-02 | Tampering | High | `src/lib/db/schema/index.ts` | mitigate | Fixed enums/checks for roles/statuses and transaction-ready tables. |
| T-02-03 | Information Disclosure | family-scoped tables | High | mitigate | `family_id` on every family-scoped table and indexes for authorization-filtered queries. |
| T-02-04 | Repudiation | `family_audit_events` | Medium | mitigate | Dedicated sanitized audit table for identity/membership/invitation/profile events. |
</threat_model>

<verification>
<automated>pnpm db:generate && pnpm db:push && pnpm test -- tests/integration/family-tenancy.test.ts</automated>
</verification>

<success_criteria>
- Schema is generated and pushed before domain/API/UI plans run.
- Every family-scoped table has `family_id` and supports later authorization tests.
</success_criteria>

<output>
Create `.planning/phases/02-family-access-tenancy-roles-and-profiles/02-03-SUMMARY.md` when done.
</output>
