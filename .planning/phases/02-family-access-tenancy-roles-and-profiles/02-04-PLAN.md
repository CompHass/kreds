---
phase: 02-family-access-tenancy-roles-and-profiles
plan: 4
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - src/lib/auth/session.ts
  - src/lib/auth/authorization.ts
  - src/lib/families/commands.ts
  - src/lib/families/timezones.ts
  - src/app/api/families/route.ts
  - src/app/page.tsx
  - src/app/family/onboarding/page.tsx
requirements: [FAM-01, FAM-04, FAM-05, FAM-07]
autonomous: true
must_haves:
  truths:
    - "Authenticated guardians with no Kreds family create the family tenant before child data per D-01."
    - "Family onboarding requires family name and canonical IANA timezone while showing readable locality per D-03."
    - "Successful family creation directs the guardian to `/family/children` per D-04."
    - "Family reads are membership-checked and never enumerate all families per D-16."
  artifacts:
    - path: "src/lib/auth/authorization.ts"
      provides: "Server-side family authorization helpers"
      exports: ["requireActiveGuardian", "requireFamilyMember"]
    - path: "src/lib/families/commands.ts"
      provides: "Transactional family creation command"
      exports: ["createFamilyForGuardian"]
    - path: "src/app/family/onboarding/page.tsx"
      provides: "Authenticated family creation UI"
  key_links:
    - from: "src/app/api/families/route.ts"
      to: "src/lib/auth/authorization.ts"
      via: "membership-checked query"
      pattern: "requireFamilyMember|requireActiveGuardian"
---

<objective>
Implement family authorization, transactional family creation, guarded family API, and onboarding UI.

Purpose: Complete the FAM-01/FAM-04/FAM-05 vertical slice after auth and schema exist, without mixing package install or migration work.
Output: Session helpers, family role authorization, family creation command, timezone constants, protected API, homepage routing, and onboarding page.
</objective>

<execution_context>
@/Users/hass/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hass/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-CONTEXT.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-RESEARCH.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-PATTERNS.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-02-SUMMARY.md
@.planning/phases/02-family-access-tenancy-roles-and-profiles/02-03-SUMMARY.md
</context>

## Source Coverage Audit

| Source Item | Coverage |
|---|---|
| GOAL: parents authenticate and create isolated family accounts | Covered by family creation command and onboarding UI. |
| REQ FAM-01/FAM-04/FAM-05/FAM-07 | Covered by identity mapping, Kreds guardian membership, family_id authorization, and audit event. |
| RESEARCH authorization service slice and family onboarding slice | Covered by session helpers, commands, protected API, and homepage/onboarding. |
| CONTEXT D-01, D-03, D-04, D-14-D-18 | Covered explicitly in actions and acceptance. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement server-side identity and family authorization helpers</name>
  <files>src/lib/auth/session.ts, src/lib/auth/authorization.ts</files>
  <behavior>
    - Test 1: unauthenticated users are rejected.
    - Test 2: inactive memberships and cross-family memberships are rejected.
    - Test 3: active guardians pass guardian-only checks; child role does not.
  </behavior>
  <action>Implement server-only helpers using `auth()` from Plan 02, `db` from `src/lib/db/index.ts`, and schema imports through `@/lib/*` aliases per `02-PATTERNS.md`. `requireAuthenticatedIdentity` must map the stable ZITADEL `sub` to local Kreds identity and never use mutable email as the primary key. `requireFamilyMember` and `requireActiveGuardian` must constrain queries by `family_id` and Kreds membership role/status per D-14, D-15, and D-16.</action>
  <verify><automated>pnpm test -- tests/unit/family-authorization.test.ts</automated></verify>
  <done>Authorization helpers enforce server-side family membership and role checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement family creation command, guarded API, and onboarding redirect</name>
  <files>src/lib/families/commands.ts, src/lib/families/timezones.ts, src/app/api/families/route.ts, src/app/page.tsx, src/app/family/onboarding/page.tsx</files>
  <behavior>
    - Test 1: `createFamilyForGuardian` creates identity, family, active guardian membership, and audit event in one transaction.
    - Test 2: family name and IANA timezone are required; readable locality labels are shown.
    - Test 3: successful family creation routes to `/family/children` per D-04.
    - Test 4: `/api/families` returns only authorized current-family data and never all rows.
  </behavior>
  <action>Implement `FAMILY_TIMEZONES` as constants following the `src/modules/glossary/terms.ts` pattern from `02-PATTERNS.md`. Implement `createFamilyForGuardian` with a Drizzle transaction per the domain-command pattern in `tests/integration/db-connection.test.ts`: create or reuse local identity, create family with canonical timezone, create active Kreds guardian membership, and write sanitized audit event per D-01, D-03, D-14-D-18. Replace the unsafe all-family `db.select().from(schema.families)` pattern in `src/app/api/families/route.ts` with membership-checked current-family behavior. Replace homepage family count with auth-aware branches and link no-family guardians to onboarding. In `src/app/family/onboarding/page.tsx`, after successful family creation redirect/route to `/family/children` per D-04; do not collect child profile fields on this page.</action>
  <verify><automated>pnpm test -- tests/unit/family-authorization.test.ts tests/integration/family-tenancy.test.ts && pnpm build</automated></verify>
  <done>Authenticated guardian can create an isolated family and is directed to first child setup while unscoped family enumeration is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser to onboarding/API | Untrusted family name/timezone enters server commands. |
| Next.js to PostgreSQL | Family reads/writes must be constrained by membership and family_id. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-05 | Information Disclosure | `/api/families`, `src/app/page.tsx` | High | mitigate | Remove all-family enumeration and require membership-filtered queries. |
| T-02-06 | Elevation of Privilege | `requireActiveGuardian` | High | mitigate | Check active Kreds guardian role by family_id, not only ZITADEL claims. |
| T-02-07 | Repudiation | `createFamilyForGuardian` | Medium | mitigate | Write sanitized audit event in the same transaction as family/membership creation. |
</threat_model>

<verification>
<automated>pnpm test -- tests/unit/family-authorization.test.ts tests/integration/family-tenancy.test.ts && pnpm build</automated>
</verification>

<success_criteria>
- Family creation redirects/routes to `/family/children` per D-04.
- No route or homepage exposes all-family counts or lists.
- Kreds family role is stored by `family_id` independent of ZITADEL claims.
</success_criteria>

<output>
Create `.planning/phases/02-family-access-tenancy-roles-and-profiles/02-04-SUMMARY.md` when done.
</output>
