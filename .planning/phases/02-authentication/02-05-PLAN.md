---
phase: 02-authentication
plan: 05
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/components/auth/auth-input.tsx
  - src/components/auth/spinner-button.tsx
  - src/components/auth/social-auth-buttons.tsx
  - src/components/auth/guardian-login-form.tsx
  - src/components/auth/password-reset-form.tsx
  - src/app/actions/guardian-auth.ts
  - src/app/login/page.tsx
  - src/app/login/reset/page.tsx
autonomous: false
requirements: [GAUTH-01, GAUTH-02, GAUTH-03, GAUTH-04, GAUTH-05]
user_setup:
  - service: zitadel
    why: "Login OIDC do responsável (signIn), federation Google/Apple e reset de senha"
    env_vars:
      - name: AUTH_ZITADEL_ID
        source: "Zitadel Console → Project → Application → Client ID (já validado em env.ts)"
      - name: AUTH_ZITADEL_SECRET
        source: "Zitadel Console → Project → Application → Client Secret"
      - name: AUTH_ZITADEL_ISSUER
        source: "URL da instância Zitadel (default https://auth.hasslab.pro)"
    dashboard_config:
      - task: "Configurar IdPs Google e Apple com nomes 'google' e 'apple' (identity_provider hint D-05)"
        location: "Zitadel Console → Identity Providers"
      - task: "Habilitar Passkey/WebAuthn como método de login (D-06)"
        location: "Zitadel Console → Login Policy"
must_haves:
  truths:
    - "Responsável vê tela de login com e-mail, senha (toggle olho), checkbox Lembrar-me, botão Entrar"
    - "Botão Entrar exibe spinner CSS branco (kredsSpin) durante loading e usa signIn('zitadel')"
    - "Botões Google/Apple usam identity_provider hint; Passkey chama signIn('zitadel')"
    - "Tela /login/reset tem form de e-mail e estado de confirmação com e-mail mascarado + reenviar"
  artifacts:
    - path: "src/components/auth/auth-input.tsx"
      provides: "Input estilizado 52px com ícone e focus ring"
      exports: ["AuthInput"]
    - path: "src/components/auth/spinner-button.tsx"
      provides: "Botão com estado loading (spinner CSS)"
      exports: ["SpinnerButton"]
    - path: "src/components/auth/social-auth-buttons.tsx"
      provides: "Botões Google/Apple/Passkey"
      exports: ["SocialAuthButtons"]
    - path: "src/components/auth/guardian-login-form.tsx"
      provides: "Form de login do responsável"
      exports: ["GuardianLoginForm"]
    - path: "src/components/auth/password-reset-form.tsx"
      provides: "Form de reset com 2 estados"
      exports: ["PasswordResetForm"]
    - path: "src/app/actions/guardian-auth.ts"
      provides: "Server Action wrappers de signIn (email/senha, google, apple, passkey)"
      exports: ["loginWithCredentials", "loginWithProvider", "loginWithPasskey"]
    - path: "src/app/login/page.tsx"
      provides: "Tela de login do responsável"
      exports: ["default"]
    - path: "src/app/login/reset/page.tsx"
      provides: "Tela de redefinição de senha"
      exports: ["default"]
  key_links:
    - from: "src/app/actions/guardian-auth.ts"
      to: "auth.ts signIn"
      via: "signIn('zitadel', ...) — caminho relativo (auth.ts na raiz, fora de @/)"
      pattern: "signIn"
    - from: "src/components/auth/social-auth-buttons.tsx"
      to: "src/app/actions/guardian-auth.ts"
      via: "loginWithProvider('google'|'apple') → signIn com identity_provider hint"
      pattern: "identity_provider"
---

<objective>
Construir o fluxo de autenticação do responsável (GAUTH-01..05): tela de login com e-mail/senha (toggle de visibilidade), checkbox "Lembrar-me" custom, botão "Entrar" com spinner de loading, botões sociais Google/Apple/Passkey via Zitadel federation, e a tela de redefinição de senha com dois estados (form → confirmação com e-mail mascarado). Componentes reutilizáveis (AuthInput, SpinnerButton) servem login e reset. Server Actions finas em `guardian-auth.ts` encapsulam as chamadas a `signIn` (auth.ts está na raiz, fora do alias `@/`).

Purpose: GAUTH-01..05 locked. O login do responsável é a porta de entrada do painel dos pais (Fase 5). D-04..D-06 locked: Google/Apple/Passkey operam via Zitadel federation (auth.ts NÃO muda). Depende do route handler do 02-02 para o fluxo signIn/callback funcionar.
Output: 5 componentes + Server Action wrapper em `src/app/` + `src/app/login/page.tsx` + `src/app/login/reset/page.tsx` + checkpoint visual aprovado.
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-UI-SPEC.md
@.planning/phases/02-authentication/02-PATTERNS.md
@.planning/phases/02-authentication/02-RESEARCH.md
@auth.ts
@tsconfig.json
@src/app/globals.css
@src/app/page.tsx
</context>

<artifacts_this_phase_produces>
| Símbolo | Arquivo | Assinatura | Consumido por |
|---------|---------|------------|---------------|
| `AuthInput` | src/components/auth/auth-input.tsx | `({ id, label, icon, type, placeholder, value, onChange, rightSlot? }) => JSX` | GuardianLoginForm, PasswordResetForm |
| `SpinnerButton` | src/components/auth/spinner-button.tsx | `({ loading, children, ...buttonProps }) => JSX` | GuardianLoginForm, PasswordResetForm |
| `SocialAuthButtons` | src/components/auth/social-auth-buttons.tsx | `() => JSX` | GuardianLoginForm/page login |
| `GuardianLoginForm` | src/components/auth/guardian-login-form.tsx | `() => JSX` | page login |
| `PasswordResetForm` | src/components/auth/password-reset-form.tsx | `() => JSX` | page reset |
| `loginWithCredentials` | src/app/actions/guardian-auth.ts | `(formData) => Promise<void>` (signIn zitadel) | GuardianLoginForm |
| `loginWithProvider` | src/app/actions/guardian-auth.ts | `(idp: 'google'\|'apple') => Promise<void>` | SocialAuthButtons |
| `loginWithPasskey` | src/app/actions/guardian-auth.ts | `() => Promise<void>` | SocialAuthButtons |
| `default` (login) | src/app/login/page.tsx | Server Component | runtime / middleware redirect |
| `default` (reset) | src/app/login/reset/page.tsx | Server Component | runtime / link "Esqueci minha senha" |
</artifacts_this_phase_produces>

<interface_context>
auth.ts (raiz) exporta `signIn`. O alias `@/*` aponta para `src/*`, então auth.ts (na raiz) NÃO é importável via `@/`.
Para chamar signIn do client: criar Server Actions finas em src/app/actions/guardian-auth.ts ('use server')
que importam signIn de auth.ts via caminho relativo correto (contar níveis a partir de src/app/actions/ →
`../../../auth` resolve para a raiz; CONFIRMAR contando, como no route.ts do 02-02). Isso mantém signIn
server-side e evita caminho relativo frágil espalhado pelos componentes.

signIn assinaturas (next-auth v5):
- email/senha: `signIn('zitadel', { redirectTo: '/family' })` (D-04: provider único Zitadel)
- Google (D-05): `signIn('zitadel', {}, { identity_provider: 'google' })`
- Apple (D-05): `signIn('zitadel', {}, { identity_provider: 'apple' })`
- Passkey (D-06): `signIn('zitadel')`
> NOTA: o nome exato do IdP ('google'/'apple') deve casar com o configurado no Zitadel (user_setup).
> Se o login OIDC falhar por IdP inexistente, é config externa, não bug de código.

kredsSpin já existe em globals.css (linha 56): var(--animate-kreds-spin) (0.7s linear infinite).
GAUTH-05 (reset): RESEARCH §Open Questions 2 — endpoint de reset do Zitadel é [ASSUMED]. Implementar a UI
de dois estados (form + confirmação mascarada) com o submit fazendo a melhor tentativa (redirect para
hosted reset do Zitadel ou no-op visual). A UI dos dois estados é o entregável verificável de GAUTH-05.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Componentes base — AuthInput, SpinnerButton, SocialAuthButtons + guardian-auth Server Actions</name>
  <files>src/components/auth/auth-input.tsx, src/components/auth/spinner-button.tsx, src/components/auth/social-auth-buttons.tsx, src/app/actions/guardian-auth.ts</files>
  <read_first>
    - .planning/phases/02-authentication/02-UI-SPEC.md §Screen 3 (Input e-mail/senha, Botão Entrar/loading, Botões Google/Apple/Passkey — medidas e cores exatas)
    - .planning/phases/02-authentication/02-PATTERNS.md §Componentes em src/components/auth/ + §Shared Patterns
    - src/app/globals.css (linha 56: kredsSpin)
    - auth.ts (signIn export)
    - tsconfig.json (alias @/* → src/*; auth.ts na raiz)
  </read_first>
  <action>
    **guardian-auth.ts (`src/app/actions/guardian-auth.ts`)** — PRIMEIRA LINHA `'use server'`. Importar `signIn` de auth.ts via caminho relativo (de src/app/actions/ → `../../../auth`; CONFIRMAR contando níveis, validar com tsc). Exportar:
    - `loginWithCredentials(formData: FormData)` — extrai email/password e chama `signIn('zitadel', { redirectTo: '/family' })` (GAUTH-01). NÃO criar Credentials provider — auth.ts não muda (D-04); o fluxo é OIDC redirect.
    - `loginWithProvider(idp: 'google' | 'apple')` — `signIn('zitadel', {}, { identity_provider: idp })` (D-05).
    - `loginWithPasskey()` — `signIn('zitadel')` (D-06).

    Componentes a seguir todos com `'use client'` na primeira linha.

    **AuthInput (`auth-input.tsx`)** — input controlado estilizado:
    - Props: `{ id: string; label: string; icon: ReactNode; type?: string; placeholder?: string; value: string; onChange: (e) => void; rightSlot?: ReactNode }`.
    - Visual (02-UI-SPEC Screen 3): altura 52px, `border-radius: 13px`, `border: 1.5px solid #E2DECF`, `bg #fff`, padding 16px horizontal. Ícone SVG à esquerda 16px cor `--color-kreds-muted`. Placeholder 15px/500 cor `--color-kreds-hint`.
    - Focus: `border-color: #3E6B4F; box-shadow: 0 0 0 3px rgba(62,107,79,.13)` (usar :focus-within no wrapper ou estado).
    - Acessibilidade: `<label htmlFor={id}>` associado (02-UI-SPEC Accessibility — não só placeholder). `rightSlot` para o toggle de olho do campo senha.

    **SpinnerButton (`spinner-button.tsx`)** — botão com loading:
    - Props: `{ loading: boolean; children: ReactNode } & ButtonHTMLAttributes`.
    - Visual (02-UI-SPEC Screen 3 Botão Entrar): 52px altura, `border-radius: 13px`, `bg #3E6B4F`, texto 15px/700 branco, `box-shadow: 0 12px 24px -12px rgba(62,107,79,.6)`.
    - Loading (GAUTH-04): renderizar spinner — `<div>` 20×20px, `border: 2.5px solid rgba(255,255,255,.3)`, `border-top-color: #fff`, `border-radius: 50%`, `style={{ animation: 'var(--animate-kreds-spin)' }}`. Setar `aria-busy="true"` e `disabled` durante loading.
    - Disabled (campos vazios): `bg #C2C9BC; cursor: not-allowed`.

    **SocialAuthButtons (`social-auth-buttons.tsx`)** — 3 botões:
    - Cada botão 44px, `border-radius: 13px`. Google: `border:1.5px solid #E2DECF; bg #fff`, texto "Continuar com Google" 12px/500. Apple: `bg #23302A` texto branco "Continuar com Apple". Passkey: `border:1.5px solid #E2DECF; bg #FBFAF5` texto "Entrar com Passkey". Logos SVG inline 20px à esquerda.
    - Ações (D-04/D-05/D-06): Google → `loginWithProvider('google')`; Apple → `loginWithProvider('apple')`; Passkey → `loginWithPasskey()` (importados de @/app/actions/guardian-auth). NÃO criar providers separados no NextAuth (D-04: auth.ts não muda).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep -iE "auth-input|spinner-button|social-auth|guardian-auth" || echo "OK: componentes base + guardian-auth type-check"</automated>
  </verify>
  <acceptance_criteria>
    - 3 componentes + src/app/actions/guardian-auth.ts criados; client components com 'use client', action com 'use server'
    - guardian-auth.ts importa signIn de auth.ts (caminho relativo validado por tsc) e NÃO modifica auth.ts (D-04)
    - SpinnerButton em loading usa var(--animate-kreds-spin) e aria-busy="true" (GAUTH-04)
    - AuthInput usa <label htmlFor> associado (não só placeholder)
    - SocialAuthButtons: Google/Apple usam loginWithProvider (identity_provider hint); Passkey usa loginWithPasskey (D-05/D-06)
    - `pnpm tsc --noEmit` sem erro nos arquivos
  </acceptance_criteria>
  <done>AuthInput, SpinnerButton, SocialAuthButtons e guardian-auth Server Actions existem; ações sociais via Zitadel federation sem alterar auth.ts.</done>
</task>

<task type="auto">
  <name>Task 2: GuardianLoginForm + PasswordResetForm + páginas /login e /login/reset</name>
  <files>src/components/auth/guardian-login-form.tsx, src/components/auth/password-reset-form.tsx, src/app/login/page.tsx, src/app/login/reset/page.tsx</files>
  <read_first>
    - .planning/phases/02-authentication/02-UI-SPEC.md §Screen 3 (ordem dos elementos, checkbox, copy) e §Screen 4 (reset: 2 estados, mascaramento) e §Copywriting Contract
    - .planning/phases/02-authentication/02-PATTERNS.md §`src/app/login/page.tsx` e §`src/app/login/reset/page.tsx`
    - src/components/auth/auth-input.tsx, src/components/auth/spinner-button.tsx, src/components/auth/social-auth-buttons.tsx (criados no Task 1)
    - src/app/actions/guardian-auth.ts (loginWithCredentials — criado no Task 1)
  </read_first>
  <action>
    **GuardianLoginForm (`guardian-login-form.tsx`)** — `'use client'`:
    - Estado: email, password, showPassword (toggle olho), rememberMe (checkbox), loading, error (mensagem OIDC).
    - Ordem (02-UI-SPEC Screen 3): título "Bem-vindo de volta" (Display 24/700), subtítulo "Entre com sua conta familiar" (15/500 muted), AuthInput e-mail (ícone envelope), AuthInput senha (ícone cadeado + rightSlot olho que alterna type password/text e aria-label "Mostrar senha"/"Ocultar senha"), linha [checkbox "Lembrar-me" + link "Esqueci minha senha" → /login/reset], SpinnerButton "Entrar na conta", divisor "ou continue com", `<SocialAuthButtons />`, rodapé "Não tem conta? Criar conta".
    - Checkbox "Lembrar-me" (GAUTH-03): 16×16px, `border-radius: 4px`, `border:1.5px solid #E2DECF`; marcado `bg #3E6B4F` + checkmark SVG branco. Controlado (rememberMe state).
    - Submit (GAUTH-01/GAUTH-04): setLoading(true), chamar `loginWithCredentials` (Server Action do Task 1). Em erro mostrar "E-mail ou senha incorretos. Tente novamente." em container `role="alert"`. Spinner durante loading (SpinnerButton loading).
    - Botão disabled quando email ou password vazios.

    **PasswordResetForm (`password-reset-form.tsx`)** — `'use client'` (GAUTH-05):
    - Estado: `step` ('form' | 'sent'), email, loading.
    - Estado 'form' (02-UI-SPEC Screen 4 Estado 1): seta voltar (40×40px) topo-esq, título "Redefinir senha", subtítulo "Digite seu e-mail para receber o link", AuthInput e-mail, SpinnerButton "Enviar link". Submit → loading → tentar reset (redirect hosted Zitadel ou no-op) → `setStep('sent')`.
    - Estado 'sent' (02-UI-SPEC Screen 4 Estado 2): ícone check verde (círculo 56px `bg #E7EFE8`, checkmark `#3E6B4F`), título "E-mail enviado!", copy "Enviamos o link para {maskEmail(email)}. Verifique sua caixa de entrada.", botão "Reenviar e-mail" (outline), link "Voltar ao login" → /login.
    - `maskEmail(email)`: manter 3 chars antes de `***`, domínio completo (ex.: `ana***@email.com`). Implementar inline.

    **Página login (`src/app/login/page.tsx`)** — Server Component shell + `<GuardianLoginForm />`. Logo no topo (48px do topo), padding lateral 24px, fundo bg-kreds-bg. NÃO importar next/font.

    **Página reset (`src/app/login/reset/page.tsx`)** — Server Component shell + `<PasswordResetForm />`. Mesmo padding/fundo.

    Copy: usar EXATAMENTE as strings do 02-UI-SPEC §Copywriting Contract.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep -iE "guardian-login-form|password-reset-form|app/login" || echo "OK: forms + páginas login/reset type-check"</automated>
  </verify>
  <acceptance_criteria>
    - GuardianLoginForm: campos e-mail/senha, toggle olho com aria-label alternante, checkbox Lembrar-me custom verde (GAUTH-03), SpinnerButton com loading (GAUTH-04), SocialAuthButtons (GAUTH-02)
    - Submit chama loginWithCredentials (signIn zitadel) (GAUTH-01); erro em container role="alert"
    - PasswordResetForm: 2 estados; estado 'sent' mostra e-mail mascarado `xxx***@dominio` + "Reenviar e-mail" (GAUTH-05)
    - Páginas em src/app/login/page.tsx e src/app/login/reset/page.tsx
    - Todas as copy strings batem com 02-UI-SPEC §Copywriting Contract
    - `pnpm tsc --noEmit` sem erro nos arquivos
  </acceptance_criteria>
  <done>Tela de login e tela de reset completas com componentes do Task 1; copy e medidas conforme 02-UI-SPEC Screens 3 e 4.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual — login e reset do responsável (GAUTH-01..05)</name>
  <action>
    Verificação humana de GAUTH-01..05 — visual/manual conforme 02-VALIDATION.md (spinner CSS, checkbox custom, toggle de olho, e-mail mascarado não têm assert automatizado; o redirect OIDC depende de config Zitadel externa). O executor NÃO escreve código nesta task: finaliza as Tasks 1-2, sobe o dev server, apresenta os passos abaixo ao usuário e pausa até "approved". Se houver divergência do 02-UI-SPEC, corrigir os componentes da Task 1/2 antes de prosseguir.
  </action>
  <what-built>
    Tela de login do responsável (/login) com e-mail/senha, toggle de olho, checkbox Lembrar-me, botão Entrar com spinner, botões Google/Apple/Passkey, e tela de reset (/login/reset) com dois estados (form + confirmação mascarada). Login social via Zitadel federation.
  </what-built>
  <how-to-verify>
    1. Rodar `pnpm dev` e abrir `http://localhost:3000/login`.
    2. GAUTH-01: confirmar layout — título "Bem-vindo de volta", subtítulo, campos e-mail e senha, botão "Entrar na conta".
    3. Toggle olho: clicar no ícone do campo senha → alterna entre ocultar/mostrar o texto digitado; o aria-label muda de "Mostrar senha" para "Ocultar senha".
    4. GAUTH-03: marcar/desmarcar "Lembrar-me" → checkbox custom fica verde (#3E6B4F) com check branco quando marcado.
    5. GAUTH-04: clicar "Entrar na conta" com campos preenchidos → spinner CSS branco aparece no botão (kredsSpin) durante o loading. (O redirect OIDC requer Zitadel configurado — ver user_setup; o spinner é o entregável verificável aqui.)
    6. GAUTH-02: confirmar 3 botões sociais (Google, Apple, Passkey) com estilos distintos (Apple escuro). Clicar Google → deve iniciar redirect OIDC com hint de IdP (se Zitadel configurado).
    7. GAUTH-05: clicar "Esqueci minha senha" → abre /login/reset. Digitar um e-mail e clicar "Enviar link" → estado muda para confirmação com ícone check verde, "E-mail enviado!" e o e-mail mascarado no formato `xxx***@dominio.com`. Botão "Reenviar e-mail" e link "Voltar ao login" presentes.
  </how-to-verify>
  <resume-signal>Digite "approved" se GAUTH-01..05 conferem com o 02-UI-SPEC, ou descreva divergências.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (login form) → next-auth signIn | credenciais cruzam para o fluxo OIDC do Zitadel |
| Browser → /login/reset | e-mail digitado cruza para tentativa de reset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-OIDC-CSRF2 | Spoofing | GuardianLoginForm signIn | mitigate | next-auth v5 aplica state+PKCE+nonce no fluxo Zitadel (auth.ts não modificado, D-04) |
| T-02-CRED-LEAK | Information Disclosure | GuardianLoginForm | mitigate | senha em campo type=password; submetida via Server Action signIn server-side; nunca logada |
| T-02-ENUM-EMAIL | Information Disclosure | PasswordResetForm | mitigate | confirmação sempre mostra sucesso genérico ("E-mail enviado!") sem revelar se o e-mail existe; evita enumeração de contas |
| T-02-RESET-ASSUME | Tampering | PasswordResetForm reset endpoint | accept | endpoint de reset do Zitadel é [ASSUMED] (RESEARCH OQ2); UI de 2 estados é o entregável; integração real validada na config Zitadel (user_setup) |
| T-02-IDP-HINT | Tampering | guardian-auth loginWithProvider | accept | identity_provider hint é parâmetro de UX; nome do IdP validado pelo Zitadel server-side; hint inválido falha graciosamente no Zitadel |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado |
</threat_model>

<verification>
Tasks 1-2: `pnpm tsc --noEmit` limpo.
Task 3: checkpoint humano valida GAUTH-01..05 (visual/manual conforme 02-VALIDATION.md).
Regressão: `pnpm test` verde.
</verification>

<success_criteria>
- GAUTH-01: login com e-mail/senha + botão Entrar via signIn('zitadel')
- GAUTH-02: botões Google/Apple (identity_provider hint) + Passkey
- GAUTH-03: checkbox Lembrar-me custom verde funcional
- GAUTH-04: spinner CSS branco (kredsSpin) durante loading
- GAUTH-05: tela de reset com form + confirmação (e-mail mascarado + reenviar)
- D-04 respeitado (auth.ts não modificado)
- Checkpoint visual aprovado
</success_criteria>

<output>
Create `.planning/phases/02-authentication/02-05-SUMMARY.md` when done
</output>
