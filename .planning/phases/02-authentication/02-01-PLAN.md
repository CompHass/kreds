---
phase: 02-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/families/child-pin.ts
  - src/lib/families/child-session.ts
  - src/lib/auth/child-guard.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "hashPin/verifyPin/validatePinFormat existem e satisfazem child-pin-management.test.ts (9 testes)"
    - "signChildSession/verifyChildSession existem e satisfazem child-auth-endpoint.test.ts (parte JWT)"
    - "checkBruteForce/recordFailedAttempt/resetAttempts bloqueiam após 5 tentativas por childId"
    - "validateChildSessionScope/extractChildProfileId/extractFamilyId satisfazem child-session-guard.test.ts (6 testes)"
    - "D-08: child-session.ts reconstruído com contrato exato dos testes (signChildSession, verifyChildSession, checkBruteForce, recordFailedAttempt, resetAttempts)"
  artifacts:
    - path: "src/lib/families/child-pin.ts"
      provides: "Hash/verify/validate de PIN com bcrypt cost 10"
      exports: ["hashPin", "verifyPin", "validatePinFormat"]
    - path: "src/lib/families/child-session.ts"
      provides: "JWT sign/verify (jose HS256) + brute force in-memory Map"
      exports: ["signChildSession", "verifyChildSession", "checkBruteForce", "recordFailedAttempt", "resetAttempts"]
    - path: "src/lib/auth/child-guard.ts"
      provides: "Validação de escopo de sessão da criança"
      exports: ["validateChildSessionScope", "extractChildProfileId", "extractFamilyId"]
  key_links:
    - from: "src/lib/families/child-session.ts"
      to: "process.env.CHILD_SESSION_SECRET"
      via: "TextEncoder().encode(secret) para assinar/verificar JWT"
      pattern: "CHILD_SESSION_SECRET"
---

<objective>
Criar os três módulos de backend puros que formam a fundação criptográfica da autenticação da criança: hash de PIN (bcrypt), sessão JWT (jose HS256) com proteção brute-force in-memory, e o guard de validação de escopo de sessão. Estes módulos têm contratos exatos definidos por testes unitários que já existem e atualmente falham com `ERR_MODULE_NOT_FOUND`.

Purpose: Sem esses três módulos nenhuma verificação de PIN, assinatura de sessão ou proteção de rota da criança funciona. São pré-requisito de TODO o fluxo da criança (Server Action, middleware, páginas).
Output: `src/lib/families/child-pin.ts`, `src/lib/families/child-session.ts`, `src/lib/auth/child-guard.ts` — 22 testes unitários passando (9 + 7 + 6).
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Símbolos criados neste plano (consumidos por planos posteriores da fase):

| Símbolo | Arquivo | Assinatura | Consumido por |
|---------|---------|------------|---------------|
| `hashPin` | src/lib/families/child-pin.ts | `(pin: string) => Promise<string>` | 02-03 (Server Action verifyChildPin não usa; usado em seeds/admin) |
| `verifyPin` | src/lib/families/child-pin.ts | `(pin: string, hash: string) => Promise<boolean>` | 02-03 Server Action |
| `validatePinFormat` | src/lib/families/child-pin.ts | `(pin: string) => boolean` | 02-03 Server Action |
| `signChildSession` | src/lib/families/child-session.ts | `(payload: { childProfileId: string; familyId: string; role: 'child' }) => Promise<string>` | 02-03 Server Action |
| `verifyChildSession` | src/lib/families/child-session.ts | `(token: string) => Promise<{ childProfileId; familyId; role }>` | guardian/child SSR futuras |
| `checkBruteForce` | src/lib/families/child-session.ts | `(childId: string) => { blocked: boolean; attemptsLeft: number }` | 02-03 Server Action |
| `recordFailedAttempt` | src/lib/families/child-session.ts | `(childId: string) => void` | 02-03 Server Action |
| `resetAttempts` | src/lib/families/child-session.ts | `(childId: string) => void` | 02-03 Server Action |
| `validateChildSessionScope` | src/lib/auth/child-guard.ts | `(session, requestedChildId: string) => boolean` | child SSR futuras (Fase 3+) |
| `extractChildProfileId` | src/lib/auth/child-guard.ts | `(session: ChildSession) => string` | child SSR futuras |
| `extractFamilyId` | src/lib/auth/child-guard.ts | `(session: ChildSession) => string` | child SSR futuras |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo child-pin.ts (bcrypt hash/verify/validate)</name>
  <files>src/lib/families/child-pin.ts</files>
  <read_first>
    - tests/unit/child-pin-management.test.ts (contrato exato: 9 testes — fonte da verdade)
    - .planning/phases/02-authentication/02-PATTERNS.md §`src/lib/families/child-pin.ts` (imports + core pattern)
    - auth.ts (convenção de imports do projeto)
  </read_first>
  <behavior>
    - validatePinFormat('1234') === true (4 dígitos)
    - validatePinFormat('123456') === true (6 dígitos)
    - validatePinFormat('123') === false (curto demais)
    - validatePinFormat('1234567') === false (longo demais)
    - validatePinFormat('abcd') === false (não numérico)
    - validatePinFormat('12 34') === false (whitespace)
    - hashPin('1234') retorna string que começa com '$2' (formato bcrypt)
    - verifyPin('1234', hash) === true quando hash veio de hashPin('1234')
    - verifyPin('5678', hash) === false quando hash veio de hashPin('1234')
  </behavior>
  <action>
    Criar `src/lib/families/child-pin.ts`. Primeira linha do arquivo: `import 'server-only'`. Segunda linha: `import bcrypt from 'bcryptjs'`.
    Declarar `const COST_FACTOR = 10` (D-10 locked: cost factor 10).
    Exportar três funções:
    1. `validatePinFormat(pin: string): boolean` — retorna `/^\d{4,6}$/.test(pin)`. Aceita 4 a 6 dígitos numéricos, rejeita whitespace e não-numéricos.
    2. `hashPin(pin: string): Promise<string>` — retorna `bcrypt.hash(pin, COST_FACTOR)`.
    3. `verifyPin(pin: string, hash: string): Promise<boolean>` — retorna `bcrypt.compare(pin, hash)` (comparação constante no tempo — mitiga timing attack, T-02-PIN-TIMING).
    NÃO usar `crypto.createHash` nem SHA — bcrypt é obrigatório (resistência a brute-force). NÃO hashar PIN no cliente.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/child-pin-management.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Comando `pnpm test -- tests/unit/child-pin-management.test.ts` mostra 9 passed, 0 failed
    - Arquivo começa com `import 'server-only'`
    - `hashPin` usa cost factor 10 (string contém `$2b$10$` ou `$2a$10$` no hash gerado)
    - Nenhum uso de `createHash` ou algoritmo SHA no arquivo
  </acceptance_criteria>
  <done>9 testes de child-pin-management.test.ts passam; módulo exporta hashPin, verifyPin, validatePinFormat com bcrypt cost 10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Módulo child-session.ts (jose JWT + brute force Map)</name>
  <files>src/lib/families/child-session.ts</files>
  <read_first>
    - tests/unit/child-auth-endpoint.test.ts (contrato exato: 7 testes — fonte da verdade)
    - .planning/phases/02-authentication/02-PATTERNS.md §`src/lib/families/child-session.ts` (Core JWT pattern + Brute force pattern)
    - .planning/phases/02-authentication/02-RESEARCH.md §Pattern 1 e §Pattern 2
  </read_first>
  <behavior>
    - signChildSession({childProfileId:'uuid-1', familyId:'fam-1', role:'child'}) retorna string que começa com 'ey'
    - verifyChildSession(tokenValido) resolve para {childProfileId:'uuid-1', familyId:'fam-1', role:'child'}
    - verifyChildSession('token-invalido') rejeita com Error
    - checkBruteForce('child-1') inicial === {blocked:false, attemptsLeft:5}
    - após 5x recordFailedAttempt('child-1'): checkBruteForce('child-1') === {blocked:true, attemptsLeft:0}
    - resetAttempts('child-1') após falha: volta a {blocked:false, attemptsLeft:5}
    - contadores isolados por childId: falhas em 'child-1' não afetam 'child-2'
  </behavior>
  <action>
    Criar `src/lib/families/child-session.ts`. Primeira linha: `import 'server-only'`. Segunda: `import { SignJWT, jwtVerify, decodeJwt } from 'jose'`.
    Declarar `const secret = new TextEncoder().encode(process.env.CHILD_SESSION_SECRET!)` (D-11 locked: assinado com CHILD_SESSION_SECRET, HS256).
    Exportar:
    1. `signChildSession(payload: { childProfileId: string; familyId: string; role: 'child' }): Promise<string>` — usar `new SignJWT(payload).setProtectedHeader({ alg: 'HS256' }).setIssuedAt().setExpirationTime('8h').sign(secret)`. Expiração 8h (Claude's Discretion documentada na CONTEXT.md).
    2. `verifyChildSession(token: string): Promise<{ childProfileId: string; familyId: string; role: 'child' }>` — usar `const { payload } = await jwtVerify(token, secret)` e retornar `payload` com cast para o tipo. jwtVerify lança Error em token inválido (comportamento esperado pelo teste).
    Brute force (D-09 locked: in-memory Map, 5 tentativas):
    - `const MAX_ATTEMPTS = 5`
    - `const attempts = new Map<string, number>()`
    - `checkBruteForce(childId: string): { blocked: boolean; attemptsLeft: number }` — count = attempts.get(childId) ?? 0; retorna `{ blocked: count >= MAX_ATTEMPTS, attemptsLeft: Math.max(0, MAX_ATTEMPTS - count) }`
    - `recordFailedAttempt(childId: string): void` — `attempts.set(childId, (attempts.get(childId) ?? 0) + 1)`
    - `resetAttempts(childId: string): void` — `attempts.delete(childId)`
    NÃO hand-roll HMAC — usar jose (edge-compatible, já instalado). `decodeJwt` é importado para uso no middleware (02-02) mas pode ficar disponível aqui também.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/child-auth-endpoint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Comando `pnpm test -- tests/unit/child-auth-endpoint.test.ts` mostra 7 passed, 0 failed
    - Arquivo começa com `import 'server-only'`
    - JWT usa alg HS256 e expiração '8h'
    - Brute force usa `MAX_ATTEMPTS = 5` e `Map<string, number>`
    - Nenhuma implementação manual de HMAC/SHA — apenas jose
  </acceptance_criteria>
  <done>7 testes de child-auth-endpoint.test.ts passam; módulo exporta signChildSession, verifyChildSession, checkBruteForce, recordFailedAttempt, resetAttempts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Módulo child-guard.ts (validação de escopo de sessão)</name>
  <files>src/lib/auth/child-guard.ts</files>
  <read_first>
    - tests/unit/child-session-guard.test.ts (contrato exato: 6 testes — fonte da verdade)
    - .planning/phases/02-authentication/02-PATTERNS.md §`src/lib/auth/child-guard.ts` (core pattern)
  </read_first>
  <behavior>
    - validateChildSessionScope({childProfileId:'c-1', familyId:'f-1', role:'child'}, 'c-1') === true
    - validateChildSessionScope({...role:'child'}, 'c-2') === false (childId diferente)
    - validateChildSessionScope(null, 'c-1') === false (sem sessão)
    - validateChildSessionScope({...role:'guardian'}, 'c-1') === false (role != child)
    - extractChildProfileId({childProfileId:'c-1',...}) === 'c-1'
    - extractFamilyId({familyId:'f-1',...}) === 'f-1'
  </behavior>
  <action>
    Criar `src/lib/auth/child-guard.ts`. Primeira linha: `import 'server-only'`.
    Declarar a interface `ChildSession { childProfileId: string; familyId: string; role: 'child' }`.
    Exportar:
    1. `validateChildSessionScope(session: ChildSession | null | { childProfileId: string; familyId: string; role: string }, requestedChildId: string): boolean` — retorna false se `!session`, false se `session.role !== 'child'`, senão `session.childProfileId === requestedChildId`. A assinatura aceita union com role: string para suportar o teste que passa role:'guardian' (cast `as 'child'`).
    2. `extractChildProfileId(session: ChildSession): string` — retorna `session.childProfileId`.
    3. `extractFamilyId(session: ChildSession): string` — retorna `session.familyId`.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/child-session-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Comando `pnpm test -- tests/unit/child-session-guard.test.ts` mostra 6 passed, 0 failed
    - Arquivo começa com `import 'server-only'`
    - validateChildSessionScope rejeita session null E session com role != 'child'
  </acceptance_criteria>
  <done>6 testes de child-session-guard.test.ts passam; módulo exporta validateChildSessionScope, extractChildProfileId, extractFamilyId.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente → Server Action | PIN em plaintext cruza para o servidor (verificado bcrypt server-side) |
| Processo Node → JWT | Segredo CHILD_SESSION_SECRET nunca sai do servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-PIN-BF | Elevation of Privilege | checkBruteForce (child-session.ts) | mitigate | in-memory Map bloqueia após 5 tentativas por childId (D-09); coberto por child-auth-endpoint.test.ts |
| T-02-PIN-TIMING | Information Disclosure | verifyPin (child-pin.ts) | mitigate | bcrypt.compare() é comparação constante no tempo; nunca comparar hashes com `===` |
| T-02-JWT-TAMPER | Tampering | verifyChildSession (child-session.ts) | mitigate | jose jwtVerify valida assinatura HMAC-SHA256; token adulterado lança Error |
| T-02-SECRET-LEAK | Information Disclosure | CHILD_SESSION_SECRET | mitigate | `import 'server-only'` impede que o módulo seja importado em bundle de cliente |
| T-02-WEAK-HASH | Tampering | hashPin (child-pin.ts) | mitigate | bcrypt cost 10 (D-10); proibido SHA/createHash |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase — jose/bcryptjs/next-auth já são deps (RESEARCH.md Package Legitimacy Audit: todos OK) |
</threat_model>

<verification>
Por task: rodar o arquivo de teste específico (acima).
Ao concluir o plano, rodar os 3 arquivos juntos:
`pnpm test -- tests/unit/child-pin-management.test.ts tests/unit/child-auth-endpoint.test.ts tests/unit/child-session-guard.test.ts`
Esperado: 22 passed (9 + 7 + 6), 0 failed.
</verification>

<success_criteria>
- Os 22 testes unitários dos 3 módulos passam (antes falhavam por ERR_MODULE_NOT_FOUND)
- Cada módulo inicia com `import 'server-only'`
- D-09 (Map, 5 tentativas), D-10 (bcrypt cost 10), D-11 (CHILD_SESSION_SECRET HS256) implementados conforme decisões locked
- Nenhum pacote novo instalado
</success_criteria>

<output>
Create `.planning/phases/02-authentication/02-01-SUMMARY.md` when done
</output>
