---
phase: 02-authentication
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - src/components/auth/pin-dot.tsx
  - src/components/auth/pin-dots.tsx
  - src/components/auth/numeric-keypad.tsx
  - src/components/auth/gate-lock.tsx
  - src/components/auth/pin-screen.tsx
  - src/app/(child)/child/[childId]/login/page.tsx
autonomous: false
requirements: [CAUTH-01, CAUTH-02, CAUTH-03, CAUTH-04, CAUTH-05]
must_haves:
  truths:
    - "Criança vê tela de PIN com 4 dots, teclado 3×4, logo e plant hero animada (kredsBreath)"
    - "Cada dot preenchido mostra SVG brotinho com kredsSprout"
    - "PIN errado dispara kredsShake nos dots e reseta após 950ms"
    - "PIN correto abre animação de portão (dois painéis cubic-bezier 1s) e redireciona ao jardim após 1.1s"
    - "Link Trocar perfil limpa o PIN e volta para /family/access/[familyId]"
  artifacts:
    - path: "src/app/(child)/child/[childId]/login/page.tsx"
      provides: "Rota da tela de PIN (route group child)"
      exports: ["default"]
    - path: "src/components/auth/pin-screen.tsx"
      provides: "Orquestrador client da tela de PIN (state, shake, gate, server action)"
      exports: ["PinScreen"]
    - path: "src/components/auth/numeric-keypad.tsx"
      provides: "Teclado numérico 3×4"
      exports: ["NumericKeypad"]
    - path: "src/components/auth/pin-dots.tsx"
      provides: "Container dos 4 dots com shake"
      exports: ["PinDots"]
    - path: "src/components/auth/pin-dot.tsx"
      provides: "Dot individual com SVG brotinho"
      exports: ["PinDot"]
    - path: "src/components/auth/gate-lock.tsx"
      provides: "Overlay de portão (dois painéis)"
      exports: ["GateLock"]
  key_links:
    - from: "src/components/auth/pin-screen.tsx"
      to: "src/app/actions/child-auth.ts (verifyChildPin)"
      via: "chamada ao Server Action ao completar 4 dígitos"
      pattern: "verifyChildPin"
    - from: "src/components/auth/pin-screen.tsx"
      to: "/family/access/[familyId]"
      via: "router.push no link Trocar perfil"
      pattern: "/family/access/"
    - from: "src/components/auth/pin-screen.tsx"
      to: "/child/[childId]/garden"
      via: "router.push após animação de portão (1.1s)"
      pattern: "/garden"
---

<objective>
Construir a tela de PIN da criança (CAUTH-01..05): a peça visual central do fluxo da criança. Inclui os componentes de dots (com SVG brotinho e kredsSprout), teclado numérico 3×4, overlay de portão, e o orquestrador `PinScreen` que gerencia o estado do PIN, dispara o shake no erro (kredsShake + reset 950ms), chama o Server Action `verifyChildPin` do 02-03, e ao acerto aciona a animação de portão (cubic-bezier 1s) redirecionando ao jardim. Termina com checkpoint visual humano (animações são manual-only conforme 02-VALIDATION.md).

Purpose: Esta tela materializa os 5 requisitos CAUTH e é o resultado observável de toda a fundação backend (02-01..02-03). Sem ela o PIN nunca é digitado, o cookie nunca é setado, e o fluxo da criança não existe.
Output: 5 componentes em `src/components/auth/` + página em `src/app/(child)/child/[childId]/login/page.tsx` + checkpoint visual aprovado.
</objective>

<execution_context>
@/Users/hass/.claude/gsd-core/workflows/execute-plan.md
@/Users/hass/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-UI-SPEC.md
@.planning/phases/02-authentication/02-PATTERNS.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-03-SUMMARY.md
@src/app/globals.css
@src/app/page.tsx
</context>

<artifacts_this_phase_produces>
| Símbolo | Arquivo | Assinatura | Consumido por |
|---------|---------|------------|---------------|
| `PinDot` | src/components/auth/pin-dot.tsx | `({ filled, error }: { filled: boolean; error: boolean }) => JSX` | PinDots |
| `PinDots` | src/components/auth/pin-dots.tsx | `({ count, error }: { count: number; error: boolean }) => JSX` | PinScreen |
| `NumericKeypad` | src/components/auth/numeric-keypad.tsx | `({ onDigit, onBackspace }: { onDigit: (d: string) => void; onBackspace: () => void }) => JSX` | PinScreen |
| `GateLock` | src/components/auth/gate-lock.tsx | `({ open }: { open: boolean }) => JSX` | PinScreen |
| `PinScreen` | src/components/auth/pin-screen.tsx | `({ childId, familyId, displayName }: {...}) => JSX` | page child login |
| `default` (page) | src/app/(child)/child/[childId]/login/page.tsx | Server Component | runtime / ProfileCard (02-03) navega para cá |
</artifacts_this_phase_produces>

<interface_context>
Animações JÁ EXISTEM em globals.css (VERIFICADO, linhas 42-56) como CSS vars:
`--animate-kreds-breath` (5s infinite), `--animate-kreds-sprout` (0.45s), `--animate-kreds-shake` (0.5s).
Aplicar via `style={{ animation: 'var(--animate-kreds-shake)' }}` ou classe utilitária.

verifyChildPin (de 02-03 src/app/actions/child-auth.ts):
`(childId: string, pin: string) => Promise<{ success: true } | { error: 'blocked'|'no-pin'|'invalid' }>`.
É Server Action — importar e chamar diretamente do client component.

GateLock pattern (de 02-PATTERNS.md §Componentes): dois divs absolutos w-1/2 h-full bg-[#27372C],
gateL translateX(-101%) quando open, gateR translateX(101%), transition transform 1s cubic-bezier(.76,0,.24,1).

params no Next 15 é Promise: `const { childId } = await params`.
displayName/familyId precisam vir para a saudação "Olá, [Nome]!" e para o link Trocar perfil —
a page (Server Component) busca via Drizzle (childProfiles por id) e passa como props ao PinScreen.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Componentes visuais — PinDot, PinDots, NumericKeypad, GateLock</name>
  <files>src/components/auth/pin-dot.tsx, src/components/auth/pin-dots.tsx, src/components/auth/numeric-keypad.tsx, src/components/auth/gate-lock.tsx</files>
  <read_first>
    - .planning/phases/02-authentication/02-UI-SPEC.md §Screen 2 (Dots do PIN, Teclado numérico, Animação de portão — medidas e cores exatas)
    - .planning/phases/02-authentication/02-PATTERNS.md §Componentes em src/components/auth/ (Animation classes + GateLock pattern)
    - src/app/globals.css (linhas 42-56 e 107-117: kredsSprout, kredsShake keyframes)
  </read_first>
  <action>
    Todos com `'use client'` na primeira linha.

    **PinDot (`pin-dot.tsx`)** — props `{ filled: boolean; error: boolean }`:
    - 16px diâmetro, `border-radius: 50%`.
    - Default (não filled): borda `2px solid #C3C9B7`, fundo transparente.
    - Filled: `bg #3E6B4F`, borda `#3E6B4F`, com SVG brotinho inline sobreposto aplicando `style={{ animation: 'var(--animate-kreds-sprout)' }}` (CAUTH-05).
    - Error: `bg #D8916B`, borda `#C06B4A` (sobrepõe o filled).

    **PinDots (`pin-dots.tsx`)** — props `{ count: number; error: boolean }`:
    - Container com `role="status"` e `aria-label={`PIN: ${count} de 4 dígitos preenchidos`}` (02-UI-SPEC Accessibility).
    - Renderiza 4 PinDot: dot i tem `filled={i < count}`, `error={error}`.
    - gap 12px entre dots.
    - Quando `error` true: aplicar `style={{ animation: 'var(--animate-kreds-shake)' }}` no container (CAUTH-02). O reset (limpar count + remover shake após 950ms) é responsabilidade do PinScreen (Task 2).

    **NumericKeypad (`numeric-keypad.tsx`)** — props `{ onDigit: (d: string) => void; onBackspace: () => void }`:
    - Grid 3×4: linhas [1,2,3],[4,5,6],[7,8,9],['',0,'⌫'].
    - Botões 1-9 e 0: 64×64px, `border-radius: 50%`, `bg #FBFAF5`, `box-shadow: 0 3px 0 #E6E1D4`, texto 20px/700. onClick → `onDigit(valor)`.
    - Célula `*` (posição [3][0]): vazia, sem render de botão.
    - Botão `⌫`: sem fundo/borda, ícone SVG backspace 20px, `aria-label="Apagar"` (02-UI-SPEC Accessibility). onClick → `onBackspace()`.
    - gap 8px entre colunas, 12px entre linhas.

    **GateLock (`gate-lock.tsx`)** — props `{ open: boolean }`:
    - Container `absolute inset-0 overflow-hidden pointer-events-none`.
    - Painel esquerdo `.kreds-gateL`: `absolute top-0 left-0 w-1/2 h-full bg-[#27372C]`, `style={{ transform: open ? 'translateX(-101%)' : 'translateX(0)', transition: 'transform 1s cubic-bezier(.76,0,.24,1)' }}` (CAUTH-03).
    - Painel direito `.kreds-gateR`: `absolute top-0 right-0 w-1/2 h-full bg-[#27372C]`, transform `translateX(101%)` quando open.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep -iE "pin-dot|pin-dots|numeric-keypad|gate-lock" || echo "OK: 4 componentes visuais type-check"</automated>
  </verify>
  <acceptance_criteria>
    - 4 arquivos criados, todos com `'use client'` na primeira linha
    - PinDot filled usa SVG brotinho com `var(--animate-kreds-sprout)` (CAUTH-05)
    - PinDots container usa `var(--animate-kreds-shake)` quando error (CAUTH-02) e tem role="status"
    - NumericKeypad é grid 3×4 com célula * vazia e ⌫ com aria-label="Apagar"
    - GateLock usa transform translateX(±101%) com transition 1s cubic-bezier(.76,0,.24,1) (CAUTH-03)
    - `pnpm tsc --noEmit` sem erro nos 4 arquivos
  </acceptance_criteria>
  <done>4 componentes visuais existem com medidas/cores/animações exatas do 02-UI-SPEC Screen 2.</done>
</task>

<task type="auto">
  <name>Task 2: PinScreen (orquestrador) + página de login da criança</name>
  <files>src/components/auth/pin-screen.tsx, src/app/(child)/child/[childId]/login/page.tsx</files>
  <read_first>
    - .planning/phases/02-authentication/02-UI-SPEC.md §Screen 2 (ordem dos elementos, timing das animações, copy)
    - .planning/phases/02-authentication/02-PATTERNS.md §`src/app/(child)/child/[childId]/login/page.tsx` (layout shell)
    - src/app/actions/child-auth.ts (verifyChildPin — criado em 02-03)
    - src/lib/db/schema/index.ts (childProfiles: displayName, familyId por id)
    - src/app/globals.css (kredsBreath para plant hero)
  </read_first>
  <action>
    **PinScreen (`pin-screen.tsx`)** — `'use client'`. Props `{ childId: string; familyId: string; displayName: string }`.
    - Estado: `const [pin, setPin] = useState('')`, `const [error, setError] = useState(false)`, `const [gateOpen, setGateOpen] = useState(false)`. `const router = useRouter()` (de `next/navigation`).
    - Importar `verifyChildPin` de `@/app/actions/child-auth` e os componentes PinDots, NumericKeypad, GateLock.
    - `onDigit(d)`: se `error` ou `gateOpen`, ignorar. Acrescentar dígito se `pin.length < 4`. Quando `pin.length` chega a 4, disparar a verificação (useEffect observando pin.length === 4, ou no próprio handler).
    - Verificação: `const result = await verifyChildPin(childId, novoPin)`.
      - `result.success` → `setGateOpen(true)`; após `setTimeout(() => router.push(`/child/${childId}/garden`), 1100)` (02-UI-SPEC: portão 1.1s antes do redirect, CAUTH-03).
      - erro (`'invalid'|'blocked'|'no-pin'`) → `setError(true)`; após `setTimeout(() => { setPin(''); setError(false) }, 950)` (CAUTH-02: shake + reset 950ms). NÃO mostrar texto de erro na tela da criança (02-UI-SPEC Copywriting: erro é só shake visual, brute force tratado silenciosamente).
    - `onBackspace()`: `setPin(p => p.slice(0, -1))` (se não em error/gate).
    - Link "Trocar perfil" (CAUTH-04): texto exato "Trocar perfil", 12px/500, cor `--color-kreds-hint`, underline on hover. onClick: `setPin(''); setError(false); router.push(`/family/access/${familyId}`)` (limpa state E navega).
    - Layout (02-UI-SPEC Screen 2, ordem top→bottom): logo, plant hero SVG com `style={{ animation: 'var(--animate-kreds-breath)' }}` (CAUTH-01), saudação `Olá, ${displayName}!` (Display 24px/700), `<PinDots count={pin.length} error={error} />`, `<NumericKeypad onDigit={onDigit} onBackspace={onBackspace} />`, link Trocar perfil. `<GateLock open={gateOpen} />` como overlay absoluto.
    - Fundo da tela: gradiente `radial-gradient(120% 100% at 50% 0%, #ECE7DB 0%, #E0DACB 100%)`.

    **Página (`src/app/(child)/child/[childId]/login/page.tsx`)** — Server Component (sem 'use client'):
    - `export default async function ChildLoginPage({ params }: { params: Promise<{ childId: string }> })`. `const { childId } = await params`.
    - Buscar displayName + familyId: `const [child] = await db.select({ displayName: childProfiles.displayName, familyId: childProfiles.familyId }).from(childProfiles).where(eq(childProfiles.id, childId)).limit(1)`. Imports db/childProfiles/eq como nos outros planos.
    - Se `!child` → renderizar fallback ou `notFound()` de `next/navigation`.
    - Renderizar `<main>` shell e `<PinScreen childId={childId} familyId={child.familyId} displayName={child.displayName} />`.
    - NÃO importar next/font (herdado do RootLayout).
    - NOTA route group: o arquivo fica em `src/app/(child)/child/[childId]/login/page.tsx` — o grupo `(child)` NÃO altera a URL (continua `/child/[childId]/login`), apenas organiza (D-12). Garantir que a URL resultante seja `/child/[childId]/login` (não `/(child)/...`).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep -iE "pin-screen|child/\[childId\]/login" || echo "OK: pin-screen + child login page type-check"</automated>
  </verify>
  <acceptance_criteria>
    - PinScreen chama `verifyChildPin(childId, pin)` ao completar 4 dígitos
    - Sucesso → setGateOpen(true) + redirect a /child/[childId]/garden após ~1100ms
    - Erro → setError(true) + reset (setPin('') + setError(false)) após 950ms; SEM texto de erro
    - Link "Trocar perfil" limpa pin e navega para /family/access/[familyId] (CAUTH-04)
    - Plant hero usa var(--animate-kreds-breath); saudação "Olá, [Nome]!" com displayName real
    - Página em src/app/(child)/child/[childId]/login/page.tsx faz await params e busca child por id
    - `pnpm tsc --noEmit` sem erro nos dois arquivos
  </acceptance_criteria>
  <done>PinScreen orquestra digitação, shake, portão e Server Action; página carrega displayName/familyId via SSR e renderiza PinScreen na URL /child/[childId]/login.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual — animações da tela de PIN (CAUTH-01..05)</name>
  <action>
    Verificação humana das 5 animações/comportamentos CAUTH — são manual-only conforme 02-VALIDATION.md §Manual-Only Verifications (CSS transforms e SVG animations não têm assert automatizado). O executor NÃO escreve código nesta task: ele finaliza as Tasks 1-2, sobe o dev server, apresenta os passos de verificação abaixo ao usuário e pausa até receber o sinal "approved". Se o usuário reportar divergência do 02-UI-SPEC, corrigir os componentes da Task 1/2 antes de prosseguir.
  </action>
  <what-built>
    Tela de PIN completa da criança em /child/[childId]/login com 5 componentes (dots+brotinho, teclado 3×4, portão), animações kredsBreath/kredsSprout/kredsShake, verificação via Server Action e animação de portão no acerto. PIN de teste para desenvolvimento: 1234 (conforme protótipo).
  </what-built>
  <how-to-verify>
    1. Garantir que existe um childProfile no banco com pinHash de '1234' (usar seed/script: hashPin('1234') do módulo 02-01, ou inserir via Drizzle). Anotar o childId e familyId.
    2. Rodar `pnpm dev` e abrir `http://localhost:3000/family/access/[familyId]` — confirmar que o avatar da criança aparece (tela de seleção do 02-03).
    3. Tap no avatar → deve navegar para `/child/[childId]/login`.
    4. CAUTH-01: confirmar 4 dots, teclado numérico 3×4, logo e plant hero com respiração suave (kredsBreath, loop infinito translateY).
    5. CAUTH-05: digitar 1 dígito → o dot preenchido mostra o SVG brotinho com animação kredsSprout.
    6. CAUTH-02: digitar PIN errado (ex.: 0000) → os 4 dots tremem (kredsShake 0.5s) e resetam sozinhos após ~950ms.
    7. CAUTH-03: digitar 1234 → animação de portão: painel esquerdo desliza para a esquerda e o direito para a direita (cubic-bezier 1s), revelando o fundo; após ~1.1s redireciona para /child/[childId]/garden (a rota garden ainda não existe na Fase 2 — 404 é esperado; o que importa é a animação + tentativa de redirect).
    8. CAUTH-04: voltar à tela de PIN, digitar 1-2 dígitos, clicar em "Trocar perfil" → os dots limpam e a navegação volta para /family/access/[familyId].
  </how-to-verify>
  <resume-signal>Digite "approved" se todas as 5 animações/comportamentos (CAUTH-01..05) conferem, ou descreva o que está divergente do 02-UI-SPEC.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente (PinScreen) → Server Action | PIN digitado cruza para verificação server-side (já mitigado em 02-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-PIN-CLIENT | Information Disclosure | PinScreen | mitigate | PIN nunca é hashado/comparado no cliente; PinScreen apenas coleta e envia ao Server Action verifyChildPin (bcrypt server-side) |
| T-02-PIN-FEEDBACK | Information Disclosure | PinScreen erro | mitigate | erro não revela motivo (invalid vs blocked vs no-pin) na UI — apenas shake silencioso (02-UI-SPEC); evita enumeração de estado |
| T-02-PIN-SPAM | Elevation of Privilege | PinScreen submit | mitigate | brute-force aplicado no Server Action (02-03); PinScreen bloqueia input durante error/gate (estado) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado |
</threat_model>

<verification>
Tasks 1-2: `pnpm tsc --noEmit` limpo nos arquivos criados.
Task 3: checkpoint humano valida CAUTH-01..05 (animações são manual-only conforme 02-VALIDATION.md §Manual-Only Verifications).
Regressão: `pnpm test` (suíte completa verde — módulos backend não devem quebrar).
</verification>

<success_criteria>
- CAUTH-01: tela com 4 dots, teclado 3×4, logo, plant hero animada (kredsBreath)
- CAUTH-02: PIN errado → kredsShake + reset 950ms
- CAUTH-03: PIN correto → animação de portão cubic-bezier 1s + redirect ~1.1s
- CAUTH-04: "Trocar perfil" limpa PIN e volta para /family/access/[familyId]
- CAUTH-05: cada dot preenchido com SVG brotinho + kredsSprout
- Checkpoint visual aprovado pelo usuário
</success_criteria>

<output>
Create `.planning/phases/02-authentication/02-04-SUMMARY.md` when done
</output>
